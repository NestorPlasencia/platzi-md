[Curso de jQuery a JavaScript 1316](https://platzi.com/cursos/jquery-js)

# Introducción [2246]

## 0010. Bienvenidos al curso [12299](https://platzi.com/clases/1316-jquery-js/12299-bienvenidos-al-curso5143/)

### Descripción:


Bienvenidos a este curso en el que aprenderás como pasar de trabajar con jQuery a JavaScript. Leonidas Esteban te estará acompañando en esta aventura y ayudándote a resolver todas tus dudas.

Puedes encontrar el repositorio del curso en la sección de archivos de la plataforma, también puede verlo en este enlace: <https://github.com/LeonidasEsteban/jquery-to-js-curso>

### Links:

* [GitHub - LeonidasEsteban/jquery-to-js-curso](https://github.com/LeonidasEsteban/jquery-to-js-curso)

### Comentarios:

* **Joss** (31) [335484](https://platzi.com/comentario/335484/) 

	Eyyy!!! me gusta la forma en la que aparece el logo de platzi y el sonidito en el punto de la ‘i’,me encanta, no lo tomen a mal pero ya me habia cansado de la cancion de otros videos…jijjiij

	* **Cristian Orozco Benjumea** [335484] (2)

		A mi me recuerda a TED, sólo que ellos usan una gota

* **Jahdiel Vargas** (11) [335101](https://platzi.com/comentario/335101/) 

	He esperado este curso desde hace mucho tiempo

	* **Gerardo Ruiz** [335101] (2)

		yo tambieeen 😂

	* **Oscar Barajas Tavares (Platzi)** [335101] (1)
¿Que te pareció el curso?

* **Byhako** (9) [336747](https://platzi.com/comentario/336747/) 

	Otro curso con el mejor profe de Plazti. Sera genial.

* **Sergio Minei** (7) [334974](https://platzi.com/comentario/334974/) 

	#ByeByeJquery!

* **devunix** (5) [515932](https://platzi.com/comentario/515932/) 

	jQuery es una librería de JavaScript que hizo su lanzamiento en el año 2006 con el fin de resolver diferentes problemáticas:
	
	• Una misma forma de acceder al DOM.  
	• Poder interactuar con datos de un servidor.  
	• Crear animaciones.
	
	El problema con jQuery surgió cuando se empezó a exagerar su uso y darle menor importancia a aprender JavaScript. Esto genero malos hábitos de aprendizaje y hasta en algunos casos no diferenciar jQuery de JavaScript.
	
	A la par de que algunos se quedaban en jQuery, estaba ocurriendo la revolución de JavaScript trayendo consigo librerías que resolvían problemas específicos.
	
	Aun con estas nuevas librerías, seguía sin resolverse el problema de hacer que uno aprenda mas JavaScript que librerías o frameworks pues uno igual puede abusar de Vue, React o Angular.
	
	Por eso en este curso aprenderás a NO depender de ninguna librería, las ventajas de esto son:
	
	• Reutilizar conocimientos.  
	• Poder implementar soluciones sin dependencias.  
	• Estar más capacitado para las grandes empresas.

* **33andres33** (4) [1076058](https://platzi.com/comentario/1076058/) 

	Sabes que va a ser un buen curso porque el profesor es Leonidas

* **Christian Erik Velázquez Morales** (3) [965144](https://platzi.com/comentario/965144/) 

	Leonidas == Aprendizaje.

* **Jesus Ricardo Moreira Cedeño** (3) [399618](https://platzi.com/comentario/399618/) 

	Es raro, porque empecé antes el curso de fundamentos de javascript jaja, aún así me veré el curso sin peros 😃

* **JPabloMayorgaM** (2) [518861](https://platzi.com/comentario/518861/) 
Es muy buen profe, me gustó mucho el curso de git y ningún

* **Ricardo Medina** (2) [424976](https://platzi.com/comentario/424976/) 

	Comenzando con este curso con el sensei Leonidas!

* **Iván Mauricio Toro Cifuentes** (2) [370819](https://platzi.com/comentario/370819/) 

	 **¡Empecemos!**
	
	Siempre es chevere el video de bienvenida que hace Leonidas en sus cursos.

* **Eduardo Cecilio Flores Ambrosio** (2) [368168](https://platzi.com/comentario/368168/) 

	Este curso es perfecto para quitar jQuery de mis proyectos en el trabajo

* **Alan Santiago** (2) [361850](https://platzi.com/comentario/361850/) 

	Mi siguiente curso despues de la carrera de Frontend 😄

* **carlosextra1** (1) [1114041](https://platzi.com/comentario/1114041/) 

	Estoy seguro que será una gran experiencia!!

* **Wandy Rafael Santana Evangelista** (1) [1070723](https://platzi.com/comentario/1070723/) 

	Comenzamos. 😃

* **Kevin Vega** (1) [1066027](https://platzi.com/comentario/1066027/) 

	Genial!, me encanta ser Desarrollador a darle!

* **JheysonEGalvis** (1) [1064471](https://platzi.com/comentario/1064471/) 

	Siguiendo la ruta de Arquitecto Frontend 😃

* **Kevin Jeremy Salazar Jimenez** (1) [988759](https://platzi.com/comentario/988759/) 

	excelente inicio!

* **Percy Tejada Donayre** (1) [915159](https://platzi.com/comentario/915159/) 

	Si es con Leonidas, esto va a estar cool!

* **Jhoan Steve Sandoval Meneses** (1) [891454](https://platzi.com/comentario/891454/) 

	Buenas esta espectacular el curso, pero que pasa con los navegadores como internet explore que no ejecutan javascript moderno como se resuelve ese problema.

* **lmoran** (1) [885676](https://platzi.com/comentario/885676/) 

	A darle!!!

* **horacio-garcia** (1) [862717](https://platzi.com/comentario/862717/) 

	amazing

* **Cristian Caraballo** (1) [862695](https://platzi.com/comentario/862695/) 

	WoooOOOOoooOOOoooOOOHHHH! 🤘😎🤘

* **Kenet Chungandro** (1) [816255](https://platzi.com/comentario/816255/) 

	Empezamos!

* **J Daniel Rodriguez Davis** (1) [805199](https://platzi.com/comentario/805199/) 

	Emocionante!

* **webrevolution** (1) [801223](https://platzi.com/comentario/801223/) 

	empezando!!!

* **Jonathan Uriel Jiménez Soveranes** (1) [799668](https://platzi.com/comentario/799668/) 

	Arrancamos!

* **Christian David Sánchez** (1) [675270](https://platzi.com/comentario/675270/) 

	Buena motivación por parte del instructor Leonidas. A darle con todo al curso.

* **Renier Ferrer** (1) [669433](https://platzi.com/comentario/669433/) 

	Estaba indeciso con el dilema de tomar o no el curso, debido a que ya casi no se usa jQuery, al enterarme de que el profesor era Leonidas, no lo dude un segundo mas.! Siempre hay un valor agregado cuando el da los cursos.

* **luis zevallos** (1) [667786](https://platzi.com/comentario/667786/) 

	sigamos la ruta de desarrollado Frontend con Leonidas excelente

* **Pozholat** (1) [656526](https://platzi.com/comentario/656526/) 

	qué bueno que el curso va a ser con Leo, es un excelente maestro

* **Byron Miguel Piedrahita Hernandez** (1) [653113](https://platzi.com/comentario/653113/) 

	Siguiendo la ruta para se un Frontend profesionalisimo 🐱‍🚀

* **Jose Luis Colmenares** (1) [606227](https://platzi.com/comentario/606227/) 

	No vaaaale. Es Leonidas el instructor. No me lo esperaba. Que fino. Me encantan tus cursos

* **jesus-gallardo** (1) [601186](https://platzi.com/comentario/601186/) 

	Empecemos !

* **devunix** (1) [515934](https://platzi.com/comentario/515934/) 

	Esto se va a descontrolar

* **devunix** (1) [515930](https://platzi.com/comentario/515930/) 

	Empezare por este

* **pablojorgeandres** (1) [482773](https://platzi.com/comentario/482773/) 

	Hola de nuevo profesor Leonidas.  
	Después del curso con Sacha en fundamentos seguiré su curso para seguir profundizando en este lenguaje tan presente y complejo que es JavaScript.  
	A estudiar!

* **Chunchunmaru** (1) [456812](https://platzi.com/comentario/456812/) 

	A comenzar!

* **Kevin Brayan Luna Figueroa** (1) [434124](https://platzi.com/comentario/434124/) 

	Es ahora de cambiar de JQuery a JavaScript 😃 !!

* **Melina Jacqueline onoriaga** (1) [356758](https://platzi.com/comentario/356758/) 

	Jquery estará muriendo ?

	* **Diego Alexander Forero Higuera (Platzi)** [356758] (7)

		jQuery llego para solucionar cosas que eran complejas hacer en su momento, ahora llegaron librerías como React o Vue o frameworks como angular que hacen mejor el trabajo que hace jQuery, aun se sigue usando en muchas cosas pero cada vez es menos usado en favor de estas nuevas tecnologías que son más rápidas y con mejor rendimiento.

* **Juan Carlos Felizzola Vega** (1) [352553](https://platzi.com/comentario/352553/) 

	Comenzaré con este y esperaré el nuevo de fundamentos de Javascript.

* **Ivan Fuentes** (0) [353773](https://platzi.com/comentario/353773/) 

	Super emocionado por comenzar, vamos con todo!

* **David Daniel Castillo Nava** (0) [349711](https://platzi.com/comentario/349711/) 

	Empecemos ver este cursillo!! Que seguro es un super curso!!

## 0020. La historia de jQuery [12300](https://platzi.com/clases/1316-jquery-js/12300-la-historia-de-jquery/)

### Descripción:


jQuery es una librería de JavaScript que hizo su lanzamiento en el año 2006 con el fin de resolver diferentes problemáticas:

• Una misma forma de acceder al DOM.  
• Poder interactuar con datos de un servidor.  
• Crear animaciones.

El problema con jQuery surgió cuando se empezó a exagerar su uso y darle menor importancia a aprender JavaScript. Esto genero malos hábitos de aprendizaje y hasta en algunos casos no diferenciar jQuery de JavaScript.

A la par de que algunos se quedaban en jQuery, estaba ocurriendo la revolución de JavaScript trayendo consigo librerías que resolvían problemas específicos.

Aun con estas nuevas librerías, seguía sin resolverse el problema de hacer que uno aprenda mas JavaScript que librerías o frameworks pues uno igual puede abusar de Vue, React o Angular.

Por eso en este curso aprenderás a NO depender de ninguna librería, las ventajas de esto son:

• Reutilizar conocimientos.  
• Poder implementar soluciones sin dependencias.  
• Estar más capacitado para las grandes empresas.

### Comentarios:

* **Sergio Minei** (81) [335007](https://platzi.com/comentario/335007/) 

	## Curso de jQuery a JavaScript
	
	jQuery es una librería de Javascript que resuelve:
	
	  * Una misma forma de acceder al DOM `$('selector')`: En ese tiempo, todos los navegadores accedian an DOM de una forma diferente.
	  * Poder interactuar con datos de un servidor `$.ajax()`: jQuery permite hacer llamadas al servidor.
	  * Crear animaciones `$.animate()`: En ese tiempo crear animaciones era muy dificil.
	
	## Problema
	
	  * Se empezó a abusar de la librería y se empezó a usar sin ver si realmente era necesario su uso o no.
	  * Uno se hacía dependiente de jQuery y sus sub-librerías a tal punto que cuando se quería modificar algo de una librería no sabía como hacerlo.
	  * No se diferenciaba qué era jQuery y qué JavaScript.
	  * Mientras unos se quedaban en jQuery, la revolución de JavaScript estaba sucendiendo.
	
	La idea es no depender de una librería sino aprender la tecnología que está detrás de cada librería.
	
	## Ventajas de usar JavaScript
	
	  * Reutilizar conocimiento en otros lados de tu aplicación.
	  * Poder implementar soluciones sin depender de una librería.
	  * Estar más capacitado para grandes empresas.
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **LEIclass** [335007] (2)

		@MineiToshio, sos un crack !!

	* **Sergio Minei** [335007] (1)

		Muchas gracias @LEIclass. Es un gusto poder ayudar =)

	* **Eduardo Cecilio Flores Ambrosio** [335007] (2)

		Muy buen resumen, excelente @MineiToshio

	* **Sergio Minei** [335007] (2)

		Gracias @fronzec 😉

	* **Jesus Ricardo Moreira Cedeño** [335007] (2)

		Gracias 😃

	* **Chunchunmaru** [335007] (2)

		Gracias!

	* **Miguel Segura** [335007] (2)

		 **Excelente resumen!**

	* **jesus-gallardo** [335007] (2)

		Buen resumen

	* **Juan Luis Rojas León** [335007] (2)

		¡Muchas gracias!

* **carlosextra1** (7) [335011](https://platzi.com/comentario/335011/) 

	Jquery es GENIAL, pero nos ha vuelto muy dependientes!! _18/07/2018 17:28_

	* **Japheth Calzada López** [335011] (2)

		lo que dice Leonidas es cierto, haciamos un frankie de js y jquery

* **Mad Manuva** (7) [334982](https://platzi.com/comentario/334982/) 

	A mi me ha servido para resolver un poco el tema de crossbrowser, todo bien con la libreria, pero te deja con la sensación de q se puede hacer mas

	* **Japheth Calzada López** [334982] (2)

		me ayudo mucho un trabajo donde estando en edge, teniamos que programar para explorer 8, era horrrible y jquery me ayudo

* **.** (6) [334976](https://platzi.com/comentario/334976/) 

	Y era genial como lo hacía!
	
	  * Una misma forma de acceder a los elementos del DOM.
	  * Poder interactuar con los datos de un servidor.
	  * animaciones.

	* **samvarcia** [334976] (1)

		pero madre mia que bloz

* **Armando Duarte Quiñones** (5) [580550](https://platzi.com/comentario/580550/) 

	¿Donde estoy?  
	Probablemente en un punto medio donde no siento que aun no conozco ni al uno ni al otro con confianza, sin embargo, se que en un futuro para nada lejano JS y yo seremos mejores amigos 😄

* **Kevin Parra Lopez** (4) [1014029](https://platzi.com/comentario/1014029/) 
Muy buen video, pero mucho drama 😅

* **asismelgarejo** (4) [881581](https://platzi.com/comentario/881581/) 

	Estoy siguiendo el orden de los cursos de la carrera de Arquitectura Frontend

* **Alejandro Gomez Maldonado** (4) [352585](https://platzi.com/comentario/352585/) 

	¿Yo donde estoy?
	
	Estoy en un proceso de mucho aprendizaje, preparandome para poder dar el ancho en un equipo de desarrollo de software, cada vez tengo más bases para poderme desarrollar profesionalmente. !Y eso me llena de mucho entusiasmo!!!

* **Juan Esteban Galvis** (3) [1089153](https://platzi.com/comentario/1089153/) 

	Ahora hay un curso de JS Vanilla en Platzi: <https://platzi.com/cursos/spa-javascript/>
	
	Me esta gustando la forma de enseñar de Leonidas.

	* **carlosextra1** [1089153] (1)

		Yo ya lo tome y esta genial, es muy corto pero efectivo!

* **andresazuara98** (3) [1020452](https://platzi.com/comentario/1020452/) 

	Leonidas Esteban: En esa historia, tu donde estas?  
	yo:brouh daaaaaaaaaaamn

* **Ambar Z. Alvarez Vélez** (3) [1018788](https://platzi.com/comentario/1018788/) 

	Vine aqui a aprender y termine enternecida y casi llorando :’)

* **hectorJosuemacias** (3) [659310](https://platzi.com/comentario/659310/) 

	Acompañenme a ver esta triste historia ;v

* **antonymedina** (3) [619392](https://platzi.com/comentario/619392/) 

	Se nota que a Leonidas esto le duele en el corazón, por un momento pensé que se iba a poner a llorar. xd

	* **nicolas-garcia-puerta** [619392] (2)

		jajajaja pensé lo mismo

* **Uralberto** (3) [593447](https://platzi.com/comentario/593447/) 

	![]( ![Ruta aprendizaje.jpg](https://static.platzi.com/media/user_upload/Ruta%20aprendizaje-0beca03a-4333-4693-bc11-47bd9658c847.jpg))
	
	Observo en esta ruta de aprendizaje promovida por Leonidas que el curso PostCss es previo al curso de jQuery a JavaScript. En esta clase observo que es mejor tomarlo después.

	* **luistua** [593447] (1)

		Me molesta un poco eso. En un curso dan un órdern, en la app otro y en web aparece otro orden y luego en media carrera aparece un curso nuevo. Luego uno se detiene dos veces a ver cuál le conviene más.

* **Uralberto** (3) [593428](https://platzi.com/comentario/593428/) 

	Buena contextualización para empezar. Estaba por iniciar el curso React.js pero me doy cuenta ahora que hubiera sido un gran error. Así que…voy por tí JavaScript.

* **jlquichimbo** (3) [416597](https://platzi.com/comentario/416597/) 

	La transición de JQuery a Javascript está ligada a la nueva versión de la librería Bootstrap 4?  
	Puesto que han quitado todo el Jquery de la librería. Asumo que para el uso de nuevas funcionalidades debemos aprender a usar Javascript puro.

	* **Jcion** [416597] (4)

		Está ligada a que JS se volvió más poderoso 😛

	* **Daniel Esteves** [416597] (1)
Esto se debe a que gracias a que la tecnología avanza cada día más no se necesita jQuery para hacer funciones o API que ya los navegadores pueden entender 😎

* **David Antonio Ordóñez Cornejo** (3) [335068](https://platzi.com/comentario/335068/) 

	Jquery es genial, pero siempre será mejor conocer la base sobre la que descansa para poder tomar desiciones según el caso.

* **MATTHEUV OSORIO** (2) [1090499](https://platzi.com/comentario/1090499/) 

	Dándole a la carrera de front-end, queriendo dominar el mundo

	* **carlosextra1** [1090499] (1)

		Mucha suerte!!

* **Kevin Vega** (2) [1066051](https://platzi.com/comentario/1066051/) 

	llegué viendo CSS y termine en Jquery, genial!

	* **luistaurik** [1066051] (1)

		Yo igual! Allá vamos

	* **luistaurik** [1066051] (1)

		Tu IMG de perfil se parece al logo de ableton

* **JheysonEGalvis** (2) [1064500](https://platzi.com/comentario/1064500/) 

	“jQuery es una biblioteca multiplataforma de JavaScript, creada inicialmente por John Resig, que permite simplificar la manera de interactuar con los documentos HTML, manipular el árbol DOM, manejar eventos, desarrollar animaciones y agregar interacción con la técnica AJAX a páginas web”.

* **Jazmin Garcia** (2) [992691](https://platzi.com/comentario/992691/) 

	Yo inicié aprendiendo y usando jQuery… llegué a pensar que javascript no era tan bueno como jQuery (mi ignorancia XD de no saber que era la base de…). En un proyecto en el trabajo me tocó ver cómo hacían las cosas únicamente con javascript 😮, quedé sorprendida y desde entonces sigo en ese camino de no depender de librerías 😃

	* **Cristian Andrés Córdova Valencia** [992691] (1)

		Hasta que llego React y tocó volver a usar una librería pero mucho mas potente 😄

* **Kenet Chungandro** (2) [979166](https://platzi.com/comentario/979166/) 

	jQuery nace para resolver problemas en navegadores poco evolucionados, fue la bomba en su momento, pero la gente empezó a abusar de él y empezaron a aprender la herramienta en vez del lenguaje en sí. Actualmente JS es lo suficientemente robusto para no depender de librerías.

* **Percy Tejada Donayre** (2) [915203](https://platzi.com/comentario/915203/) 

	A todo esto: Podemos desarrollar cualquier proyecto sin depender de frameworks o librerías de Javascript?

	* **MiguelBonifaz** [915203] (2)

		Hola Ppjcode…! Eso depende, por decir, yo en un proyecto estoy usando una librería de jquery que me proporciona un DatePicker para una webapp donde se hará reservaciones en linea, este datepicker me deja manipular muchos aspectos como por ejemplo: Desactivando días, cambiar idioma etc, a veces no tenemos que reinventar la rueda, pero en el caso de manipular el DOM, si sería bueno hacerlo con Javascript en vez de jquery, o hacer un menú con su submenu, cae bien hacerlo solo con js en vez de Jquery, todo va a depender del caso en el que te encuentres, no debes limitarte siempre a que todo debe ser js y no librerías, saludos…!

* **Carlos Roberto Villatoro Barrios** (2) [892811](https://platzi.com/comentario/892811/) 

	Yo estoy siguiendo la carrera de arquitectura frontend, para así poder cumplir una de las metas de convertirme en desarrollador web. (“No seré dependiente de jQuery”)

* **Jonathan Uriel Jiménez Soveranes** (2) [799682](https://platzi.com/comentario/799682/) 

	Precisamente, en eso ando, a vece no veo la diferencia entre JS y jQ! 😦

* **Jean Lucas Castillo Vasquez** (2) [785646](https://platzi.com/comentario/785646/) 

	No sé que tan importante sea tomar este curso de jQuery para avanzar con React pero para salir de dudas lo tomo

	* **webrevolution** [785646] (1)

		tamos en las mismas

	* **Daniel Esteves** [785646] (1)
Hola! Lo importante es si tienes un background en jQuery y estás pasando a JS Vanilla el curso te ayudará a tener una transición más amena; ya que React es JavaScript puro

* **Jose Antonio Sarria Garcia** (2) [622572](https://platzi.com/comentario/622572/) 

	Mi historia es ya inicie el Frontend en JavaScript y por temas laborares estoy aprendiendo JQuery y con la introducción que dio el profe Leonidas se que JavaScript es la base

* **luis-fernando-yupanqui-taco** (2) [561366](https://platzi.com/comentario/561366/) 

	La historia de jQuery  
	jQuery es una librería de JavaScript que hizo su lanzamiento en el año 2006 con el fin de resolver diferentes problemáticas:
	
	• Una misma forma de acceder al DOM.  
	• Poder interactuar con datos de un servidor.  
	• Crear animaciones.
	
	El problema con jQuery surgió cuando se empezó a exagerar su uso y darle menor importancia a aprender JavaScript. Esto genero malos hábitos de aprendizaje y hasta en algunos casos no diferenciar jQuery de JavaScript.
	
	A la par de que algunos se quedaban en jQuery, estaba ocurriendo la revolución de JavaScript trayendo consigo librerías que resolvían problemas específicos.
	
	Aun con estas nuevas librerías, seguía sin resolverse el problema de hacer que uno aprenda mas JavaScript que librerías o frameworks pues uno igual puede abusar de Vue, React o Angular.
	
	Por eso en este curso aprenderás a NO depender de ninguna librería, las ventajas de esto son:
	
	• Reutilizar conocimientos.  
	• Poder implementar soluciones sin dependencias.  
	• Estar más capacitado para las grandes empresas.

* **pablojorgeandres** (2) [482909](https://platzi.com/comentario/482909/) 

	## La historia de jQuery
	
	jQuery es una librería de JavaScript.  
	Cada librería resuelve un problema específico.
	
	Las ventajas de jQuery fueron:
	
	  * Una única forma de acceder al DOM de manera omogenea;
	
	Anteriormente se accedía al DOM por medio de selectores. Pero en esta época de incompatibilidad de browser y de Internet Explorer las formas de embeber JavaScript en nuestro código era diferente para cada browser.  
	jQuery unificaba esta forma.
	```           $.('selector')
	        
	```
	
	  * Interactuar con datos del servidor
	
	jQuery implementa Ajax (XML-HTTP-Request) para interactuar con servidores.  
	También ayudó a trabajar con la incompatibilidad de los selectores.
	```           $.ajax()
	        
	```
	
	  * Animaciones
	
	Hacer animaciones era algo muy complejo también por la incompatibilidad de browsers y css tampoco tenía todas las funcionalidades que hoy tiene.
	```           $.animate()
	        
	```
	
	Con el paso del tiempo y de la implementación de jQuery comenzaron a aparecer “plugins” para esta librería que todavía facilitaban más las cosas.  
	Pero esto trajo la complicación de que nuestro código se mezclaba a tal punto que no se entendía qué era jQuery y qué era JavaScript, y con esto, la dificultad de hacer modificaciones en el mismo.
	
	Luego de esto aparecieron librerías para problemas específicos como **BackBone.js** que se apollaba sobre **underscores.js** para la funcionalidad y sobre **jQuery** para otras cosas.  
	Las tecnologías evolucionaron.  
	Actualmente han aparecido otros frameworks y ecosistemas más robustos como _REACT, ANGULAR o VUE_.
	
	Pero al fin y al cabo todo este sistema y nuevas tecnologías se apoyan en JavaScript Vanilla o en puro JavaScript.
	
	**Ventajas de Aprende JavaScript**
	
	    * Reutilizar conocimiento en otros lados de tu aplicación
	    * Poder implementar soluciones sin depender de una librería
	    * Estar más capacitado para las grandes empresas

* **leotangram** (2) [482468](https://platzi.com/comentario/482468/) 

	Han Pasado 10 años! 😄 10YearsChallenge!

* **JanethM** (2) [452340](https://platzi.com/comentario/452340/) 

	vaya no me habia puesto a pensar pero es muy cierto lo que dice Leonidas, el año pasado apoye en un proyecto y era una mezcla entre javascript y jquery a tal grado que no se lograba diferenciar cual era cual 😮

* **gosunkugi** (2) [384290](https://platzi.com/comentario/384290/) 

	    1. Tan cute el profesor
	    2. Recuerdo que antes de usar jQuery, por allá por el 2008, yo usaba una librería que se llama prototype y … otra que no me acuerdo que era especialmente para animaciones, y conocí jQuery y me voló la mente!
	    3. ¡Exacto! lo que dice el profe es muy cierto… de hecho acabo de venir del curso de fundamentos del profe Sasha… cuando ví que invocó jQuery me dió alergia mental y corrí a este curso. Muy a la expectativa ò__ó
	    4. ¿Ese fondo dónde es? ¿Bogotá?

* **Jonatan David Padilla** (2) [344090](https://platzi.com/comentario/344090/) 

	Unos de mis objetivos y creo que para muchos lo es, es poder implementar todas la funcionlidades que tienen los frameworks y librerias con javaScript puro.  
	espero de corazon lograrlo.

* **Virginia Velásquez Soto** (2) [335472](https://platzi.com/comentario/335472/) 

	Que lastima! no tiene subtitulo, hace mucho que pedir un favor. No lo hizo nada… No tienes segundo tiempo??? Hay software subtitulo, es facíl.

	* **Diego Alexander Forero Higuera (Platzi)** [335472] (4)

		Muchas gracias por tu sugerencia, vamos a tenerla en cuenta para agregar subtitulos a todos los cursos. Es un trabajo complejo ya que los sistemas de subtitulación automáticos no son muy fiables ya que tienen que ser entrenados.

	* **Miguel Matos** [335472] (1)

		Sería genial si pudiera participar en ese proyecto de realizar transcripciones. La única dificultad es tener acceso a los videos mientras dure ese proceso. Bien puede ser que lo creen primero con un software automatizado, y luego hacer curacion de los textos obtenidos, ahi es donde podrian obtener apoyo de personas, y me gustaria ingresar a ello, si no es mucho pedir.

* **Juan Manuel Viveros Martinez** (2) [335281](https://platzi.com/comentario/335281/) 
Actualmente uso mucho jQuery, pero requiero pasarme a un framewor estoy entre angular y vue alguna recomendación??

	* **Luis Angel Vásquez Nepomuceno** [335281] (8)

		React 💙

	* **Luis Antonio Schmiel Paredes** [335281] (4)

		entre aguar y vue te recomiendo react jaja

	* **jsechirinos** [335281] (4)

		El ecosistema de React, es genial…  
		Después si te vas al desarrollo mobile con ReactNative…

	* **Oscar Barajas Tavares (Platzi)** [335281] (4)

		Te recomendaría React para proyectos con un grado de crecimiento.

	* **Juan David Castro (Platzi)** [335281] (5)

		Vue es el más rápido y “sencillo” de aprender. Incluso encuentras mucha información sobre #jQuery2Vue 😛
		
		Pero para proyectos más grandes y proyectos creo que es mejor opción React, es la librería que usan Netflix, Airbnb y muchas otras empresas grandes 👌

	* **Juan Manuel Viveros Martinez** [335281] (3)

		Muchas gracias a todos por responder, ahora tengo una pregunta al momento de hacer maquetaciones complejas cual es mas eficiente o mas adaptable para hacer este tipo de maquetaciones? gracias. algunos me dicen que angular es muy complejo.

	* **Oscar Barajas Tavares (Platzi)** [335281] (3)

		No es cual es mejor, te recomiendo utilizar Atomic design  
		<https://platzi.com/blog/por-que-atomic-design/>

	* **Oscar Barajas Tavares (Platzi)** [335281] (3)

		Olvidar mencionar que esta metodología la puedes utilizar tanto en React, Vue, Angular.

	* **Yefferson Guarnizo** [335281] (1)

		pues ya seria el que mas te guste, y al que mas te adaptes, personalmente te recomendaría vue.js, por su simpleza y poder. pero react es muy potente también.

	* **tomas-alegre146** [335281] (1)

		Angular siempre 😃

* **jlquichimbo** (2) [45173](https://platzi.com/comentario/416597/) 
La transición de JQuery a Javascript está ligada a la nueva versión de la librería Bootstrap 4? Puesto que han quitado todo el Jquery de ...

	* **Jcion** [45173] (4)

		Está ligada a que JS se volvió más poderoso 😛

* **carlosextra1** (1) [1114080](https://platzi.com/comentario/1114080/) 

	Excelente clase. Jquery es genial! Y vale la pena siempre recordarlo!! Pero ahora hay que ir un paso mas adelante con vanilla!

* **Sara Galván Ortega** (1) [1090663](https://platzi.com/comentario/1090663/) 

	Excelente clase!!! Sí es muy común, que uno a veces toma una herramienta que ya está pero que cuando necesita personalizarla, se mete en un lío!

* **33andres33** (1) [1076072](https://platzi.com/comentario/1076072/) 

	otro mas para la carrera de fronted

* **Wandy Rafael Santana Evangelista** (1) [1070771](https://platzi.com/comentario/1070771/) 

	Emocionado de aprender completamente JS Vanilla.

* **Christian Erik Velázquez Morales** (1) [965159](https://platzi.com/comentario/965159/) 

	Hoy en día, leo mucho acerca de “javascript vanilla”. Al principio creía que, se trataba de una “moda”, el que tendrías que aprender javascript puro. Ahora, esta introducción, me respondió las dudas.

* **ronaldjgarciav** (1) [917817](https://platzi.com/comentario/917817/) 

	Excelente 😄 siempre he pensado que es importante son las bases para crear un aprendizaje realmente solido

* **Ambar de Alejandria** (1) [899573](https://platzi.com/comentario/899573/) 

	Buena, me acorde de ahi por el 2012 yo usaba jQuery sin saber qué era de javascript y qué cosas eran de jQuery pero lo veia normal pensaba que asi se movia el mundo, pasaron los años y abandone el camino web para mudarme a las tecnologias mobiles y veme ahora dee regreeso al mundo de javascript con este tremendo curso.

* **Santiago Botero Ruiz** (1) [893272](https://platzi.com/comentario/893272/) 

	En lo personal creo que me pasó también un poco lo que cuenta Leonidas en la historia, aunque bien aprendí a trabajar con Angular y no dependía de jQuery, aun sentía que tenía huecos de conocimiento en cuanto a Js y acá estoy aprendiendo y haciendo el cambio.

* **jacamachoc** (1) [886458](https://platzi.com/comentario/886458/) 

	He usado muy poco Jquery, espero aun asi este curso sea de ayuda, quiero especializarme en JS

* **lmoran** (1) [885740](https://platzi.com/comentario/885740/) 

	Muy interesante la historia vamos a aprenderlo pero no a depender de ello.

* **Roberto Ocando Morales** (1) [868451](https://platzi.com/comentario/868451/) 

	Muy necesaria para mi esta aclaración, ahora entiendo mucho mas sobre el código js que he leido.

* **Cristian Caraballo** (1) [862712](https://platzi.com/comentario/862712/) 

	🤘

* **Christian David Sánchez** (1) [808914](https://platzi.com/comentario/808914/) 

	Excelente, igual yo al inicio me enfoque a aprender jQuery antes que JS.

* **J Daniel Rodriguez Davis** (1) [805200](https://platzi.com/comentario/805200/) 

	Exelente. yo no sabia que era jquery haha gracias profe! haha

* **Jonathan Uriel Jiménez Soveranes** (1) [799672](https://platzi.com/comentario/799672/) 

	Buena info

* **John Steven Bernal Gonzalez** (1) [740615](https://platzi.com/comentario/740615/) 
jQuery

* **FrederickO** (1) [718561](https://platzi.com/comentario/718561/) 

	JQuery sin lugar a dudas fue una parte importante en el pasado, aun hoy en día, siguen habiendo paginas con JQuery y se siguen manteniendo, ahí radica la importancia de entenderlo, pero enfocándonos a profundizar en el conocimiento profundo de JS.

* **darwistg** (1) [706867](https://platzi.com/comentario/706867/) 

	Yo aprendí primero JQuery y me paso que luego no diferenciaba JQuery de javascript. sin duda este curso me servirá.

* **Camilo Andrés Santana Lizcano** (1) [683958](https://platzi.com/comentario/683958/) 

	Despues de conocer esta gran historia tengo más ganas de conocer las bases de jQuery

* **Carlos Huarcaya** (1) [681909](https://platzi.com/comentario/681909/) 
Recuerdo cuando inicie con PHP, php puro, despues un tiny framework y seguir con laravel :)

* **ponchout** (1) [678157](https://platzi.com/comentario/678157/) 

	Excelente explicación, siempre tuve la duda de si JQuery era una librería de Javascript.
	
	5 estrellas

* **Renier Ferrer** (1) [669466](https://platzi.com/comentario/669466/) 

	Después de ver este video, tengo la certeza de haber tomado la decisión correcta. Gracias Leonidas

* **Gustavo83** (1) [621092](https://platzi.com/comentario/621092/) 

	La verdad no me veo programando sin librerías , así de dependiente =(

* **luis-fernando-yupanqui-taco** (1) [587345](https://platzi.com/comentario/587345/) 

	Esto me huele a los plugin de wordpress

* **Alejandro González Reyes** (1) [574854](https://platzi.com/comentario/574854/) 

	Yo conocí jQuery de la mano del profesor jesús cónde en el 2011, pero no fué hasta el 2013 que lo apliqué para el desarrollo de un proyecto en la Maestría.  
	Por un tiempo si me hice dependiente de la librería, pero al abordar frameworks como angular y Vue. Me dí cuenta que aprender las bases del lenguaje era obligatorio.  
	Y mejor aun cuando conocí nodejs, el trabajar con frameworks como express, hacen que si o si aprendas javascript.

* **Xappie** (1) [571973](https://platzi.com/comentario/571973/) 

	Espectacular introducción, ame cada minuto de esto y me siento plenamente identificado con lo que dices, a poner mucha atención y a seguir progresando.

* **Félix Alejandro Zelaya Orellana** (1) [526505](https://platzi.com/comentario/526505/) 

	Genial!!

* **Brayan Jeshua** (1) [520219](https://platzi.com/comentario/520219/) 

	Tienes toda la razón soy un desarrollador que me ha dado de cuenta con el tiempo de que tengo pocos conocimientos pero gracias a reconocer eso estoy aprendiendo las cosas cada vez mejor al lado de platzi.

* **pablojorgeandres** (1) [482915](https://platzi.com/comentario/482915/) 

	 **En esta historia, ¿Dónde estás?**
	
	_ Es evidente que las tecnologías se apoyan en el código puro de los lenguajes y también que van cambiando bastante rápido.  
	Por eso me parece que entender los lenguajes de programación en profundidad será siempre la clave para entender cualquier tipo de problemática en diferentes tecnologías.

* **Martin Silva Flores** (1) [468726](https://platzi.com/comentario/468726/) 

	Estoy de acuerdo con lo que se menciona en el video. Toda exageración con el uso de las librerias plugin u otros es nociva. en el sentido de que nos crean dependencia. Por lo tanto, es necesario ser equilibrados en el uso de estos.

* **JuanDAC** (1) [466081](https://platzi.com/comentario/466081/) 

	A mi nunca me gustó jQuery prefería hacerlo todo con vainilla Javascript / sólo Javascript, aparte me parecía código espagueti y eso que era bien joven y estaba aprendiendo js nunca le dedique tiempo a jQuery aún así sabiendo su potencial. Tampoco quise aprender algunas librerías de css por que tenían jQuery.  
	Tomo está curso para saber aún más de Javascript y no de jQuery apesar que he aprendido bastante en Javascript pero nunca está de más

* **Jean Manzo** (1) [460089](https://platzi.com/comentario/460089/) 

	jQuery ha hecho mi vida más fácil durante los años que tengo como desarrollador. Incluso, se siente el nivel de dificultad menor ante ciertas necesidades. Algunas cosas se solucionan mucho mejor y más limpiamente que con Javascript solo. La verdad que siempre veo muchos hacer bullying a los que usan jQuery, pero ha sido importante en mi vida de desarrollador.

* **IvanAlexO** (1) [455962](https://platzi.com/comentario/455962/) 

	Lo que sucede hoy en día es que el pensamiento de muchos es decir que ya no se tiene que reinventar la rueda, es decir, que ya todo está hecho, hablando en específico del área de desarrollo, existen muchas librerías, como menciona Leonidas, estas ayudan a resolver o facilitan hacer algo, haciendo que el desarrollador se haga dependiente de ello sin conocer la lógica detrás de esas librerías. Es bueno conocer cómo funcionan las cosas y así demostrar que hay mucho que aportar, mejorar e innovar.  
	Saludos.

* **santiagoPalacios** (1) [440763](https://platzi.com/comentario/440763/) 

	Creo que la mayoría de los que estamos aquí sabemos que jquery esta muriendo y venimos a hacer el duelo de una vez…

* **jlquichimbo** (1) [416594](https://platzi.com/comentario/416594/) 

	Dónde estoy yo:  
	Lamentablemente aprendí JQuery antes que Javascript y por la misma razón que menciona Leonidas me volví dependiente y me costaba diferenciar el uno del otro.  
	Ahora que tengo que dejar JQuery puesto que como dijo Freddy en uno de sus vídeos: “Es como terminar con tu novia, sabes que fue muy bonito pero que no se puede continuar”. Es necesario aprender para mi y mi equipo a usar Javascript Vanilla porque además todos los navegadores están lanzando alertas en la consola: “Dejá de utilizar Ajax por Dios, supéralo”, bueno algo así dice el mensaje.  
	Agradezco el curso Leonidas, eres de los mejores tutores y este curso será de gran ayuda para nuestro siguiente proyecto.

* **ivan_acg** (1) [415900](https://platzi.com/comentario/415900/) 

	Angular o React?

	* **Jair Israel Avilés Eusebio** [415900] (1)

		Depende de tus necesidades, no hay mejor framework.

* **Santiago Patiño** (1) [414475](https://platzi.com/comentario/414475/) 

	Espero dejar de ser tan dependiente de JQuery.

* **Ernesto Cruz Bravo** (1) [411348](https://platzi.com/comentario/411348/) 

	Si queremos ser desarrolladores JS profesionales  
	debemos aspirar a ser como los desarrolladores de éstos frameworks/librerias  
	y no solo aspirar a aprender a usar estos frameworks/librerias.

* **Alex Fabian Moreno Acero** (1) [407763](https://platzi.com/comentario/407763/) 

	que buen video

* **Jesus Ricardo Moreira Cedeño** (1) [399624](https://platzi.com/comentario/399624/) 

	Este video me motivó :’)

* **José Iván Sandoya** (1) [397073](https://platzi.com/comentario/397073/) 

	Qué buena explicación Leonidas!  
	Este curso va a ser legendario 👏🏻

* **alejandromarcano** (1) [397067](https://platzi.com/comentario/397067/) 

	Lo último que utilicé de Jquery fue para desarrollar sistemas de información en la universidad, usaba los plugins para deslizar menus y ejecutar animaciones. Ahora, a aprender másdel nuevo y poderoso javascript

* **DiegoKardona** (1) [395667](https://platzi.com/comentario/395667/) 

	Puedo tomar este curso para empezar desde casi cero en Javascript?

	* **davidtoca (Platzi)** [395667] (1)

		te recomiendo primero tomar el curso de programación básica, servido por el curso de fundamentos de javascript, después de eso sí te recomiendo que tomes este cursoy

	* **DiegoKardona** [395667] (1)

		Ah okey, muchas gracias, entonces empezare por el de fundamentos de Javascript

* **Andrés Madrigal** (1) [386408](https://platzi.com/comentario/386408/) 
Excelente clase!!!

* **Julián Fernando Amaya Díaz** (1) [379087](https://platzi.com/comentario/379087/) 

	Todos los días uso jQuery y en parte puedo decir que es una de las tecnologías que mas me ha generado ingresos. Gracias por esta lección Leonidas. Los fundamentos de tú explicación me han motivado mucho a seguir este curso, conocer el poder del JavaScript puro, y abrir la mente a nuevos conocimientos.

* **Iván Mauricio Toro Cifuentes** (1) [371023](https://platzi.com/comentario/371023/) 

	El enfoque del curso de: _no depender de ninguna librería_ me agrada mucho, es importante ( **mucho** ) usar librerías especificas que nos ayuden hacer cosas que normalmente nos tomen mucho tiempo, pues como desarrolladores la reutilización de código es una característica importante, sin embargo no conocer la estructura del lenguaje base y solo depender de las librerías no es algo que hagan los devs pro.

* **ClareRodri** (1) [367872](https://platzi.com/comentario/367872/) 

	Excelente presentación, hasta el momento no me había preguntado sobre la historia de jQuery, creo que entré en esa parte donde aprendes a usar el framework y no conoces (no quieres o no te interesa) otra manera de hacerlo. Nada mejor que aprender buenas prácticas.

* **Alan Santiago** (1) [361866](https://platzi.com/comentario/361866/) 

	Ya tome el de PostCSS 😄

* **Alan Santiago** (1) [361857](https://platzi.com/comentario/361857/) 

	Siempre habia querido aprender jQuery. Creo que ahora no es necesario Jajaja 😄

* **Joel Alejandro Morales García** (1) [359570](https://platzi.com/comentario/359570/) 

	JQuery era o es tan utilizado que muchísimos proyectos en la industria siguen corriendo con esta libreria

* **Kong Chen** (1) [357758](https://platzi.com/comentario/357758/) 

	Estoy totalmente de acuerdo, gracias a todas esas librerias nos hemos vuelto dependientes a ello y por lo tanto dejamos de aprender a desarrollar algo desde cero y cuando queremos algo propio o personalizado no sabemos como sin usar esas librerias.

* **Jose Enrique Marquez** (1) [356881](https://platzi.com/comentario/356881/) 

	Jquery fue la primera libreria que utilice para comenzar a programar y me ayudo mucho para crear un proyecto para la universidad. Cuando la descubri toda mi forma de hacerlo habia cambiado completamente.

* **Ivan Fuentes** (1) [353808](https://platzi.com/comentario/353808/) 

	Que nos permite/ayuda hacer Jquery:
	
	  * Una unica forma de acceder al DOM-> $(‘selector’)
	  * Interacturar con datos del servidor -> $.ajax()
	  * Animación -> $.animate()

* **David Daniel Castillo Nava** (1) [349715](https://platzi.com/comentario/349715/) 

	Opino que se puede usar la librería pero como menciona Leonidas. Hay que aprender el lenguaje en que se hizo la librería y luego que seas experto en el. Pues usa la librería que quieras o el framework que mas te guste.

* **eltenis** (1) [345361](https://platzi.com/comentario/345361/) 

	como juntas el frontend con el backend??? con php? a mi me gusta javascript pero parace que tengo que aprender php pq wordpress… es muy bueno para vender a mis clientes… pq como vender webs para clientes?  
	con javascript y firebase’?

	* **Diego Alexander Forero Higuera (Platzi)** [345361] (2)

		Necesitas un lenguaje de backend ya sea php, python, ruby, java, JavaScript (con node).

* **Oscar Barajas Tavares (Platzi)** (1) [338806](https://platzi.com/comentario/338806/) 

	Yo empece a utilizar JQuery por el año de 2007 aproximadamente, aun debo de tener el código de un proyecto por eso recordar el año.

* **Joss** (1) [335519](https://platzi.com/comentario/335519/) 

	Lo que queda claro con la explicación de leonidas este framework es de mucha ayuda, pero se debe usar de la forma adecuada para lo que fue creado sin depender demasiado de ella ya que tener demasiados pluyins es todo un problema a la larga, adquiriendo malos hábitos de aprendizaje.

* **Alberto Gómez Juan** (1) [76322](https://platzi.com/comentario/885255/) 
A parte de los proyectos que fueron creados con jQuery y deben ser mantenidos hoy en día ¿Sigue siendo útil jQuery para nuevos desarrollo...

	* **Manuel Ojeda** [76322] (2)

		Para proyectos nuevos es preferible utilizar VanillaJS, JS ha mejorado muchísimo en comparación de los tiempos de jQuery.  
		Que además también evitaremos problemas de compatibilidad en diferentes frameworks si usamos únicamente Vanilla.

* **Gustavo83** (1) [61395](https://platzi.com/comentario/621083/) 
Consulta tonta, JQuery ya fue totalmente reemplazado por Javascript o se sigue utilizando?

	* **Ana Lima (Platzi)** [61395] (5)

		¡Hola Gustavo!
		
		Jquery es una de las librerías más usadas por los desarrolladores web en la actualidad, pero para nadie es un secreto que es lenta y añade bytes adicionales a la carga de nuestro sitio por la que muchos dicen que es totalmente innecesario su uso.
		
		Muchas veces Jquery es mal utilizado solo para asignar un evento a unos botones o colocar alguna animación a algún elemento, cosas que podemos hacer con simple JavaScript sin escribir mucho más código y sin necesidad de importar librerías.

* **DiegoKardona** (1) [43286](https://platzi.com/comentario/395667/) 
Puedo tomar este curso para empezar desde casi cero en Javascript?

	* **davidtoca (Platzi)** [43286] (1)

		te recomiendo primero tomar el curso de programación básica, servido por el curso de fundamentos de javascript, después de eso sí te recomiendo que tomes este cursoy

* **Gonzalo Iván Peñaranda Araque** (0) [371793](https://platzi.com/comentario/371793/) 

	En esta historia ando totalmente perdido, o más bien, soy un novato en Javascript/Jquery y demás librerías y frameworks similares.
	
	Qué bueno que estos cursos existan.

## 0030. Presentación del proyecto [12287](https://platzi.com/clases/1316-jquery-js/12287-presentacion-del-proyecto3022/)

### Descripción:


Vamos a ver el proyecto en el que estaremos trabajando durante este curso. Te voy a entregar la maquetación previamente hecha y estaremos trabajando con JS para dar dinamismo a la página.

Para este curso primero vamos a hacer todo esto en Vanilla JS, y te esteremos dando consejos de cómo hacer estos ejercicios con Jquery.

### Comentarios:

* **Sergio Minei** (18) [335008](https://platzi.com/comentario/335008/) 

	Platzi Video sin react! o.O!

	* **jsechirinos** [335008] (4)

		Esto se va descontrolar…!!!

	* **Melina Jacqueline onoriaga** [335008] (2)

		jajajaja pero es un curso de javascript peladito, por eso jajaja

	* **jesus-gallardo** [335008] (2)

		😮

* **Luis Fernando Valladares Castro** (12) [552485](https://platzi.com/comentario/552485/) 

	Hola a todos, soy un viejo alumno del curso y cree el _proyecto_ de 0 y detalle a detalle como se presento y desarrollo a lo largo del curso más pequeñas correcciones y ciertas mejoras.
	
	Me alegra poder compartirlo para que puedan _utilizarlo como referencia_ a lo largo del curso y comentarles que cuenta con su _repositorio de GitHub_ para que puedan clonarlo.
	
	Pagina Web: <https://luisvalladaresc.github.io/platzi-video/>  
	Repositorio: [https://github.com/luisvalladaresc/platzi-video](https://github.com/LuisValladaresC/platzi-video)  
	![](https://static.platzi.com/media/porfilio/platzi-video_5954d8c1-a95e-4bff-a6d1-9883cd4f0db2.PNG)

* **.** (9) [334979](https://platzi.com/comentario/334979/) 

	Falta que tengamos Platzi Video en Angular y Vue!

	* **Oscar Barajas Tavares (Platzi)** [334979] (2)

		¡Seria un buen reto!

	* **.** [334979] (2)

		Si o que? Como tu curso de testing! Ya rifate 😄

	* **Yefferson Guarnizo** [334979] (1)

		uy siiii yo quiero 😄

* **Michael  Bolaños Vargas** (6) [357264](https://platzi.com/comentario/357264/) 

	+1 por `localStorage.clear();`

	* **pablojorgeandres** [357264] (1)

		Sí, en todas las clases se aprende algo, está buenísimo.

* **luismjaquez** (5) [335051](https://platzi.com/comentario/335051/) 

	Se puso buena la cosa.

* **JheysonEGalvis** (3) [1064521](https://platzi.com/comentario/1064521/) 

	Tener la oportunidad de ver el proyecto antes de crearlo motiva mucho 😃

* **Iván Mauricio Toro Cifuentes** (3) [371029](https://platzi.com/comentario/371029/) 

	Vanilla JS = JS puro

* **erickblanco** (3) [337732](https://platzi.com/comentario/337732/) 

	interesante los mensajes subliminales de la vaca verde para marketing digital

* **Percy Tejada Donayre** (2) [915214](https://platzi.com/comentario/915214/) 

	Buen proyecto! Los animo a que puedan realizarlo a su propio estilo y colocarlo en su portafolio.  
	Manos a la obra!

* **Camilo Alexander Velandia Velandia** (2) [858467](https://platzi.com/comentario/858467/) 

	buen proyecto

* **roDRYgo_0** (2) [631140](https://platzi.com/comentario/631140/) 

	Todo un master

* **sergioandresmaurysterling** (2) [625496](https://platzi.com/comentario/625496/) 

	vamos con toda curso!!!

* **Jhon Alexander Perez Valencia** (2) [597474](https://platzi.com/comentario/597474/) 

	 ** _Esto se va a DESCONTROLAR_**

* **Micael Martinez** (2) [488699](https://platzi.com/comentario/488699/) 

	localStorage.clear();  
	Esa línea de código es para borrar la caché de la página?

	* **Jair Israel Avilés Eusebio** [488699] (4)

		Ese metodo es para borrar el contenido del objeto _Storage_ del tipo **localStorage**. Hoy en dia, en los navegadores modernos se tiene definido un API de para almacenar datos de manera mas intuitiva que el uso de cookies. Te recomiendo leer el siguiente articulo para mayor informacion.
		
		[API de almacenamiento web - MDN Mozilla  
		](https://developer.mozilla.org/es/docs/Web/API/API_de_almacenamiento_web)

* **pablojorgeandres** (2) [482959](https://platzi.com/comentario/482959/) 

	JavaScript purito!  
	Sí señor!

	* **Nicolas Esteban Prieto Sarmiento** [482959] (2)

		jajaja

* **santiagoPalacios** (2) [440765](https://platzi.com/comentario/440765/) 

	Estaría bueno que cuando dejen archivos para descargar incluir un link de github para clonarlo.  
	Saludos!

	* **Jair Israel Avilés Eusebio** [440765] (3)

		El repositorio de Github viene en la descripcion de la primera clase de este curso, de cualquier forma lo encuentras en la siguiente URL
		
		<https://github.com/LeonidasEsteban/jquery-to-js-curso>

* **edesalla17** (2) [342000](https://platzi.com/comentario/342000/) 

	pinta bueno

* **carlosextra1** (1) [1114130](https://platzi.com/comentario/1114130/) 

	Todo se ve muy bien! y vamos a codear!!!

* **carlosextra1** (1) [1114116](https://platzi.com/comentario/1114116/) 

	```    localStorage.clear()
	    
	```

* **33andres33** (1) [1076136](https://platzi.com/comentario/1076136/) 

	excelente

* **luistua** (1) [1027560](https://platzi.com/comentario/1027560/) 

	Gracias por no sólo enseñar el jquery sino mostrar el javascript puro que lo hace posible. Con la introducción anterior ya me había desanimado un poco pero con este video me regresó el ánimo-

* **Cristian Caraballo** (1) [919307](https://platzi.com/comentario/919307/) 

	¿En que curso desarrolla el diseño?

	* **DanielVM19** [919307] (2)

		Es unos de los proyectos de css grid

* **Ruben Padilla** (1) [830181](https://platzi.com/comentario/830181/) 

	💲 👋
	
	💲✅ Repositorio del curso: [https://github.com/LeonidasEsteban/jquery-to-js-curso](url)
	
	💲./ HappyCode

* **Christian David Sánchez** (1) [809448](https://platzi.com/comentario/809448/) 

	😳😳 que buen proyecto.

* **J Daniel Rodriguez Davis** (1) [805201](https://platzi.com/comentario/805201/) 

	Esto se va a poner bueno!

* **webrevolution** (1) [801464](https://platzi.com/comentario/801464/) 

	donde esta la maquetacion?

	* **Ruben Padilla** [801464] (1)

		💲 👋
		
		💲✅ Repositorio del curso: <https://github.com/LeonidasEsteban/jquery-to-js-curso>
		
		💲./ HappyCode

* **carlossotoperez** (1) [728388](https://platzi.com/comentario/728388/) 

	Se ve interesante el curso con esta presentación !!!

* **benitezconzeta** (1) [695563](https://platzi.com/comentario/695563/) 

	Que buena presentación!!! Un Crack el profe!!!

* **Byron Miguel Piedrahita Hernandez** (1) [653148](https://platzi.com/comentario/653148/) 

	Haciendo caso a los que saben 🙌

* **sam-rojas** (1) [547160](https://platzi.com/comentario/547160/) 

	A por ello

* **Enrique Mont** (1) [538297](https://platzi.com/comentario/538297/) 
	
	![](%5Burl%5D\(%60url%60\))

* **Félix Alejandro Zelaya Orellana** (1) [526508](https://platzi.com/comentario/526508/) 

	Que genial!!

* **crismoreno** (1) [448902](https://platzi.com/comentario/448902/) 

	Qué interesante proyecto, me siento como un futuro desmovilizado de jquery y reinsertado al mundo original de javascript

* **Jesus Ricardo Moreira Cedeño** (1) [399791](https://platzi.com/comentario/399791/) 

	😃

* **David Vargas Domínguez** (1) [376429](https://platzi.com/comentario/376429/) 

	buaaa, tío, esto va a ser genial. Gran proyecto!

* **Joel Alejandro Morales García** (1) [359601](https://platzi.com/comentario/359601/) 

	Genial, platzi video everywhere 😃

* **Ivan_Romero** (1) [354753](https://platzi.com/comentario/354753/) 

	Excelente!!!

* **viciotec** (1) [351557](https://platzi.com/comentario/351557/) 

	Como podemos pasar todos los plugins de Jquery a Javascript?

	* **Diego Alexander Forero Higuera (Platzi)** [351557] (4)

		Tienes que reescribir el plugin eliminando el uso de jQuery y usando todo con vanilla js.

* **David Daniel Castillo Nava** (1) [349716](https://platzi.com/comentario/349716/) 

	Creo que usare lo aprendido en el curso de expresiones regulares en ese buscador!! **o.O!!!**

* **Codehero** (1) [345099](https://platzi.com/comentario/345099/) 

	Todo está bien excepto cuando Leonidas hace zoom in y zoom out a la pantalla XD, 😕

* **Juan David Castro (Platzi)** (1) [340973](https://platzi.com/comentario/340973/) 

	Uff, se ve super el proyecto!: :tad

* **fersot7** (1) [336437](https://platzi.com/comentario/336437/) 

	Excelente el proyecto…

* **jesus gutierrez gonzalez** (1) [335951](https://platzi.com/comentario/335951/) 

	como podrias hacer algo similar a lo que hace dataTables de jquery usando solo javascript

	* **jesus gutierrez gonzalez** [335951] (1)
como podrias hacer algo similar a lo que hace dataTables de jquery usando solo javascript

	* **viciotec** [335951] (1)

		Me gustaria saber como pasar todo tipo de plugins

# De jQuery a JavaScript [2247]

## 0040. Variables y Funciones [12288](https://platzi.com/clases/1316-jquery-js/12288-variables-y-funciones/)

### Descripción:


En la primera parte de este curso vamos a buscar traer datos de un servicio externo, para ello vamos a utilizar la mezcla de varias combinaciones: Promesas, ajax/fecth y funciones asíncronas. Antes de implementar una Promesa debes saber dos cosas necesarias: Variables y Funciones.

Dentro de JavaScript tenemos tres formas de declarar una variable las cuales son: var, const y let.

  * var era la forma en que se declaraban las variables hasta ECMAScript 5.
  * const y let es la forma en que se declaran las variables a partir de ECMAScript 6, const sirve para declarar variables que nunca van a ser modificadas y en -  
cambio let son variables que pueden ser modificadas.

Las funciones son piezas de código que puedes reutilizar y se declaran con la palabra function.

### Comentarios:

* **Sergio Minei** (45) [335019](https://platzi.com/comentario/335019/) 

	## Procesos asíncronos
	
	Un proceso asíncrono es un código que se está ejecutando, pero no ha terminado de ejecutarse antes de que se ejecute un código que está después de él.
	
	Esto permite que la aplicación no se cuelque mientras está ejecutando un proceso muy largo.
	
	## Variables
	
	  * `var` es la forma de crear variables hasta ES5.
	  * `const` es para declarar constantes.
	  * `let` es para crear variables que cambian.
	
	## Funciones
	```     functioncambiarNombre(nuevoNombre) {
	      cambia = nuevoNombre;
	    }
	    
	    //Desde EC6, las funciones se pueden declarar como arrow functions.
	    cambiarNombre = (nuevoNombre) => {
	      cambia = nuevoNombre;
	    }
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **samvarcia** [335019] (5)

		Gran apunte 😃

	* **Oscar Barajas Tavares (Platzi)** [335019] (2)

		Muy clara la explicación.

	* **Sergio Minei** [335019] (2)

		Gracias! Espero les sea de ayuda 😃

	* **Oscar Barajas Tavares (Platzi)** [335019] (2)
Para pasar el examen 😎

	* **tomas-alegre146** [335019] (2)

		buena nota mental

	* **Chunchunmaru** [335019] (2)

		Gracias!

* **Carlos Martínez** (12) [335003](https://platzi.com/comentario/335003/) 

	 **const** varibles que no cambian en el tiempo (constantes)  
	**let** variables que luego podemos cambiar  
	**var** método anterior para definir variables

* **Cristian Orozco Benjumea** (7) [660309](https://platzi.com/comentario/660309/) 

	Chicxs le voy a compartir un tip que encontré hace poco.
	
	Le dan **doble click derecho** sobre el video y escogen la opción
	
	  * Imagen en imagen
	
	Y listo, así podran ver el video mientras van escribiendo su código sin necesidad de estar cambiando entre pestañas.
	
	Espero les sirva
	
	Nota: tambien sirve con YouTube

* **erickblanco** (7) [373620](https://platzi.com/comentario/373620/) 

	un datillo “Ctrl + L” limpia la consola rapido y sencillo

* **samvarcia** (7) [336506](https://platzi.com/comentario/336506/) 

	Si quieres aprender mas sobre funciones <https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Funciones>

* **lmoran** (6) [886509](https://platzi.com/comentario/886509/) 

	Dentro de JavaScript tenemos tres formas de declarar una variable las cuales son: var, const y let.
	
	    1. **Var:** Era la forma en que se declaraban las variables hasta ECMAScript 5. Casi ya no se usa porque es de forma global y tiene las siguientes características:
	  * **Se puede reinicializar** : osea todas las variables se inicializan, por ejemplo:  
	Var pokemonType = ‘electric’ entonces reinicializar es:  
	Var pokemonType = ‘grass’ osea la misma variable con diferentes datos el último dato predomina.
	
	  * **Se puede reasignar:** osea la variable ya inicializada le reasignamos otro valor por ejemplo: inicializamos la variable: Var pokemonType = ‘electric’ ahora la reasignamos pokemonType = ‘grass’ ya no va var
	
	  * **Su alcance es función global** : osea inicializamos la variables pero la podemos llamar desde cualquier bloque (una llave abierta y una cerrada {}) pero hay que tener mucho cuidado con ello ya que puede haber peligro, no es recomendable usar VAR.
	
	**const y let es la forma en que se declaran las variables a partir de ECMAScript 6, **
	
	    1. **const **sirve para declarar variables que nunca van a ser modificadas:
	  * **No se puede reinicilizar:** es una const única no puede haber otra inicializada con el mismo nombre. const pokemonType = ‘electric’ no puede haber:  
	const pokemonType = ‘grass’
	
	  * **No se pude re asignar:** una vez que la hayamos inicializado no la podemos reasignar solo con su nombre: const pokemonType = ‘electric’ no puede ejecutarse:  
	pokemonType = ‘grass’
	
	  * **No es inmutable:** osea puede cambiar con objetos:
	```     let pokemonType = "electric";
	          if (pokemonType === "electric") {
	            const pokemonName = "pikachu";
	            const poke = {
	              name: "pikachu"
	            };
	            poke.name = "raichu";
	    
	```
	
	    1. **let** son variables que pueden ser modificadas se pueden cambiar:
	
	-** No se puede re inicializar:** es un let único no puede haber otra inicializada con el mismo nombre. let pokemonType = ‘electric’ no puede haber: let pokemonType = ‘grass’
	
	  * **Se puede reasignar: ** osea la variable ya inicializada le reasignamos otro valor por ejemplo: inicializamos la variable: let pokemonType = ‘electric’ ahora la reasignamos pokemonType = ‘grass’
	
	  * **Su contexto es de bloque: **solo funciona dentro de un bloque {} fuera de ello no funciona, también podemos hacer esto:
	```     let pokemonType = "electric";
	         if (pokemonType === "electric") {
	            const pokemonName = "pikachu";
	    		pokemonType = “electrico”```
	    
	    Las funciones son piezas de código que puedes reutilizar y se declaran con la palabra function y se llaman con los paréntesis:
	    
	    
	    
	```
	
	let cambia = “@LeonidasEsteban”; /* cambia en el tiempo */
	
	function cambiarNombre(nuevoNombre) {  
	cambia = nuevoNombre;  
	}
	```     
	    
	```

* **Andres Condo** (4) [1015144](https://platzi.com/comentario/1015144/) 

	Me gusta hacer todas las maquetas guiado me con el proyecto de Leonidas, aunque me equivoque(siempre me equivoco en algo) y romper el código, siento que de esa manera veo mas el código, lo reviso y lo analizo.  
	Lo siento mas divertido y educativo. Y se lo recomiendo a todos, no solo hagan copia y pega a todo el código, guíense y háganlo palabra por palabra, así aunque se demoré mas, entenderán mejor el código de otros.

* **Camilo Andrés Santana Lizcano** (4) [684271](https://platzi.com/comentario/684271/) 

	A partir del 2015 ( lanzamiento de ECMAScript 6 ), se ve un poco anticuado usar **var** para definir variables.  
	Es mucho más profesional usar **const** o **let** , que nos permiten tener mayor control del **contexto** de nuestras variables.

* **LeandroVidela** (4) [375325](https://platzi.com/comentario/375325/) 

	Entendiendo VAR:
	```     for(var i = 0; i < 3; i++){
	    	console.log(i);
	    }
	    
	    console.log("Fuera del ciclo For: ", i);
	    
	    // Output
	    // 0
	    // 1
	    // 2
	    // Fuera del ciclo For: 3
	    
	```
	
	**Conclusión:**  
	Al declarar la variable utilizando “var”, el valor de esta se vuelve global y por ello es que tenemos acceso a esta misma ( var i) por fuera del ciclo for.  
	Podemos comprobar esto de la siguiente manera
	```     console.log(window.i); // 3
	    
	```
	
	Cómo hacemos para no contaminar el ambiente? (con VAR)
	```     functionimprimirValor(){
	    	for(var i = 0; i < 3; i++){
	    		console.log(i);
	    	}
	    }
	    
	    imprimirValor();
	    console.log("Fuera del ciclo For: ", i);
	    
	    // Output
	    // 0
	    // 1
	    // 2
	    // Reference Error: i no está definida
	    
	```
	
	**Conclusión:**  
	Al declarar la variable “ **i** ” dentro de la función **imprimirValor** estamos limitando su scope(alcance) al “block scope” de la función **imprimirValor**. El block scope es todo lo que está entre llaves “{ block scope }”
	```     functionimprimirValor(){
	    	BLOCK SCOPE
	    }
	    
	```
	
	**Cómo funciona con LET:**  
	Con ES6 se introduce lo que se llama “block scope”, un scope(alcance) delimitado por un “block”. Un “block” es un conjunto de llaves **{ }** que no necesariamente es una función, puede ser un IF, FOR, Function, etc.
	
	Veamos un ejemplo:
	```     if(true){
	    	let i = 1;
	    	console.log(i);
	    }
	    
	    console.log(i); // Reference error.
	    
	```
	
	El valor de **let i** solo puede ser accedido dentro del block scope del IF().

* **Victor Lozada** (4) [342372](https://platzi.com/comentario/342372/) 

	Si estas viendo este curso sin seber de javascript te recomiendo mucho el curso de fundamentos de javascript !

* **JheysonEGalvis** (3) [1064643](https://platzi.com/comentario/1064643/) 

	"let cambia, const no cambia, así de simple 😃 "

* **urieelmm** (3) [837025](https://platzi.com/comentario/837025/) 

	Realmente nunca he usado muy a fondo jQuery, pero quiero reforzar algunos temas de JavaScript

* **hectorJosuemacias** (3) [659358](https://platzi.com/comentario/659358/) 

	Leonidas es el julio profe de la programación.

* **Jhon Alexander Perez Valencia** (3) [597479](https://platzi.com/comentario/597479/) 

	`CONTROL + L` para limpiar la consola

* **AryRosvall** (3) [472155](https://platzi.com/comentario/472155/) 

	Si no han visto el curso de fundamentos de javascript, no es que var no se utilice más, de hecho la diferencia entre var y let es el scope que cada uno de ellos tiene.
	
	El scope de let es dentro del bloque en el que se encuentra. A diferencia de var que su scope es dentro de la función que lo contiene.
	
	En este ejemplo, let no se imprimirá marcando un error de que la variable no está definida.
	
	Ojito con eso ya que en un ciclo for si se declara la i como let el ciclo no funcionará!!
	```     {
	      letvariable_let = 'valor variable let';
	      var variable_var = 'valor variable var';
	      console.log('var dentro de bloque: ' + variable_var);
	      console.log('let dentro de bloque: ' + variable_let);
	    }
	    
	    console.log('var fuera de bloque: ' + variable_var);
	    console.log('let fuera de bloque: ' + variable_let);
	    
	```

* **Ivan Fuentes** (3) [354358](https://platzi.com/comentario/354358/) 

	**Scope de las variables**  
	¿Que es es? : El scope es el alcance de una variable, puede ser de dos tipos, global y local. Una variable cuyo scope es global se puede acceder desde cualquier parte del código, pero si el scope es local solo se puede acceder desde la función que la contiene. Ejemplo:
	
	**Scope global:**
	```     var x = 1;
	    functionglobal(){
	    	console.log(x);
	    }
	    
	    global(); // 1
	    console.log(x); // 1 
	    
	```
	
	En este caso x es una variable global ya que podemos acceder tanto fuera como por dentro de una función debido a haberla definido fuera de la función
	
	**Scope local: **
	```     functionlocal(){
	       var x = 3;	
	    	console.log(x); // 3
	    }
	    
	    local();
	    console.log(x); //ReferenceError: x is not defined 
	    
	```
	
	En este caso la variable x es local ya que la definimos dentro de la función local(), esto quiere decir que solo podemos acceder a ella dentro de la función. Cuando ejecutamos local() nos va a mostrar el valor de x correctamente, pero si hacemos un console.log(x) fuera de la función nos va a dar error porque x no está definida, para el scope global nuestra variable x no existe

* **Jose Rodriguez Luis** (3) [335190](https://platzi.com/comentario/335190/) 

	var: metodo para definir variables hasta ecma5  
	const y le:t formas de definir variables a partir de ecma6

* **LeoEsp** (2) [993223](https://platzi.com/comentario/993223/) 

	Maquete el proyecto de esta forma, les dejo mi repo por si quieren clonarlo  
	<https://github.com/LeoCode0/PlatziVideoJqueryJS>
	
	Ahora voy a seguir el curso para integrar Jquery 😄
	
	![Screenshot_2020-02-18 PlatziVideo.png](https://static.platzi.com/media/user_upload/Screenshot_2020-02-18%20PlatziVideo-9cbe6a89-a8e2-4436-a55d-24e0c28c3b78.jpg)

	* **sebastian-hernandez-caro** [993223] (1)

		muchas gracias

* **asismelgarejo** (2) [881649](https://platzi.com/comentario/881649/) 

	Tome previamente el curso de Fundamentos de JavaScript

* **Jonathan Uriel Jiménez Soveranes** (2) [799695](https://platzi.com/comentario/799695/) 

	hahah excelente profesor Leonidas!

* **Enrique Felix Garcia Stave** (2) [679121](https://platzi.com/comentario/679121/) 

	 **Anotaciones:**
	
	**const:** Permite almacenar un valor sin permitir cambiarlo redefiniendo o redeclarando  
	**let:** Permite almacenar un valor permitiendo cambiarlo redefiniendo o redeclarando.  
	**function nombreDeLaFuncion(argumentos):** Permite declarar una función con sus respectivos argumentos.  
	**clear():** Permite limpiar la consola del inspector de elementos.

* **Enrique Felix Garcia Stave** (2) [679075](https://platzi.com/comentario/679075/) 

	No hay que olvidar que a pesar de que las constantes no permitan redefinir sus valores, existe la excepcion con los objetos, a los cuales se les pueden cambiar sus propiedades y esto no necesariamente es redefinir.
	```     const datos = {
	      nombre : 'primerNombre',
	      apellido : 'garcia'
	    }
	    
	    datos["nombre"] = 'primerOtroNombre'
	    
	    console.log(datos["nombre"]) // primerOtroNombre
	    
	```

* **maoacrlearn** (2) [590803](https://platzi.com/comentario/590803/) 

	Definitivamemte Leonidas es el Master de masters enseñando

* **DemetrioDelCarmen** (2) [590465](https://platzi.com/comentario/590465/) 

	Tipos de variables:
	
	var: su valor puede ser modificado/cambiar (Usado en ES5)  
	let: su valor puede ser modificado/cambiar (Usado en ES6)  
	const: su valor no puede ser modificado/cambiado (Usado en ES6)

* **Julián Fernando Amaya Díaz** (2) [379109](https://platzi.com/comentario/379109/) 

	Con ES6 es obligatorio usar punto y coma o con el nuevo estándar ya no es necesario ?

	* **juand_silva** [379109] (3)

		No es necesario usar semicolon ( ; ) en JavaScript. Eso dependen de tu equipo de desarrollo cuando utilicen linters como [standar](https://standardjs.com/) que por ejemplo no aceptan el ( ; ) o el linter de [AirBNB](https://github.com/airbnb/javascript) que si lo requiere.  
		Personalmente dejé de usar el semicolon hace tiempo, porque vi muy pocas o ninguna diferencia si se usa o no.

	* **Oscar Barajas Tavares (Platzi)** [379109] (2)

		En mi caso prefiero ponerlo por que puedo darle seguimiento a cada linea que estoy escribiendo, se puede omitir, pero cuando ejecutas una función que se llama así misma si es necesario terminarla con el “;”

	* **juand_silva** [379109] (2)

		@gndx tienes razón pero las funciones IIFE se pueden reescribir de una forma distinta para que no utilicen el ( ; ). Hace mucho tiempo no me he encontrado con una función [IIFE](https://programacionymas.com/blog/funciones-javascript-invocadas-inmediatamente-IIFE) xD

	* **Oscar Barajas Tavares (Platzi)** [379109] (2)

		He revisado algunos proyectos “legacy” donde aun me encuentro este tipo de funciones 😦

* **Ivan_Romero** (2) [354762](https://platzi.com/comentario/354762/) 

	Entonces ya puedo sustituir el uso de var por let y no me representa ningún problema en mis desarrollos?

	* **Diego Alexander Forero Higuera (Platzi)** [354762] (3)

		Si usas babel no vas a tener problema, ya que los navegadores viejos no tiene soporte para let, por lo tanto es recomendable usar babel para que transpile el código para todos los navegadores.

* **Ivan Fuentes** (2) [354294](https://platzi.com/comentario/354294/) 

	[Procesos asíncronos ](http://lemoncode.net/lemoncode-blog/2018/1/29/javascript-asincrono)

* **Nelson Meza** (2) [337908](https://platzi.com/comentario/337908/) 

	const varibles que no cambian en el tiempo (constantes)  
	let variables que luego podemos cambiar  
	var método anterior para definir variables

* **der__leguan** (2) [336371](https://platzi.com/comentario/336371/) 

	no me quedo claro tu comentario sobre var y let  
	información sobre let
	
	[https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/let](url)
	
	**creo que let no hace desaparecer var sino que son para casos distintos  
	**

	* **Sergio Minei** [336371] (5)

		Lo que sucede es que con `let` ya no es necesario usar `var`.
		
		Cuando creas una variable con `var`, esta se crea al nivel de la función, mientras que `let` se crea para el bloque que lo encierra.
		```     functionusemosVar() {
		      //la variable i es visible aquí
		      for(var i=0; i<5; i++) {
		        console.log(i)
		      }
		      //la variable i es visible aquí
		    }
		    
		    functionusemosLet() {
		      //la variable i no es visible aquí
		      for(let i=0; i<5; i++) {
		        console.log(i)
		      }
		      //la variable i no es visible aquí
		    }
		    
		```
		
		La idea es crear la variable solo en el contexto en el cual se vaya a usar. por eso se recomienda siempre usar **let** y ya no usar **var**.

	* **der__leguan** [336371] (1)

		por lo que veo yo var se utiliza a nivel gloval y let a nivel del scop

	* **Sergio Minei** [336371] (5)

		Si, pero si tu declaras let afuera de todo también se declara como global. La idea es que la variable esté declarada solo donde lo vas a usar. No en todos lados.
		```     let a = 0;
		    
		    functionsumar() {
		     a = 5 + 10;
		    }
		    
		    functionrestar() {
		      a = 6 - 1;
		    }
		    
		```

* **carlosextra1** (1) [1114785](https://platzi.com/comentario/1114785/) 

	Excelente explicación de variables y sus entornos!

* **webrevolution** (1) [1085479](https://platzi.com/comentario/1085479/) 

	ehhh

* **33andres33** (1) [1076179](https://platzi.com/comentario/1076179/) 

	var: se utiliza de forma global  
	let: de forma local osea si se utiliza dentro de una funcion ciclo o cualquier otra ejecucion se utiliza ahi dentro.  
	const: solo se puede utilizar esa variable una vez en todo el codigo

* **Kenet Chungandro** (1) [983140](https://platzi.com/comentario/983140/) 

	Todo ok, todo correcto, siguiente clase 😄

* **adersonrangel** (1) [883053](https://platzi.com/comentario/883053/) 

	 **const y let** es la forma en que se declaran las variables a partir de ECMAScript 6, **const** sirve para declarar variables que nunca van a ser modificadas y en cambio **let **son variables que pueden ser modificadas

* **J Daniel Rodriguez Davis** (1) [809856](https://platzi.com/comentario/809856/) 

	soy programador vba y hasta ahorita todo va bien. Esperemos seguir asi! ya que es mas o menos lo mismo! haha

* **Luis Rangel Castro** (1) [786134](https://platzi.com/comentario/786134/) 

	[Vanilla JavaScript and HTML - No frameworks. No libraries. No problem.](https://dev.to/pluralsight/vanilla-javascript-and-html-no-frameworks-no-libraries-no-problem-2n99?utm_source=digest_mailer&utm_medium=email&utm_campaign=digest_email)

* **Luis Rangel Castro** (1) [785299](https://platzi.com/comentario/785299/) 

	Using “VanillaJS” means using plain JavaScript without any additional libraries like jQuery. People use it as a joke to remind other developers that many things can be done nowadays without the need for additional JavaScript libraries. Here’s a funny site that jokingly talks about this: <http://vanilla-js.com/>

* **christianmock** (1) [673397](https://platzi.com/comentario/673397/) 

	Acá les comparto una lista de atajos en la consola [Captura de pantalla 2019-08-01 a la(s) 22.37.15.png](https://static.platzi.com/media/user_upload/Captura%20de%20pantalla%202019-08-01%20a%20la%28s%29%2022.37.15-6a905e12-894a-4473-aca3-8172dcd3b6c7.jpg)

* **Juan Sebastián Joya Rodríguez** (1) [643585](https://platzi.com/comentario/643585/) 

	Personalmente, acorde con el curso de introducción a JS, a mi me gusta usar var para las variables que voy a usar a lo largo del archivo, o en otros archivos, y let para variables que estan dentro de una función, que son como variables auxiliares, también cuando voy a usar un for, seria algo como for(let i = 0, 15, i++)

* **emanueljtc** (1) [588974](https://platzi.com/comentario/588974/) 

	refactor de la funcion cambiarNombre a una arrow function
	```     letcambiarNombre = nuevoNombre => cambia = nuevoNombre;
	    
	    
	```

* **emanueljtc** (1) [588972](https://platzi.com/comentario/588972/) 

	Lo de tipos var, const o let es tan discutible… porque por ejemplo en el curso de Fundamentos JS, Sacha determina que con var podemos definir las variables de entorno global y con let a aquellas que podemos acceder dentro de su propio entorno local y const si claro esta no cambiantes… yo soy mas de guiarme por eso que sostiene Sacha… ¿a quien creer?

	* **Julio César Noguera López** [588972] (2)

		mmm, pues mira, te dejo aquí las diferencia entre las 3.
		
		**var** declara una variable de scope global o local para la función sin importar el ámbito de bloque. Permite hoisting.
		
		**let** declara una variable de scope global, local para la función o de bloque. Es reasignable y no permite hoisting.
		
		**const** declara una variable de scope global, local para la función o de bloque. **No es reasignable** , pero es mutable. No permite hoisting.

	* **emanueljtc** [588972] (1)

		Pues si amigo @noguerajulioces opino lo mismo pero como @leonidasesteban recomienda no usar var es lo que confunde.

	* **Iair Poloniecki** [588972] (1)

		En clase de Sacha en la que habla de la diferencia de las 3, dice que lo recomendable es no usar nunca `var` que es más antigua, y en cambio usar `let` para las variables que cambian y `const` para la mayoría de los casos.

* **Pablo Domínguez Durán** (1) [584775](https://platzi.com/comentario/584775/) 

	Quizá haya un despistado que no sepa que puede decargar todo los recursos de una sola vez.
	
	Escrolea hacia abajo y estará el botón de _Descargar todo_

* **Alejandro González Reyes** (1) [574909](https://platzi.com/comentario/574909/) 

	Apuntes de esta clase
	```     // Impimir información por consola
	    console.log('Curso de JQuery a Javascript Vanilla')
	    console.warn('Platzi México')
	    
	    // Declaración de una constante a nivel global
	    // Las constantes se deben declarar e inicializar al mismo tiempo
	    // No cambian, si reasignamos un nuevo valor se lanza un error
	    const cedula = '01515-AHS77M'
	    
	    // Declaración de variables a nivel global
	    // Las variables se pueden declarar sin inicializarlas o inicicalizarlas al mismo tiempo
	    // Cambian durante la ejecución del programa. (Se pueden resignar nuevos valores) 
	    let nombre = 'Alejandro González Reyes'
	    
	    // Las funciones agrupan un conjunto de instrucciones para ejecutar una deterinada tarea en particular
	    // La definicón del cuerpo de una función puede recibir argumentos
	    // En este caso newName es el argumento de la funcion
	    // Y cunado se invoca estos arguemenetos son reemplazados por los parametros (valores pasados ala función)
	    functioncambiarNombre(newName) {
	    	// Sobre-escribir el valor actual de una variable global
	    	nombre = newName
	    }
	    
	    // Probar ejecuntando desde la consola del navegador
	    // La función clear() limpia la consola del navegador
	    
	```

* **Isaias Chávez** (1) [548653](https://platzi.com/comentario/548653/) 

	De funciones a promesas, jajajaja nooo, primero vean el curso de fundamentos de js, en 2 semanas lo culminan!

* **Félix Alejandro Zelaya Orellana** (1) [526534](https://platzi.com/comentario/526534/) 

	Genial!

* **heysoypaez** (1) [492065](https://platzi.com/comentario/492065/) 

	#Ideas claves
	
	Javascript es asincrono
	
	  * AJAX nos permita traer datos de servidores externos
	
	* * *
	
	Recordando lo bàsico
	
	**Variables**
	
	var = variable global  
	let = variable local que cambia  
	const = variable local que NO cambia, es decir una constante
	
	_Funciones_  
	Capsulas de código
	```     functionhazAlgo(){ 
	    return"Hice algo"
	     }
	    
	```
	
	* * *
	
	#Acciones:
	
	Decidi que planteare mis apuntes y modificaciones del curso usando git, cada clase sera un commit clave que lo expresare con una etiqueta principal (1.0, 2.0, 3.0 )

	* **Jean León** [492065] (1)

		Javascript es síncrono

	* **heysoypaez** [492065] (1)

		Corrigiendo, ciertamente es sincrono pero podemos hacer que se comporte de manera asincrona

* **pablojorgeandres** (1) [483000](https://platzi.com/comentario/483000/) 

	El asincronismo explicado muy sencillo y rápido por Sacha [aquí](https://platzi.com/clases/1339-fundamentos-javascript/12957-como-funciona-el-asincronismo-en-javascript/)
	
	Y más técnico pero también muuuy claro en este [video](https://www.youtube.com/watch?v=rgmej4Jx4WM).

* **pablojorgeandres** (1) [482997](https://platzi.com/comentario/482997/) 

	## Variables y Funciones
	
	Para traer datos de un servicio externo vamos a usar en combinación:
	
	  * Promesas
	  * ajax/fetch
	  * funciones asíncronas
	
	**Procesos Asíncronos**
	
	Un proceso asíncrono es una petición que hace javaScript al browser y este la devuelve una vez cumplida a la _cola de tareas_ que será ejecutada al final del EventLoop o del flujo de funciones síncronas dentro de nuestro código.  
	Por ejemplo cargar la página mientras pido al browser que obtenga todos los usuarios y los devuelva una vez cargado el sitio y cuándo esté listo.
	
	Pero antes de implementar una Promesa demos tener en claro dos cosas necesarias: Variables y Funciones.
	
	**Variables**
	
	Dentro de JavaScript tenemos tres formas de declarar una variable las cuales son:
	
	  * _var_ \- ECMAScript 5
	  * _let_ \- A partir de ECMAScript 6 - Para declarar variables que pueden ser modificadas
	  * _const_ \- A partir de ECMAScript 6 - Para declarar variables que no pueden ser modificadas
	
	**Funciones**
	
	Las funciones son piezas de código que puedes reutilizar y se declaran con la palabra function.
	```     function estaEsMiFuncion(string){
	        console.log(string)
	    }
	```

* **Jolivero** (1) [453140](https://platzi.com/comentario/453140/) 

	¿No hay un repositorio de este Curso?

	* **israelhuaman** [453140] (1)

		No , pero puedes descargarlo todo el material , te recomiendo que lo hagas en .rar

	* **Nathaly Stefani Riaño Bejarano** [453140] (4)

		Acá está el repositorio del curso <https://github.com/LeonidasEsteban/jquery-to-js-curso>

* **Jolivero** (1) [453136](https://platzi.com/comentario/453136/) 

	No hay un repositorio de este Curso?

* **Jesus Ricardo Moreira Cedeño** (1) [399805](https://platzi.com/comentario/399805/) 

	Nice 😃

* **Hector Cardona** (1) [393433](https://platzi.com/comentario/393433/) 

	Hola, tengo una duda, y me gustaría saber si me la pueden responder, viendo el código html del trabajo finalizado no observo como una sección donde se añadan las películas directamente desde el código html, me podrían explicar por que?.  
	Muchas gracias.

	* **juan carlos ramirez ustarroz** [393433] (2)

		El codigo que esta en aportes no es de peliculas si no de musica y la otra porque puede que lo traiga desde un api para explicar el asincronismo

	* **Sergio Santiago González** [393433] (2)

		Las listas de las películas se agregan al HTML por medio de un template (plantilla) que se creó en Javascript, las cuales se rellenan mediante una llamada asíncrona a una api.

	* **Hector Cardona** [393433] (1)

		@DeoXyx182 @JvaN_Karl0z Muchas gracias!

* **juliocefe** (1) [335299](https://platzi.com/comentario/335299/) 

	Se escucha muy bajo el audio

	* **juliocefe** [335299] (1)

		En la presentación se escuchaba normal

* **.** (1) [334983](https://platzi.com/comentario/334983/) 

	Donde están los archivos?

	* **Carlos Martínez** [334983] (1)

		Ahora ya están disponibles en la pestaña Archivos

	* **.** [334983] (2)

		Me acabo de fijar, gracias!

* **Nilton Rodriguez Condori** (1) [78090](https://platzi.com/comentario/915114/) 
En conclusión, const es variable inmutable y let es mutable. Ademas, que es recomendable usar let en cambio de var.

* **Gustavo83** (1) [61444](https://platzi.com/comentario/621777/) 
Entonces, es recomendable dejar de utilizar el término VAR y en vez de ello utilizar LET ?

	* **Favio Náquira** [61444] (2)

		El tema es que con **let** uno controla mucho mejor los **scope** donde trabajará esa variable. Con **var** quedaba la duda si la variable iba a afectar más allá de su declaración dentro de algo, muchas veces por repetir un nombre o n motivos.  
		Así que si, mejor let para que tu código sea más entendible y mantenible.

* **emanueljtc** (1) [59245](https://platzi.com/comentario/588972/) 
Lo de tipos var, const o let es tan discutible… porque por ejemplo en el curso de Fundamentos JS, Sacha determina que con var podemos def...

	* **Julio César Noguera López** [59245] (2)

		mmm, pues mira, te dejo aquí las diferencia entre las 3.
		
		**var** declara una variable de scope global o local para la función sin importar el ámbito de bloque. Permite hoisting.
		
		**let** declara una variable de scope global, local para la función o de bloque. Es reasignable y no permite hoisting.
		
		**const** declara una variable de scope global, local para la función o de bloque. **No es reasignable** , pero es mutable. No permite hoisting.

* **Jolivero** (1) [48299](https://platzi.com/comentario/453140/) 
¿No hay un repositorio de este Curso?

	* **israelhuaman** [48299] (1)

		No , pero puedes descargarlo todo el material , te recomiendo que lo hagas en .rar

* **Hector Cardona** (1) [43088](https://platzi.com/comentario/393433/) 
Hola, tengo una duda, y me gustaría saber si me la pueden responder, viendo el código html del trabajo finalizado no observo como una sec...

	* **juan carlos ramirez ustarroz** [43088] (2)

		El codigo que esta en aportes no es de peliculas si no de musica y la otra porque puede que lo traiga desde un api para explicar el asincronismo

* **Julián Fernando Amaya Díaz** (1) [41729](https://platzi.com/comentario/379109/) 
Con ES6 es obligatorio usar punto y coma o con el nuevo estándar ya no es necesario ?

	* **juand_silva** [41729] (3)

		No es necesario usar semicolon ( ; ) en JavaScript. Eso dependen de tu equipo de desarrollo cuando utilicen linters como [standar](https://standardjs.com/) que por ejemplo no aceptan el ( ; ) o el linter de [AirBNB](https://github.com/airbnb/javascript) que si lo requiere.  
		Personalmente dejé de usar el semicolon hace tiempo, porque vi muy pocas o ninguna diferencia si se usa o no.

* **Ivan_Romero** (1) [39428](https://platzi.com/comentario/354762/) 
Entonces ya puedo sustituir el uso de var por let y no me representa ningún problema en mis desarrollos?

	* **Diego Alexander Forero Higuera (Platzi)** [39428] (3)

		Si usas babel no vas a tener problema, ya que los navegadores viejos no tiene soporte para let, por lo tanto es recomendable usar babel para que transpile el código para todos los navegadores.

* **der__leguan** (1) [37739](https://platzi.com/comentario/336371/) 
no me quedo claro tu comentario sobre var y let información sobre let https://developer.mozilla.org/es/docs/Web/JavaScript/...

	* **Sergio Minei** [37739] (5)

		Lo que sucede es que con `let` ya no es necesario usar `var`.
		
		Cuando creas una variable con `var`, esta se crea al nivel de la función, mientras que `let` se crea para el bloque que lo encierra.
		```     functionusemosVar() {
		      //la variable i es visible aquí
		      for(var i=0; i<5; i++) {
		        console.log(i)
		      }
		      //la variable i es visible aquí
		    }
		    
		    functionusemosLet() {
		      //la variable i no es visible aquí
		      for(let i=0; i<5; i++) {
		        console.log(i)
		      }
		      //la variable i no es visible aquí
		    }
		    
		```
		
		La idea es crear la variable solo en el contexto en el cual se vaya a usar. por eso se recomienda siempre usar **let** y ya no usar **var**.

## 0050. Promesas [12289](https://platzi.com/clases/1316-jquery-js/12289-promesas0723/)

### Descripción:


Las promesas sirven para manejar nuestro código asíncrono.

“Una Promesa es un objeto que representa la terminación o el fracaso eventual de una operación asíncrona”, o dicho de forma más cotidiana, se va a mandar una función para ver si falla o se ejecuta con éxito.

Al crear una Promesa debemos pasarle por argumento la función que vamos a ejecutar de forma asíncrona, dicha función va a recibir dos parámetros para evaluar si se ejecuto bien la función o si fallo.

Nuestra promesa va a tener dos métodos para saber si todo salió bien o fallo. El método then se encarga cuando la promesa se cumplió exitosamente, mientras que el método catch se encarga cuando la promesa falla.

Dentro de JavaScript tenemos dos funciones para ejecutar una función después de algún tiempo, estas funciones son:

• setInterval: ejecutara una función cada x tiempo.  
• setTimeout: ejecutara una función después de x tiempo.

Si queremos resolver varias promesas a la misma vez, Promise cuenta con un método llamado all que recibe un array de promesas como parámetro. Este método se termina cuando todas las promesas del array se terminan de ejecutar. Si una de las promesas falla entonces el método all saltara un error mandándote al método catch.

Promise también cuenta con el método race que te regresa los resultados de la promesa que termine primero.

### Comentarios:

* **samvarcia** (50) [336528](https://platzi.com/comentario/336528/) 

	.then() cuando sale bien 😃  
	.catch() cuando sale mal 😦

	* **tomas-alegre146** [336528] (6)

		me gustaron los emoticones al final XD

* **Sergio Minei** (32) [335344](https://platzi.com/comentario/335344/) 

	## Promesas
	
	Una promesa es un objeto que representa la terminación o el fracaso eventual de una operación asíncrona.
	```     //Crear una promesa
	    const getUser = newPromise(function(todoBien, todoMal) {
	      //llamar a un API
	      todoBien("todo bien");
	    })
	    
	    //Consumir una promesa
	    getUser
	      .then(function(msn) { 
	        //maneja cuando la promesa funciona correctamente.
	      })
	      .catch(function(msn) {
	        //maneja cuando hay un error en la promesa.
	      })
	    
	    //Consumir varias promesas a la vez.
	    //El then se ejecutan cuando terminen todas las promesas.
	    //El catch se ejecuta en el primer error.
	    Promise.all([
	      promesa1,
	      promesa2
	    ])
	    .then(function() {})
	    .catch(function() {})
	    
	    //Se ejecuta el then de la promesa que termine primero.
	    Promise.race([
	      promesa1,
	      promesa2
	    ])
	    .then(function() {})
	    .catch(function() {})
	    
	```
	
	## Timers
	
	  * `setInterval()` se ejecuta cada cierto tiempo.
	  * `setTimeout()` se ejecuta una sola vez luego de un periodo de tiempo.
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **edesalla17** [335344] (3)

		buen recurso es que estar armando. gracias

	* **Sergio Minei** [335344] (8)

		Gracias a ti @edesalla17! Es una alegría poder ayudar.
		
		Estoy armando resúmenes de todos los cursos que llevo. Si deseas puedes verlos **[aquí](https://github.com/MineiToshio/CursosPlatzi)**.

	* **SEBASTIAN PADUANO** [335344] (2)

		gracias eres el mejor

	* **pablojorgeandres** [335344] (7)

		Señor @MineiToshio, el tomar notas de cada clase me ha resultado muy efectivo para incorporar los contenidos.  
		Este útil hábito lo he tomado de su propio gesto de hacerlo.  
		Es por esto que estoy ‘replicando’ su método de notas de cada clase.
		
		Ya tengo un resumen completo (y mi primero) del curso de Fundamentos de JavaScript en [GitHub](https://github.com/pablojorgeandres/notas-clases/blob/master/fundamentosJS.md) si quieres echar un vistazo.
		
		En fin, si no le molesta seguiré con esta práctica.
		
		Gracias por ser una inspiración.  
		Saludos.

	* **Fidelia Torrez Cupiticona** [335344] (2)

		Buenísimo, gracias!!

	* **Sergio Minei** [335344] (1)

		Gracias por tus palabras @pablojorgeandre. Me inspiras a seguir adelante. 😄
		
		Éxitos en todo!

* **.** (25) [335004](https://platzi.com/comentario/335004/) 

	Promise.race para ponerlas a competir!

	* **samvarcia** [335004] (1)

		jaja

	* **Oscar Barajas Tavares (Platzi)** [335004] (1)
Siempre hay una finalidad para cosa

	* **jasanhdz** [335004] (1)

		Muy buena analogía jaja 😄

	* **Jesus Ricardo Moreira Cedeño** [335004] (1)

		xd

* **Iván Darío Sánchez Jiménez** (9) [346683](https://platzi.com/comentario/346683/) 

	Tener presente:
	
	  * Una promesa solo puede ser exitosa o fallar una sola vez
	  * No puede ser exitosa y fallar dos veces
	  * No puede pasar de exitosa a fallar y viceversa
	  * No puede ser exitosa y fallar al mismo tiempo
	
	[Mayor Información](https://developers.google.com/web/fundamentals/primers/promises?hl=es)

	* **SEBASTIAN PADUANO** [346683] (1)

		gracias

* **Andrés Felipe Eslava Zuluaga** (6) [640735](https://platzi.com/comentario/640735/) 

	Les comparto un código que es una simulación de una promesa que debe ser recibida en menos de 6 segundos.  
	Saludos!
	```     /** PROMESA: Callback o llamada a un Servicio Web que devuelve 'algo' para ser usado en mi app */
	    const SEGUNDOS = 10000;
	    let timeout = Math.random() * SEGUNDOS
	    const getUser = newPromise(function (promesaExitosa, promesaRechazada){  /** Promise: recibe una función como parámetro que a su vez recibe dos funciones como parámetro */
	        if(timeout < 6000) {
	            setTimeout(function(){
	                promesaExitosa()
	            }, timeout)
	        } else {
	            setTimeout(function() {
	                promesaRechazada('Se acabó el tiempo')
	            },5000)
	        }
	    })
	    //console.log(timeout) /** Para saber cuánto tiempo tarda */
	    getUser
	        .then(function() {
	            console.log('Promesa exitosa!!')
	        })
	        .catch(function(message) {
	            console.log(message)
	        })
	    
	    
	```

* **Iván Darío Sánchez Jiménez** (6) [346672](https://platzi.com/comentario/346672/) 

	Para complementar el tema de promesas revivo un antiguo video de platzi [ver](https://www.youtube.com/watch?v=tSc9nQ6gZQ4) pero modificando algunos detalles del código.
	
	**HTML**
	```     <!DOCTYPE html>
	    <html>
	    <head>
	    	<metacharset="utf-8">
	    	<title>Example Promises</title>
	    	<styletype="text/css">
	    		html, body{
	    			height: 100%;
	    			display: flex;
	    			align-items: center;
	    			justify-content: center;
	    		}
	    		#messages{
	    			font-size: 32px;
	    			font-family: 'Arial';
	    		}
	    	</style>
	    </head>
	    <body>
	    	<divid="messages"></div>
	    
	    	<scriptsrc="https://code.jquery.com/jquery-3.3.1.min.js"integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" 
	    		crossorigin="anonymous"></script>
	    	<scripttype="text/javascript"src="promise_example.js"></script>	
	    </body>
	    </html>
	    
	```
	
	Este primer ejercicio consta de una función que ejecuta una promesa cada 2 segundos que permite mostrar un mensaje en el documento.
	
	promise_example.js
	```     var $msg = $('#messages')
	    
	    functionwait2Seconds(){
	    	returnnewPromise(function(resolve, reject){
	    		setTimeout(function(){
	    			resolve();		
	    		},2000)	 
	    	})
	    }
	    
	```
	
	Ahora podemos crear una cadena de promesas anidadas como primer solución
	```     $msg.text('START')
	    	
	    wait2Seconds()	
	    .then(function(){
	    	$msg.text('Ejecutando paso 1');
	    	wait2Seconds()
	    	.then(function(){
	    		$msg.text('Ejecutando paso 2');
	    		wait2Seconds()
	    		.then(function(){
	    			$msg.text('Ejecutando paso 3');
	    		})
	    	})
	    })
	    .catch(function(err){
	    	$msg.text('Error');
	    	
	    })
	    
	```
	
	Ahora bien este anidamiento puede resultar algo muy familiar en aspecto a un callback hell.
	
	Para presentar con una sintaxis mas ordenada las promesas se pueden encadenar si retornamos una función u objeto en cada promesa
	```     $msg.text('START')
	    	
	    wait2Seconds()	
	    .then(function(){
	    	$msg.text('Ejecutando paso 1');
	    	return wait2Seconds()
	    })
	    .then(function(){
	    	$msg.text('Ejecutando paso 2');
	    	return wait2Seconds()
	    })
	    .then(function(){
	    	$msg.text('Ejecutando paso 3');
	    })
	    .catch(function(err){
	    	$msg.text(err);
	    	
	    })
	    
	    
	```
	
	Por último, las promesas se pueden rechazar utilizando throw() deteniendo toda la cadena de promesas.
	```     $msg.text('START')
	    	
	    wait2Seconds()	
	    .then(function(){
	    	$msg.text('Ejecutando paso 1');
	    	return wait2Seconds()
	    })
	    .then(function(){
	    
	    	throw 'Fin de la ejecución por error' //Hasta aquí se ejecuta
	    	$msg.text('Ejecutando paso 2');
	    	return wait2Seconds()
	    })
	    .then(function(){
	    	$msg.text('Ejecutando paso 3');
	    })
	    .catch(function(err){
	    	$msg.text(err);
	    	
	    })
	    
	    
	```
	
	Espero les sirva. 😃

* **Alan Santiago** (5) [361924](https://platzi.com/comentario/361924/) 

	Los parametros de la funcion que se le pasa a **Promise** tienen de nombre: **resolve** , **reject**. Por convencion 😄

* **Juan Gaybre Flores** (4) [925344](https://platzi.com/comentario/925344/) 

	Les comparto algunos ejemplos retomando la idea de @rubbenpad de incluir Math.random() para hacer que las promesas tengan 50 50 de éxito y falla. Usé una variable para eso para poder imprimirla junto a cada ejemplo y hacer mas notable el resultado.  
	![proyecto.JPG](https://static.platzi.com/media/user_upload/proyecto-be05103b-0d5d-4480-aca7-ba42c0e38639.jpg)![diasSemana.JPG](https://static.platzi.com/media/user_upload/diasSemana-5ca0ca95-9178-45f7-9ccf-73fad6cdf55d.jpg)![getStatus.JPG](https://static.platzi.com/media/user_upload/getStatus-dfc209b4-9275-4855-932d-64ef825b0005.jpg)![vacaciones.JPG](https://static.platzi.com/media/user_upload/vacaciones-293f11cb-4d2c-4a74-9375-b8d3410b9ff0.jpg)

* **Alejandro González Reyes** (4) [574959](https://platzi.com/comentario/574959/) 

	Mi apunte con mayor contexto
	```     // Declaración de una promesa
	    // Las promesas son utiles para manejar nuestro código asincrono (código que en un determinado momento
	    // puede resolverse correctamente o fallar. Como una petición a una API)
	    // 
	    // Las promesas son objetos que representan el exito o fracaso de una operacon asincrona
	    // 
	    // Al crear una promesa debemos pasarle como argumento una función, la cual contiene instrucciones que se ejecutaran de manera asincrona
	    // 
	    // El primer parametro es una función que se ejecutará si la promesa se resuelve correctamente,
	    // El segundo parametro es una función que se ejecutara si la promesa falla
	    const getUser = newPromise(function(resolve, reject) {
	    	// Simular una llamada a una API via AJAX
	    	// Este código se ejecuta despues de 3000 milisegundos. No bloquea las instrucciones que están fuera de el.
	    	setTimeout(function() {
	    		// Luego de 3 segundos se ejecuta este bloque de código.
	    		// Simulamos que la promesa se resuleve correctamente invocando la función del primer parametro
	    		let datos = ['María', 'Demetrio', 'Miguel Angel', 'Alejandro'];
	    		// Podemos enviar parametros a dicha función para que la  parte solicitante haga algo con ellos.
	    		// En este caso serían los resultados obtenidos a través de la consulta a la API
	    		// resolve()
	    		resolve(datos)
	    		// 
	    		// Si la promesa falla debo ejecutar la función del segundo parametro
	    		// Podemos enviar parametros a dicha función para que la parte solicitante tenga mayor contexto sobre lo que ha pasado
	    		// reject()
	    		// reject('Error interno en el servidor')
	    	}, 3000)
	    })
	    
	    // Invocar una promesa
	    getUser
	    	.then(function(data) {
	    		// Esta función se ejecuta si la promesa se resuelve
	    		// El valor devuelto por la promesa depende internamente de su implementación.
	    		console.log('Excelente, la promesa se resolvío satisfactoriamente: ' + data)
	    	})
	    	.catch(function(error) {
	    		// Esta función se ejecuta si la promesa falla
	    		// Es como un decorador para hacer algo a nivel de interfaz como una notificación.
	    		// El valor devuelto de error, depende de la implementación interna de la promesa (invocacion reject())
	    		console.error('Lo sentimos, la promesa no se resolvió: ' + error)
	    	})
	    
	    
	    // Declaración de promesas adicionales para que posteriormente se ejecuten de forma paralela
	    // En este caso se simulan operaciones asincronas con tiempo en milisegundos diferentes
	    const getDias = newPromise((resolve, reject) => {
	    	setTimeout(() => {
	    		let dias = ['Lunes', 'Martes', 'Miércoles', 'Jueves', 'Viernes']
	    		//resolve(dias)
	    		reject('Error en el servidor de DÍAS')
	    	}, 5000)
	    })
	    const getEstaciones = newPromise((resolve, reject) => {
	    	setTimeout(() => {
	    		let estaciones = ['Primavera', 'Verano', 'Otoño', 'Invierno']
	    		resolve(estaciones)
	    		// reject('Error en el servidor de ESTACIONES')
	    	}, 1000)
	    })
	    
	    
	    // Ejecutar promesas en paralelo 
	    // Inspeccionar que todas las promesas se resuelvan para realizar una tarea determinada
	    // Una analogía sería como una transacción de promesas
	    Promise.all([getUser, getEstaciones, getDias])
	    	.then(function(data) {
	    		// Esta función se ejecuta si y solo si se resuelven todas las promesas
	    		console.log('Felicidades las promesas se resolvieron correctamente')
	    		// La data contiene un array con los datos devueltos por cada una de las promesas. Si es que
	    		// su interfaz interna asi lo decide (que envien datos)
	    		console.log(data)
	    	})
	    	.catch(function(error) {
	    		// Esta función se ejecuta si alguna de las promesas listadas es rechazada
	    		console.error('Rayos, alguna de las promesas falló')
	    		// En este caso el error devuelto corresponde a la promesa que ha fallado
	    		console.error(error)
	    	})
	    
	    
	    // Ejecutar promesas en paralelo por carrera
	    // inspeccionar que promesa se resuelve primero y realizar una tarea determinada
	    // 
	    // Una analogía sería, que todas las promesas se comuniquen a diferentes servidores de replica apuntando al mismo recurso.
	    // dependiendo de que servidor responda primero, se resolvera su promesa relacionada.
	    // Ante esto, damos por terminada la ejecución en paralelo
	    Promise.race([getUser, getEstaciones, getDias])
	    	.then(data => {
	    		// Se ejecuta esta función con la data devuelta por la promesa que se resolvío primero
	    		// Despues de eso. se da por terminado el proceso.
	    		console.log('Felicidades, alguna de las promesas se ha resulto primero que las demás')
	    		console.log(data)
	    	})
	    	.catch(error => {
	    		// Esta función se ejecuta si la promesa en atenderse primero falla
	    		console.error('Lo sentimos, una de las promesas ha fallado')
	    		console.info(error)
	    	})
	    
	```

	* **Cinthya Cabanzo** [574959] (0)

		Hola,
		
		No estaba corriendo porque tenia escribto NewPromise en vez de new Promise
		```     // Declaración de una promesa
		    // Las promesas son utiles para manejar nuestro código asincrono (código que en un determinado momento
		    // puede resolverse correctamente o fallar. Como una petición a una API)
		    // 
		    // Las promesas son objetos que representan el exito o fracaso de una operacon asincrona
		    // 
		    // Al crear una promesa debemos pasarle como argumento una función, la cual contiene instrucciones que se ejecutaran de manera asincrona
		    // 
		    // El primer parametro es una función que se ejecutará si la promesa se resuelve correctamente,
		    // El segundo parametro es una función que se ejecutara si la promesa falla
		    const getUser = newPromise(function(resolve, reject) {
		    	// Simular una llamada a una API via AJAX
		    	// Este código se ejecuta despues de 3000 milisegundos. No bloquea las instrucciones que están fuera de el.
		    	setTimeout(function() {
		    		// Luego de 3 segundos se ejecuta este bloque de código.
		    		// Simulamos que la promesa se resuleve correctamente invocando la función del primer parametro
		    		let datos = ['María', 'Demetrio', 'Miguel Angel', 'Alejandro'];
		    		// Podemos enviar parametros a dicha función para que la  parte solicitante haga algo con ellos.
		    		// En este caso serían los resultados obtenidos a través de la consulta a la API
		    		// resolve()
		    		resolve(datos)
		    		// 
		    		// Si la promesa falla debo ejecutar la función del segundo parametro
		    		// Podemos enviar parametros a dicha función para que la parte solicitante tenga mayor contexto sobre lo que ha pasado
		    		// reject()
		    		// reject('Error interno en el servidor')
		    	}, 3000)
		    })
		    
		    // Invocar una promesa
		    getUser
		    	.then(function(data) {
		    		// Esta función se ejecuta si la promesa se resuelve
		    		// El valor devuelto por la promesa depende internamente de su implementación.
		    		console.log('Excelente, la promesa se resolvío satisfactoriamente: ' + data)
		    	})
		    	.catch(function(error) {
		    		// Esta función se ejecuta si la promesa falla
		    		// Es como un decorador para hacer algo a nivel de interfaz como una notificación.
		    		// El valor devuelto de error, depende de la implementación interna de la promesa (invocacion reject())
		    		console.error('Lo sentimos, la promesa no se resolvió: ' + error)
		    	})
		    
		    
		    // Declaración de promesas adicionales para que posteriormente se ejecuten de forma paralela
		    // En este caso se simulan operaciones asincronas con tiempo en milisegundos diferentes
		    const getDias = newPromise((resolve, reject) => {
		    	setTimeout(() => {
		    		let dias = ['Lunes', 'Martes', 'Miércoles', 'Jueves', 'Viernes']
		    		//resolve(dias)
		    		reject('Error en el servidor de DÍAS')
		    	}, 5000)
		    })
		    const getEstaciones = newPromise((resolve, reject) => {
		    	setTimeout(() => {
		    		let estaciones = ['Primavera', 'Verano', 'Otoño', 'Invierno']
		    		resolve(estaciones)
		    		// reject('Error en el servidor de ESTACIONES')
		    	}, 1000)
		    })
		    
		    
		    // Ejecutar promesas en paralelo 
		    // Inspeccionar que todas las promesas se resuelvan para realizar una tarea determinada
		    // Una analogía sería como una transacción de promesas
		    Promise.all([getUser, getEstaciones, getDias])
		    	.then(function(data) {
		    		// Esta función se ejecuta si y solo si se resuelven todas las promesas
		    		console.log('Felicidades las promesas se resolvieron correctamente')
		    		// La data contiene un array con los datos devueltos por cada una de las promesas. Si es que
		    		// su interfaz interna asi lo decide (que envien datos)
		    		console.log(data)
		    	})
		    	.catch(function(error) {
		    		// Esta función se ejecuta si alguna de las promesas listadas es rechazada
		    		console.error('Rayos, alguna de las promesas falló')
		    		// En este caso el error devuelto corresponde a la promesa que ha fallado
		    		console.error(error)
		    	})
		    
		    
		    // Ejecutar promesas en paralelo por carrera
		    // inspeccionar que promesa se resuelve primero y realizar una tarea determinada
		    // 
		    // Una analogía sería, que todas las promesas se comuniquen a diferentes servidores de replica apuntando al mismo recurso.
		    // dependiendo de que servidor responda primero, se resolvera su promesa relacionada.
		    // Ante esto, damos por terminada la ejecución en paralelo
		    Promise.race([getUser, getEstaciones, getDias])
		    	.then(data => {
		    		// Se ejecuta esta función con la data devuelta por la promesa que se resolvío primero
		    		// Despues de eso. se da por terminado el proceso.
		    		console.log('Felicidades, alguna de las promesas se ha resulto primero que las demás')
		    		console.log(data)
		    	})
		    	.catch(error => {
		    		// Esta función se ejecuta si la promesa en atenderse primero falla
		    		console.error('Lo sentimos, una de las promesas ha fallado')
		    		console.info(error)
		    	})
		    
		```

* **pablojorgeandres** (4) [483242](https://platzi.com/comentario/483242/) 

	## Promesas
	
	“Una Promesa es un objeto que representa la terminación o el fracaso eventual de una operación asíncrona”, o dicho de forma más cotidiana, se va a mandar (a dónde?) una función para ver si falla o se ejecuta con éxito.
	
	Para crear una promesa:
	```     new Promise()
	    
	```
	
	La guardamos en una nueva variable:
	```     const getUser = new Promise()
	    
	```
	
	Las _Promesas_ reciben un argumento; este argumento es una función:
	```     const getUser = new Promise(function(){ })
	    
	```
	
	Esta función que va a recibir como parámetro, a su vez recibe dos parámetros que son dos objetos; resolve y reject que nos van a indicar si nuestra _Promesa_ funcionó y no.  
	El primero (resolve) será el que nos informe del resultado positivo y viceversa.  
	A estos los podemos llamar de otra manera:
	```     const getUser = new Promise( function(todoBien, todoMal) { })
	    
	```
	
	Si mi request a una cierta API o de cualquier otro tipo es exitoso, la función parámetro de nuestra promesa devolverá el primer parámetro, si no el segundo.  
	Para ver cómo se comporta forzaremos la respuesta de esta función:
	```     const getUser = new Promise(function(todoBien, todoMal) {
	        todoBien()
	    })
	    
	```
	
	Y para invocar a la promesa invoco a la variable _getUser_ encadenada al método _.then()_ que también recibe una función como variable
	```     getUser.then(function(){
	        console.log(`El resultado de tu promesa es positivo.`)
	    })
	    
	```
	
	Para simular un delay en la _Promesa_ vamos a usar un _setTimeout()_. _**setTimeout()**_ es un método de JS que ejecuta una función después de un tiempo establecido. Y justamente recibe 2 parámetros; una función y una cantidad de tiempo en milisegundos.
	```         const getUser = newPromise(function(todoBien, todoMal) {
	            setTimeout(function(){
	               todoBien()
	            }, 3000)
	        })
	        
	        getUser.then(function(){
	            console.log(`El resultado de tu promesa es positivo.`)
	        })
	        // 3 seg. después:  El resultado de tu promesa es positivo.
	        
	    
	```
	
	Si el resultado de la _Promesa_ por cualquier cosa da un error o no funciona, llamamos al método _.catch()_ para capturar ese estado y al igual que con _.then()_ pasando una función como parámetro podemos manejar este error:
	```         const getUser = newPromise(function(todoBien, todoMal) {
	            setTimeout(function(){
	               todoMal()
	            }, 3000)
	        })
	    
	        getUser
	    		.then(function(){
	         	   console.log(`El resultado de tu promesa es positivo.`)
	        	})
	    		.catch(function(){
	            	console.log(`El resultado de tu promesa es negativo.`)
	        	})
	        
	    
	    
	```
	
	También podemos enviar parámetros a través de _todoBien()_ y _todoMal()_ :
	```         const getUser = newPromise(function(todoBien, todoMal) {
	            setTimeout(function(){
	               todoMal(`El resultado de tu promesa es negativo.`)  // paso el texto como parámetro
	            }, 3000)
	        })
	    
	        getUser
	    		.then(function(){
	         	   console.log(`El resultado de tu promesa es positivo.`)
	        	})
	    		.catch(function(message){  // recibo con 'message' el parámetro
	            	console.log(message)
	        	})
	        
	    
	    
	```
	
	Si quiero eniar varias _Promesas_ de forma paralela?  
	Para esto uso el método .all() de _Promise_ que va a recibir un _‘objeto[]’_ con todas las _Promesas_ previamente declaradas:
	```         const getUser = newPromise ( (todoBien, todoMal) => setTimeout( () => todoBien(`El resultado de tu promesa es positivo.`), 1000 ))
	        const getUser1 = newPromise((todoBien, todoMal) => setTimeout(() => todoBien(`El resultado de tu promesa es positivo II.`), 2000))
	        const getUser2 = newPromise( (todoBien, todoMal) => setTimeout( () => todoBien(`El resultado de tu promesa es positivo III.`), 3000 ) )
	    
	        Promise.all([
	            getUser,
	            getUser1,
	            getUser2
	        ])
	            .then( message => console.log(message))
	            .catch( message => console.log(message))
	    
	        //depués de 3sec: (3) ["El resultado de tu promesa es positivo.", "El resultado de tu promesa es positivo II.", "El resultado de tu promesa es positivo III."]
	    
	    
	```
	
	Si aunque sea 1 de los Promises da error saldrá por .catch() y ninguna de las promesas funcionará.
	
	Otro método usual con _Promesas_ es .race(). Este dara resultados a partir de la promesa que primero se resuelva.
	```         const getUser = newPromise ( (todoBien, todoMal) => setTimeout( () => todoBien(`El resultado de tu promesa es positivo.`), 1000 ))
	        const getUser1 = newPromise((todoBien, todoMal) => setTimeout(() => todoBien(`El resultado de tu promesa es positivo II.`), 2000))
	        const getUser2 = newPromise( (todoBien, todoMal) => setTimeout( () => todoMal(`El resultado de tu promesa es negativo.`), 3000 ) )
	    
	        Promise.race([
	            getUser,
	            getUser1,
	            getUser2
	        ])
	            .then( message => console.log(message))
	            .catch( message => console.log(message))
	    
	        // El resultado de tu promesa es positivo.
	    
	    
	    
	```

	* **Alirio Gomez** [483242] (3)

		de no ser por tu explicacion no le comprendo muy bien esta clase xd

* **Eduardo Campo** (4) [392236](https://platzi.com/comentario/392236/) 

	Igual que en la vida las Promesas siempre tardan un tiempo en cumplirse! hahahah

* **Wandy Rafael Santana Evangelista** (3) [1078337](https://platzi.com/comentario/1078337/) 

	Es bueno tambien leerse un poco de documentacion, aqui tienen amantes de la lectura: [Promise](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise)

	* **carlosextra1** [1078337] (1)

		Gracias!

* **lmoran** (3) [888446](https://platzi.com/comentario/888446/) 

	Muy buena clase les recomiendo que refuercen con el siguiente video de promesas con @LeonidasEsteban en YT.  
	[Promises](https://www.youtube.com/watch?v=EVQR8TjjAWA&list=PLHsJ3-ff9dpFtoXtiij_zOIJLWYJaUwdX&index=2)

* **Ruben Padilla** (3) [830527](https://platzi.com/comentario/830527/) 

	💲Hey!
	
	Pueden usar este codigo para probar las promesas, el 50% de las veces que se ejecute el codigo lanzara error. Para no hacerlo manualmente.
	```     const getUser = new Promise((todoBien, todoMal) => {
	       if (Math.random() < 0.5) {
	    	todoBien();
	       } else {
	    	todoMal('Se acabo el tiempo');
	      }
	    });
	    
	    getUser
	      .then(() =>console.log('todo bien'))
	      .catch((message) =>console.log(message))
	    
	    
	```

* **J Daniel Rodriguez Davis** (3) [809858](https://platzi.com/comentario/809858/) 

	El unico incomveniente es acostumbrarte a la sintaxis. Eso me esta partiendo en dos haha!

* **David Alejandro Mosquera Moreno** (2) [1093134](https://platzi.com/comentario/1093134/) 

	Aquí un pequeño ejemplo de **Promesas** tomando como escenario solicitar un viaje en **Uber** y de ejemplo este vídeo  
	**_[JavaScript - Promise in 100 Seconds](https://www.youtube.com/watch?v=RvYYCGs45L4)_**  
	.  
	![promise-uber.png](https://static.platzi.com/media/user_upload/promise-uber-0779a22b-532b-4285-9595-5f51e0b3aaf3.jpg)

	* **carlosextra1** [1093134] (2)

		El ejemplo muy claro y efectivo! Gracias!

* **haroldolopezdev** (2) [1058527](https://platzi.com/comentario/1058527/) 

	Entendí mejor que en fundamentos de JS. Gracias Leonidas!! 😄

* **Angélica María Rocha García** (2) [1049006](https://platzi.com/comentario/1049006/) 

	Muchas gracias Leonidas tu como siempre el mejor profesor me quedo más claro que en fundamentos de JS, gracias por ese refuerzo.

* **Rodrigo Cuenca Mayuri** (2) [1021543](https://platzi.com/comentario/1021543/) 

	Muy buena explicación. La verdad es que en el curso de Fundamentos con Javascript con Sacha Lifszyc no entendí muy bien el concepto de promesas. Pero, siempre estará leonidas para explicarte de manera más entendible las cosas 😄.

* **richie** (2) [983314](https://platzi.com/comentario/983314/) 

	Excelente clase, logré entender perfectamente las promesas

* **Enrique Felix Garcia Stave** (2) [688601](https://platzi.com/comentario/688601/) 

	 **Apuntes:**  
	Una promise, es un objeto que es interpretado como el éxito o el fracaso de una operación asíncrona (entiéndase como operación asíncrona un evento callback, o una función, que dentro de sus sentencias, ejecuta el llamado a otra función y la primera de estas es pasada al stack of frames).  
	Una promesa tiene tres estados: **pendiente, resuelta o rechazada.**
	
	**setInterval:** Nos permite repetir una acción cada cierto momento.  
	**SetTimeout:** Espera un tiempo definido para hacer la ejecución de una acción (o acciones).

* **jesus-gallardo** (2) [601513](https://platzi.com/comentario/601513/) 

	 **.then()** lo usamos cuando la promesa se resuelve.  
	**.catch()** lo usamos para manejar errores en la promesa.

* **Kingsman7** (2) [587944](https://platzi.com/comentario/587944/) 

	Por suerte no eres Thanos

* **Lucas Czumadewski** (2) [586433](https://platzi.com/comentario/586433/) 

	Este video me parece demasiado malo, no entiendo amiguito, explicas todo muy por encima, rapido y sin mucha precisión. mal curso este leonidas, somos personas no computadoras para que nos cargues la información de la función de esta forma

	* **Aram Ceballos Gonzalez** [586433] (3)

		Si te interesa aprender acerca de promesas mas a profundidad puedes ir al curso de Fundamentos de JavaScript, ahi se explica de una manera mas detallada

	* **Favio Náquira** [586433] (1)

		Este curso asume que ya has llevado otros de la carrera para tener la base correcta, por eso se concentra en la carnecita de lo que debemos aprender, no repasar cosas que ya deberíamos saber para este. Te recomiendo checar los cursos básicos, que al inicio yo también salté por un tema de vanidad, pero luego descubrí tenían muchas joyas como estos conceptos.

	* **eduardo110298** [586433] (3)

		Que curioso, justamente yo me sentía un poco incómodo pero era por lo contrario a lo que dices. Pienso que ya estas cosas fueron explicadas en el curso de fundamentos de Javascript. Pero luego de ver el curso de fundamentos, he escuchado lo de las promesas en el curso básico de nodejs y ahora también en este curso. Pienso sería bastante cómodo definir los cursos requisito para iniciar otro curso. Y así se evita andar repitiendo las cosas cada vez. Si algo no queda claro en un curso, tenemos la posibilidad de repetir el video. Y si el problema es la explicación, tenemos disponible una sección de preguntas. Pienso que hay un problema de duplicación de temas.

	* **Miguel Angel Morales Larriega** [586433] (1)

		Aunque coincido con Eduardo, tambien he visto los cursos anteriores Js y Node, lo que puedo rescatar es que cada profesor tiene su método de explicación y ayudan a entender mejor sobre el tema.

* **Hattori** (2) [568651](https://platzi.com/comentario/568651/) 

	Promesas: Vamos a mandar una función para ver si esta falla o hace las cosas que debería de hacer  
	Una promesa se añade con new Promise, esto recibe una función que a su vez recibe dos parámetros, que se ejecutarán en algún momento dependiendo de si su promesa funciona o no  
	.then() cuando la promesa funciona  
	.catch() cuando la promesa no funciona
	
	con setTimeout() se ejecuta solo una vez dentro de un tiempo  
	con Promise.race() se visualizará el resultado que se resuelva antes

* **Moises Aceves** (2) [510553](https://platzi.com/comentario/510553/) 

	Exelente con javacript

* **Juan Camilo Lezcano Benitez** (2) [483888](https://platzi.com/comentario/483888/) 

	¿El punto y coma (😉 al final de las sentencias es algo opcional en Javascript?

	* **JhonColorado07** [483888] (4)

		Si por supuesto. Leonidas en la clase lo usa quizas por costumbre porque desde hace algunas versiones de Javascript ya no es obligatorio. Pero si trabajas con más desarrolladores es ideal que te pongas de acuerdo con ellos para ver si utilizan el punto y coma.

	* **Ondigital S.A.S** [483888] (4)

		Si es opcional, en linters de código puedes configurar si lo quieres usar o no para que tu equipo de desarrollo escriba el código con ciertos estándares y así todos trabajen bajos ciertos patrones.

* **JhonColorado07** (2) [481884](https://platzi.com/comentario/481884/) 

	Excelente explicación de promesa. Muy claro con el ejemplo dado por Leonidas

* **SEBASTIAN PADUANO** (2) [439278](https://platzi.com/comentario/439278/) 

	leonidas es el mejor profesor de todos

* **marcoguerreroalfaro** (2) [359181](https://platzi.com/comentario/359181/) 

	El uso de promesas es solo con ECMAScript 6?

	* **Joel Alejandro Morales García** [359181] (3)

		Así es, es parte de las nuevas especificaciones a partir de ES6

	* **juand_silva** [359181] (3)

		El objeto [Promise](https://promisesaplus.com/) de JavaScript se creo para resolver el problema de los callback hell, pero para ES2017 se creó un sintax sugar que hace más fácil su uso, que es la función [async/await](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/funcion_asincrona) que particularmente es mucho más limpio, más fácil de entender y código más ordenado.

* **Juan Camilo Lezcano Benitez** (2) [50820](https://platzi.com/comentario/483888/) 
¿El punto y coma (😉 al final de las sentencias es algo opcional en Javascript?

	* **JhonColorado07** [50820] (4)

		Si por supuesto. Leonidas en la clase lo usa quizas por costumbre porque desde hace algunas versiones de Javascript ya no es obligatorio. Pero si trabajas con más desarrolladores es ideal que te pongas de acuerdo con ellos para ver si utilizan el punto y coma.

* **IvanAlexO** (2) [48547](https://platzi.com/comentario/456034/) 
Muy bueno el video y la explicación. La promesas funcionan como Threads?? o qué es lo que los diferencian??

	* **Eduardo Hidalgo Díaz Rugama** [48547] (5)

		en esta pregunta respondo muy bien que son las promesas y su razón de ser (: te invito a leerlo:
		
		<https://platzi.com/comentario/446170/>

* **carlosextra1** (1) [1114873](https://platzi.com/comentario/1114873/) 

	Que es Primise.all() y como funciona! Ojo esto es un marcador!

* **Pau Pregoni Juan** (1) [1085001](https://platzi.com/comentario/1085001/) 

	Les dejo la práctica que me llevó a entender perfectamente este concepto, puede sonar un poco bizarro pero lo entendí a la primera jaja!
	
	![Promise Ejemplo.png](https://static.platzi.com/media/user_upload/Promise%20Ejemplo-b6871a70-c280-471c-bf54-5de3595adf42.jpg)

* **JheysonEGalvis** (1) [1064798](https://platzi.com/comentario/1064798/) 

	“La gestión de errores en tiempo de ejecución tiene como elementos principales las sentencias try – catch, throw, y el tipo de datos definido por JavaScript Error”.

* **Cristian Marin** (1) [1021516](https://platzi.com/comentario/1021516/) 

	Leonidas debería dar todos los cursos de Platzi !!!

* **neontigermx** (1) [1018681](https://platzi.com/comentario/1018681/) 

	Me encantan las clases de Leonidas, entendido lo que es una promesa 😃

* **Kenet Chungandro** (1) [985181](https://platzi.com/comentario/985181/) 

	Excelente 😄, completamente entendido las Promesas.

* **Alvaro ponguta** (1) [965957](https://platzi.com/comentario/965957/) 

	Tengo problema con esta promesa al ejucutarla en el navegador cual podria ser el error .

	* **Ronald  Gamaliel Perez Talavera** [965957] (1)

		Por ejemplo que en ese momento se desconecte del internet

* **Alvaro ponguta** (1) [965954](https://platzi.com/comentario/965954/) 

	//promesas  
	const getdata=new Promise(function(Todobien,todo_mal){  
	todo_mal();  
	Todobien();  
	})
	
	getdata  
	.then(function() {  
	console.log(‘Se acabó el tiempo’)  
	})

	* **degranda (Platzi)** [965954] (1)

		El error es que estás mandando llamar primero la función de “todo_mal”, lo que te dará un “rejected”.
		
		Manda llamar solo la función de “Todobien” y prueba.

* **Juan Gaybre Flores** (1) [925354](https://platzi.com/comentario/925354/) 

	También los mismos ejemplos aplicando Promise.all y Promise.race  
	.  
	Promise.all ejecutará todas las promesas y mostrará el resultado hasta que todas terminen, si alguna falla ahí muestra el error y el resto no se ejecutará.  
	![promiseAllCode.JPG](https://static.platzi.com/media/user_upload/promiseAllCode-59ad3552-5d63-4636-be3d-9508dd171acf.jpg)  
	![promiseAll.JPG](https://static.platzi.com/media/user_upload/promiseAll-be0b60f2-c3a2-4402-9e46-21892d461642.jpg)  
	.  
	Promise.race ejecutará solamente la Promesa que primero se resuelva sea exitosa o fallida  
	![promiseRace.JPG](https://static.platzi.com/media/user_upload/promiseRace-475ff6ee-d9e9-4675-9e3f-223ff50349c6.jpg)

* **Percy Tejada Donayre** (1) [917789](https://platzi.com/comentario/917789/) 

	Podemos encontrar más conceptos en la documentación de MDN, aquí les va el link:  
	<https://developer.mozilla.org/es/docs/Web/JavaScript/Guide/Usar_promesas>

	* **Christian Erik Velázquez Morales** [917789] (2)

		Gracias.

* **JarlLuis** (1) [771051](https://platzi.com/comentario/771051/) 

	“Asi que nuestra promesa de la clase anterior aun no se ha resuelto” jajajaj

* **hendersonnozartgibajareyes** (1) [762310](https://platzi.com/comentario/762310/) 

	Alguien me puede recomendar un libro bueno sobre javascripts

	* **JarlLuis** [762310] (1)

		Te recomiendo leerte todo el tutorial de w3s, tiene muy buena documentación

	* **Ruben Padilla** [762310] (1)

		[Eloquent Javascript](https://eloquentjavascript.net/)

* **jhonatan-chahuayo-boza** (1) [731666](https://platzi.com/comentario/731666/) 

	que pasa si mandamos dos promesas con distintos resultados…??

	* **Jonathan Uriel Jiménez Soveranes** [731666] (1)

		Lo mismo me pregunto 😕

	* **Jonathan Uriel Jiménez Soveranes** [731666] (1)

		Al parecer lo que ocurre es que entra al catch porque uno de los dos falló!

	* **Daniel Esteves** [731666] (1)
Cuando se hace Promise.all si alguna de las dos Falls retornará el error de la promesa que falló y el código para ahí

* **Camilo Andrés Santana Lizcano** (1) [684285](https://platzi.com/comentario/684285/) 

	Excelente explicación sobre el funcionamiento de las promesas.  
	Para los que se topen con este tema por primera vez, les recomiendo entenderlo por completo antes de avanzar en el curso

* **Jose Luis Colmenares** (1) [669744](https://platzi.com/comentario/669744/) 

	Me encantó el inicio de esta clase cuando no explican como la promesa de explicarnos las Promesas no se había resuelto porque se estaba ejecutando de forma asíncrona jajaja

* **Anderson Ricardo Ventosilla Trujillo** (1) [610147](https://platzi.com/comentario/610147/) 
No se deja ver el vídeo!!! Porque?

	* **Jair Israel Avilés Eusebio** [610147] (2)

		Podrias intentar cambiando el servidor y esperar que recargue el reproductos de video.

	* **Daniel Esteves** [610147] (1)
Hola Ander! Esto se debe muchas veces a tu conexión hasta el servidor de las clases, prueba por favor en los ajustes de la clase utilizar cualquiera de los tres servidores y si te sigue dando error por favor comenta por acá

* **segmax** (1) [600582](https://platzi.com/comentario/600582/) 

	Muy interesante las variables y el ECMA. No conocía sobre ello y no sabía sobre las demás palabras reservadas, siempre use el var, ahora utilizaré las otras ya que muchas veces queremos variables que no cambien durante toda la ejecución!

* **Jhon Alexander Perez Valencia** (1) [597493](https://platzi.com/comentario/597493/) 

	 ** _javascript_** manejo de tiempos
	```     setInterval(function(){}, tiempo);
	    setTimeout(function(){}, timepo);
	    
	```

* **DemetrioDelCarmen** (1) [590467](https://platzi.com/comentario/590467/) 

	Cuando realizamos una promesa, podemos esperar dos respuestas de la misma: que se resuelva y traiga datos del servidor, y que no se resuelva y tenga que hacer otra cosa porque no pudo culminar su función.
	
	Para estos casos se utilizan las siguientes instrucciones:  
	.then() -----> en caso de que resuelva, define el qué va a hacer con la data que obtenga.
	```     		.catch() -----> en caso de que la promesa no se resuelva, qué hará para compensar la falla.
	```

* **Nicolas Esteban Prieto Sarmiento** (1) [578300](https://platzi.com/comentario/578300/) 

	Fácil acordarse de then ya que suena con ten

* **Hattori** (1) [575071](https://platzi.com/comentario/575071/) 

	setInterval() se va a ejecutar cada cierto tiempo  
	setTimeout() se va a ejecutar una sola vez en un determinado tiempo
	
	.then() si todo va correcto  
	.catch() si algo sale mal

* **Carlos Zea** (1) [573006](https://platzi.com/comentario/573006/) 

	jaja, literalmente lo que te de la gana.

* **CARLOS  ANDRÉS VALENCIA NIETO** (1) [555384](https://platzi.com/comentario/555384/) 

	x

* **Antonio Nicasio** (1) [533515](https://platzi.com/comentario/533515/) 

	gracias excelente conceptos.

* **Félix Alejandro Zelaya Orellana** (1) [526600](https://platzi.com/comentario/526600/) 

	Gwnial!! Al fin aprendi promesas jaja

	* **Andrés Felipe Eslava Zuluaga** [526600] (1)

		Siiii jajaja

* **heysoypaez** (1) [492428](https://platzi.com/comentario/492428/) 

	Una promesa nos permite manejar funciones asincronas a traves de código javascript nativo.
	
	Dejo mis apuntes de como maneje el código
	```     /*
	    Autor: Daniel Páez
	    Titulo: De jquery a javascript
	    Descripcion: pasando De jquery a javascript
	    */
	    
	    /*INICIALIZACION DE ARCHIVO
	    ======================================================*/
	    
	    console.log('Inicializando archivo');
	    
	    /*DECLARACION DE VARIABLES
	    =========================================================*/
	    
	    const nombreUsuario = "Daniel";
	    let usuario = "@heysoypaez"
	    
	    
	    
	    /* DECLARACION DE FUNCIONES
	    =========================================================*/
	    
	    functioncambiarNombre(nuevoNombre) {
	      cambia = nuevoNombre
	    }
	    
	    
	    /* DECLARACION DE PROMESAS
	    =========================================================*/
	    
	    
	    //Creando una promesa
	    const getUser = newPromise(function(todoChevere,error) {
	    
	    	// llamar un api
	    	setTimeout(function(){
	    		//Luego de 3 segundos se ejecuta esto
	    		todoChevere("GANE? No, llegue en 3 segundos");
	    	}, 3000)
	    
	    	if(!todoChevere) {
	    
	    	error("Damas y caballeros... hubo un error")
	    	
	    	}
	    
	    })
	    
	    //Creando una promesa
	    const getUserAll = newPromise(function(todoChevere,error) {
	    
	    	// llamar un api
	    	setTimeout( function() {
	    		//Luego de 3 segundos se ejecuta esto
	    		todoChevere("GANE A LOS 2 SEGUNDOS");
	    	}, 2000)
	    
	    	if(!todoChevere) {
	    
	    	error("Damas y caballeros... hubo un error")
	    
	    	}
	    
	    	
	    })
	    
	    
	    /*EJECUCION (CONSUMO) DE PROMESAS
	    =========================================================*/
	    
	    
	    	/*PROMISE.RACE()
	    	Este método envia a la promesa que termine primero */
	    
	    	Promise.race([
	    	getUser,
	    	getUserAll
	    	])
	    
	    	.then(function(mensaje) {
	    	console.log(mensaje)
	    	})
	    
	    
	    
	    
	    //Metodos si todo va bien
	    getUser
	    	.then( function (mensaje) {
	    
	    	console.log(`HEY ${mensaje}`)
	    
	    	})
	    
	    	
	    	/* PROMISE.ALL() 
	    	Espera a que todas las promesas esten listas 
	    	y las ejecuta todas en el orden declarado */
	    
	    	Promise.all([
	    	getUser,
	    	getUserAll,
	    	])
	    
	    	.then(function(mensaje) {
	    
	    	console.log(`Ya en serio ${mensaje}`)
	    
	    	})
	    	
	    
	    	
	    	//Metodo de Promise cuando las cosas van mal
	    	.catch( function(mensaje) {
	    
	    	console.error(mensaje)
	    
	    	})
	    
	    
	```

* **IvanAlexO** (1) [456034](https://platzi.com/comentario/456034/) 

	Muy bueno el video y la explicación.  
	La promesas funcionan como Threads?? o qué es lo que los diferencian??

	* **Eduardo Hidalgo Díaz Rugama** [456034] (5)

		en esta pregunta respondo muy bien que son las promesas y su razón de ser (: te invito a leerlo:
		
		<https://platzi.com/comentario/446170/>

* **gosunkugi** (1) [444796](https://platzi.com/comentario/444796/) 

	gente… si mal no entendí, all devuelve los resultados en un array en caso que todas las promesas se resuelvan exitosamente, pero race las va ejecutando a medida que se van resolviendo, ¿si?

	* **Eduardo Hidalgo Díaz Rugama** [444796] (1)

		según: <https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Promise/race>
		```     El método Promise.all(iterable) devuelve una promesa que termina correctamente cuando todas las promesas en el argumento iterable han sido concluídas con éxito, o bien rechaza la petición con el motivo pasado por la primera promesa que es rechazada.
		    
		```
		
		La descripción es bastante explícita.

* **Gerardo Ruiz** (1) [442667](https://platzi.com/comentario/442667/) 

	No me quedó muy claro de que sirve hacer el Promise.all()

* **Juan Diego Villamizar Leal** (1) [431984](https://platzi.com/comentario/431984/) 

	Quien tiene el zip de los archivos completos que se utilizan en el curso no me esta dejando descargar el proyecto… Gracias

	* **Edward Steven Ramos Palacios** [431984] (1)

		Aún tienes problemas para descargar el proyecto?

	* **Daniel Esteves** [431984] (1)
Hola! Aún tienes problemas para descargarlos? Usualmente puede arreglarse con solo recargar la página, si tienes alguna duda o no te funciona responde este comentario 💚

* **Flor Viviana  Mireles Rivera** (1) [417404](https://platzi.com/comentario/417404/) 

	siempre tienes que poner 2 opciones en Promise ?  
	(en este caso todoBien y todoMal)

	* **Jcion** [417404] (2)

		No sé si sea obligatorio pero tendrías que, para manejar lo que pasa si se realiza todo bien o si hay algún error.

	* **Daniel Esteves** [417404] (1)
Es lo más recomendable ya que si el código falla das feedback de lo que pasó y si todo bien pues retornas el resultado 💚

* **Flor Viviana  Mireles Rivera** (1) [416008](https://platzi.com/comentario/416008/) 

	Promise siempre debe tener 2 variables?

	* **Andrés Yoshimar Aguilar Salgado** [416008] (4)

		si, las promesas siempre reciben 2 parámetros que son las funciones resolve y reject

* **gonzoblasco** (1) [413725](https://platzi.com/comentario/413725/) 

	Uso de Promesas /// .then().catch()

* **R3mb** (1) [392632](https://platzi.com/comentario/392632/) 

	muy buenos ejemplo

* **Cesar Anzules** (1) [374126](https://platzi.com/comentario/374126/) 

	Cuando tengo algunas promesas ejemplo: tengo 3 promesas donde cada una de ella necesita datos de la otra , osea tengo promesa1 que la promesa 2 va utilizar su datos y que a su vez promesa 3 va utilizar de la promesa 2 , al utilizar Promise.all(promsea1,2,3) me aseguro que las promesas se van ejecutar en ese orden ?

* **Nelson Estuardo Rodríguez Matul** (1) [374047](https://platzi.com/comentario/374047/) 

	Definitivamente con ES6 hay que empezar a usar el Js vanilla

* **danielmorales917** (1) [85980](https://platzi.com/comentario/1086217/) 
Para qué sirve el Promise.race? En la clase se dice que es para ver cual promesa termina primero, pero no entiendo porque no muestra el r...

	* **Wonder Jhonny Diaz Gonzalez** [85980] (1)

		Hola, el método Promise.race retorna una promesa que se cumplirá o no tan pronto como una de las promesas del argumento iterable se cumpla o se rechace, con el valor o razón de rechazo de ésta. En el siguiente articulo se muestra un ejemplo: [Promise.race](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Promise/race)

* **Ang.O** (1) [74361](https://platzi.com/comentario/851321/) 
en los console.log tengo la palabra message ¿porque no se imprimio la palabra literal message y si el string que tengo en todo bien?

	* **Juan David Castro (Platzi)** [74361] (1)

		Puedes mostrarnos tu código y te ayudamos a encontrar por qué te pasas eso. 😉

* **Víctor Hugo Arango Madrid** (1) [73011](https://platzi.com/comentario/826646/) 
Hola intento pegar todo el código y porque al hacerlo lo reduce a la linea que se ve a continuación, que es lo que pasa, porque no me dej...

	* **Moisés Cedeño** [73011] (1)

		¿No crees que se te ha olvidado algo, amigo? 😄
		
		Te faltó añadir el código en cuestión.

* **Víctor Hugo Arango Madrid** (1) [73010](https://platzi.com/comentario/826642/) 
const getUserAll = new Promise ( function (todoBien, todoMal) { setTimeout(function () { todoBien() }, 5000) }) const getU...

	* **Emmanuel García** [73010] (1)

		Le falta un espacio new Promise, por eso no te funciona. PD. probado con tu código
		```     newPromise 
		    
		    
		```

* **Víctor Hugo Arango Madrid** (1) [73009](https://platzi.com/comentario/826640/) 
¿Porque al ejecutar la función así: todoBien() y no así: todoBien(‘ Se acabó el tiempo’), el resultado es tan diferente pues lo que se mu...

	* **Daniel Esteves** [73009] (1)

		Hola! Porque lo más recomendable siempre es hacer un console.log “de la vieja escuela” para saber qué está pasando en el código

* **Víctor Hugo Arango Madrid** (1) [73008](https://platzi.com/comentario/826637/) 
¿Porque al ejecutar la función así: todoBien() y no así: todoBien(‘ Se acabó el tiempo’), el resultado es tan diferente pues lo que se mu...

* **webrevolution** (1) [71652](https://platzi.com/comentario/801651/) 
mucho embole las promesas!!

	* **Kevin Javier Morales (Platzi)** [71652] (2)

		Pueden ser complicadas de entender, pero actualmente están siendo muy usadas para muchas cosas en JS. Es normal no entenderlas a la primera, tomate tu tiempo y ve el video las veces que sea necesario hasta que logres aprenderlas. Tu puedes 💪

* **gosunkugi** (1) [47583](https://platzi.com/comentario/444796/) 
gente… si mal no entendí, all devuelve los resultados en un array en caso que todas las promesas se resuelvan exitosamente, pero race las...

	* **Eduardo Hidalgo Díaz Rugama** [47583] (1)

		según: <https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Promise/race>
		```     El método Promise.all(iterable) devuelve una promesa que termina correctamente cuando todas las promesas en el argumento iterable han sido concluídas con éxito, o bien rechaza la petición con el motivo pasado por la primera promesa que es rechazada.
		    
		```
		
		La descripción es bastante explícita.

* **evertescalante1** (0) [397779](https://platzi.com/comentario/397779/) 

	Hola a todos Comunidad tengo la siguiente duda, tal vez alguien puede ayudarme con ella:
	
	cuando creamos un timer como el siguiente
	```     setTimeout(resolve, 2000)
	    
	```
	
	entiendo que se ejecuta y el timer y este contiene un delay de 2 segundos para llamar a una referencia de el metodo resolve y se ejecuta bien
	
	-pero si necesito ponerle parametros entonces debo hacerlo asi
	```     setTimeout(() => resolve(‘se acabo el tiempo 2’), 2000)
	    
	```
	
	-de esta forma entiendo estoy colocando un llamado a al metodo resolve y no una referencia,
	
	  * porque si lo hago asi entonces no funciona
	```     setTimeout(resolve(‘se acabo el tiempo 2’), 2000)
	    
	```
	
	La duda es, ¿por que? sucede asi, gracias a quien pueda responder

	* **davidtoca (Platzi)** [397779] (2)

		Porque `setTimeout` en el segundo ejemplo recibe una funcion, esa funcion la ejecuta al pasar de los 2 segundos, en cambio en el tercer ejemplo tu estas **evaluando directamente la funcion** y luego **el resultado de esa funcion** se la pasas como parametro a setTimeout

* **Eden Gomez Gress** (0) [387724](https://platzi.com/comentario/387724/) 

	Saludos companeros alguien sabe porque me marca error la consola cuando escribo exactamente el codigo en consola que el profe nos ensena?
	
	![](https://snag.gy/294YOm.jpg)

	* **Eden Gomez Gress** [387724] (1)
![](https://snag.gy/ODNhiT.jpg)

	* **Martín Leyva** [387724] (2)

		EdenGg
		
		Disculpa que no voy a ser formal en mi respuesta, solo voy a indicar la fuente del error:
		
		    1. En la línea 13 de tu código:
		
		**getUser** es una **constante** a la que le fue asignada una función, no es una función,++ elimina los paréntesis++, y debe funcionar.
		
		    1. Parece ser que el error de la línea 9 de tu código, es consecuencia del error de la línea 13, (esto no lo puedo asegurar) pero prueba corregir el error de la línea 13, yo lo hice y ¡funcionó!.
		```     const getUser = newPromise(function(allGood, notGood) {
		    
		      setTimeout(function () {
		    
		        notGood();
		    
		      }, 3000)
		    })
		    
		    getUser
		      .then(function () {
		      console.log('All Good')
		    })
		    
		    .catch(function () {
		      console.log('Its all Bad')
		      })<code>
		    
		```
		
		Voy a intentar colocar una imagen:

	* **Martín Leyva** [387724] (2)
![](https://image.ibb.co/iqqC2U/BUG.png)

	* **Eden Gomez Gress** [387724] (1)

		ya lo resolvi companero tenas razon muchas gracias por tu ayuda!! 😃

## 0060. Tutorial de Ajax en jQuery y Javascript [12290](https://platzi.com/clases/1316-jquery-js/12290-tutorial-de-ajax-en-jquery-y-javascript/)

### Descripción:


Una característica muy solicitada en cualquier sitio dinámico es solicitar datos a un servidor, denominado API. Para esto normalmente se utiliza Ajax.

Ajax recibe dos parámetros los cuales son la url de la API y un objeto donde pondrás la configuración que se usara para realizar la petición. En la configuración se añaden dos funciones para manejar cuando la petición se realizo correctamente y cuando falla.

JavaScript internamente cuenta con una función llamada fetch que también realiza peticiones a una API. Al igual que Ajax necesita dos parámetros, una url y una configuración, pero si solo le mandas la url fetch usará una configuración por defecto donde el método HTTP será GET.  
fetch te regresa una promesa, esa promesa al resolverse te da los datos de respuesta y tiene un método llamado json que te regresa otra promesa con los datos en formato JSON.

Las promesas resuelven el problema del Callback Hell haciendo que una promesa pueda devolver otra promesa y en lugar de ser anidadas como los callback, estas promesas son encadenadas.

### Comentarios:

* **Sergio Minei** (41) [335357](https://platzi.com/comentario/335357/) 

	## Ajax
	
	**jQuery**
	```     $.ajax("url", {
	      method: "GET", //POST, PUT, DELETE
	      success: function(data) {
	        //se ejecuta cuando todo sale bien
	        //data: lo que devuelve el api
	      },
	      error: function(error) {
	        //se ejecuta cuando hay un error
	        //error: mensaje de error del api
	      }
	    })
	    
	```
	
	**JavaScript**
	```     fetch("url")
	      .then(function(response) {
	    
	      })
	      .catch(function(response) {
	    
	      })
	    
	```
	
	Fetch devuelve una promesa. Esta promesa, a su vez, tiene un método llamado `json()` que regresa otra promesa con los datos en formato JSON.
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **Alan Santiago** [335357] (6)

		Definitivamente se deberia dejar de usar jQuery 😄

	* **Jesus Ricardo Moreira Cedeño** [335357] (3)

		Gracias por el aporte 😃

	* **Sergio Minei** [335357] (2)

		Gracias @Jesucristhorr!

	* **tomas-alegre146** [335357] (5)

		En respuesta al comentario de arriba no se debe “dejar” de usar jQuery si no que saber cuando ocupar que cosa.

	* **Sergio Minei** [335357] (5)

		Hola @tomas-alegre146! Concuerdo que es importante entender cómo funcionan las cosas y no simplemente usar una librería, pero en el caso de jQuery, la verdad es que en la actualidad no aporta nada. Todo lo que hace jQuery se puede hacer muy facilmente con vanilla js y al final jQuery solo hace que tu app sea mucho más pesada.

	* **Liantony Pozo** [335357] (3)

		concuerdo que aprender un poco de jQuery para entender como se hacían las cosas es importante pero no dedicarle tanto tiempo ya que es algo que esta casi obsoleto

	* **Sergio Minei** [335357] (2)

		Concuerdo contigo @liantony02

	* **Alfonso Navarro** [335357] (4)

		Excelente no había dado aun en como remplazar el ajax con Jquery es de las pocas cosas q’ me faltaba, aunq’ no he migrado al 100% allí vamos haciéndole. Animos q’ si se puede

* **Michael  Bolaños Vargas** (14) [357496](https://platzi.com/comentario/357496/) 

	Random User:  
	<https://randomuser.me/api>
	
	Otro API de prueba - Star Wars Information  
	<https://swapi.co/>

	* **Joel Alejandro Morales García** [357496] (1)

		Geniales para pruebas 😄

	* **Jose Rueda** [357496] (1)

		otra api  
		[](https://pokeapi.co/)

	* **Jose Rueda** [357496] (4)

		otra api  
		<https://pokeapi.co/>

	* **Kevin Humberto León Santamaria** [357496] (1)

		Gracias compañeros

	* **jesus-gallardo** [357496] (2)

		Tambien pueden buscar la api de MARVEL

	* **alexabellerman** [357496] (1)

		Tambien esta bien jsonplaceholder

* **AryRosvall** (11) [480039](https://platzi.com/comentario/480039/) 

	Mucho más limpio usando arrow functions 😄
	```     fetch('https://randomuser.me/api/')
	      .then(response => response.json())
	      .then(user => console.log('user', user.results[0].name.first))
	      .catch(() =>console.log('algo fallo'))```
	    
	```

* **Byhako** (7) [340481](https://platzi.com/comentario/340481/) 

	¿fetch o axios?

	* **Oscar Barajas Tavares (Platzi)** [340481] (5)

		Fetch, aunque hay que destacar que con axios, aseguramos que siempre funcionará en cualquier navegador y la respuesta será obtenida en una promesa.

	* **eddyarellanes** [340481] (3)

		En un proyecto real, yo uso Axios, porque así desarrollo mucho más rápido y el código se mantiene pequeño. Fetch lo uso para practicar Javascript Moderno.

	* **Juan David Castro (Platzi)** [340481] (7)

		💡 Has probado Trae?  
		👉 <https://github.com/Huemul/trae>  
		🖖 Uno de los desarrolladores de esta librería es el profesor de los cursos de Vue en Platzi!

	* **viciotec** [340481] (4)

		Yo e utilizado Trae y la verdad esta muy buena

* **Aaron Felipe Isaacs** (5) [625758](https://platzi.com/comentario/625758/) 

	😎
	```     fetch('https://randomuser.me/apiaa/')
	    .then(res => res.json())
	    .then(data => console.log(data.results[0].name.first))
	    .catch(error => console.log(error))
	    
	```

* **doctumcl** (5) [386356](https://platzi.com/comentario/386356/) 

	Para aquellos que esten trabajando con laravel, les dejo un pequeño ejemplo de como usar el fetch con post
	```     	let token = "{!! csrf_token() !!}";
	    	fetch("{{route('name-your-route')}}", {
	    		method: 'POST',
	    		headers: {
	            	"X-CSRF-Token": token
	          	},
	    		body: JSON.stringify({
	    			name: 'luis',
	    			age: 28
	    		})
	    	}).then(function (data) {
	      		console.log('Request success: ', data);
	    	}).catch(function (error) {  
	      		console.log('Request failure: ', error);
	    	});
	    
	```

	* **Alfonso Navarro** [386356] (1)

		A probar haber q tal, gracias compañero…

	* **jorge-winder-avila** [386356] (1)

		Gran aporte. Muchas gracias!!

* **Japheth Calzada López** (5) [340122](https://platzi.com/comentario/340122/) 

	me encanto como quedo con fetch

	* **Oscar Barajas Tavares (Platzi)** [340122] (5)

		Fetch es muy potente

* **Emanuel Valero** (4) [919540](https://platzi.com/comentario/919540/) 

	Los métodos HTTP forman parte del protocolo de transferencia de datos en internet, permitiendo la comunicación entre clientes y servidores. Aunque los métodos HTTP más comunmente utilizados son GET, POST, PUT y DELETE, existen otros como HEAD o CONNECT que también son útiles en circunstancias particulares.
	
	Aca un par de enlaces para complementar el tema de HTTP:
	
	  * MDN: <https://developer.mozilla.org/es/docs/Web/HTTP/Methods>.
	  * W3S: <https://www.w3schools.com/tags/ref_httpmethods.asp>

* **Antonio7u7r** (4) [676921](https://platzi.com/comentario/676921/) 

	Aquí cambie las functions a arrow functions
	```     console.log('hola mundo!');
	    const noCambia = "Leonidas";
	    
	    let cambia = "@LeonidasEsteban"
	    
	    const cambiarNombre = (nuevoNombre) =>  cambia = nuevoNombre
	    
	    
	    const getUserAll = new Promise((todoBien, todoMal) => {
	      // llamar a un api
	      setTimeout(() => {
	        // luego de 3 segundos
	        todoBien('se acabó el tiempo');
	      }, 5000)
	    })
	    
	    const getUser = new Promise((todoBien, todoMal) => {
	      // llamar a un api
	      setTimeout(() => {
	        // luego de 3 segundos
	        todoBien('se acabó el tiempo 3');
	      }, 3000)
	    })
	    
	    // getUser
	    //   .then(function() {
	    //console.log('todo está bien en la vida')
	    //   })
	    //   .catch(function(message) {
	    //console.log(message)
	    //   })
	    
	    Promise.race([
	      getUser,
	      getUserAll,
	    ])
	    .then((message) =>console.log(message))
	    
	    .catch((message) =>console.log(message))
	    
	    // $.ajax('https://randomuser.me/api/', {
	    //   method: 'GET',
	    //   succes: (data) =>console.log(data),
	    //   error: (error) =>console.log(error)
	    // })
	    
	    fetch('https://randomuser.me/api/')
	      .then((response) => response.json())
	      .then((user) =>console.log('user', user.results[0].name.first))
	      .catch(() =>console.log('algo falló'))
	    
	```

* **Jhon Alexander Perez Valencia** (4) [597495](https://platzi.com/comentario/597495/) 

	## fetch
	```     fetch("url")
	      .then(function(response) {
	    
	      })
	      .catch(function(response) {
	    
	      })
	    
	```

* **pablojorgeandres** (4) [483364](https://platzi.com/comentario/483364/) 

	## Tutorial de Ajax en jQuery y Javascript
	
	Una característica muy solicitada en cualquier sitio dinámico es solicitar datos a un servidor, denominado API.
	
	Para hacer esto tenemos dos opciones:
	
	  * _$.ajax_ con jQuery
	  * _fetch_ con Vanilla JS
	
	**_Ajax_ \- jQuery**
	
	Ajax recibe dos parámetros los cuales son la url de la API y un objeto donde pondrás la configuración que se usara para realizar la petición. En la configuración se añaden dos funciones para manejar cuando la petición se realizo correctamente y cuando falla.
	
	_Referencia:<http://api.jquery.com/jquery.ajax/>_
	```     	$.ajax('htpps://randomuser.me/api/', {
	    		method: 'GET',
	    		success: function (data){
	    			console.log(data)
	    		},
	    		error: function (error) {
	    			console.log(error)
	    		}
	    	})
	    
	    
	```
	
	**_fetch_ \- JavaScript**
	
	JavaScript internamente cuenta con una función llamada _fetch_ que también realiza peticiones a una API.  
	Al igual que Ajax necesita dos parámetros, una url y una configuración, pero si solo le mandas la url fetch usará una configuración por defecto donde el método HTTP será GET.  
	_fetch_ te regresa una promesa, esa promesa al resolverse te da los datos de respuesta y tiene un método llamado _json()_ que te regresa otra promesa con los datos en formato JSON.
	```     	fetch("url")
	    	  .then( response => return response.json() )
	    	  .then( user => console.log(user) ) // datos en formato JSON
	    	  .catch( error => console.log(error) )
	    
	    
	```
	
	Las promesas resuelven el problema del Callback Hell haciendo que una promesa pueda devolver otra promesa y en lugar de ser anidadas como los callback, estas promesas son encadenadas.

* **devdanielgo** (4) [444377](https://platzi.com/comentario/444377/) 

	Con
	```     console.table()
	    
	```
	
	puedes ver la información recibida como una tabla

* **tomas-alegre146** (4) [440244](https://platzi.com/comentario/440244/) 

	Otra API: en mi tiempo libre hice una API que guarda y envia los sismos que ocurrieron en Chile.
	
	Le dejó la API por si alguien quiere usarla en algún momento.
	
	<https://api.alphacode.cl/sismos/>
	
	😃

* **hec_lopz** (3) [981036](https://platzi.com/comentario/981036/) 

	Información sobre JSON: [linka JSON](https://www.json.org/json-en.html)  
	y más sobre `fetch()` : [link a Fetch](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch)

* **Hattori** (3) [571087](https://platzi.com/comentario/571087/) 

	Una de las características mas solicitadas al momento de desarrollar una app dinámica en javaScript es que tu puedas pedir nuevos datos a un servidor o algún servicio al cual denominamos API y normalmente para hacer esto se utiliza una técnica llamada AJAX
	
	//Lo que realmente es el código de arriba es un XMLHttpRequest, que sirve para pedir datos de un servicio externo  
	$.ajax(‘<https://randomuser.me/api/dededede>’,{  
	method: ‘GET’,  
	success: function(data){  
	console.log(data)  
	},  
	error: function(error){  
	console.log(error)  
	}  
	})
	
	//JS VANILLA  
	fetch(‘<https://randomuser.me/api/>’)  
	.then(function(response){  
	[//console.log](//console.log)(response)  
	return response.json()  
	})  
	.then(function(user){  
	console.log(‘user’, user.results[0].name.first)  
	})  
	.catch(function(){  
	console.log(‘algo falló’)  
	})

* **Edward Acu** (3) [533463](https://platzi.com/comentario/533463/) 

	Una de las ventajas que yo veía en $.ajax es el manejo del beforeSend, específicamente lo utilizaba para mostrar loadings al momento que se ejecutaba la función, muestro un ejemplo:
	```     $.ajax({
	        beforeSend:function(){
	           openloading();
	        },
	        success: function(){
	           closeloading();
	        },
	        error: function(){ 
	           closeloading();
	        }
	    });
	    
	```
	
	como puedo tener esta funcionalidad con fetch? ya que lo que comprendí es que podría hacer algo como esto:
	```     fetch('https://randomuser.me/api/')
	      .then(  function(response) {
	        openloading();
	        return response.json()
	       })
	      .then(function(user){
	        closeloading();
	        console.log('user', user.results[0].name.first)
	      })
	      .catch(() =>console.log('algo fallo'))
	    
	```
	
	Si alguna alma bondadosa me puede corregir

	* **Eduardo Hidalgo Díaz Rugama** [533463] (5)

		con un flag. Un flag es una variable booleana que sirve como estado de algo.
		
		tu flag podria llamarse “loading”, y cuando sea true, significa que debe mostrar un progress bar, o una animación de loading, etc.
		
		En tu fetch, justo antes de ejecutarlo, debes hacer loading = true. cuando termine, debes hacer loadin = false.

* **feer_dimas** (3) [373155](https://platzi.com/comentario/373155/) 

	existe un curso para hacer API?

	* **mglBrv** [373155] (2)

		A mí también me interesa aprender como se crean las API. Como la de Star Wars (<https://swapi.co/>)

	* **Josue David Pablo Ixbalan** [373155] (2)

		En alguno de los cursos de BackEnd de platzi van a encontrar como crear una API REST

	* **Mramon** [373155] (1)

		API REST : <https://platzi.com/clases/jee/>

* **Eduardo Ignacio Denis** (3) [356368](https://platzi.com/comentario/356368/) 

	Diferencia entre **fetch **y **$.ajax**[https://developer.mozilla.org/es/docs/Web/API/Fetch_API/Utilizando_Fetch](url)
	
	La especificación fetch difiere de JQuery.ajax() en dos formas principales:
	
	El objeto Promise devuelto desde fetch() no será rechazado con un estado de error HTTP incluso si la respuesta es un error HTTP 404 o 500. En cambio, este se resolverá normalmente (con un estado ok configurado a false), y este solo sera rechazado ante un fallo de red o si algo impidio completar la solicitud.
	
	Por defecto, fetch no enviará ni recibirá cookies del servidor, resultando en peticiones no autenticadas si el sitio permite mantentener una sesión de usuario (para mandar cookies, credentials de la opción init deberan ser configuradas). Desde el 25 de agosto de 2017. La especificación cambió la politica por defecto de las credenciales a same-origin. Firefox cambió desde la versión 61.0b13.

* **viciotec** (3) [352979](https://platzi.com/comentario/352979/) 

	En fetch, no ocupas especificar si es get o post?

	* **davidtoca (Platzi)** [352979] (10)

		por defecto es GET, pero si es POST deben especificarlo con un atributo “method”
		```     fetch(url, {
		      method: "POST",
		      body: JSON.stringify(data),
		      headers: {
		        "Content-Type": "application/json"
		      },
		      credentials: "same-origin"
		    })
		    
		    
		```

* **carlosextra1** (2) [1114960](https://platzi.com/comentario/1114960/) 

	uff debo confesar que no conocía la función de fetch y es genial. Pero solo se usa para hacer peticiones GET?

	* **José María Cuevas Ramírez** [1114960] (1)

		No, también le puedes pasar un objeto de configuración especificando el método
		```      fetch('https://api.com/api/', {
		                    	method: 'POST',
		                   		body: {}
		                })
		    .then(response => {
		    	// code
		     }).catch(error => {
		      //code
		    })
		    
		```
		
		😃

* **Diego Fernando Rojas Quintero** (2) [1082366](https://platzi.com/comentario/1082366/) 

	Qué es AJAX y como funciona
	
	<https://www.youtube.com/watch?v=_ybgWmSCAu8>

	* **luistaurik** [1082366] (1)

		Gracias por el dato Diego.

* **Rodrigo Cuenca Mayuri** (2) [1024026](https://platzi.com/comentario/1024026/) 

	Hola!,¿ Alguien sabe cómo solucionar este problema?  
	He creado un mostrador de pokemones desde la API [https://pokeapi.co/](PokeAPI). Sin embargo, tengo un error. Al parecer mi petición al servidor siempre entra el método catch(), cuando este sólo debería entrar cuando se produjo un error en la petición. Les comparto los links:
	
	Link del repositorio: [https://github.com/RCuencam/PokeAPI/](url)
	
	Link de la página: [https://rcuencam.github.io/PokeAPI/](url)
	
	Si abren la consola e interactúan con la ventana verán que siempre entra al método catch().

	* **Diego Alexander Forero Higuera (Platzi)** [1024026] (3)

		El catch esta mal definido debes poner algo como  
		`.catch((error)=>console.log("(Error) Siempre entra al catch de la API Fetch."))`  
		Con esto ya te funciona.

	* **Rodrigo Cuenca Mayuri** [1024026] (1)

		Es verdaaad recibe una función. Casi siempre me suelo confundir en error de sintaxis. Graciass!!

* **havelka** (2) [998442](https://platzi.com/comentario/998442/) 

	fetch(‘<https://randomuser.me/api/>’)  
	.then((response) =>{  
	console.log(response)  
	return response.json()  
	})  
	.then((data) =>{  
	console.log(‘user’, data.results[0].name.first)  
	})  
	.catch(()=>{  
	console.error(‘Algo falló’)  
	})

* **pinfradev** (2) [947606](https://platzi.com/comentario/947606/) 

	<https://randomuser.me/>

* **lmoran** (2) [898887](https://platzi.com/comentario/898887/) 

	Tenemos cuatro métodos method:  
	• GET: traemos datos desde alguna API.  
	• PUSH: enviamos datos a algún servidor.  
	• PUT: Actualizar datos.  
	• DELETE: borrar datos.

	* **Christian Erik Velázquez Morales** [898887] (1)

		Gracias por el dato.

* **Jonathan Uriel Jiménez Soveranes** (2) [800302](https://platzi.com/comentario/800302/) 

	```    var misCabeceras = new Headers();
	    
	    var miInit = { method: 'GET',
	                   headers: misCabeceras,
	                   mode: 'cors',
	                   cache: 'default' };
	    
	    fetch('flores.jpg',miInit)
	    .then(function(response) {
	      return response.blob();
	    })
	    .then(function(miBlob) {
	      var objectURL = URL.createObjectURL(miBlob);
	      miImagen.src = objectURL;
	    });
	    
	```

* **Enrique Felix Garcia Stave** (2) [688767](https://platzi.com/comentario/688767/) 

	**Apuntes:**  
	XMLHttpRequest se trata de un objeto de JavaScript diseñado por Microsoft y adoptado por los principales navegadores, por lo que actualmente es un estándar de W3C. Esta herramienta nos proporciona una manera sencilla de tener información de un link sin tener que hacer un refresh completo de la página, por lo que una página solo tendría que actualizar parcialmente su información sin interrumpir el resto de procesos. XMLHttpRequest es actualmente muy usado en AJAX.
	
	Por otro lado, la Fetch API nos permite proporcionarnos una interfaz en JavaScript para acceder al canal HTTP de peticiones y respuestas, lo que también nos proporciona un método fetch() que nos permite acceder a estos recursos de manera asíncrona fácilmente.

* **Renier Ferrer** (2) [673954](https://platzi.com/comentario/673954/) 

	OMG… Bravooo.!!

* **Jesús Duarte García** (2) [564868](https://platzi.com/comentario/564868/) 

	¿Hay necesidad de usar un doble catch justo como el doble then? Entiendo que al usar then se devuelve una segunda promesa en formato json pero ¿Esa promesa tiene forma de fallar?

	* **Jair Israel Avilés Eusebio** [564868] (1)

		Depende, te recomiendo leer el siguiente [thread](https://stackoverflow.com/questions/26076511/handling-multiple-catches-in-promise-chain) en StackOverflow para contestar tu respuesta 😄

* **Nicolas Esteban Prieto Sarmiento** (2) [561571](https://platzi.com/comentario/561571/) 

	se le puede añadir atributos al api por el link,  
	ejemplo
	```     "https://uinames.com/api/?region=colombia"
	    
	```
	
	Con el **`?`** se le puede añadir el atributo **(todo esto siguiendo la documentación del api)**
	```     "https://uinames.com/api/?region=colombia&gender=female"
	    
	```
	
	conel **`&`** se le puede añadir 2 o mas atributos

* **Julián Fernando Amaya Díaz** (2) [527627](https://platzi.com/comentario/527627/) 

	Hola a todos, han probado Fetch vs Ajax respecto al tiempo que se demora en hacer una petición ? lo probé en mi trabajo haciendo peticiones get y resulta que con la prueba que implementé Ajax es más rápido que Fetch. Para un ejemplo simple a Ajax le toma 16 ms traer los datos mientras que con Fetch toma 105 ms. Realicé varias pruebas y casi siempre arrojo resultados similares respecto a la diferencia. Teóricamente no sé si Ajax es más rápido que Fetch pero está prueba me sorprendió. Me gustaría saber sus opiniones si alguien ha explorado más el tema. Gracias.

	* **Oscar Barajas Tavares (Platzi)** [527627] (3)

		Yo probaría sobre, XMLHttpRequest, Fetch, Ajax y Axios y elegir el mejor para tu proyecto, cada uno de ellos tiene sus características particulares que lo harían destacar sobre esos elementos.

* **jlquichimbo** (2) [416634](https://platzi.com/comentario/416634/) 

	Inicio conversión Ajax a Vanilla JS.

* **jasanhdz** (2) [378137](https://platzi.com/comentario/378137/) 

	```    // Básicamente vamos a tener una variable global que va a estar declarada en jquery que es $
	    // Pero primero debemos importar la libreria de jQuery a nuestro html, de lo contrario no nos
	    // va a funcionar 
	    // Pedir datos a un servidor, o algún servicio a lo cuál denominamos API
	    // Usando JQuery 
	    // Mandamos 2 párametros, 1° una URL, y 2° una configuración que a su vez va a ser un objeto
	    // ajax de jquery tiene un montón de métodos de párametros pero los más sencillos son decirle
	    // porque método yo estoy trayendo algo, si yo estoy trayendo datos de un servidor seguramente
	    // va a ser un método de obtener datos, un método GET. Si yo estoy mandando datos como haciendo
	    // un comentario, entonces ocuparia POST, también esta PUB, DELETE.
	    // también otra cosa que necesitamos dentro de esta función son otros 2 párametros
	    // uno que se llama success y otro que se llama error.
	    // Success me va a servir como una función que se va a llamar cuando todo este bien
	    // cuando me ha halla devuelto datos este ajax.
	    
	    $.ajax('https://randomuser.me/api/', {
	      method: 'GET',
	      success: function(data) {
	        console.log(data)
	      },
	      // Algo más que tembién tenemos es el párametro de error que también va a ser una función
	      error: function(error) {
	        console.log(error)
	      }
	    })
	    
	      // AJAX no es más que un XMLHttpRequest
	    
	      // Pidiendo datos o llamando un API con Vanilla JS
	      // fetch va a recibir una URL y una configuración, pero si no le paso la configuración no pasa nada
	      // va a mandar los datos por defecto, por defecto va a utilizar el método GET auque también podemos lanzar POS o PUB
	      // El elemento de fetch lo que va a ser a ser es devolverme una promesa. Entonces claramente tenemos un método
	      // que se llama .then() de este fetch, que a su vez va a recibir una función 
	      fetch('https://randomuser.me/api/jfjfj')
	      // A su vez tenemos esos datos que son la respuesta 
	      .then(function(response) {
	          // console.log(response);
	          return response.json()
	      })
	      .then(function(user) {
	        console.log('user', user.results[0].email)
	      })
	      // También debemos Validar nuestra respuesta si algo falla
	      .catch(function(err) {
	        console.log(`lo siento, algo fallo :(`)
	      })
	    
	```

	* **Yezid Garcia Medina** [378137] (1)

		Muy buenos apuntes 😄

* **Angel Illescas Malagón** (2) [376340](https://platzi.com/comentario/376340/) 

	Una pregunta:  
	Si mi petición es GET y necesita parámetros para traer datos específicos, ¿cómo puedo poner estos parámetros dentro del fetch() ?

	* **Diego Alexander Forero Higuera (Platzi)** [376340] (4)

		los pones dentro de la url en el fetch
		```     fetch('https://misitio/?q="lo que sea") {
		     ....
		    
		```

	* **doctumcl** [376340] (4)

		También le puedes pasar en el body los parámetros:
		```     	fetch("https://misitio/", {
		    		method: 'GET',
		    		body: JSON.stringify({
		    			name: 'luis',
		    			age: 28
		    		})
		    	}).then(function (data) {
		      		console.log('Request success: ', data);
		    	}).catch(function (error) {  
		      		console.log('Request failure: ', error);
		    	});
		    
		```

	* **Angel Illescas Malagón** [376340] (1)

		Perfecto!!!, muchas gracias

* **Jose Rueda** (2) [365522](https://platzi.com/comentario/365522/) 

	empieza ecmascript 6

* **Facundo Ríos** (2) [355935](https://platzi.com/comentario/355935/) 

	Buenas muy bueno el curso. Muy entretenida la forma de desarrollo del mismo.

* **webrevolution** (1) [1098746](https://platzi.com/comentario/1098746/) 

	ajax!!!

* **joaquin-fontela** (1) [1097615](https://platzi.com/comentario/1097615/) 

	Alguien sabe que esta pasando aca? El codigo no me imprime nada en pantalla, tampoco lanza ningun error, nada.  
	Este es mi HTML:
	```     <!DOCTYPE html>
	    <htmllang="en"dir="ltr">
	      <head>
	        <metacharset="utf-8">
	        <title></title>
	      </head>
	      <body>
	        <scriptsrc="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
	        <scriptsrc="index.js"></script>
	      </body>
	    </html>
	    
	    
	```
	
	Y este el de JavaScript:
	```     $.ajax({
	      url: 'https://rickandmortyapi.com/api/',
	      method: 'GET',
	      sucess: function (data) {
	        console.log(data)
	      },
	      error: function (error) {
	        console.log(error)
	      }
	    })
	    
	```
	
	Aclaro que ya intente tambien poniendo la URL como primer parametro de la funcion, pero no se soluciono.

	* **Jhon** [1097615] (2)

		tienes un problema la colocar los atributos de la etiqueta **< script>**  
		lo tienes de esta manera  
		<scriptsrc=“url”></script>
		
		**MANERA CORECTA**  
		<script src=“url”></script>
		
		tiene que haber un espacio entre los atributos y el nombre de la etiqueta **< script>**

	* **joaquin-fontela** [1097615] (1)

		Gracias Jhonny por la ayuda!

* **JheysonEGalvis** (1) [1064890](https://platzi.com/comentario/1064890/) 

	<https://randomuser.me/>

* **Kenet Chungandro** (1) [985204](https://platzi.com/comentario/985204/) 

	```    //js vanilla|fetch retorna una promesa
	    fetch('https://randomuser.me/api/')
	    .then(function(response){
	      //json retorna una promesa
	      return response.json()
	    })
	    .then(function(user){
	      console.log('user', user.results[0].name.first)
	    })
	    .catch(function(){
	      console.log('algo falló')
	    })
	    
	```

* **Santiago Botero Ruiz** (1) [893355](https://platzi.com/comentario/893355/) 

	El uso de las promesas ayuda mucho evitando el infierno de los callback, cuando ya empezamos a tener muchos callback, nuestro código empieza a crecer horizontalmente en lugar de verticalmente y el mantenimiento empieza a ser más complejo.

* **fryma** (1) [855080](https://platzi.com/comentario/855080/) 

	Genial, que buena clase

* **Manuel Rivera** (1) [853784](https://platzi.com/comentario/853784/) 

	Algo de los poderes de JS 😉, increíble

* **Luis Rangel Castro** (1) [786148](https://platzi.com/comentario/786148/) 

	[Web API reference](https://developer.mozilla.org/es/docs/Web/API)

* **jhonatan-chahuayo-boza** (1) [732222](https://platzi.com/comentario/732222/) 

	sin una API actualizada no puedo continuar con la clase 😦

	* **Daniel Esteves** [732222] (1)

		¡Hola Jhonatan! Podrías usar esta alternativa: `https://jsonplaceholder.typicode.com/users`

	* **Daniel Andrés Palacios Carabalí** [732222] (1)

		<https://randomuser.me/>

* **jhonatan-chahuayo-boza** (1) [732220](https://platzi.com/comentario/732220/) 

	la API que se usa en clases, no puede ser usada actualmente

	* **Daniel Esteves** [732220] (1)

		¡Hola Jhonatan! Podrías usar esta alternativa: `https://jsonplaceholder.typicode.com/users`

* **Jose Luis Colmenares** (1) [669841](https://platzi.com/comentario/669841/) 

	Explicación de como se haría la petición con Vanilla JS.

* **Gustavo83** (1) [623262](https://platzi.com/comentario/623262/) 

	de momento aun me cuesta diferencias entre Javascript y JQuery de seguro con el correr de las clases voy a ir diferenciando mejor

	* **Jair Israel Avilés Eusebio** [623262] (1)

		jQuery es una libreria que extiende las capacidades de Javascript. Como tip, recuerda que hay un objeto global que es `$` el cual es asociado a este 😃

* **Angel Vazquez** (1) [555564](https://platzi.com/comentario/555564/) 

	```    fetch('hhttps://randomuser.me/api')
	    .then(function(response){
	    	//console.log(response)
	    	return response.json()
	    })
	    
	    .then(function(user){
	    	console.log('user', user.results[0].name.firts)
	    })
	    
	```
	
	buenas tardes,queria consultar, para que le pone el return response.json()???
	
	no estaría terminado el ejercicio con la simpleza de haber devuelto los resultados en el console.log(response)?? osea
	
	fetch(‘hhttps://randomuser.me/api’)  
	.then(function(response){  
	console.log(response)  
	})
	
	y consulta dos, el return response.json() devuelve los datos que obtengo de la promesa y que en este caso lo guarda en el parametro response,y lo convierte en json, que a la vez trae otra promesa?? y por lo tanto Leonidas para imprimir le agrega el último then para al final poder imprimirlos??  
	muchas gracias

	* **Oscar Barajas Tavares (Platzi)** [555564] (1)

		La respuesta que recibimos es un texto plano por eso debemos de transformar la respuesta json pasandolo por json, aca puedes leer un poco mas al respecto: <https://developer.mozilla.org/en-US/docs/Web/API/Body/json>

* **Nicolas Esteban Prieto Sarmiento** (1) [547266](https://platzi.com/comentario/547266/) 

	que es json? **nunca me quedo claro**

	* **Angel Hernandez** [547266] (5)

		Segun WIkipedia: JSON es un formato de texto sencillo para el intercambio de datos. Se trata de un subconjunto de la notación literal de objetos de JavaScript, aunque, debido a su amplia adopción como alternativa a XML, se considera un formato de lenguaje independiente.
		
		En otras palabras 😄 Json te viene a solucionar la vida, es un objeto el cual puede tener cualquier cantidad de propiedades, todas ordenadas de una manera muy simple y sencilla, en codigo se ve asi:
		```     const personas = {
		    	{
		    		nombre: "Pablo",
		    		edad: 19
		    	},
		    	{
		    		nombre: "Mangostino",
		    		edad: 14,
		    		ciudad: {
		    			nombre: "Medellin",
		    			ubicacion: {
		    				latitud: 19.0192121,
		    				longitud: 5.141212
		    			}
		    		}
		    	}
		    }
		    
		```
		
		La forma en la que trabajas un objeto en JS es super facil y lo mejor es que esta data puede ser leida desde cualquier lenguaje de programacion, facilita el intercambio de informacion entre cliente / servidor

* **jfgmolina** (1) [539028](https://platzi.com/comentario/539028/) 

	Intento hacer el ejemplo pero el navegador me da este error en la consola:
	
	Solicitud desde otro origen bloqueada: la política de mismo origen impide leer el recurso remoto en <http://randomuser.me/api/> (razón: falta la cabecera CORS ‘Access-Control-Allow-Origin’).
	
	He probado varias api y en todas me da el mismo error. Que tengo que hacer para solucionarlo y porqué falla el código si esta igual que en el video?

	* **Eduardo Hidalgo Díaz Rugama** [539028] (4)

		primero antes que nada, debo notificarte ese tema de los “CORS” tiene relación con los siguientes puntos:
		
		    1. hacer fetch a http o https son cosas distintas.
		    2. correr tu aplicación en “http localhost” es muy distinto que hacerlo en https en producción.
		    3. hacer fetch no es solo una url, el método y el body. También hay unos datos llamados “headers” que influyen fuertemente.
		
		la documentación oficial sobre esto se encuentra aqui <https://fetch.spec.whatwg.org/+>
		
		Dicho esto, importante: si estás corriendo tu aplicación en localhost, todos los llamados a apis que hagas deben empezar por HTTPS (hago enfasis en la “S”, debe ser seguros todos los llamados de API). Veo que compartiste la url de una API pero la tienes con “http”. Eso es erróneo. Ojo.
		
		¿Qué es lo que esta pasando realmente? muy facil, como no tienes seguridad via http entonces la API a la que llamas te pregunta “¿tienes el header Access-Control*?” pero como tampoco lo tienes, entonces truena.
		
		No cumples con http-s y no cumples con el protocolo de CORS, entonces no puedes acceder a la API.
		
		aquí puedes averiguar más sobre CORS <https://en.wikipedia.org/wiki/Cross-origin_resource_sharing>

	* **jfgmolina** [539028] (1)

		Como puedo solucionar el problema? no utilizo un servidor local. Abro la pagina web directamente en el navegador.

	* **Eduardo Hidalgo Díaz Rugama** [539028] (2)

		ya te dije. debes hacer fetch a urls con http-s, o agregar el header a tu fetch de “access-control” (no recuerdo exactamente como es, tendrás que investigar el header).

	* **jfgmolina** [539028] (1)

		Gracias por todo. He instalado Xampp y se me ha solucionado el problema. Me ha generado la consulta sin problemas

* **Manuel Alférez Ruiz** (1) [532596](https://platzi.com/comentario/532596/) 

	Cuando se realiza un return, ¿lo que devuelve se le hace el user? Porque buscando dentro del inspector de elementos no existe por ningún lado un json que tenga name de algún user
	```     <fetch('https://randomuser.me/api/')
	        .then((response) => {
	            console.log(response)
	           return response.json()
	        })
	        .then((user)=>{
	            console.log(user.results[0].name.first);
	        })
	        .catch(()=>{
	            console.log("Error :(")
	        })>
	    
	```

	* **AlexGarrixen** [532596] (2)

		Hola manuelalferez ,el el momento que haces el return devuelve la respuesta como objeto json.  
		veras esto :  
		{  
		info :{…}  
		results : [{…}]  
		}
		
		el ‘user’ es como tal la respuesta(objeto json) solo que pasa por parametro en el callback(funcion) para poder utilizarlo, pero en realidad puedes ponerle cualquier nombre

* **silvermiguel96** (1) [527894](https://platzi.com/comentario/527894/) 

	Quiero mostrarle un ejemplo de como haceruna función simple y pasarla a función flecha o arrow function.
	```     // Api StarWard
	    fetch('https://swapi.co/api/people')
	      .then(function(response) {
	        return response.json()
	      })
	      .then(function(user) {
	        console.log('Person', user.results[1].name)
	      })
	      .catch( function(error) {
	        console.log(error)
	        console.error
	      })
	    // Api StartWard funcion Flecha
	    fetch('https://swapi.co/api/people')
	      .then( response  => response.json())
	      .then( user => console.log('Star', user.results[2].name))
	      .catch(error => console.log(error));```
	    
	```

	* **silvermiguel96** [527894] (1)

		Recordemos que console también tiene una propiedad llamada **error**.

* **Félix Alejandro Zelaya Orellana** (1) [526741](https://platzi.com/comentario/526741/) 

	Genial!! Nunca imagine el poder de ambos

* **Brayan Jeshua** (1) [524965](https://platzi.com/comentario/524965/) 

	Es genial que enseñes tanto JQuery y Vanilla.js porque enseñas desde donde viene todo. Excelente tutorial, que debería ver cualquier programador en este año.

* **Ollendorf  Ticona Valdivia** (1) [456992](https://platzi.com/comentario/456992/) 

	 **Async - Await**
	```     let getUser = async (url) => {
	    
	        let userResponse = await fetch(url)
	        let userData = await userResponse.json()
	    
	    
	        return userData
	    }
	    
	    getUser('https://randomuser.me/api/')
	        .then( (userData ) =>console.log(userData))
	        .catch( (err) =>console.log(err))```
	    
	```

* **Sebastián Pineda Duque** (1) [451101](https://platzi.com/comentario/451101/) 

	¿Para qué sirve ese método **.json**?

	* **Alfonso Navarro** [451101] (2)

		Hola spineda, el metodo .json sirve para formatear la data q trae el fetch, o la respuesta del promise, osea para q su lectura se pueda realizar de una forma mas facil.
		
		Te dejo este link donde puedes leer un poco mas…  
		[](https://developer.mozilla.org/es/docs/Learn/JavaScript/Objects/JSON)

* **Mramon** (1) [412542](https://platzi.com/comentario/412542/) 

	Un poco más de información:  
	<http://www.cristalab.com/tutoriales/introduccion-a-ajax-con-php-y-formularios-c165l/>  
	@Cristalab

* **Adriann Felipe Sanchez Sierra** (1) [397445](https://platzi.com/comentario/397445/) 

	Para algo mas corto en ajax, se podría usar:
	```     $.get(url)
	    
	```
	
	Ó
	```     $.post(url, params)
	    
	```

	* **Diego Alexander Forero Higuera (Platzi)** [397445] (1)

		Esto es si usas jQuery, en este curso precisamente se enseña a no usar jQuery para las cosas y usar solo JavaScript y sus funciones nativas como fetch

	* **Alfonso Navarro** [397445] (1)

		Efectivamente esto es utilizado con Jquery se podría decir q todo lo q’ en JavaScript tenga el signo $ pesos delante es Jquery…

* **LeandroVidela** (1) [385122](https://platzi.com/comentario/385122/) 

	```    const url = 'https://randomuser.me/api/';
	    
	    fetch(url)
	      .then(response => {
	        return response.json()  
	      })
	      .then(data => {
	        console.log(`User Name ${data.results[0].name.first.toUpperCase()}`);
	      })
	      .catch(error => {
	        console.log(error);
	      })
	    
	```

* **Julián Fernando Amaya Díaz** (1) [379415](https://platzi.com/comentario/379415/) 

	Esta lección fue genial.

* **eltenis** (1) [366833](https://platzi.com/comentario/366833/) 

	Javascript mola
	```     fetch('....mi peticion con su url...., configuracion') 
	      .then(function(respuesta){
	       return  respuesta.json()
	       })
	      .then(function(usuario){
	      const resultadoUsuario =  usuario.results[0]
	      console.log(`el nombre del usuario es: ${resultadoUsuario.name.first} y su mail 
	       ${resultadoUsuario.email} `)
	       })
	       .catch(function() {
	       alert('Vaya Castaña de Api....!La Culpa es de la Api no del programador :)!')
	       })
	    
	```
	
	')

* **Jose Rueda** (1) [365512](https://platzi.com/comentario/365512/) 

	entonces ya no es recomendable usar el clasico XMLHttpRequest ???
	```     functiongetAPI (url, callback)
	    {
	        // Create a request variable and assign a new XMLHttpRequest object to it.
	        var request = new XMLHttpRequest();
	    
	        // Open a new connection, using the GET request on the URL endpoint
	        request.open('GET', url, true);
	        const OK = 200;
	        const DONE = 4;
	    
	```

	* **Daniel374** [365512] (2)

		Últimamente se utilizan las web api’s como fetch desarrollada por Whatwg: <https://fetch.spec.whatwg.org/>. es mejor en cuanto a funcionalidad y sintaxis, asi se hace posible desarrollar mas fácil en Javascript.

* **Jheysson ML** (1) [364419](https://platzi.com/comentario/364419/) 

	todo bonito si solo usamos para nosotros, pero los usuarios son multiplataformas y multiversiones. Qué hacer cuando no hay compatibilidad?

	* **Leonidas Esteban Gonzalez** [364419] (4)

		Antes de esto debes corroborarlo muy bien, si esto pasa puedes usar axios por ejemplo

* **Cristopher Alejandro Silva Amdahl** (1) [363737](https://platzi.com/comentario/363737/) 

	De donde estan llegando lo parametros como message, user, data, etc

	* **Alan Santiago** [363737] (2)

		El parametro **user** del segundo **.then()** llega desde el primer **.then()** que devuelve el valor de **response.json()**.
		
		El parametro **data** del metodo **.ajax()** viene desde la peticion **get** que se esta haciendo. Lo que hace **get** es traer datos desde algun lugar y cuando se obtienen esos datos, estos se almacenan en **data**

* **Alejandro Moraga** (1) [338762](https://platzi.com/comentario/338762/) 

	Completamente hermoso, lastima que no es compatible con internet explorer 7

	* **Oscar Barajas Tavares (Platzi)** [338762] (6)

		Ya es un navegador obsoleto, como frontends no deberiamos dar mas soporte a este navegador.

	* **Alejandro Moraga** [338762] (1)

		Sep, pero lastimosamente aun se trabaja con tecnologías de principio de siglo.

	* **Japheth Calzada López** [338762] (2)

		Asi estaba hace dos trabajos y es horrible trabajar asi, estaba trabajando con explorer 8, y cuando aprendias algo veias que no era compatible, amigo, consejo, salte de ahi

* **Diego Fernando Rojas Quintero** (1) [85733](https://platzi.com/comentario/1080612/) 
Todas la apis arrojan un formato json?

	* **Matias Nicolas Caballero Gomez** [85733] (1)

		JSON es el mas utilizado por ahora, tambien puedes encontrarlo en formato XML y en YAML

* **Diego Fernando Rojas Quintero** (1) [85653](https://platzi.com/comentario/1078536/) 
Tengo una duda, cuando leoninas en el minuto 1:04 dice que importo un cdn, ¿de donde lo saco?

	* **José Bryan Aranda Figueroa** [85653] (1)

		se trata de un fragmento de codigo que debes agregar al html, lo que hace por medio de internet integrar, integrar jQuery en este caso a tu aplicación web.  
		par aobtenerlo ya sea que copie el codigo que tiene leonidas en el video o descargues el proyecto en la sección de archivos, támbien puedes obteneder el link en este sitio <https://code.jquery.com/>

* **wlm.morant** (1) [84109](https://platzi.com/comentario/1043079/) 
Tengo una duda muy seria cuando envias el primer .then del fetch recibes un objeto y cuando envias el segundo .then recibes otro objeto c...

	* **mafevito** [84109] (1)

		¡Hola @wlm.morant!
		
		  * El primer `.then` esta capturando la respuesta que se obtiene al hacer la petición a la API.
		
		  * El segundo `.then` esta capturando un dato especifico de la respuesta, en este caso el nombre del usuario aleatorio que se esta obteniendo.

* **wlm.morant** (1) [84074](https://platzi.com/comentario/1042294/) 
la pagina randomuser.me esta caida como lo hago ?? T.T ?

	* **santiagobarrios** [84074] (2)

		usa esta api de stars wars <https://swapi.co/documentation>

* **Ang.O** (1) [82246](https://platzi.com/comentario/1000985/) 
fetch es lo mismo .ajax o depende de?

	* **Luis Arturo Lira Cerda** [82246] (1)

		Si, en jQuery se usaba ajax para el asincronismo, pero ahora en JavaScript tenemos las promesas y Async/await, que son totalmente métodos nativos del lenguaje.

* **Ang.O** (1) [81486](https://platzi.com/comentario/982324/) 
que devolvio el response.json

	* **miguelangelsoler** [81486] (1)

		La respuesta en formato json

* **josedavid7310** (1) [78326](https://platzi.com/comentario/919272/) 
Y si quedo sin internet?

* **Ang.O** (1) [74441](https://platzi.com/comentario/852906/) 
que es eso de vanilla?

	* **Gabriel De Andrade (Platzi)** [74441] (4)

		Vanilla hace referencia a algo sin modificar, o como vino por defecto, si hablamos de JavaScript vanilla es el JavaScript de toda la vida, sin frameworks/bibliotecas encima 😄

* **Ang.O** (1) [74439](https://platzi.com/comentario/852901/) 
eso de ajax es lo mismo que un api?

	* **Gabriel De Andrade (Platzi)** [74439] (1)

		Ajax es símplemente el protocolo de conexión que sigue el frontend, el API es toda una serie de reglas y lineamientos que se siguen para conectarse a un servidor, con el [Curso de Fundamentos de Ingeniería de Software](https://platzi.com/clases/ingenieria/) te quedan súper claros estos conceptos 😄

* **Ang.O** (1) [74438](https://platzi.com/comentario/852899/) 
como se cuando se usa jquery y cuando es javascript?

	* **Gabriel De Andrade (Platzi)** [74438] (2)

		Práctica, pero a manera general jQuery utiliza mucho el símbolo “$” y tiene la palabra jQuery en muchas partes, aunque algunos desarrolladores todavía utilizan la simbología “$” para señalar que un elemento es traído del DOM

## 0070. Funciones asíncronas [12291](https://platzi.com/clases/1316-jquery-js/12291-funciones-asincronas/)

### Descripción:


Una función asíncrona va a ser como una función normal, pero poniendo código asíncrono de forma que sea más fácil de leer de forma síncrona.

Para declarar una función asíncrona se usa la palabra reservada async, luego de eso declaras tu función de forma normal. Dentro de una función asíncrona cuentas con otra palabra reservada llamada await, lo que hará esta palabra es indicar que se debe esperar a que termine de ejecutarse ese fragmento de código antes de continuar.

Vamos a realizar peticiones con fetch a la API de yts para pedirle películas según su categoría y mostrarlas dentro de PlatziVideo. Sin el uso de funciones asíncronas para cada fetch tendríamos que usar los métodos then y catch, en cambio gracias a async/await solo debemos escribir la palabra await antes de cada promesa.

### Comentarios:

* **Daniee** (43) [337014](https://platzi.com/comentario/337014/) 

	Gran clase! Pienso que consumir API’s es imprescindible para cualquier desarrollador hoy en día.
	
	[Aquí ](http://www.enrique7mc.com/2016/05/lista-apis-publicas/) les dejo un enlace con unas cuantas API’s públicas para experimentar con sus proyectos.
	
	Les invito a compartan también las suyas para que lleguen a mucha más gente ! 😄

	* **jsechirinos** [337014] (4)

		Gracias por compartir…!!!  
		El api del curso no me funciona, pero en el Link encontré api’s muy interesantes…

	* **juand_silva** [337014] (13)

		Aquí dejo mi aporte de una lista muy completa con explicación de como utilizar las Api’s
		
		<https://medium.freecodecamp.org/want-to-build-something-fun-heres-a-list-of-sample-web-app-ideas-b991bce0ed9a>

	* **Julián Fernando Amaya Díaz** [337014] (2)

		Gracias que buen aporte!

	* **Erick Lugo** [337014] (2)

		Gracias por compartir, excelente información!

	* **Luis Angel José Portillo Arévalo** [337014] (2)

		La api del curso si me funcionó uwu

	* **Antonio Madrid** [337014] (3)

		Mientras más voy aprendiendo, más me doy cuenta de la importancia de tener este tipo de recursos.
		
		**Practicar, toquetear, investigar, probar, divertirse.**  
		Así es como se consigue la auténtica maestría y los avances!
		
		Gracias

	* **Kevin Humberto León Santamaria** [337014] (2)

		Gracias compañeros

* **Sergio Minei** (22) [335589](https://platzi.com/comentario/335589/) 

	## Funciones asíncronas
	
	Una función asíncrona va a ser como una función normal, pero poniendo código asíncrono de forma que sea más fácil de leer de forma síncrona.
	
	Para declarar una función asíncrona se usa `async` / `await`:
	
	  * async: declara quye una función es asíncrona.
	  * await: indica que se debe de terminar con el fragmento de código para continuar con la ejecución de la función.
	```     asyncfunctionload() {
	      const response = await fetch("url") 
	    }
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

* **Miguel Segura** (10) [658582](https://platzi.com/comentario/658582/) 

	Hay una observacion importante que recien note  
	En el curso se usa la URL con:  
	<https://yts.am>
	
	Ahora es:
	
	<https://yts.lt>
	
	Si no corrigen esto esto tendran un error que dira No ‘Access-Control-Allow-Origin’

	* **Juan Pablo Felipe Delgado Salcedo** [658582] (1)

		Muchas gracias compañero, buen apunte.

	* **Cristobal Vega** [658582] (1)

		Muchas gracias por el aporte 👍

	* **dipacata** [658582] (1)

		gracia]!

* **Jhon edison lozano Montenegro** (10) [336328](https://platzi.com/comentario/336328/) 

	```    const urlApi = 'https://yts.am/api/v2/list_movies.json?genre=';
	    (asyncfunctionload(){
	    
	      asyncfunctiongetData(url){
	          const response = await fetch(url);
	        const data = await response.json()
	        return data;
	    
	      }
	      const actionList = await getData(urlApi+'action')
	      const dramaList = await getData(urlApi+'drama')
	      console.log('actionList', actionList, 'dramaList',dramaList);
	    
	```

* **Pablo Benedix** (9) [608315](https://platzi.com/comentario/608315/) 

	Hola amigos, vengo del futuro a salvarles la vida (o eso creo), Leonidas utiliza el link <https://yts.am/api/v2/list_movies.json?genre=action>**PERO** el link se ha tranformado un chiquitito y se usa este <https://yts.lt/api/v2/list_movies.json?genre=action>

	* **Claudio Jesus Vázquez Villanueva** [608315] (1)

		Gracias literal Héroe sin capa!

	* **MatiasBZ** [608315] (0)

		Gracias … te debo una cerveza =)

	* **José Bryan Aranda Figueroa** [608315] (1)

		GRACIAS HEROE

* **ShadowAeon** (9) [339979](https://platzi.com/comentario/339979/) 

	Utilizar el await a cada rato no le quita el sentido a las funciones asíncronas? Ya que no se cumple el objetivo de ellas que es que nuestra página no se bloquee mientras espera que termine la ejecución de la función.

	* **Japheth Calzada López** [339979] (3)

		Me imagino que tanto await fue de ejemplo

	* **swapuruguay** [339979] (3)

		El uso de await es un sintax sugar, para que el código se vea síncrono, internamente la función se sigue ejecutando en forma asincrónica. Solo que como necesitamos manejar la respuesta de esta manera es más fácil que usando .then y obviamente mejor que los callbacks

	* **xhino** [339979] (3)

		Si bien es cierto que la web se trata de evitar que el usuario espere, hay procesos complejos como compras y ventas de articulos que requieren de varias llamadas y estas requieren que se vayan ejecutando de manera sincrona para evitar malos ratos o transacciones financieras inconclusas

	* **MatiasBZ** [339979] (2)

		No le quita el sentido, porque el asincrinismo se produce igual. Todo lo que se ejecuta afuera del array de promesas se ejecutarade manera asíncrona. La funciones dentro del array de promesas sí serán síncronas, pero es una forma de agruparlas y ordenarlas. El efecto visual que esto produce es una aplicación web con la carga de componentes de una manera mas fluida y no tan trabada o desordenada. Es una forma de controlar el caos que se producirían si los componentes dinámicos de la pagina se cargan en cualquier orden.

	* **jesus-gallardo** [339979] (2)

		De hecho el objetivo es lograr un codigo **Sincrono**. Te explico.
		
		Imagina que tienes una pagina de comics y consumes la Api de Marvel.
		
		Tienes una funcion que hace la peticion y una que dibuja en el DOOM la imagen del comic, el titulo, fechas, etc…
		
		Para que esto funcione, la funcion de dibujar en el doom solo se debe ejecutar luego de que la peticion termine, para ello usamos **Promises** o **Async y Await**. Porque sino fuese de esta forma tendrias un error en consola, intentando imprimir informacion que aun no tienes. Se entiende ?
		
		Puedes ver la clase de asincronismo en el curso de **Fundamentos de JavaScript** Y estoy seguro de que esto te quedara mucho mas claro.

* **Kenet Chungandro** (7) [985244](https://platzi.com/comentario/985244/) 

	 **link de la API:**<https://yts.mx/api/v2/list_movies.json?genre=action>

* **Christian Erik Velázquez Morales** (6) [967558](https://platzi.com/comentario/967558/) 

	La “Api”, mi antivirus me la marcó como “malicioso”. Por eso, para realizar el ejercicio, utilicé: <https://pokeapi.co/>

* **brapastor** (6) [337154](https://platzi.com/comentario/337154/) 

	<https://yts.am/api>

* **rafaelzamora** (5) [753171](https://platzi.com/comentario/753171/) 

	Por si tienen problemas con la url  
	_ ‘[https://yts.lt/api/v2/list_movies.json?genre=action’_](https://yts.lt/api/v2/list_movies.json?genre=action%E2%80%99_)  
	la api la cambio

	* **Jean Lucas Castillo Vasquez** [753171] (3)

		Una capa sin héroe

	* **Jonathan Uriel Jiménez Soveranes** [753171] (1)

		ahha gracias!

	* **Germán González** [753171] (1)

		HEROE

	* **Cesar Eliezer Gomez Gutierrez** [753171] (1)

		Héroe

* **juand_silva** (5) [379996](https://platzi.com/comentario/379996/) 

	En el minuto 06:05 el ( ; ) no es tan importante en JavaScript como menciona el Leonidas, si no que es en uno de los pocos casos por no decir el unico en el que se utiliza ( ; ) en las funciones [IIFE](https://programacionymas.com/blog/funciones-javascript-invocadas-inmediatamente-IIFE)

* **Iván Darío Sánchez Jiménez** (5) [350343](https://platzi.com/comentario/350343/) 

	Cordial saludo compañeros
	
	Para tener una mejor referencia en el uso de **funciones asíncronas** les comparto una aplicación desarrollada por mi utilizando **Async- Await** y utilizando la API de <https://swapi.co/> espero les sea muy útil para sus estudios [Aplicación STARWARS](https://drive.google.com/open?id=11LiR5_Ol9MU7TBJ1eRDXB68idpUvys8u)
	
	**Descargar el .rar y abrir el index con toda confianza**

* **Jonatan David Padilla** (5) [345069](https://platzi.com/comentario/345069/) 

	Cual es la verdadera funcion de `fetch`?

	* **Oscar Barajas Tavares (Platzi)** [345069] (5)

		La principal finalidad es recuperar recursos, es comúnmente utilizada para hacer peticiones a algún recurso en internet (api) y resuelve una promesa con la respuesta.

	* **Juan Carlos Pinzón** [345069] (4)

		Es como una especie de $ajax de jquery, solo que en este caso es JS Puro o vanilla

* **Camilo Andrés Santana Lizcano** (4) [685462](https://platzi.com/comentario/685462/) 

	 **Excelente explicación !**  
	Pero … cuando usamos una función async  
	**¿Cómo capturamos el error?**
	
	resulta que podemos hacer uso de **try{}** y **catch{}**
	```     (asyncfunctionload() {
	      
	      asyncfunctiongetData(url){
	        
	        const response = await fetch(url)
	        const data = await response.json()
	        
	        return data;
	      }
	    
	      try{
	    //aquí dentro es donde se compeltaran nuestras promesas !
	        const actionList = await getData('https://yts.lt/api/v2/list_movies.json?genre=action')
	        const dramaList = await getData('https://yts.lt/api/v2/list_movies.json?genre=drama')
	    
	        console.log(actionList,dramaList)
	      }
	    
	    catch{
	    //aquí iremos a parar si algo sale mal con alguna de nuestras promesas
	        console.log('Algo salio mal')
	      }
	      
	     })()
	    
	```

	* **Enrique Felix Garcia Stave** [685462] (1)

		Buen dato!

	* **Manuel Rivera** [685462] (1)

		Muchas gracias !! 😉

	* **Eduardo Aguilar Escalante** [685462] (1)

		Yo no soy tan conocedor del tema, pero en la clase cuando se pone .then() es cuando la promesa se resuelve, y en caso de que no se resuelva pues se utiliza .catch(), que a mi ver es algo muy parecido a utilizar el try{} catch{} que mencionas

* **Navarrock33** (4) [632729](https://platzi.com/comentario/632729/) 

	Un API cool para hacer pruebas [The Rick & Morthy API](https://rickandmortyapi.com) 😄

	* **dipacata** [632729] (1)

		utilice esa, porque la de las películas no me funcione 😃

* **pablojorgeandres** (4) [483464](https://platzi.com/comentario/483464/) 

	## Funciones Asíncronas
	
	Una función asíncrona va a ser como una función normal, pero poniendo código asíncrono de forma que sea más fácil de leer de forma síncrona.
	
	Para declarar una función asíncrona se usa la palabra reservada _**async**_. Luego se declara la función de forma normal.  
	Dentro de una función asíncrona se usa otra palabra reservada llamada _**await**_ , lo que hará este comando es indicar que se debe esperar a que termine de ejecutarse ese fragmento de código antes de continuar.
	```     	asyncfunctionload() {
	    		await 
	    	}
	    	
	    
	```
	
	Puedo llamar la función de manera normal:
	```     	asyncfunctionload() {
	    		await 
	    	}
	    	load()
	    
	    
	```
	
	O puedo hacer que se autoejecute envolviéndola entre paréntesis:
	```     	(asyncfunctionload() {
	    		await 
	    	})()
	    
	    
	```
	
	Para comenzar a trabajar en el ejercicio del curso usaremos la API de <https://yts.am/api>.  
	Genero un _fetch_ con la URL que necesito y lo guardo en una constante bajo el comando _**await**_  
	El código allí se detiene hasta que la promesa es devuelta y continúa con el código.  
	Llamamos entonces al método _json()_ para obtener el objeto Promise con la ‘data’.
	```     	(asyncfunctionload(){
	    		const result = await fetch('https://yts.am/api/v2/list_movies.json?genre=action')
	    		const data = await result.json()
	    		console.log(data)
	    	})()
	    
	    
	```
	
	Ahora quiero traer tres promesas que corresponden a tres géneros diferentes de películas:
	```     	(asyncfunctionload(){
	    		
	    		asyncfunctiongetData(url) {
	    			const result = await fetch(url)
	    			const data = await result.json()
	    			return data
	    		}
	    		
	    		const URLroot = 'https://yts.am/api/v2/list_movies.json?genre='
	    		
	    		const actionList = await getData(`${URLroot}action`)
	    		const horrorList = await getData(`${URLroot}horror`)
	    		const animationList = await getData(`${URLroot}animation`)
	    		
	    		console.log(actionList, horrorList, animationList)
	    	})()
	    
	    
	```
	
	PD: Lo que más me gusta de usar vanilla JS es que pegás el código en la consola y ya sabés si funciona o no, no hay que generar siquiera un html, es genial =)

* **ClareRodri** (4) [367984](https://platzi.com/comentario/367984/) 

	Teniendo en cuenta que se vieron los temas de promesas y funciones asíncronas (que fueron manejadas como sincronas),
	
	    1. ¿Cual de los dos métodos es buena práctica?
	    2. ¿Que tan bueno es forzar una función asíncrona para que sea síncrona?, viendo que hay un comentario similar, quiero recalcar, ¿No le quita el sentido a las funciones asíncronas?

	* **Diego Alexander Forero Higuera (Platzi)** [367984] (5)

		En mi opinión lo mejor es usar promesas ya que no bloqueas la ejecución del código esperando una respuesta y cuando llega la respuesta simplemente la muestras.
		
		Una función asíncrona no debería forzarse a ser sincrona salvo que algo realmente lo necesite pero para esto solo es elimina el uso de async await y queda como una función normal.

	* **evertescalante1** [367984] (2)

		Entonces @GOLLUM23 seria mejor utilizar promesas de vanilla JS o la libreria axios que trabaja con promesas?

	* **Diego Alexander Forero Higuera (Platzi)** [367984] (5)

		@evertescalante1 si tienes axios ya instalado porque lo usas en muchas partes entonces no hay problema, pero no deberías instalar una librería solo si lo vas a usar en una sola parte.

* **neontigermx** (3) [1018931](https://platzi.com/comentario/1018931/) 

	Usar horror en lugar de terror, asi salen mas 😃

* **Christian Erik Velázquez Morales** (3) [967544](https://platzi.com/comentario/967544/) 

	Otros ejercicios (canal de Leonidas)  
	<https://www.youtube.com/user/LeonidasEsteban/videos>

* **José Bryan Aranda Figueroa** (3) [726521](https://platzi.com/comentario/726521/) 

	hay algún curso que deba tomar antes que este? o alguien que me pueda explicar alguna forma de tomar de manera correcta estos cursos, debido a que tomo las clases pero me da la impresión que no se explica en si muy aspectos como las partes de una función asíncrona, la teoría de lo que puede llevar, no solo las cosas que el usara para la aplicación o proyecto, vengo de cursos como el de responsive desing donde Leonidas explica al menos mejor los conceptos y las bases antes de ponerse a escribir código a lo loco sin explicar bien las bases.

	* **Yoel Ribero** [726521] (3)

		Te recomiendo hacer el curso de Fundamentos de JavaScript. Es una muy buena base para luego encarar este curso.

	* **efrainjaimes** [726521] (2)

		El curso de fundamentos de JavaScript es lo que buscas. Luego de ese curso, entenderás muchísimo mejor este.

	* **Daniel Esteves** [726521] (3)

		Hola! Cómo te comentan los compañeros debes tomar primer Fundamentos de JavaScript y después cuando lo termines inicias con Profesional de JavaScript y por último tomas este 💚

	* **nivamol_** [726521] (3)

		Según escuche de la gente de platzi es mejor este primero y luego con fundamentos.

	* **Daniel Esteves** [726521] (3)

		Hola @nivamol_ depende mucho del background que tengas, si vienes de jQuery es mejor tomar este primero, y si no es mejor ir directamente a Fundamentos de JavaScript

	* **José Bryan Aranda Figueroa** [726521] (1)

		Gracias a todos por responder

* **Espasa** (3) [698395](https://platzi.com/comentario/698395/) 

	```    (asyncfunctionload() {
	    
	      asyncfunctiongetData(url) { // Get a json of an HTTP request
	        const response = await fetch(url);
	        const data = await response.json();
	        return data;
	      }
	    
	      const API_URL = 'https://yts.lt/api/v2/list_movies.json?genre=';
	      constlist = {
	        action: await getData(API_URL + 'action'),
	        drama: await getData(API_URL + 'drama'),
	        animation: await getData(API_URL + 'animation')
	      } 
	    
	      for (genre inlist) {
	        console.log(list[genre]);
	      }
	    })();
	    
	```

* **Enrique Felix Garcia Stave** (3) [690485](https://platzi.com/comentario/690485/) 

	**Apuntes:**  
	**Función Async**  
	Nos permite declarar una función asíncrona.  
	**IIFE**  
	Las siglas significan “Immediately-invoked function expressions” y son aquellas funciones que como su nombre lo indica, son invocadas inmediatamente, sin tener que llamarlas en otra parte del código.  
	`(function () { // algún código aquí })();`

* **Rodolfo Nicacio Ugalde Ochoa** (3) [378181](https://platzi.com/comentario/378181/) 

	Les comparto mi aporte pero con arrow functions. Saludos.
	```     (async () => {
	        const getData = async (url) => {
	            const response = await fetch(url);
	            const data = await response.json()
	            return data;
	        }
	        const actionList = await getData('https://yts.am/api/v2/list_movies.json?genres=action')
	        const crimeList = await getData('https://yts.am/api/v2/list_movies.json?genres=crime')
	        const DramaList = await getData('https://yts.am/api/v2/list_movies.json?genres=drama')
	        const thrillerList = await getData('https://yts.am/api/v2/list_movies.json?genres=thriller')
	        console.log('actionList -->', actionList);
	        console.log('crimeList -->', crimeList);
	        console.log('DramaList -->', DramaList);
	        console.log('thrillerList -->', thrillerList);
	    })()
	    
	```

	* **evertescalante1** [378181] (1)

		Se ve re pro, la forma en que declaras la funcion en la linea 1 con arrow function pero, si es anonima, y si tienes varias funciones mas como esa, sucede un error. Esto podria generar conflicto ?
		
		para el metodo getData es mas sencillo entender porque supongo toma ese nombre getData

* **errrTote** (3) [361752](https://platzi.com/comentario/361752/) 

	Como se manejan los errores utilizando el ultimo método sin .then() ni .catch()?

	* **Alan Santiago** [361752] (4)

		Cuando creas la function usando **async** puedes hacer que al llamar la funcion se le tenga que pasar un **callback** y que este callback se encargue de los errores:
		
		  * Mostrando un modal, haciendo un **console.log** , etc.
		
		Si no sucede ningun error, el **callback** no sera llamado.
		```     cb(null, savedTask);
		    
		```
		
		Por eso deber llamar a ese **callback** al final de tu **async function**.
		
		Puedes hacer esto para reconocer un error y llamar al callback:
		```     constlist = await getData('unaapimuycool.api')
		    if(!list) return cb('No user found');
		    
		```

* **David Daniel Castillo Nava** (3) [350132](https://platzi.com/comentario/350132/) 

	Yo lo hice así 😄
	```     const api = 'https://yts.am/api/v2';
	    
	    (asyncfunctionload(){
	    
	    	let [ reqAction, reqDrama, reqAnimation ] = awaitPromise.all([
	    		fetch(`${api}/list_movies.json?genre=action`),
	    		fetch(`${api}/list_movies.json?genre=drama`),
	    		fetch(`${api}/list_movies.json?genre=animation`)
	    	])
	    
	    	let actionList = await reqAction.json()
	    	let dramaList = await reqDrama.json()
	    	let animationList = await reqAnimation.json()
	    
	    	console.log('actionList',actionList)
	    	console.log('dramaList',dramaList)
	    	console.log('animationList',animationList)
	    })()
	    
	```

* **danielmorales917** (2) [1086765](https://platzi.com/comentario/1086765/) 

	Que bien, es obviamente mucho más legible el código con await.

* **Pau Pregoni Juan** (2) [1085056](https://platzi.com/comentario/1085056/) 

	 **Una aclaración:**  
	Al llamar a la **función asíncrona** no hace falta el segundo **await** en **response.json()** , ya que los datos ya están guardados en response al hacer **fetch()**.  
	Lo pongo en estos términos en la vida real:
	
	    1. Yo en mi casa realizo diversas tareas ( **el programa tiempo de ejecución** )
	    2. Realizo un pedido de pizza por delivery ++llamada asíncrona ++( **async** )
	    3. Ahora debo esperar a que llegue a casa ( **await** ).
	    4. El paquete ya llegó, ahora puedo abrir la caja, comerme la pizza, etc. No necesito esperar más nada, estoy en mi casa y puedo manipular la caja de pizza y su contenido a gusto. Por eso ya no necesito otro **await**
	
	Dejo el código de ejemplo:
	```     asyncfunctiongetData(url) {
	        const response = await fetch(url)
	        const data = response.json()
	        return data
	      }
	    
	```

	* **David Alejandro Mosquera Moreno** [1085056] (1)

		Pero al hacer esto la **Promesa** queda como <pendiente>  
		.  
		**Promise** `{<pending>}`

	* **Pau Pregoni Juan** [1085056] (1)

		Sí, deberías de poder resolverla en un bloque try catch, o .then .catch en caso de que utilices promises.

	* **carlosextra1** [1085056] (1)

		bien, gracias por el aporte!

* **Kivindlg** (2) [955399](https://platzi.com/comentario/955399/) 

	recién empieza y ya estoy 500% de emocionado…

* **luisglopez7777** (2) [928114](https://platzi.com/comentario/928114/) 

	En JS normalmente escribo sin punto y coma, pero aqui hay una excepcion. Si declararon la url como yo lo hice, deben de poner punto y coma antes del async porque si no les marca error
	```     consturl = 'https://yts.lt/api/v2/list_movies.json'
	    const action = '?genre=action';
	    
	    (asyncfunctionload(){
	      const response = await fetch(url + action)
	      const data = await response.json()
	      console.log(data)
	    })()
	    
	```
	
	Me tarde mucho en darme cuenta, ojala a alguien le sirva

* **Juan Gaybre Flores** (2) [926298](https://platzi.com/comentario/926298/) 

	Comparto un ejercicio de consulta a la API de Pokemon  
	.  
	![poke.JPG](https://static.platzi.com/media/user_upload/poke-6fc6d33c-e4c5-4622-96a1-1bcf09544bf7.jpg)

	* **Rodrigo Cuenca Mayuri** [926298] (3)

		podrías simplificar:  
		const urlPokemon = `https://pokeapi.co/api/v2/pokemon/${id}`

	* **Juan Gaybre Flores** [926298] (2)

		Es correcto rodrocuenca, solo que en ese proyecto estaba utilizando esa constante como global y la agregé dentro de la función solo para el ejemplo. Gracias.

* **Cristian Caraballo** (2) [916497](https://platzi.com/comentario/916497/) 

	La url tiene un problema “CORS policy” ¿¿Que es?? ¿Que significa?

	* **Eduardo Aguilar Escalante** [916497] (3)

		me encontre, con esto indagando un poquito en lo que comentas, en el CORS y el CORB  
		<https://www.chromium.org/Home/chromium-security/corb-for-developers>

	* **Eduardo Aguilar Escalante** [916497] (3)

		Igual para resolverlo, yo baje una extensión de chrome:
		
		<https://chrome.google.com/webstore/detail/moesif-orign-cors-changer/digfbfaphojjndkpccljibejjbppifbc?hl=en>
		
		y solamente haces click en el ícono de la extensión y al switch que sale, le das “off”

* **Daniel Hita Martínez** (2) [882743](https://platzi.com/comentario/882743/) 

	¿Alguien puede ayudarme a resolver este error?
	
	Al declarar response me dice:  
	_GET<https://yts.lt/api/v2/list_movies.json?genre=action> net::ERR_CONNECTION_RESET_
	
	y después en la última línea donde cierran los paréntesis y los curli…  
	_Uncaught (in promise) TypeError: Failed to fetch  
	_
	```       (asyncfunctionload() {
	    
	        asyncfunctiongetData(url){
	          const response = await fetch(url)
	          const data = await response.json()
	          return data
	        }
	    
	        const actionList = await getData('https://yts.lt/api/v2/list_movies.json?genre=action')
	        console.log('actionList', actionList)
	    
	      })()
	    
	```
	
	Gracias!!!

	* **guillermoleniz** [882743] (0)

		Tengo el mismo problema que tu, probe con esta api (<https://yts.ae/api/v2/list_movies.json?genre=action>), ya que al parecer lt no sigue funcionando, pero aun sigo teniendo el mismo problema.

* **Alfredo Gonzalez** (2) [783486](https://platzi.com/comentario/783486/) 

	Alguien puede aclararme  
	¿Porque es que cuando hacemos un request a un API con jQuery no tenemos que usar el método .json() ?  
	Gracias :3

	* **Juan Carlos Pinzón** [783486] (2)

		Primero porque en jQuery $.ajax es muy distinto al fetch así que no necesariamente lo que se hace en uno se hace en otro. En jQuery no es necesario ya que hay se puede colocar una propiedad que se llama dataType con valor json y con eso se parsea en Fetch se usa el .json() para que se parsee esa respuesta, es decir nos devuelve otra promesa pero con la data ya parseada a json

	* **Jonathan Uriel Jiménez Soveranes** [783486] (2)

		así es, como dice Carlos
		```     $.ajax({
		      url: 'https://randomuser.me/api/',
		      dataType: 'json',
		      success: function(data) {
		        console.log(data);
		      }
		    });
		        ```
		    
		```

* **czabala847** (2) [656915](https://platzi.com/comentario/656915/) 

	Lo hice combinando async await y promesas
	```     asyncfunctionload(url) {
	      const response = await fetch(url)
	      const data = await response.json()
	      return data
	    }
	    
	    const actionMovieList = load('https://yts.lt/api/v2/list_movies.json?genre=action')
	    const dramaMovieList = load('https://yts.lt/api/v2/list_movies.json?genre=drama')
	    const animationMovieList = load('https://yts.lt/api/v2/list_movies.json?genre=animation')
	      // .then(function(movie){
	      //   console.log(movie)
	      // })
	    
	    Promise.all([actionMovieList, dramaMovieList, animationMovieList])
	      .then(function(result){
	        console.log(result)
	        return result
	      })
	      .then(function(action){
	        console.log('Peliculas de acción')
	        console.log(action[0])
	      })
	      .catch(function(error){
	        console.log(`Hubo un error ${error}`)
	      })```
	    
	```

* **MatiasBZ** (2) [640135](https://platzi.com/comentario/640135/) 

	Y como manejo los errores … con async await, si ya no es necesario then ni catch ???

	* **Jair Israel Avilés Eusebio** [640135] (2)

		Pudes utilizra bloques `try...catch` para [ello](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/try...catch)

* **Aaron Felipe Isaacs** (2) [625786](https://platzi.com/comentario/625786/) 

	En realidad el genero se llama horror
	```     const load = async () => {
	      
	      const API_URL = 'https://yts.lt/api/v2/list_movies.json'
	      const getData = async (url, genre) => {
	        const action = await fetch(`${url}?genre=${genre}`)
	        const data = await action.json()
	        return data
	      }
	    
	      const dataAction = await getData(API_URL, 'action')
	      const dataHorror = await getData(API_URL, 'horror')
	      const dataDrama = await getData(API_URL, 'drama')
	      const dataAnimation = await getData(API_URL, 'animation')
	      console.log(dataAction)  
	      console.log(dataHorror)
	      console.log(dataDrama)
	      console.log(dataAnimation)
	    } 
	    
	    load()
	    
	```

* **Kingsman7** (2) [605964](https://platzi.com/comentario/605964/) 

	en que clase hicieron el loader. necesito aprender como se hace

	* **ferroblesh** [605964] (3)

		Seguramente en el curso de animaciones. Existen varios recursos en línea que te ayudan a hacerlo

	* **Jair Israel Avilés Eusebio** [605964] (1)

		Puedes usar un simple gif para algo mas rapido aunque el costo en el request de tu asset podria ser mayor comparado con hacerlo en CSS

	* **Daniel Esteves** [605964] (1)

		Hola! En estas clases se utilizan recursos ya hechos para darle dinamismo y continuar con el foco de jQuery a JavaScript, en el repositorio del curso podrás encontrar todos los recursos que se usaron y cómo funciona 💪

* **Facundo Nicolás García Martoni (Platzi)** (2) [595697](https://platzi.com/comentario/595697/) 

	 _ **¿No te funcionó la API? AQUÍ UNA ALTERNATIVA**_
	
	Hola a todos, seguramente a día de hoy (mayo de 2019) el servidor les rechace la request, así que después de horas de búsqueda encontré una API que funciona casi igual que la que se utiliza en el curso:
	
	<http://omdbapi.com/>
	
	En primer lugar deben solicitar una clave (key), la página los guía en ese proceso. Luego, el código les quedaría de esta manera
	```     const KEY = '&apikey=INSERTA_AQUI_TU_CLAVE';
	    const URL = `http://www.omdbapi.com/`;
	    
	    (asyncfunctionload(){
	      const getData = async (url) =>{
	        const response = await fetch(url)
	        const data = await response.json();
	        return data;  
	      }
	    
	      const actionList = await getData(`${URL}?s=horse${KEY}`); 
	      const terrorList = await getData(`${URL}?s=cow${KEY}`); 
	      const animationList = await getData(`${URL}?s=child${KEY}`); 
	    
	      console.log(actionList)
	      console.log(terrorList)
	      console.log(animationList)
	    
	    })()
	    
	    
	```
	
	Donde dice “horse”, “cow” y “child” pueden poner lo que ustedes quieran, es lo que buscarán, retornando la promesa un array con los resultados de esa búsqueda.
	
	Espero que les haya servido como a mi 😁

* **Juan Manuel Megia Navarro** (2) [567169](https://platzi.com/comentario/567169/) 

	Imagino que irá más adelante en el curso, pero seguro que a mucha gente le surge la duda en este punto igual que a mi, ¿Que pasa se algo sale mal en la solicitud cuando usamos async await en lugar de las promesas?, no disponemos de la función catch.
	
	Para solucionarlo se utiliza try catch de esta forma :
	
	async function nombreFuncion(){  
	try{  
	const response=fetch(url);  
	}catch(error){  
	console.log('acurrió el siguiente error : '+error)  
	}  
	}

* **Ollendorf  Ticona Valdivia** (2) [457005](https://platzi.com/comentario/457005/) 

	```    let URL = 'https://yts.am/api/v2/list_movies.json'
	    
	    asyncfunctiongetData(url, genre){
	    
	        const response = await fetch(`${url}?genre=${genre}`)
	        const data = await response.json()
	    
	        return data
	    }
	    
	    asyncfunctionMoviesGenreList(){
	    
	        let actionList = await getData(URL, 'action')
	        let dramaList = await getData(URL, 'drama')
	        let animationList = await getData(URL, 'animation')
	    
	        returnPromise.all([actionList, dramaList, animationList])
	    }
	    
	    MoviesGenreList()
	        .then(res => {
	            for(let listRes of res) {
	                let movies = listRes.data.movies
	    
	                for(let movie of movies) {
	                    console.log(movie.title);
	                }
	            }
	        })
	        .catch(err => {
	            console.log(err);
	        })
	    
	    
	    
	```

* **Flor Viviana  Mireles Rivera** (2) [420382](https://platzi.com/comentario/420382/) 

	Es lo mismo las funciones asíncronas que las promesas ?y si lo son cual es de ellas es mejor utilizar o para que tipo de acciones es mejor utilizar cada una de ellas?

	* **Luis Miguel Ramos Hernández** [420382] (3)

		En si la asincronia es una propiedad del lenguaje, es decir tu puedes controlar la ejecución puedes hacer otr ejecución sin necesidad de que termine la anterior, a diferencia de otros lenguajes sincronos que una linea de codigo se ejecuta hasta que termine la anterior y como tal las funciones asincronas pueden contener callbacks o promesas, los callback tienden a llevarte muchas veces a los [callback hells](https://codearmy.co/que-es-el-callback-hell-y-como-evitarlo-4af418a6ed14) para evitar ello se utilizan las promesas

* **xhino** (2) [349646](https://platzi.com/comentario/349646/) 

	Tambien podriamos usar los template strings para minimizar el uso de codigo de esta forma:
	```     asyncfunctiongetData(url) {
	            const response = await fetch(`https://yts.am/api/v2/list_movies.json?genre=${url}`);
	            const data = await response.json();
	            return data;
	    const actionList = await getData("action");
	        }```
	    
	```

* **Jonatan David Padilla** (2) [347766](https://platzi.com/comentario/347766/) 

	[AQUI esta el enlace](https://yts.am/api) de la API que usa leonidas.

* **edesalla17** (2) [342043](https://platzi.com/comentario/342043/) 

	muy bueno!

* **carlosextra1** (1) [1115056](https://platzi.com/comentario/1115056/) 

	Mi código!
	```     const API =  'https://yts.mx/api/v2/list_movies.json';
	    (asyncfunctionload() {
	    
	      console.time()
	      console.log('Iniciando....')
	      asyncfunctiongetData($genre) {
	        const result = await fetch(API+'?genre='+$genre)
	        returnawait result.json();
	      }
	      const actionList =  await getData('action');
	      const dramaList =  await getData('drama');
	      const animationList =  await getData('animation');
	      console.log(actionList, dramaList, animationList);
	      console.timeEnd();
	    })()
	    
	```

* **wlm.morant** (1) [1043249](https://platzi.com/comentario/1043249/) 

	xq tienen abierto la ventana de carpeta de VS no se aprecia todo el codigo por momentos

* **luistua** (1) [1034380](https://platzi.com/comentario/1034380/) 

	Yo, sólo de ver hasta eñ 1:44: WAIT,WAIT,WAIT,WAIT,WAIT

* **miguelangelsoler** (1) [1010056](https://platzi.com/comentario/1010056/) 

	```    asyncfunctiongetData(genre){
	      const response = await fetch(`https://yts.mx/api/v2/list_movies.json?genre=${genre}`)
	      const data = await response.json()
	      return data;
	    }
	    
	    (asyncfunctionstoreData(){
	      const actionList = await getData('action')
	      const terrorList = await getData('horror')
	      const animationList = await getData('animation')
	    })()
	    
	```

* **Omar Garrido García** (1) [988921](https://platzi.com/comentario/988921/) 

	amigos no tengo tan claro el uso del metodo json me queda un poco confuso alguna buena alma que desee darme alguna clase de tip para entenderlo

	* **miguelangelsoler** [988921] (7)

		Imagina que la respuesta viene de la siguiente forma:
		```     '{name:'miguel',age:28,skills:['javascript','ruby']}'
		    
		    
		```
		
		Por ejemplo, si quieres acceder a la propiedad name, no lo podrás hacer porque la respuesta es un string (o puede estar en otro formato).  
		Con el método .json, nos aseguramos de parsear o traer la respuesta en formato JSON (Javascript Object Notation) y de esta forma podremos acceder a las propiedades, filtrar y realizar y procesar la información de una forma más eficiente en nuestra aplicacion.  
		Lo anterior con el metodo.json, lo tendremos disponible de esta forma:
		```     {
		      name:'miguel',
		      age:28,
		      skills:['javascript','ruby']
		    }
		    
		```

	* **Andrés Gutierrez** [988921] (2)

		lo que pasa es que te traen los datos de esa api en un string, y Lo que hace es pasarlo a un formato llamado JSON(significa javascript object notation) para que podamos extraer sus propiedades, como puede ser el nombre de la película(poniendo un ejemplo)

* **ronaldjgarciav** (1) [920382](https://platzi.com/comentario/920382/) 

	nice. 😄

* **mxmacias** (1) [866201](https://platzi.com/comentario/866201/) 

	Alguien sabe por que no me imprime ningun dato, acaso las url estan mal?  
	![Captura.PNG](https://static.platzi.com/media/user_upload/Captura-4cf3b5d2-a572-4af6-823c-b5e95c2a3b12.jpg)

	* **Germán González** [866201] (1)

		[https://yts.lt/api/v2/list_movies.json?genre=action’_](https://yts.lt/api/v2/list_movies.json?genre=action%E2%80%99_)  
		esa es la api

* **Jose Luis Colmenares** (1) [670037](https://platzi.com/comentario/670037/) 

	Aquí indican como hacer para una función asíncrona se ejecute

* **Gustavo83** (1) [623389](https://platzi.com/comentario/623389/) 

	Fetch con una función asíncrona es un código mas simplificado!

* **frank16g** (1) [613078](https://platzi.com/comentario/613078/) 

	Alguien sabe como sería el código si yo tengo una base de datos en un servidor como xampp? en vez de la url que debo poner? Gracias!

	* **hnavarrete** [613078] (3)

		Hola
		
		Tendrías que poner la ruta a un archivo PHP que regrese los resultados de la base de datos en formato JSON.
		
		Por ejemplo, suponiendo que tienes una base de datos en MySQL, y tu archivo PHP en la misma ruta que tu archivo JS pondrías:
		
		**getData.php**
		```     include('conexion.php');
		    $link = Conectarse();
		    $items = array();
		    
		    $query = mysqli_query($link, "SELECT * FROM peliculas LIMIT 10");
		    while($row = mysqli_fetch_assoc($query)) {
		    	array_push($items, $row);
		    }
		    echo json_encode($items);
		    
		```
		
		En javascript tendrias que poner:
		```     (asyncfunctionload() {
		    
		        asyncfunctiongetData(url) {
		            const response = await fetch(url)
		            const data = await response.json()
		            return data
		        }
		        const movies = await getData('getData.php')
		        console.log(movies)
		    })()
		    
		```
		
		Esto te devuelve un array con los resultados:
		```     (10) [{…}, {…}, {…}, {…}, {…}, {…}, {…}, {…}, {…}, {…}]
		    
		```

* **Juan Pablo Calao Pérez** (1) [602463](https://platzi.com/comentario/602463/) 

	Mi forma de hacerlo fue la siguiente:
	```     // action
	    // horror
	    // animation
	    getMoviesOfGenre('action')
	    getMoviesOfGenre('horror')
	    getMoviesOfGenre('animation')
	    
	    asyncfunctiongetMoviesOfGenre(genre) {
	      const API_URL = `${MOVIE_LIST_URL}?genre=${genre}`
	    
	      const movieList = await fetchFromApi(API_URL)
	      console.log(genre, movieList)
	    }
	    
	    asyncfunctionfetchFromApi(url) {
	      const response = await fetch(url)
	      const data = await response.json()
	      return data
	    }
	    
	```
	
	Me gusta separar bien las tareas. Una función que llame a la api, y otra que utilice los datos dentro de la app

* **Jhon Alexander Perez Valencia** (1) [597499](https://platzi.com/comentario/597499/) 

	## traer datos de un API de forma asincrona
	```     asyncfunctiongetData(){
	    	returnawait fetch("url")
	    }
	    
	```

* **Danilo Pazos** (1) [571406](https://platzi.com/comentario/571406/) 

	Este es un ejemplo con GET y si mi petición fuese POST, como sería?  
	Así estaría bien? O alguien que me corrija 😄
	```     asyncfunctionload(){
	    const response = await fetch(‘https://ruta.com/api/ruta/id’, {
	    method: 'POST',
	    headers : new Headers(),
	    body:JSON.stringify({Id:1})
	    });
	    const data = await response.json();
	    return data;
	    }
	    
	    
	```

* **Betty Rossana Jimenez Cedeño** (1) [563013](https://platzi.com/comentario/563013/) 

	¿por qué invoca el json?

	* **Diego Alexander Forero Higuera (Platzi)** [563013] (1)

		json() es un método que tiene el response y lo que permite es tener la respuesta en formato json porque realmente lo que retorna es la respuesta de una promesa.

* **DiegoRP** (1) [559052](https://platzi.com/comentario/559052/) 

	Tengo un caso que no he podido resolver aún:  
	Estoy consumiendo una API con autenticación CHAP, por lo cual al hacer el call a la URL me retorna un codigo de error, pero me da unos datos para hacer la autenticación (esto es lo que debería pasar). Sin embargo, veo que debido al código de error simplemente para completamente la ejecución de la promesa y no tengo acceso a los datos. ¿alguien sabe cómo solucionarlo?

	* **Diego Alexander Forero Higuera (Platzi)** [559052] (1)

		Puedes compartir tu código, y la documentación de al api que estas usando, es probable que este llegando algo que no esperas y haga que se rompa el código.

	* **DiegoRP** [559052] (1)

		Estuve investigando un montón sobre el tema y encontré algo llamado ‘CORS’. Básicamente es una medida de seguridad (que se valida directamente en el navegador) que no permite el llamado a un recurso que esté fuera del mismo dominio, a menos que el servidor WEB o recurso lo permita de manera explícita mediante unos encabezados particulares.  
		Mi conclusión, desde la inexperiencia, es que ese llamado debo hacerlo entonces desde el backend y no desde el frontend pues es imposible cambiar la manera en la que está construido el recurso que debo consultar.  
		Agradezco mucho tu ayuda, y quisiera preguntar si existe alguna ‘mejor práctica’ para el consumo de API’s (por ejemplo si es mejor hacerlo desde frontend o backend), o simplemente es algo muy específico de cada desarrollo.  
		Un saludo.

	* **Daniel Esteves** [559052] (1)

		Hola Diego! Las API son básicamente un backend que tú pedirás desde un frontend, so esa API te pide que debes pasarle una solicitud en CORS para que pueda funcionar creo que el código si te funcionaría normalmente, pero las peticiones a las API siempre se hacen desde un frontend

* **Deiby Johany Avila Gutierrez** (1) [547561](https://platzi.com/comentario/547561/) 

	Como seria el manejo de errores con esa opcion resumida de Leonidas??

	* **aragonesteban (Platzi)** [547561] (3)

		Cuando uses **async/await** siempre envuelve la petición dentro de un **try/catch** , de esta manera tendrás control de algún error en la petición.
		
		![](https://firebasestorage.googleapis.com/v0/b/login-ionic-app.appspot.com/o/carbon%20\(14\).png?alt=media&token=a6bcc316-e2ca-41b3-ad93-45b4fdf58152)

	* **Miguel Angel Morales Larriega** [547561] (1)

		Esteban, si tengo 3 await, está bien envolver a los await que tenga dentro del mismo try?

* **Félix Alejandro Zelaya Orellana** (1) [526783](https://platzi.com/comentario/526783/) 

	Genial!! Me encantó la clase!

* **ajosue98** (1) [517735](https://platzi.com/comentario/517735/) 

	Aqui mi codigo 😃
	```     (asyncfunctionload(){
	      //await
	     asyncfunctiongetData(url){
	        const response = await fetch(url)
	        const data = await response.json()
	        return data
	      }
	    
	      const actionList = await getData('https://yts.am/api/v2/list_movies.json?genre=action')
	      const animationList = await getData('https://yts.am/api/v2/list_movies.json?genre=animation')
	    
	      for(let i=0;i<20;i++){
	        console.log('actionList',actionList.data.movies[i].title)
	      }
	      for(var i=0;i<20;i++){
	        var frase=`Animada #${i+1}`
	        console.log(frase,animationList.data.movies[i].title )
	      }
	    })()
	    
	```

* **JhonColorado07** (1) [483615](https://platzi.com/comentario/483615/) 

	Excelente. Me queda muy claro las funciones asíncronas, las promesas y las funciones sincronas.

* **Luis Angel José Portillo Arévalo** (1) [477551](https://platzi.com/comentario/477551/) 

	xd cuando usé el genero “Terror” solo me devolvió una peli que era de la WWE, así que cambié el genero de “terror” a “horror”.

* **Alfonso Navarro** (1) [475374](https://platzi.com/comentario/475374/) 

	Bueno no se si sera mas adelante, pero falto el manejo de errores en las funciones asyncronas o mas bn cuando utiliza
	```     const actionList = await getData('https://yts.am/api/v2/list_movies.json?genres=action')
	    
	```
	
	en ves de
	```      .then() y .catch()
	    
	```

* **jorge-winder-avila** (1) [461588](https://platzi.com/comentario/461588/) 

	¿Cómo paso parámetros datos a un recurso api rest con fetch?

	* **aragonesteban (Platzi)** [461588] (1)

		Hola Jorge!
		
		Mira, puede ser de esta manera, normalmente envías parámetros cuando haces una petición POST, te dejo el código:
		
		![](https://firebasestorage.googleapis.com/v0/b/login-ionic-app.appspot.com/o/carbon%20\(15\).png?alt=media&token=d1704084-e830-4702-aab2-48a3cb5bdb8f)

* **Matias Sebastian Perez** (1) [457666](https://platzi.com/comentario/457666/) 

	El error estaba en que el genero es horror, no terror jajajjajaja

* **gosunkugi** (1) [445725](https://platzi.com/comentario/445725/) 

	me di cuenta que al realizar las promesas el código sigue ejecutándose mientas que haciendo un await el código se tranca hasta que se cumpla la promesa, así que igual, cada uno tiene sus aplicaciones, ¿no?

* **Melina Jacqueline onoriaga** (1) [443793](https://platzi.com/comentario/443793/) 

	Encerrar en paréntesis a la función es como andar en bici sin rueditas! jajaja

* **Carlos Jesús Fernández Salazar** (1) [436845](https://platzi.com/comentario/436845/) 

	 **Importante** , la palabra reservada **await** sólo se puede usar dentro de funciones **async**.

* **dcortesnet** (1) [429855](https://platzi.com/comentario/429855/) 

	Más adelante se enseña a consumir una api con tokens?, Muy Buena clase Saludos

* **Flor Viviana  Mireles Rivera** (1) [425741](https://platzi.com/comentario/425741/) 

	para que se usa el .json()?

	* **Edward Steven Ramos Palacios** [425741] (1)

		Formatea el valor a un JSON válido.

* **Jcion** (1) [421585](https://platzi.com/comentario/421585/) 

	Horror, no Terror 😄

	* **Jcion** [421585] (1)

		<https://www.imdb.com/feature/genre/>

* **jlquichimbo** (1) [416906](https://platzi.com/comentario/416906/) 

	Qué pasa si la data que espero recibir es un html? Es decir necesito el contenido de un modal para mostrar. Es correcto traer un html con fetch? Se puede hacer?  
	O cuál sería la solución correcta.  
	Muchas gracias profe Leonidas. Excelente clase

	* **Jcion** [416906] (1)

		¿de dónde traerías ese html?

	* **jlquichimbo** [416906] (1)

		Uso el framework Django.  
		Tengo un template con el contenido html que quiero ubicar en el template padre. Segun lo que he visto en el curso me parece que lo correcto es tener el html ya listo en el template padre y ponerlo como visible mediante javascript verdad?  
		Muchas gracias.

* **ing-michaelvillanueva** (1) [398980](https://platzi.com/comentario/398980/) 

	await solo funciona si y solo si la función es asincrona

* **juand_silva** (1) [379333](https://platzi.com/comentario/379333/) 

	Sé que async/await es un sintax sugar del objeto Promise, pero como lo utilizo cuando quiero utilizar los métodos de Promise.all() y Promise.race() del objeto Promise()?

	* **Edward Steven Ramos Palacios** [379333] (1)

		Talvez van en contextos separados. con **async/await** solo esperas que se resuelva una promesa.
		
		El **Promiso.all y Promise.race** puedes usarlos en otra función o contexto. De hecho cuando tengas que resolver un array de promises verás que es mas útil usar estas ultimas dos.

* **Fabián Veliz** (1) [374951](https://platzi.com/comentario/374951/) 

	Buenas!  
	Me gustaría mencionar algo importante y es el manejo de errores en este tipo de funciones.
	```     async function myAsyncFunction () {
	      letresult = null;
	      try {
	        result = await getData('URL');
	        // Si la función de arriba falla por alguna razón, va a entrar por el catch.
	      } catch (e) {
	        // Acá retornamos el error para que no continúe ejecutando.
	        return console.error(e);
	      }
	      // Si llegamos hasta acá, es porque no hubo error. Podemos seguir trabajando. 
	      console.log(result);
	    }
	    
	```
	
	Saludos!

* **Gerardo Ruiz** (1) [374550](https://platzi.com/comentario/374550/) 

	la función getData funciona tanto para el método post como get de una API?

	* **Jonathan_Buitrago** [374550] (2)

		lo único que hace el método post y get entre muchos mas que hay, es que permite realizar distintas acciones dependiendo de lo que se requiera, ya sea una petición al servidor para traer, obtener, poner información en determinado lugar de la pagina

* **delfo1128** (1) [365829](https://platzi.com/comentario/365829/) 

	Una pregunta, estoy desarrollando el 8 ejercicio, en el tiempo 6:14 que se esta ejecutando la función asíncrona no me correo, no me muestra lo mismo.
	
	(async function load() {  
	//await  
	// devuelve una promesa  
	// codigo asincrona que se lee de una manera sincrona  
	const response = await fetch (‘<https://yts.am/api/v2/make_comment.json?genre=action>’)  
	const data = await response.json()  
	console.log(data);  
	}) ()
	
	Me muestra error en la funcion fetch

	* **delfo1128** [365829] (1)

		Que pena me equivoque al pegar la URL de la paginade la API

* **Alan Santiago** (1) [362980](https://platzi.com/comentario/362980/) 

	Esto significa que las funciones **async** van a evitar que el sitio se congele mientras llegan los datos, no? 😄

* **eltenis** (1) [345620](https://platzi.com/comentario/345620/) 

	no entiendo lo que es ?genre=action …no se como hacer consultas…en que curso de platzi explica esto?
	
	Muchas gracias

	* **Diego Alexander Forero Higuera (Platzi)** [345620] (4)

		Se conocen como parámetros get, estos sirven para pasar valores de consultas, se usan para consultar datos a API y se pueden ver que parámetros recibe el API leyendo su documentación, en este caso se esta consultando el genero del video.

	* **eltenis** [345620] (1)

		Me gustaria saber en que curso de Platzi se explica todo lo que me acabas de comentar.
		
		Muchas Gracias

	* **juand_silva** [345620] (2)

		Son parámetros de tipo GET. El símbolo ‘?’ le indica al servidor desde dónde comienza los recursos que se solicitan en la petición, es decir, a cuales recursos se refiere en la Api con su respectivo nombre y valor separados por el símbolo '='  
		En el caso  
		`?genre=action` tráigame los recursos de la api correspondientes al genero de acción.  
		Si se desea más recursos se puede hacer con concatenandolos con el símbolo ‘&’

* **wlm.morant** (1) [84116](https://platzi.com/comentario/1043270/) 
minuto 10:38 linea 85-86 porque no tienes problemas de HOISTING

* **Ang.O** (1) [83338](https://platzi.com/comentario/1024820/) 
sin el await como se ejecutaria??

	* **Diego Alexander Forero Higuera (Platzi)** [83338] (1)

		Si usas async tienes que usar await para que funcione el asincronismo si no va a lanzar un error.

* **hec_lopz** (1) [81518](https://platzi.com/comentario/982857/) 
Poner await al ejecutar la función getData no es redundante? La función getData ya es asíncrona, p...

	* **Angel Hernandez** [81518] (1)

		El await lo que logra es que se genere una espera en lo que se resuelve cada funcion, cuando debemos hacer varios await lo ideal es ejecutar Promise.all()
		```     const [respuesta1, respuesta2 ] = awaitPromise.all(getData("1"), getData("2"))
		    
		```
		
		<https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/all>

* **Ang.O** (1) [81488](https://platzi.com/comentario/982423/) 
en la async function getData para que necesitamos la constanet data, que acaso en la constante response no recibimos ya la informacion co...

* **Ang.O** (1) [81487](https://platzi.com/comentario/982404/) 
este curso tiene repositorio en githun github?

	* **Gabriel De Andrade (Platzi)** [81487] (3)

		[Curso de JQuery a JS](https://github.com/LeonidasEsteban/jquery-to-js-curso) 😄

* **Jhoan Steve Sandoval Meneses** (1) [75386](https://platzi.com/comentario/869064/) 
¿ En caso de que fetch retorne un html y quiera mostrarlo en un div como seria ? intente de esta forma pero no me funciona. fetch(‘...

	* **Gabriel De Andrade (Platzi)** [75386] (1)

		Puedes mostrarnos cual es el contenido del response? Puede que no esté llegando bien, tienes que asegurarte que sea un string 😄

* **Ang.O** (1) [74448](https://platzi.com/comentario/852938/) 
async es algo que entiende la maquina para poder asignarle despues un orden de ejecucion?

	* **Gabriel De Andrade (Platzi)** [74448] (2)

		Eso lo vemos en el [Curso de Fundamentos de JavaScript](https://platzi.com/clases/fundamentos-javascript/) 😄

* **Ang.O** (1) [74446](https://platzi.com/comentario/852917/) 
que hace respnse.jason?

	* **Gabriel De Andrade (Platzi)** [74446] (2)

		El `response.json` es el nombre que se le da muchas veces al archivo que obtienes cuando haces una solicitud de datos a través de una API, este generalmente va a contener los datos y si la operación fue exitosa o no

* **Ang.O** (1) [74444](https://platzi.com/comentario/852910/) 
api es base de datos? no recuerdo que lo hayan explicado, que clase fue eso?

	* **Gabriel De Andrade (Platzi)** [74444] (2)

		No, la API son una serie de reglas que existen para que el front se comunique con el backend 😄

* **cmrestrepoc** (1) [69088](https://platzi.com/comentario/751268/) 
Si en las promesas then catch el método catch se usa para controlar los errores, como controlo los errores con async-await? Bajo este esq...

	* **Juan David Castro (Platzi)** [69088] (1)

		Usamos un bloque **`try catch`** :
		```     // Con Promesas
		    fetch('url')
		      .then(res => res.json())
		      .then(data => console.log(data))
		      .catch(err => console.err(err))
		    
		    // Con async await
		    asyncfunction () {
		      try {
		        const res = await fetch('url')
		        const data = await res.json()
		        console.log(data)
		      } catch(err) {
		        console.err(err)
		      }
		    }
		    
		```
		
		👉 [Curso Profesional de JavaScript](https://platzi.com/clases/javascript-profesional/)

* **Eduardo Aguilar Escalante** (0) [916413](https://platzi.com/comentario/916413/) 

	Pienso que tambien vale la pena adicionar, que no solo funciona para consumir API´s de terceros y mostrarlas en nuestro DOM, sino que tambien funciona con URL´s para traer datos de alguna base de datos en MYSQL, o incluso para hacer uso de alguna ruta de un backend ya sea con PHP o con python, por nombrar ejemplos.  
	También las funciones asíncronas pueden ser utilizadas para ejecutar un controlador en un backend y en base a esto, mostrar los datos que se retornen y manipular el DOM.

## 0080. Selectores [12292](https://platzi.com/clases/1316-jquery-js/12292-selectores/)

### Descripción:


Un selector nos sirve para poder manipular un objeto del DOM, puedes buscar dicho objeto ya sea por su id, clase, atributo, etc.

Para PlatziVideo necesitamos un selector de un contenedor para ponerle dentro la lista de películas.

En jQuery hacemos un selector de la siguiente forma:
```     const $home = $(‘ .home ’);
    
```

Por convención una variable que este represente un objeto del DOM lleva el signo $, esto es para tener claro que estamos manipulando un objeto del DOM y no algún tipo de información o dato.

Dentro de JavaScript existen distintas funciones para hacer selectores, entre ellas se encuentra:

• getElementById: recibe como parámetro el id del objeto del DOM que estás buscando. Te regresa un solo objeto.  
• getElementsByTagName: recibe como parámetro el tag que estas buscando y te regresa una colección html de los elementos que tengan ese tag.  
• getElementsByClassName: recibe como parámetro la clase y te regresa una colección html de los elementos que tengan esa clase.  
• querySelector: va a buscar el primer elemento que coincida con el selector que le pases como parámetro.  
• querySelectorAll: va a buscar todos los elementos que coincidan con el selector que le pases como parámetro.

### Comentarios:

* **Sergio Minei** (50) [335624](https://platzi.com/comentario/335624/) 

	## Selectores
	
	Los selectores nos permites seleccionar un elemento del DOM con el fin de poder manipularlos.
	
	Por convención, las variables que son elementos del DOM comienzan con una `$`.
	
	**jQuery**
	```     const $home = $(".home") //Elemento con la clase home
	    const $home = $("#home") //Elemento con el id home
	    
	```
	
	**JavaScript**
	```     //Retorna un elemento con el id home
	    document.getElementById("home")
	    
	    //Retorna una lista de elementos con la clase home
	    document.getElementsByClassname("home")
	    
	    //Retorna una lista de elementos con el tag div
	    document.getElementsByTagName("div")
	    
	    //Devuelve el primer elemento que coincida con el query de búsqueda.
	    document.querySelector("div .home #modal")
	    
	    //Devuelve todos los elementos que coincidan con el query de búsqueda.
	    document.querySelectorAll("div .home #modal")
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **Joel Alejandro Morales García** [335624] (2)

		Muy buen resumen de la clase 😄

	* **Sergio Minei** [335624] (2)

		muchas gracias [@naojamg](https://platzi.com/@naojamg)

	* **SEBASTIAN PADUANO** [335624] (2)

		gracias por el resumen eres el mejor

	* **Sergio Minei** [335624] (2)

		Qué bueno que te haya ayudadi @soypaduano.
		
		Te dejo mi lista de resúmenes por si te interesa: <https://github.com/MineiToshio/CursosPlatzi>

	* **denver15jh** [335624] (2)

		Guao excelente @MineiToshio muchas gracias, muy buenos tus resumenes 😃

	* **Sergio Minei** [335624] (1)

		Gracias por tus palabras @denver15jh

	* **Aaron Chacon** [335624] (2)

		Muy útil tu resumen, es de mucha ayuda. GRACIAS

* **leshz** (13) [336469](https://platzi.com/comentario/336469/) 

	Resolviendo una pregunta que tenia desde hace tiempo .  
	Cuando usan :
	```     getElementsByClassName
	    getElementsByTagName
	    
	```
	
	Retornan un elemento de tipo HTMLCOLLECTION
	
	mientras si usan
	```     querySelectorAll
	    
	```
	
	Retorna un elemento tipo NODELIST
	
	¿Que diferencias tienen ?
	
	Tanto NodeList como HTMLCollection son colecciones de nodos del DOM . la diferencia es que mientras NodeList puede contener cualquier tipo de nodo , HTMLCollection solo va a contener cualquier elemento del nodo .
	
	La informacion la encontre [aqui ](https://teamtreehouse.com/community/understanding-the-difference-between-an-htmlcollection-and-a-nodelist)

* **Carlos Andres Castañeda Osorio** (10) [708527](https://platzi.com/comentario/708527/) 

	Sólo una corrección al código de Leonidas, al momento de usar el método _.getElementById(‘id-elemento’)_ no se debe usar el **#** ; por ende la forma correcta sería:
	```     const $form = document.getElementById('form')
	    
	```

	* **Camilo Alexander Velandia Velandia** [708527] (1)

		tambien vi eso

	* **Diego Andres Cabrera Rojas** [708527] (1)

		x2 , tambien se me hizo raro cuando lo vi

* **oteka21** (10) [340000](https://platzi.com/comentario/340000/) 

	En el minuto 10:00 hay un error, con:
	```     document.getElementById('#drama');
	    document.getElementById('#animation');
	    
	```
	
	debe cambiarse por:
	```     document.getElementById('drama');
	    document.getElementById('animation');
	    
	```

	* **Japheth Calzada López** [340000] (4)

		cierto, seria por id no por selector

	* **Juan David Castro (Platzi)** [340000] (6)

		No te preocupes, se resuelve en la clase [Reutilizando funciones](https://platzi.com/clases/jquery-js/concepto/de-jquery-a-javascript/reutilizando-funciones/material/) 😉.

	* **SEBASTIAN PADUANO** [340000] (1)

		wow muchas gracias

* **David Antonio Ordóñez Cornejo** (5) [335498](https://platzi.com/comentario/335498/) 

	Las variables que comienzan con $ son variables que contienen elementos del DOM
	```     const $elemento = document.getElementById('elemento .clase')
	    
	```

* **miguelangelsoler** (4) [1010061](https://platzi.com/comentario/1010061/) 

	Palabra del día: **Jquerosa**

	* **Rodrigo Cuenca Mayuri** [1010061] (1)

		jajaj

* **Juan Gaybre Flores** (3) [927998](https://platzi.com/comentario/927998/) 

	Que buenos son los selectores de CSS en JS, yo solo había ocupado el getElementById.

* **Gustavo83** (3) [626798](https://platzi.com/comentario/626798/) 

	La forma JQuerosa…jajaja

	* **Smarin-jpg** [626798] (9)

		Entiendo el comentario pero pienso que el mundo de la programación web le debe mucho a Jquery

	* **Jonathan Uriel Jiménez Soveranes** [626798] (1)

		ahahha°!

* **pablojorgeandres** (3) [483886](https://platzi.com/comentario/483886/) 

	## Selectores
	
	Los selectores sirven para seleccionar objetos del DOM con el fin de manipularlos.
	
	Una buena práctica es asignar una constante _const_ con cada uno de ellos, listarlos al inicio del código.
	
	Una convención entre algunos programadores es agregarle a estas constantes un signo _**$**_ al comienzo de su nombre para diferenciarlas del resto de las variables del código.
	```     	const $selector
	    
	```
	
	En **jQuery** generamos un selector de la siguiente forma:
	```     	$('.home') // class
	    	$('#home') // id
	    	$('div')  // HTML tag
	    
	```
	
	Dentro de **JavaScript** existen distintas funciones para generar selectores:
	
	  * **getElementById** : recibe como parámetro el **id** del objeto. Regresa un solo objeto.
	```         	document.getElementById('home')
	        
	```
	
	  * **getElementByTagName** : recibe como parámetro el **tag** que estas buscando y te regresa una colección html de los elementos que tengan ese tag.
	```         	document.getElementByTagName('span')[0] // traerá el primer span del código html
	        
	```
	
	  * **getElementByClassName** : recibe como parámetro la **clase** y te **regresa una colección** html de los elementos que tengan esa clase.
	```         	document.getElementByClassName('home')
	        
	```
	
	  * **querySelector** : va a buscar **el primer elemento que coincida con el selector** que le pases como parámetro.
	```         	document.querySelector('#container > h2')
	        
	```
	
	  * **querySelectorAll** : va a buscar **todos los elementos** que coincidan con el selector que le pases como parámetro.
	```         		document.querySelectorAll('#video-content > div > div > div.VideoMaterialLayout-timeline > span > div > aside > section > div.TimelineNav-materials > a')
	        
	        // En la página de una clase de Platzi arroja:
	        
	        // NodeList(10) [a.TimelineNav-material, a.TimelineNav-material, a.TimelineNav-material, a.TimelineNav-material, a.TimelineNav-material.is-active, a.TimelineNav-material, a.TimelineNav-material, a.TimelineNav-material, a.TimelineNav-material, a.TimelineNav-material]
	        
	```

* **Enrique Felix Garcia Stave** (2) [716783](https://platzi.com/comentario/716783/) 

	**Selectores en jQuery**
	
	Para retornar la referencia a un elemento HTML5 (sea tag, ID o class) se usa la siguiente expresión:  
	**$(‘selector .clase #id’)**  
	Esta expresión puede simplificar funciones para obtener referencias a elementos HTML como tags, clases o ids.
	
	**Selectores con Vanilla JS**  
	Para retornar la referencia a un elemento en Javascript puro, se pueden usar las siguientes expresiones:  
	**document.querySelector:** Nos retorna el primer elemento que cumpla con los criterios dados. Ejemplo: document.querySelector(‘.clase’).  
	**document.getElementById:** Seria el equivalente a hacer document.querySelector(‘#unId’).  
	**document.getElementByClassName:** Seria el equivalente a hacer document.querySelector(‘.unaClase’), la diferencia con el anterior es que este retorna absolutamente todos los elementos con la referencia especificada y el de Id, retorna solo el primer elemento en cuestión.

* **Alan Santiago** (2) [363124](https://platzi.com/comentario/363124/) 

	No se consiguiria algo mas parecido usando **document.querySelector()**?

* **Alejandro González Reyes** (2) [58323](https://platzi.com/comentario/575619/) 
¿Es obligatorio almacenar el resultado de la busqueda de algún nodo o lista de ellos en una constante? ¿Por que no en una variable?

	* **Diego Alexander Forero Higuera (Platzi)** [58323] (5)

		Hola, se acostumbra a usar una constante ya que tu selector no va a cambiar, es una parte del html que no cambia, puede cambiar el contenido pero como tal la etiqueta va a ser siempre el mismo.

* **carlosextra1** (1) [1115080](https://platzi.com/comentario/1115080/) 

	Práctica del $ en variables!

* **Kenet Chungandro** (1) [989810](https://platzi.com/comentario/989810/) 

	Se pone mejor 😄

* **Kivindlg** (1) [955537](https://platzi.com/comentario/955537/) 

	como hago para copiar una linea de comido en visual studio, haci mismo como pasa en el video???

	* **David Raygoza Garcia** [955537] (4)

		Alt + Shift + Flecha Arriba o Flecha Abajo

	* **Emmanuel García** [955537] (2)

		Lo que te entendí es duplicar una línea … Yo lo hago con `cmd` \+ `shift` \+ `D`.

* **Carlos Roberto Villatoro Barrios** (1) [893090](https://platzi.com/comentario/893090/) 

	DOM => Siglas en ingles para Modelo de Objetos del Documento  
	Los selectores hacen fácil acceder a los elementos del HTML DOM.

* **urieelmm** (1) [838565](https://platzi.com/comentario/838565/) 

	Excelente información ! Usar selectores CSS en JS

* **Alejandro González Reyes** (1) [575619](https://platzi.com/comentario/575619/) 

	¿Es obligatorio almacenar el resultado de la busqueda de algún nodo o lista de ellos en una constante?  
	¿Por que no en una variable?

	* **Diego Alexander Forero Higuera (Platzi)** [575619] (5)

		Hola, se acostumbra a usar una constante ya que tu selector no va a cambiar, es una parte del html que no cambia, puede cambiar el contenido pero como tal la etiqueta va a ser siempre el mismo.

* **Jose Padron (Platzi)** (1) [544297](https://platzi.com/comentario/544297/) 

	Alguien sabe como Leonidas duplica código tan rápido?  
	Yo tengo que seleccionar, copiar y pegar siempre.

	* **Nicolas Esteban Prieto Sarmiento** [544297] (1)

		Ctrl + c = copiar  
		Ctrl + v = pegar  
		Ctrl + a = seleccionar toda una linea de texto  
		Ctrl + mayus+ flecha derecha = ir seleccionando varias lineas de texto mientras presionas varias veces la flecha derecha y izquierda para devolverte
		
		**en ves de mayus pueds usar alt (pero te selecciona toda la linea completa)**

	* **Nicolas Esteban Prieto Sarmiento** [544297] (2)

		alt con la flecha de abajo

	* **Mateo Gonzales Navarrete** [544297] (2)

		En Atom lo puedes hacer con ctrl+shift+d.  
		Busca los shortcuts en la documentacion de tu editor. Un buen programador siempre se sabe los shortcuts porque se ahorra muchisimo tiempo 😃

	* **Kevin Humberto León Santamaria** [544297] (4)

		Depende de tu editor de código, en cada uno es diferente, en mi caso lo hago con shif + alt + flecha abajo o arriba dependiendo de donde lo quieras copiar, y con alt + flecha abajo o arriba simplemente mueves la linea de código seleccionada a otra posición

	* **miguel angel goda molina** [544297] (1)

		si es visual studio code es con shitf + alt + flecha abajo

	* **Arnaldo Chura Condori** [544297] (1)

		En visual studio code es mayus + alt + flecha arriba o abajo

* **Félix Alejandro Zelaya Orellana** (1) [526838](https://platzi.com/comentario/526838/) 

	Buenísima clase!!

* **gosunkugi** (1) [445727](https://platzi.com/comentario/445727/) 

	ve… cuando comenzó la clase era por la tarde y ya está de noche xD

* **ivan_acg** (1) [416543](https://platzi.com/comentario/416543/) 

	Este curso está demaciado Pro, esperé desde que aprendí desarrollo web para entender esta parte y la de la clase que viene.

	* **SEBASTIAN PADUANO** [416543] (1)

		gracias

* **David Farinango** (1) [413428](https://platzi.com/comentario/413428/) 

	Me quedo loco con está clase! Justo lo q necesitaba!

* **luistua** (1) [83819](https://platzi.com/comentario/1036459/) 
¿Así que cuando hablamos del **DOM **se refieren al documento html?

	* **Erik Ochoa (Platzi)** [83819] (2)

		Se refiere a la interpretación que hace el navegador del documento HTML en forma de un árbol de nodos,. Mira la [documentación de MDN](https://developer.mozilla.org/es/docs/Glossary/DOM).

* **Ang.O** (1) [82412](https://platzi.com/comentario/1004743/) 
cuando se esta usando jquery ¿cuando se usa el $? y para que se usa jquery si todo lo que se puede hacer en jquery parece poder hacerce e...

	* **Luis Arturo Lira Cerda** [82412] (2)

		En jQuery se usa el $ para referirse a la palbra jQuery, es decir, para acceder a las funcionalidades de jQuery como sus métodos u otras opciones.
		
		Justamente la intención de este curso, jQuery actualmente es una tecnología que normalmente no se usa para crear proyectos nuevos ya que todo se puede hacer con Javascript puro.
		
		jQuery nació como un librería para poder crear un estándar del lenguaje entre navegadores, ya que antes todo el tema de Javascript era un caos y por eso jQuery se hizo tan popular.
		
		Actualmente hay varias empresas que están migrando su código de jQuery a Javascript puro (Github es una de ellas), también los creadores del framework como Bootstrap, para la versión 5 están planeado que se deje de usar jQuery.

* **Jose Padron (Platzi)** (1) [55716](https://platzi.com/comentario/544297/) 
Alguien sabe como Leonidas duplica código tan rápido? Yo tengo que seleccionar, copiar y pegar siempre.

	* **Nicolas Esteban Prieto Sarmiento** [55716] (1)

		Ctrl + c = copiar  
		Ctrl + v = pegar  
		Ctrl + a = seleccionar toda una linea de texto  
		Ctrl + mayus+ flecha derecha = ir seleccionando varias lineas de texto mientras presionas varias veces la flecha derecha y izquierda para devolverte
		
		**en ves de mayus pueds usar alt (pero te selecciona toda la linea completa)**

## 0090. Creación de templates [12301](https://platzi.com/clases/1316-jquery-js/12301-creacion-de-templates/)

### Descripción:


Vamos a crear una plantilla con nuestro elemento base, dicha plantilla será recibirá valores dinámicos.

Dentro de jQuery, la creación de un template seria con un texto base y si nuestro texto cuenta con distintas líneas más aparte tuviera valores dinámicos se vería de la siguiente forma:
```     ‘<div class=”container”>’ +
        ‘<p id=’+ id +’>Hola Mundo<p>’ +
    ‘<div>’
    
```

Desde ECMAScript 6 contamos con una nueva característica llamada template literals que se representan con las comillas invertidas ``, el ejemplo anterior pasaría a verse de esta forma:
```     `<div class=”container”>
        <p id=${id}>Hola Mundo<p>
    <div>`
    
```

### Comentarios:

* **Sergio Minei** (35) [335685](https://platzi.com/comentario/335685/) 

	## Templates
	
	**jQuery**
	
	En jQuery se tiene que poner todo el html dentro de una cadena de texto.
	```     functionvideoItemTemplate(src, title) {
	      return (
	        '<div class="primaryPlaylistItem">' +
	          '<div class="primaryPlaylistItem-image">' +
	            '<img src="' + src + '">' +
	          '</div>' +
	          '<h4 class="primaryPlaylistItem-title">' +
	            title +
	          '</h4>' +
	        '</div>'
	      )
	    }
	    
	```
	
	**JavaScript**
	
	Se usa una característica de ES6 que se llama `template literals`.
	```     functionvideoItemTemplate(src, title) {
	      return (
	        `<div class="primaryPlaylistItem">
	          <div class="primaryPlaylistItem-image">
	            <img src="${src}">
	          </div>
	          <h4 class="primaryPlaylistItem-title">
	            ${title}
	          </h4>
	        </div>`
	      )
	    }
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **Juan David Castro (Platzi)** [335685] (26)

		🤔 Técnicamente en jQuery también puedes usar template literals.  
		👲 El verdadero punto es que precisamente jQuery no aporta nada más que peso muerto para estas características 🏀 😛

	* **Sergio Minei** [335685] (5)

		tienes toda la razón @juandc

	* **Juan David Castro (Platzi)** [335685] (3)

		Por cierto, excelentes tutoriales @MineiToshio!

	* **Sergio Minei** [335685] (10)

		Muchas gracias @juandc. Es un gusto poder ayudar. 😄
		
		Estoy haciendo tutoriales de todos los cursos que estoy llevando. Si deseas puedes verlos **[aquí](https://github.com/MineiToshio/CursosPlatzi)**. 😉

	* **Juan David Castro (Platzi)** [335685] (3)

		Ahí tiene su merecida estrellita buen hombre 😉

	* **Sergio Minei** [335685] (4)

		hehehe muchas gracias! 😃

	* **jsechirinos** [335685] (3)

		@MineiToshio, acabo de ver tus resúmenes, excelentes aportes, muy claros… + 1 star

	* **Sergio Minei** [335685] (1)

		Gracias @jsechirinos! Me alegra mucho saber que los resúmenes están siendo de ayuda.

	* **Freddy Córdova Arana** [335685] (2)

		Muchas gracias @MineiToshio, lo que haces es inspirador.

	* **Sergio Minei** [335685] (2)

		Gracias @freddyco! Tus palabras me inspiran a continuar. 😄

	* **Santiago Patiño** [335685] (3)

		Gracias, no sabes cuanto me ayuda lo que haces 😃

	* **Sergio Minei** [335685] (2)

		Qué bueno @santipmartinez! Es un gusto poder ayudar 😄

	* **Luis Carlos Juarez Azuara** [335685] (1)

		Gracias

* **Jonatan David Padilla** (11) [345902](https://platzi.com/comentario/345902/) 

	`debugger;` Es como un punto de interrupción o pausa en el script invocando la funcionalidad de depuracion.
	
	[debugger](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/debugger)

* **ronaldjgarciav** (8) [923892](https://platzi.com/comentario/923892/) 

	Mi maestro de Js Sasha ya me habia explicado esto :3 su curso también es buenísimo. echenle un ojo realmente vale la penes un gran maestro de Js. Además me dejo la maña de no terminar sentencia con ;

	* **Emmanuel García** [923892] (1)

		También se me hace más cómodo y limpio escribir sin " ; ". Es un buen maestro @Sasha

* **Jonatan David Padilla** (7) [345944](https://platzi.com/comentario/345944/) 

	Los templates son cadenas de texto entre comillas invertidas que podemos insertar en nuestro html de manera dinámica, por ejemplo, en la clase vimos como insertar un template con el contenido de una api cuantas veces la api lo indicara, **estuvo genial.**

* **Germán González** (6) [1020815](https://platzi.com/comentario/1020815/) 

	La API de yts cambió ahora es [yts.mx](http://yts.mx), de nada.

* **Manuel Rivera** (6) [854348](https://platzi.com/comentario/854348/) 

	Pensé que me bastaba con el curso de fundamentos de JavaScript para pasar a React.js pero  
	me hubiese perdido de mucha información en este curso, el mejor curso que he tomado hasta el momento

	* **ssant1_** [854348] (2)

		Opino completamente lo mismo jeje este curso es un complemento necesario del curso de fundamentos de JS

	* **Juan Gaybre Flores** [854348] (2)

		Llegué aquí por la misma razón, de JSFundamentos quería empezar con reactJS, pero que bueno es pasar por este curso y complementar las bases.

	* **Christian Erik Velázquez Morales** [854348] (2)

		Muchos pensamos igual

	* **Karla Agraz** [854348] (2)

		Yo quiero tomar la escuela de JS después de este curso, siento que me faltó seguir profundizando 😃

* **pablojorgeandres** (4) [484137](https://platzi.com/comentario/484137/) 

	## Creación de Templates
	
	Con JavaScript es posible crear templates insertando código HTML en el DOM a través de los selectores. Dentro de este código puedo insertar datos dinámicos de acuerdo a la necesidad.
	
	Con una característica de ES6, “ _template literals_ ” (que son estas comillas invertidas ‘`’, o acento grave) puedo generar este código, tanto en javascript como con jQuery:
	```     
	    	functiontemplate(someTitle, someContent) {
	    		return(`<div class="Description">
	    			<div class="Description-title"> ${someTitle} </div>
	    			<div class="Description-agenda">
	    				<p> ${someContent} </p>
	    			</div>
	    		</div>`)
	    	}
	    
	    
	```

* **Mateo Aquino** (3) [855703](https://platzi.com/comentario/855703/) 

	La primera vez que veo una aplicacion para la interpolacion de textoo!! 😄

* **Wandy Rafael Santana Evangelista** (2) [1078744](https://platzi.com/comentario/1078744/) 

	Realmente con la funcion donde estaba al fondo del bloque, igual se ejecutaba bien por el **hoisting** en JS, pero es buena practica tener variables y funciones al comenzar el bloque.

* **Cesar Eliezer Gomez Gutierrez** (2) [893488](https://platzi.com/comentario/893488/) 

	Wow, esto es increíble

* **fryma** (2) [881294](https://platzi.com/comentario/881294/) 

	Siempre hay que recordar que para que se resuelva la funcion:
	
	async function getData(url){  
	const response = await fetch(url);  
	const data = await response.json();  
	return data;  
	}
	
	Hay que llamarla con await, yo me olvidaba y se quedaba como Pending, pero con esto devolvera la data.  
	const actionList = await getData(‘<https://yts.lt/api/v2/list_movies.json?genre=action>’);

* **iridian** (2) [880628](https://platzi.com/comentario/880628/) 

	muchas gracias por la clase, por fin logre entender muchas cosas 😂

* **Jonathan Uriel Jiménez Soveranes** (2) [800442](https://platzi.com/comentario/800442/) 

	 **Alt+96** para obtener el acento grave.

* **Enrique Felix Garcia Stave** (2) [718685](https://platzi.com/comentario/718685/) 

	 **Apuntes:**
	
	**Templates**
	
	![](https://scontent.fctg1-2.fna.fbcdn.net/v/t1.15752-9/69387532_538775566690943_106790028193038336_n.png?_nc_cat=109&_nc_oc=AQneXAQBdPnGWGH9_pMUBY5BOMFzKSYss5S7drYM5_n2umhMOlHxauPub-ru29yWuiw&_nc_ht=scontent.fctg1-2.fna&oh=b5868b1e2fc2ee02bc172f08101fd8a0&oe=5E088D9D)

* **Arnaldo Chura Condori** (2) [676070](https://platzi.com/comentario/676070/) 

	backtick no me funciona  
	function videoItemTemplate(src, title ){  
	return (  
	`<div class="primaryPlaylistItem"> <div class="primaryPlaylistItem-image"> <img src="src/images/covers/midnight.jpg"> </div> <h4 class="primaryPlaylistItem-title"> Titulo de la peli </h4> </div>`  
	)  
	}

	* **ilan.vivanco** [676070] (1)

		Te falta el backtick en tu ejemplo.  
		Prueba con
		```     function videoItemTemplate(src, title ){
		    return (`
		    <div class="primaryPlaylistItem"> <divclass="primaryPlaylistItem-image"><imgsrc="src/images/covers/midnight.jpg"></div><h4class="primaryPlaylistItem-title"> Titulo de la peli </h4></div>
		    `)
		    }
		    
		```

	* **Daniel Esteves** [676070] (1)

		Hola Arnaldo! Te hace falta las comillas simples dentro de los paréntesis del return 💪 me avisas si te sirvió

* **isra_lopez** (2) [654756](https://platzi.com/comentario/654756/) 

	Que genial!!! ¿Por qué no vi este curso antes?.. Llevo años programando en Jquery… lo unico que odio de Javascript son los selectores, esos si me dan flojera 😛…
	
	No lograba sacar esas comillas invertidas pero ya lo descubri, en mi teclado latino y en Ubuntu puede ser AltGr + (tecla de cerrar llave).

* **heysoypaez** (2) [501332](https://platzi.com/comentario/501332/) 

	Ventajas del template string:
	
	  * Nos permite mejor legibilidad al hacer multilinea
	  * Nos permite mejor lectura del codigo al evitar el signo de suma ( + ) para concatenar textos
	
	**Dejo mi código de la clase**
	
	_Función con el template string:_
	```     function videoItemTemplate(src , title) {
	    	
	    	return(
	    		 `<divclass="primaryPlaylist">
	                <h3 class="primaryPlaylist-topic">Para llorar</h3>
	                <h2 class="primaryPlaylist-title">${title}</h2>
	    
	                <divclass="primaryPlaylist-list"id="${title}">
	                  <divclass="primaryPlaylistItem">
	                    <divclass="primaryPlaylistItem-image">
	                      <img src=${src}>
	                    </div>
	                    <h4 class="primaryPlaylistItem-title">
	                      ${title}
	                    </h4>
	                  </div>
	                </div>
	    
	              </div>`
	    		)
	    }
	    
	    
	```
	
	Y la función asincrona donde se ejecuta esto es …
	```     (async function load() {
	    	
	    
	    	async function getData(url) {
	    
	    	//Espera a que esto se ejecute
	    	const response = await fetch(url) //como es una funcion asincrona no hace falta el .then
	    
	    	const movieRequest = await response.json() //respuesta del metodo json
	    
	    	return movieRequest;
	    	}
	    
	    	const api = "https://yts.am/api/v2/list_movies.json"	
	    
	    	//esto necesita el await porque devuelve una promesa arriba y tiene que esperar
	    	const actionList = await getData(`${api}?genre=action`)
	    	const dramaList = await getData(`${api}?genre=drama`)
	    	const animationList = await getData(`${api}?genre=animation`)
	    
	    /*
	    	console.log("actionList", actionList);
	    	console.log("dramaList", dramaList);
	    	console.log("animationList", animationList);
	    */
	    
	    	/*SELECTORES
	    	=============*/
	    
	    	const $featuringContainer = document.querySelector("#featuring") 
	    	const $form = document.querySelector("#form") 
	    	const $home = document.querySelector("#home") 
	    
	    	//El "$" es una convención para decir que es un elemento del DOM
	    	const $modal = document.getElementById("modal")
	    
	    
	    	const $mContainer = document.querySelector("#action")
	    	const $animationContainer = document.getElementById("animation")
	    	const $dramaContainer = document.getElementById("drama")	
	    
	    	
	    	const $overlay = document.getElementById("overlay")
	    	const $hideModal = document.getElementById("hide-modal")
	    
	    	//para optimizar el codigo buscamos en modal
	    	const $modalImage =  $modal.querySelector("img")
	    	const $modalTitle =  $modal.querySelector("h1")
	    	const $modalDescription =  $modal.querySelector("p")
	    
	    
	    	//Funcion para mostrar en consola las peliculas
	    	actionList.data.movies.forEach( (movie) => {
	    	const HTMLString = videoItemTemplate(movie);
	    	console.log(HTMLString)
	    })
	    
	    
	    })() //esta funcion que se autoejecuta gracias a los ultimos parentesis
	    
	    
	```

* **Santiago Patiño** (2) [414813](https://platzi.com/comentario/414813/) 

	¿Como se coloca esas comillas?

	* **Santiago Patiño** [414813] (2)

		Ya lo encontré, se llaman acento grave y se colocan con ALT + 96

* **ernestomtz** (2) [356564](https://platzi.com/comentario/356564/) 

	@leonidasEsteban En explorer no funciona las comillas invertidas. Alguien sabe de algún polifil para que los templates funcione en Explorer?

	* **Diego Alexander Forero Higuera (Platzi)** [356564] (3)

		Tienes que usar babel para que transpile el código a JavaScript que entienda Internet Explorer.

	* **Adriann Felipe Sanchez Sierra** [356564] (1)

		@GOLLUM23 como se podría hacer ello?

	* **Camilo Castillo** [356564] (2)

		En el curso de javascrit te enseñan a manejar babel

	* **Jcion** [356564] (1)

		@camilocas ¿cuál de todos? Porque en el de fundamentos no.

	* **Fernando Alejandro Yerena Ramos** [356564] (1)

		@Jcoin Lo puedes encontrar en el [Curso de Fundamentos de JavaScript - 2017](https://platzi.com/clases/1099-fundamentos-javascript-2017/6618-escribiendo-el-codigo-de-nuestro-paquete6356/).  
		que es la versión anterior del que esta actualmente.

* **Yezid Garcia Medina** (2) [55610](https://platzi.com/comentario/543124/) 
Alguien sabe como hacer que en Visual Code se coloree las comillas invertidas ` y la instruccion ${ } , asi como le aparece a Leonidas ? ...

	* **Angel Hernandez** [55610] (2)

		Podrías instalar una extensión llamada Prettier, es muy buena coloreando caracteres y ordenando tu código 😄

* **carlosextra1** (1) [1115693](https://platzi.com/comentario/1115693/) 

	Excelente! y como siempre debugger haciendo bien su chamba! una de las funciones de JS que más se usan en el día a día!

* **33andres33** (1) [1076759](https://platzi.com/comentario/1076759/) 

	increible!

* **Kenet Chungandro** (1) [989997](https://platzi.com/comentario/989997/) 

	Esta genial! 😄

* **Kenet Chungandro** (1) [989973](https://platzi.com/comentario/989973/) 

	```    <div class="primaryPlaylistItem">
	                    <divclass="primaryPlaylistItem-image">
	                      <imgsrc="src/images/covers/midnight.jpg"alt="">
	                    </div>
	                    <h4class="primaryPlaylistItem-title">
	                      Titulo
	                    </h4>
	      </div>
	    
	```

* **urieelmm** (1) [838631](https://platzi.com/comentario/838631/) 

	Excelente curso para aprender a consumir APIS que es justo lo que necesitaba

* **Alejo Fonseca** (1) [688560](https://platzi.com/comentario/688560/) 

	Las plantillas de cadena de texto se delimitan con el carácter de comillas o tildes invertidas (``) (grave accent) , en lugar de las comillas simples o dobles. Las plantillas de cadena de texto pueden contener marcadores, identificados por el signo de dólar, y envueltos en llaves **(${expresión})**. Las expresiones contenidas en los marcadores, junto con el texto entre ellas, son enviados como argumentos a una función. La función por defecto simplemente concatena las partes para formar una única cadena de texto. Si hay una expresión antes de la plantilla de cadena de texto **(i.e. tag)** , llamamos a esta plantilla de cadena de texto “plantilla de cadena de texto con etiqueta”. En este caso, la expresión de etiqueta (típicamente una función) es llamada a partir de la cadena resultante de procesar la plantilla de cadena de texto, que luego puede ser manipulada antes de ser devuelta. En caso de querer escapar una comilla o tilde invertida en una plantilla de cadena de texto, pon un **backslash** / antes de la comilla o tilde invertida.

* **Gustavo83** (1) [628062](https://platzi.com/comentario/628062/) 

	primera vez que hago un debugger…Gracias Leonidas!

* **Gustavo83** (1) [628050](https://platzi.com/comentario/628050/) 

	Alguien maneja como mover un bloque de código arriba o abajo sin Ctrl+C / Ctrl+V tal como lo hace Leonidas en VSCode

	* **Matias Nicolas Caballero Gomez** [628050] (2)

		Con el ALT.  
		Haces click en el renglon que queres mover, en caso de ser varios los seleccionas todos con el mouse y luego mantene ALT y move con las flechas para arriba o abajo.

	* **Matias Nicolas Caballero Gomez** [628050] (1)

		También podes llevarte un renglón entero, haciendo click en cualquier parte del renglon, recortarlo con Ctrl+X y pegarlo en donde quieras.

	* **Juan Sebastián Joya Rodríguez** [628050] (1)

		En atom lo haces mantenineod oprimido Ctrl y con las flechas

	* **Gustavo83** [628050] (1)

		Gracias muchachos! Sirvió la info!

	* **Nadir Antonio Soza Solis** [628050] (1)

		Te recomiendo ver la documentación de vscode [](https://code.visualstudio.com/docs/getstarted/keybindings#_default-keyboard-shortcuts)

	* **Nadir Antonio Soza Solis** [628050] (2)

		aqui el link [visualstudio.com/docs/](https://code.visualstudio.com/docs/getstarted/keybindings#_default-keyboard-shortcuts)

* **Aaron Felipe Isaacs** (1) [625957](https://platzi.com/comentario/625957/) 

	Genial!..
	```     const videoItemTemplate = ({ medium_cover_image, title }) => {
	      return (
	      `<div class="primaryPlaylistItem">
	        <divclass="primaryPlaylistItem-image">
	          <imgsrc="${medium_cover_image}">
	        </div>
	        <h4class="primaryPlaylistItem-title">
	          ${title}
	        </h4>
	      </div>`
	      )
	    }
	    
	```

	* **ilan.vivanco** [625957] (1)

		Gracias!

* **jesus-gallardo** (1) [602694](https://platzi.com/comentario/602694/) 

	Recuerden que en el caso de creación de templates, tambien podemos usar las comillas inversas en jQuery, ese es precisamente el punto, que jQuery no aporta nada a esta funcionalidad, sino que es 100% nativo.

	* **Juan Sebastián Joya Rodríguez** [602694] (2)

		lo que pasa es que la comilla inversa es una funcionalidad de ECS6, jQuery te apoya cunado tu quieres meter ese template dentro del dom, puedes escribr algo como
		
		$("#contenedor").html(template)

* **Nicolas Esteban Prieto Sarmiento** (1) [586487](https://platzi.com/comentario/586487/) 

	Alguien me puede explicar en otras palabras como funciona forEach?

	* **ebar0n (Platzi)** [586487] (4)

		Hola, **forEach** en todos los lenguajes programacion es una extencion de un for tradicional, con la variante de que no es necesario manejar indices, sino, la misma funcion itera de forma dinamica y va devolviendo solo el objeto en cada paso del ciclo, sin complicarse con manejo de indices para acceder al arreglo y demas.

	* **Nicolas Esteban Prieto Sarmiento** [586487] (1)

		WOW! gracias  
		en otras palabras es un for automático dependiendo de los objetos?

	* **alexabellerman** [586487] (2)

		nombreArray.forEach(( nombreOpcional ) => {  
		console.log(nombreOpcional);  
		});
		
		Se podria leer como “a este array con nombre NombreArray, haceme un console.log de cada item que lo compone (nombreOpcional, que como su nombre lo dice, es opcional: podrias poner item, index, i, etc)”.

* **Yezid Garcia Medina** (1) [543124](https://platzi.com/comentario/543124/) 

	Alguien sabe como hacer que en Visual Code se coloree las comillas invertidas ` y la instruccion ${ } , asi como le aparece a Leonidas ? pues en mi caso, todo ese párrafo sale del mismo color y es difícil identificar esos caracteres ???

	* **Angel Hernandez** [543124] (2)

		Podrías instalar una extensión llamada Prettier, es muy buena coloreando caracteres y ordenando tu código 😄

	* **Yezid Garcia Medina** [543124] (1)

		gracias, voy a probarla

	* **German Tellez Vanegas** [543124] (1)

		tienes que instalar el tema “Seti Monokai”

* **Félix Alejandro Zelaya Orellana** (1) [526885](https://platzi.com/comentario/526885/) 

	Genial!!

* **JhonColorado07** (1) [483687](https://platzi.com/comentario/483687/) 

	Me gusta mucho como se puede manejar un template con javascript.

* **dcortesnet** (1) [435527](https://platzi.com/comentario/435527/) 

	Al crear elementos de html con templates se comportan como uno como tal?  
	es equivalente a hacer document.createElement(’ Elemento’) ??

	* **Edward Steven Ramos Palacios** [435527] (1)

		En teoria si puedes hacerlo dado que el template finalmente es una etiqueta `script` al que le asignas un tipo.  
		Entonces para crearlo seria  
		`document.createElement('script')`  
		y le vas agregando los atributos que necesites.  
		Solo debes ver si realmente es práctico ya que hoy en dia tenemos mejores herramientas para ello, como React y vue.

* **wlm.morant** (1) [84383](https://platzi.com/comentario/1049788/) 
Que es un template ,la pagina entera es un template y por que el titulo de la clase es templates si solo estamos sobre una template(pagin...

	* **María José Ledesma (Platzi)** [84383] (1)

		¡Hola _wlm.morant_!  
		jQuery nos proporciona muchas funcionalidades para poder potencializar el uso de JavaScript, como poder crear templates con un plugin llamado **jquery template** , que esta basado en la idea de _motores de plantillas_ o _template engine_.
		
		**¡Nunca pares de aprender!** (Y practicar) 🦄

* **wlm.morant** (1) [84343](https://platzi.com/comentario/1048908/) 
porque usas const HTMLString , cuando el contenido de esa variable cambia o es el inicio del scoop cada vez que inicia la funcion lo que ...

* **diego rodriguez** (1) [82934](https://platzi.com/comentario/1015602/) 
que significa cuando el dice iterar las constantes de las listas?

	* **Manuel Rivera** [82934] (2)

		Hola bro, pasar por cada una de ellas, saludos ;😄

* **arlex_ee10** (1) [81593](https://platzi.com/comentario/984792/) 
con este codigo `` console.log(actionList, dramaList, animationList);``` Imprimimos los objetos. en la consola y toda la información de l...

* **Mxnboy** (1) [81014](https://platzi.com/comentario/971509/) 
Esa parte del código no viene dentro del repositorio, supongo que el teacher lo agregó para la clase, me toca practicarlo

* **Ang.O** (1) [74692](https://platzi.com/comentario/857361/) 
como se cuando usar el $ y no solo escribir la direccion

	* **Gabriel De Andrade (Platzi)** [74692] (2)

		Hola! En este curso Leonidas usa el símbolo $ para denotar cuando el valor de una variable es un Elemento HTML, es decir cuando se está eligiendo un elemento que pertenece al DOM y no es una variable con algún otro valor 😄

* **Ang.O** (1) [74691](https://platzi.com/comentario/857353/) 
para que era el + cuando paso el html al archivo jscript?

	* **Gabriel De Andrade (Platzi)** [74691] (1)

		Hola era para combinar diferentes strings, que en este caso contienen código html, en uno solo, lo puedes hacer en una sola línea pero sería muy difícil de leer y por ende de editar 😄

* **German Tellez Vanegas** (1) [68836](https://platzi.com/comentario/747042/) 
Se supone que debi ver algun video de Javascript antes de este curso? Cual me recomiendan?

	* **Juan David Castro (Platzi)** [68836] (2)

		¡Hola! Veo que ya tomaste el [Curso de Fundamentos de JavaScript](https://platzi.com/clases/fundamentos-javascript/). Ese sería el curso que te recomendaría tomar antes. 😉

* **Nicolas Esteban Prieto Sarmiento** (1) [59076](https://platzi.com/comentario/586487/) 
Alguien me puede explicar en otras palabras como funciona forEach?

	* **ebar0n (Platzi)** [59076] (4)

		Hola, **forEach** en todos los lenguajes programacion es una extencion de un for tradicional, con la variante de que no es necesario manejar indices, sino, la misma funcion itera de forma dinamica y va devolviendo solo el objeto en cada paso del ciclo, sin complicarse con manejo de indices para acceder al arreglo y demas.

* **evertescalante1** (0) [400618](https://platzi.com/comentario/400618/) 

	Hola, Alguien sabe como solucionar este error
	
	No ‘Access-Control-Allow-Origin’ header is present on the requested resource. Origin ‘null’ is therefore not allowed access. The response had HTTP status code 404. If an opaque response serves your needs, set the request’s mode to ‘no-cors’ to fetch the resource with CORS disabled.

	* **haazmx** [400618] (2)

		Checa este artículo quizás te pueda servir  
		<https://crunchify.com/how-to-fix-access-control-allow-origin-issue-for-your-https-enabled-wordpress-site-and-maxcdn/>

	* **deveeup** [400618] (3)

		Hola! La razón por la cual ella fetch se ve afectado es que el navegador impide que el código acceda a la respuestas de l a que proviene (url), esto se debe a que la respuesta carece de Access-Control-Origin.
		
		Te adjunto el ejemplo, pero con un proxy CORS:
		```     (asyncfunctionload() {
		      const proxyurl = "https://cors-anywhere.herokuapp.com/";
		      asyncfunctiongetData(url) {
		        const response = await fetch(proxyurl + url);
		        const data = await response.json();
		        return data;
		      }
		    
		      const actionList = await getData(
		        "https://yts.am/api/v2/list_movies.json?genre=action"
		      );
		      console.log(actionList);
		    })();
		    ```?
		    
		    
		    
		    
		```

## 0100. Creación de DOM [12302](https://platzi.com/clases/1316-jquery-js/12302-creacion-de-dom/)

### Descripción:


La plantilla que creamos la clase anterior de momento es puro texto, no es un elemento HTML que podamos poner dentro del navegador pues si los imprimimos en el navegador lo único que veremos es texto.  
Vamos a insertar la plantilla dentro de nuestro container, para ello recuerda que JavaScript se lee de arriba hacia abajo entonces debemos declarar la variable del container antes de llamar a algún método de este.  
Para convertir nuestra plantilla de texto a un Document Object Model necesitamos crear dentro de memoria un documento HTML, esto es posible gracias al método document.implementation.createHTMLDocument. A este documento HTML le vamos a añadir al body, mediante innerHTML, nuestra plantilla de texto. Una vez añadida le pedimos al body el primer elemento hijo que tenga y este lo añadimos a nuestro container.  
Este flujo es la magia que hay detrás de varias librerías y frameworks que nos ayudan a crear interfaces.

### Comentarios:

* **oteka21** (31) [339964](https://platzi.com/comentario/339964/) 

	Al insertar nuestras películas al contenedor de películas de acción creo que podríamos evitarnos unas lineas de código haciendolo de esta manera.
	```     actionList.data.movies.forEach((movie) =>{
	    		constHTMLString = videoItemTemplate(movie);
	    		$actionContainer.innerHTML += HTMLString;
	    });
	    
	```
	
	¿Existe alguna ventaja al hacerlo de la forma que explica el profe Leonidas o al final solo depende del estilo de cada programador?

	* **Juan David Castro (Platzi)** [339964] (4)

		Ya que ambas soluciones funcionan, depende de tu estilo 👍.

	* **Sergio Minei** [339964] (2)

		es solo cuestión de preferencias y gustos.

	* **Alan Santiago** [339964] (3)

		Yo prefiero tu metodo Jajaja 😄

	* **José Roberto Diaz Rodriguez** [339964] (8)

		Este metodo esta incompleto, porque innerHTML se encarga de sobreescribir el HTML del elemento en cuestion, lo que pasaria con estas instrucciones que pones es que solo se veria 1 pelicula al final que seria la ultima.  
		Si en cambio lo defines asi funcionaria (como yo lo hice):
		```     let stringHTML = ""
		      accion.forEach((movie) => {
		        constHTMLString = videoItemTemplate(movie);
		      	stringHTML += constHTMLString
		      })
		      $actionContainer.innerHTML = stringHTML
		    
		```

	* **Edward Steven Ramos Palacios** [339964] (3)

		@jrdiaz Depende de gustos. Pero igual la respuesta de @oteka21 es mas limpia. Usando el prefijo + antes del igual obtienes la concatenación.-

	* **lantog** [339964] (1)

		Que funcionen igual no significa que uno no sea mejor que otro, no soy un fan de JS pero por sentido común creo que tu solución es mejor, ya que con la del profe (quizas más legible para algunos) creas un documento HTML por cada movie que hay, lo que lo debe hacer más lento o por lo menos a mí me funciona mejor con tú método

	* **Giancarlo Vera** [339964] (3)

		Leonidas nos muestra otra forma de insertar html en el DOM. Prefiero el método de concatenación con innerHTML, es más legible y menos código.

	* **jesus-gallardo** [339964] (1)

		Tecnicamente son lo mismo, asi que enfoca tu decición en la siguiente pregunta.
		
		¿Cuál es la forma que se ve mas legible?

	* **Camilo Sosa** [339964] (2)
Igual puede ser por seguridad ya que si se mete data directo en innerHTML puedes ser blanco de ataques XSS y con el método que uso Leonidas primero ese texto lo convierte y después se agrega con el append que es más seguro al elemento

	* **David Alejandro Mosquera Moreno** [339964] (1)

		Las dos son excelentes formas, según el estilo de cada desarrollador. Aunque la que propones es más limpia y pude observar que el gif de carga desaparecía cuando utilicé la segunda forma de . Esto daba un mejor aspecto al cargar las películas.

	* **Luis Rangel Castro** [339964] (1)

		```    function videoItemTemplate(src, title) {
		      return (
		        `<div class="primaryPlaylistItem">
		          <div class="primaryPlaylistItem-image">
		            <img src="${src}">
		          </div>
		          <h4 class="primaryPlaylistItem-title">
		            ${title}
		          </h4>
		        </div>`
		      )
		    }
		    
		    async function loadMovie(type) {
		      try {
		        const res = await fetch('https://yts.lt/api/v2/list_movies.json?genre=' + type);
		        const data = await res.json();
		        const $movieContainer = document.getElementById(type);
		    
		        let stringHTML = "";
		        data.data.movies.forEach((movie) => {
		          const HTMLString = videoItemTemplate(movie.medium_cover_image, movie.title);
		          stringHTML += HTMLString;
		        });
		        $movieContainer.innerHTML = stringHTML;
		    
		      }
		      catch (err) {
		        console.log('error al ejectura loadMovie', err);
		      }
		    }
		    
		    loadMovie('drama');
		    loadMovie('action');
		    loadMovie('animation');
		    
		```

* **Sergio Minei** (19) [335721](https://platzi.com/comentario/335721/) 

	## Usando Templates
	
	La plantilla no puede puede ser llamada de frente puesto que en el html se mostraría como texto. Primero se hace una transformación de la plantilla para recién agregarla al contenedor que se desee.
	```     functiontitleTemplate(title) {
	      return (
	        `<h1>${title}</h1>`
	      )
	    }
	    
	    //se trae la plantilla y se guarda en una variable.
	    const HTMLString = titleTemplate(movie);
	    //se crea un documento html vacío
	    const html = document.implementation.createHTMLDocument();
	    //se agrega la plantilla al innerHTML del documento html 
	    //esto hace que la plantilla en texto se convierta a elementos DOM
	    html.body.innerHTML = HTMLString;
	    //se agrega el primer hijo (que es donde se encuentra la plantilla) al contenedor donde se quiere agregar la plantilla
	    $actionContainer.append(html.body.children[0]);
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

* **JuanDAC** (9) [468731](https://platzi.com/comentario/468731/) 

	Muchos se pregunta por qué Leónidas Esteban creo un Dom desde Javascript vanilla y ahí inserto los elementos html para posteriormente acceder a este domingo y copiar el contenido de este en el elemento del Dom visible.  
	En ves de insertar directamente en el elemento del Dom visible.  
	La respuesta a esto el no las al final de la clase, Leónidas Esteban nos comenta que esto funciona semejante a react el cual crea un Dom virtual, sólo que react hace más cosas con dicho Dom virtual sería más complicado de imitar en una sola clase.

	* **pablojorgeandres** [468731] (2)

		Gracias, muy útil tu comentario.  
		Saludos.

	* **Nathaly Stefani Riaño Bejarano** [468731] (2)

		Excelente aclaración!

* **Joel Alejandro Morales García** (9) [359621](https://platzi.com/comentario/359621/) 

	React sin react :😀

* **ilan.vivanco** (5) [678356](https://platzi.com/comentario/678356/) 

	No sería mucho más simple usar un
	```     $actionContainer.innerHTML += html.body.children[0];
	    
	```
	
	De este modo no tendráimos que crear el elemento HTML y pasarle el string. Hay alguna desventaja en hacerlo de este modo? O ventajas usando el append() en su defecto??

	* **Daniel Esteves** [678356] (1)

		Hola Ilan, de hecho las dos opciones están bien pero la tuya es mucho mejor y más condensada, felicidades 💪

* **konstantino91** (5) [441950](https://platzi.com/comentario/441950/) 

	El método append no es único de JQuery? o también lo tiene JS Vanilla

	* **oteka21** [441950] (5)

		En javascript puedes utilizar dos métodos
		```      appendChild(que_vas_a_insertar)
		    //insertBefore recibe dos parametros
		    insertBefore(que_vas_a_insertar, antes_de_que_elemento_lo_vas_a_insertar)
		    
		```
		
		te dejo la documentación de cada uno:  
		[appendChild](https://www.w3schools.com/jsref/met_node_appendchild.asp)  
		[insertBefore](https://www.w3schools.com/jsref/met_node_insertbefore.asp)

	* **noemk2** [441950] (1)

		Tmb tenia esa misma duda, creo q eso falto explicar…

	* **konstantino91** [441950] (2)

		Muchas gracias compañero!

	* **Camilo Sosa** [441950] (1)
Así es js es su nueva versión también lo tiene, así como el prepend, after, before

* **ivan-rodriguez** (4) [446802](https://platzi.com/comentario/446802/) 

	Error: uncaught (in promise) type error cannot read property 'append' of null. el cual se me da por: document.getElementById('#action') no se utiliza el '#' correcta declaracion: document.getElementById('action');

* **pablojorgeandres** (3) [485022](https://platzi.com/comentario/485022/) 

	## Creación de DOM
	
	Para imprimir nuestro código del template en el documento primero lo transformamos en código HTML, de otra manera se imprimiría sólo texto y se mostraría el código como texto en nuestra página.
	
	Para esto primero creamos un elemento HTML que nos arroja un documento html básico(esta parte del proceso es sobre todo para mostrar el mecanismo de algunos frames y librerías):
	```     	const html = document.implementation.createHTMLDocument()
	    	
	    	// #document
	    	// <!doctype html>
	    	//   <head>
	    	//   </head>
	    	//   <body>
	    	//   </body>
	    	// </html>
	    
	    
	```
	
	Y con el método _.innerHTML_ genero el código a partir de nuestro string estructurado:
	```     	const html = document.implementation.createHTMLDocument()
	    	html.body.innerHTML = HTMLString
	    
	    
	```
	
	Y ahora sí podemos insertar este código generado en nuestro documento con el método _.children_ :
	```     	$actionContainer.append(html.body.children[0])
	    
	    
	```
	
	Este flujo se parece mucho a lo que hacemos con librerías o frameworks de JavaScrip; lo que haría backBone, Angular, Vue, React. Con estos sólo pasamos unas propiedades, declaramos unos componentes que sería lo más parecido a nuestro template, y luego de eso se imprime en nuestra página. También nos dan características especiales; como por ejemplo si actualizamos alguna propiedad, esta se autoactualiza dentro de ese elemento, dentro de ese HTML sin tener que hacerlo a mano.  
	Con este proceso hemos aprendido la forma de hacerlo con Vanilla JS para poder comprender todo el background detras de estas librerías y frames.
	
	Hay otras formas de hacer este proceso en Vanilla JS.  
	Notese por ejemplo que el método _.append_ es nativo de jQuery y no de JS. Con Vanilla JS usaríamos _.appendChild()_ por ejemplo.

* **Yassr** (3) [364429](https://platzi.com/comentario/364429/) 

	Tengo una duda en lo siguiente, por qué crear un nuevo documento HTML con
	```     const html = document.implementation.createHTMLDocument();
	    
	```
	
	si puede imprimirse directamente en
	```     $actionContainer
	    
	```
	
	Es como una buena práctica, debe hacerse cada vez que se imprime algo ? \
	
	Gracias!

	* **Leonidas Esteban Gonzalez** [364429] (13)

		No hay lío en como quieras hacerlo, la diferencia de hacer esto en una función aparte es la capacidad que reutilización

	* **evertescalante1** [364429] (0)

		Que bien esto me aclara esa duda!

	* **pablojorgeandres** [364429] (1)

		Sí, muy buena aclaración.  
		Gracias @Yassr por tu consulta y gracias @Leonidas por tu respuesta.  
		Saludos.

	* **Isaias Chávez** [364429] (1)

		Reutilización. ahora queda más claro.

* **ivan guadaña quiroz** (3) [335033](https://platzi.com/comentario/335033/) 

	no se puedo ver lo que se hizo en el código

	* **Diego Alexander Forero Higuera (Platzi)** [335033] (3)

		Hola acabo de ver la clase completa y siempre se muestra el código cuando Leonidas esta escribiendo, puedes mencionar en que parte a la que te refieres.

* **konstantino91** (3) [47372](https://platzi.com/comentario/441950/) 
El método append no es único de JQuery? o también lo tiene JS Vanilla

	* **oteka21** [47372] (5)

		En javascript puedes utilizar dos métodos
		```      appendChild(que_vas_a_insertar)
		    //insertBefore recibe dos parametros
		    insertBefore(que_vas_a_insertar, antes_de_que_elemento_lo_vas_a_insertar)
		    
		```
		
		te dejo la documentación de cada uno:  
		[appendChild](https://www.w3schools.com/jsref/met_node_appendchild.asp)  
		[insertBefore](https://www.w3schools.com/jsref/met_node_insertbefore.asp)

* **johan.n** (2) [1053887](https://platzi.com/comentario/1053887/) 

	Creo que no esta funcionando el API

	* **unabase** [1053887] (1)

		desactiva el bloqueo de cors

	* **joaquin-fontela** [1053887] (1)

		La API ahora comienza con [yts.mx/](http://yts.mx/)…, ya no con [yts.am/](http://yts.am/)…

	* **carlosextra1** [1053887] (1)

		A mi hoy 09 de abril 2020 me esta funcionando a la perfección!

* **Kenet Chungandro** (2) [990256](https://platzi.com/comentario/990256/) 

	Esta genial! 😃

* **edwintrumpet** (2) [605500](https://platzi.com/comentario/605500/) 

	¿Por qué no se insertó el template en el navegador con **innerHTML** en lugar de con el **append**?  
	Yo lo probé usando el **innerHTML** directamente en el navegador sin crear el DOM virtual que se creó para insertarlo y funcionó.

	* **Miguel Angel Morales Larriega** [605500] (1)

		@juanDAC responde a ésto unos comentarios más abajo, es bueno leer comentarios de la comunidad, a veces pueden ser muy útiles.

	* **Daniel Esteves** [605500] (2)

		Hola Edwin, lo que pasa es que hay diferentes maneras de hacerlo, en la clase el profesor usa el `append()` pero es una **muy** buena alternativa usea el innerHTML

* **Cristian Orozco Benjumea** (2) [544233](https://platzi.com/comentario/544233/) 

	Hemos hecho 3 pasos para incluír la lista de películas dentro del navegador:
	
	    1. Convertimos nuestros datos a un elemento iterable gracias a la función `forEach`
	```     actionList.data.movies.forEach((movie) => {
	    
	```
	
	    1. Hemos convertido a un template y luego a un HTML
	```     const HTMLString = videoItemTemplate(movie);
	    const html = document.implementation.createHTMLDocument();
	    
	```
	
	    1. Por último se pone dentro del navegador.
	```     html.body.innerHTML = HTMLString;
	    
	```

* **Cristian Orozco Benjumea** (2) [544153](https://platzi.com/comentario/544153/) 

	**htmlizado:** 😄  
	Yo htmlizo  
	Tú htmlizas  
	Vosotros htmlizaís.

* **alexanderquispequino** (2) [457974](https://platzi.com/comentario/457974/) 

	Porque append() ? no se trataba de JQuery a Javascript puro?

	* **Daniel Esteves** [457974] (1)

		Hola Alexander, de hecho `appendChild()` es nativo de JavaScript no exclusivo de jQuery, puedes ver un poco más aquí <https://www.w3schools.com/jsref/met_node_appendchild.asp>

* **odpinerosh** (2) [395475](https://platzi.com/comentario/395475/) 

	¿Por qué tiene que hacer un archivo HTML completo para el ejemplo? ¿Acaso no hay ya uno funcionando donde se muestra el título Platzi Video y las películas hacen parte de ese HTML?

	* **Diego Alexander Forero Higuera (Platzi)** [395475] (5)

		Se hace con fines didácticos para enseñar como poder manipular y crear un DOM completo usando JavaScript puro.

* **Efrén Sánchez** (2) [395140](https://platzi.com/comentario/395140/) 

	Uff me está constando muchismo entender todo ésto de JavaScript 😭 … Y ésto es lo más básico?

	* **davidtoca (Platzi)** [395140] (2)

		puedes probar ver el curso de programación básica su estás muy perdido

	* **Efrén Sánchez** [395140] (3)

		Creo que tenía que haber empezadod con Fundamentos de JavaScript… :-s

	* **zalotesta** [395140] (1)

		Amí me pasó algo parecido pero decidí hacer ambos cursos en paralelo y va mejor 😉

* **hdmjesus** (2) [83652](https://platzi.com/comentario/1031494/) 
En este punto justamente no entiendo muy bien, Leionidas crea la const html, en cual crea por asi decirlo una estructura html , a la cual...

* **noemk2** (2) [47226](https://platzi.com/comentario/440387/) 
min 1:30 , el metodo .append() sirve tmb para Js Vanilla?

	* **Eduardo Hidalgo Díaz Rugama** [47226] (2)

		.append() es un método de JQuery. Para Javascript puedes hacerlo manualmente haciendo una concatenación
		```     vardiv = document.getElementById('divID');
		    
		    div.innerHTML += 'Extra stuff';
		    
		```
		
		o puedes usar una función de vanilla que se usa para agregar a un elemento padre más elementos como hijos
		```     var node= document.getElementById("myList2").lastChild;
		    document.getElementById("myList1").appendChild(node);
		    
		```

* **Sara Galván Ortega** (1) [1104090](https://platzi.com/comentario/1104090/) 

	Excelente la clase!! como siempre Leonidas excelente profesor!!

	* **carlosextra1** [1104090] (1)

		Es correcto! muy buen profesor!

* **danielmorales917** (1) [1091162](https://platzi.com/comentario/1091162/) 

	Buenisima clase, no sabia que eso se podía hacer solo con JS

* **wlm.morant** (1) [1050227](https://platzi.com/comentario/1050227/) 

	Este flujo parece como trabaja React y a}Angular

	* **carlosextra1** [1050227] (1)

		No conozco react pero si es muy similar a angular

* **Rodrigo Cuenca Mayuri** (1) [1032772](https://platzi.com/comentario/1032772/) 

	Hola! quería compartirles una forma de resumir el código.
	```       actionList.data.movies.forEach((movie)=>{
	        const HTMLString=videoItemTemplate(movie)
	        $actionContainer.innerHTML += `${HTMLString}`
	        console.log(HTMLString)
	      })
	    
	```

	* **Rodrigo Cuenca Mayuri** [1032772] (1)

		la parte de${HTMLString} puede llamarse solo con el nombre de la variable: HTMLSTring

* **hdmjesus** (1) [1031491](https://platzi.com/comentario/1031491/) 

	duda

* **Andres Condo** (1) [1021293](https://platzi.com/comentario/1021293/) 

	Alguien sabe porque en algunas partes usa el **querySelector** , y en otras el **getElementById();**.  
	ahora que estaba usando el el segundo mencionado en todo el código en ves del querySelector, y no me funcionaba el mostrar en pantalla la cartelera. E investigando encontré que el **append** se usa cuando se usa el jquery. Pensé que este curso seria una forma de usar 100% de vanila JS.

	* **andresazuara98** [1021293] (1)

		I guess you can replace the append with push

* **Eleazar Carreón Álvarez** (1) [999045](https://platzi.com/comentario/999045/) 

	Cuando hacia mis propias practicas, siempre el document html me lo volvía a repintar todo a pesar que solo quería modificar una parte.

* **lmoran** (1) [995684](https://platzi.com/comentario/995684/) 

	Todo muy claro!

* **arlex_ee10** (1) [984804](https://platzi.com/comentario/984804/) 

	:V jajaj que chido man, Buena clase

* **Kivindlg** (1) [957146](https://platzi.com/comentario/957146/) 

	ya quiero ser programador .

* **dipacata** (1) [696297](https://platzi.com/comentario/696297/) 

	como haríamos para borrar el gif de carga ?

	* **Enrique Felix Garcia Stave** [696297] (2)

		Si te fijas en el documento index.html, hay una etiqueta de imagen con el gif de carga, tan solo tendrias que borrarlo:  
		![](https://scontent.fctg1-1.fna.fbcdn.net/v/t1.15752-9/70259107_2380698292143497_7749311605573156864_n.png?_nc_cat=104&_nc_oc=AQk3Ec-s4hMLnBq1o63ySHVD6RvASIx0YUByprvq6fEjpjm-4PXPUY3xnxfPw91pLDY&_nc_ht=scontent.fctg1-1.fna&oh=bdc20f4692389f0be7474c3c2fe3b13b&oe=5DFC8E1C)

	* **Jonathan Uriel Jiménez Soveranes** [696297] (4)

		colocas un id al loader
		```     <img src="src/images/loader.gif"width="50" height="50" alt="" **id="loaderaction"**>
		    
		```
		
		lo buscas
		```     const $gifaction = document.getElementById('loaderaction');
		    
		```
		
		y por último, lo escondes
		```     actionlist.data.movies.forEach((movie) => {
		            **$gifaction.hidden = true;**
		            const HTMLString = videItemTemplate(movie);
		            const html = document.implementation.createHTMLDocument(); //Crar un odcumento HTML
		            html.body.innerHTML = HTMLString;
		            $actionContainer.append(html.body.children[0]);
		            
		            console.log(HTMLString);
		        })
		    
		```

	* **Kenet Chungandro** [696297] (1)

		Coloca esto antes del forEach:
		```     $actionContainer.innerHTML='';
		    
		```
		
		lo que hace es vaciar el div antes de cargar los datos de la Api.

	* **Kenet Chungandro** [696297] (1)

		quedaría así:
		```     $actionContainer.innerHTML='';
		       actionList.data.movies.forEach((movie)=>{
		         const HTMLString = videoItemTemplate(movie);
		         const html = document.implementation.createHTMLDocument();
		         html.body.innerHTML=HTMLString;
		         
		         $actionContainer.append(html.body.children[0]);
		    
		```

* **sjimen13** (1) [691637](https://platzi.com/comentario/691637/) 

	Que buena clase !!!

* **Camilo Andrés Santana Lizcano** (1) [687229](https://platzi.com/comentario/687229/) 

	Wow !  
	Tema que merece ser entendido a un 100%

* **alexmilano** (1) [634798](https://platzi.com/comentario/634798/) 

	Excelente Leonidas! Una de las mejores clases que he visto en la plataforma.

* **Gustavo83** (1) [628157](https://platzi.com/comentario/628157/) 

	es como desglosar un proceso que ya hace automáticamente lso frameworks como React, Angular, Vue etc. lo que sucede tras bambalinas

* **Cristhian Julian Gómez Lizcano** (1) [583886](https://platzi.com/comentario/583886/) 

	Me encanta como Platzi enseña paso a paso estos procesos desde su raíz!!.. sé que existe react , etc… pero nada mejor que entender todo desde su mas mínima expresión!!!

* **Isaias Chávez** (1) [549061](https://platzi.com/comentario/549061/) 

	Esta cláse la siénto cómo mi primer hola mundo en las peticiones asíncronas 😄

* **Isaias Chávez** (1) [549056](https://platzi.com/comentario/549056/) 

	¡Qué emoción !!!

* **Cristian Orozco Benjumea** (1) [544232](https://platzi.com/comentario/544232/) 

	Hemos hecho 3 pasos para incluír la lista de películas dentro del navegador:
	
	    1. Convertimos nuestros datos a un elemento iterable gracias a la función `forEach`
	```     actionList.data.movies.forEach((movie) => {
	    
	```
	
	    1. Hemos convertido a un template y luego a un HTML
	```     const HTMLString = videoItemTemplate(movie);
	    const html = document.implementation.createHTMLDocument();
	    
	    
	```
	
	    1. Por último se pone dentro del navegador.
	```     html.body.innerHTML = HTMLString;
	    
	```

* **Félix Alejandro Zelaya Orellana** (1) [529120](https://platzi.com/comentario/529120/) 

	Genial!!

* **noemk2** (1) [440387](https://platzi.com/comentario/440387/) 

	min 1:30 , el metodo .append() sirve tmb para Js Vanilla?

	* **Eduardo Hidalgo Díaz Rugama** [440387] (2)

		.append() es un método de JQuery. Para Javascript puedes hacerlo manualmente haciendo una concatenación
		```     vardiv = document.getElementById('divID');
		    
		    div.innerHTML += 'Extra stuff';
		    
		```
		
		o puedes usar una función de vanilla que se usa para agregar a un elemento padre más elementos como hijos
		```     var node= document.getElementById("myList2").lastChild;
		    document.getElementById("myList1").appendChild(node);
		    
		```

	* **ilan.vivanco** [440387] (2)

		`.append()` es un método de JS vainilla también. <https://developer.mozilla.org/en-US/docs/Web/API/ParentNode/append>

* **JhonyBurbano08** (1) [422691](https://platzi.com/comentario/422691/) 

	```    const $actionContainer = document.getElementById('action');
	    
	      actionList.data.movies.forEach((movie) => {
	          const HTMLString = videoItemTempate(movie);
	          const html = document.implementation.createHTMLDocument();
	          html.body.innerHTML = HTMLString;
	          $actionContainer.append(html.body.children[0]);
	          //$actionContainer.innerHTML += HTMLString;
	      });
	    
	```

* **jlquichimbo** (1) [417872](https://platzi.com/comentario/417872/) 

	Resumen 3 pasos para enviar HTML a container.

* **ivan_acg** (1) [417154](https://platzi.com/comentario/417154/) 

	Por si a alguien mas le pasó , tengan cuidado al escribir
	```     const html =  document.implementation.createHTLMDocument();
	    
	```
	
	Y era :
	```     const html =  document.implementation.createHTMLDocument();
	    
	```
	
	notese que me equivoque en HTLM y era HT M L y por eso no me traía las peliculas

* **danielmorales917** (1) [86241](https://platzi.com/comentario/1091166/) 
Porque el content de las peliculas tiene un scroll inferior y no me deja mover las peliculas arrastrandolas como hace leonidas ?

	* **Juan David Castro (Platzi)** [86241] (1)

		¿Podrías mostrarnos tu código y un screenshotcito de tu navegador con el error que dices? Así sin verlos es muy difícil (casi imposible) saber con certeza cuál es el error y, más importante, cómo solucionarlo.

* **wlm.morant** (1) [84398](https://platzi.com/comentario/1050129/) 
porque me sale asi que solucion le doy

* **Ang.O** (1) [74741](https://platzi.com/comentario/858205/) 
para que es append, en $actioncontainer.append

	* **Erik Ochoa (Platzi)** [74741] (2)

		Agrega un nodo hijo al contenedor `$actioncontainer`, mira un ejemplo:
		```     var contenedorPadre = document.createElement("div");
		    var parrafo = document.createElement("p");
		    contenedorPadre.append(parrafo); // agrega la etiqueta <p> al <div>
		    
		    console.log(contenedorPadre.childNodes); // NodeList [ <p> ]
		    
		```
		
		Aquí te dejo la [documentación](https://developer.mozilla.org/es/docs/Web/API/ParentNode/append).

* **Ang.O** (1) [74740](https://platzi.com/comentario/858199/) 
6:00 html.body.innerhtml lo podria poner en cualquiero orden o debe estar abajo del document.implementation.createhtmldocument();?

	* **Erik Ochoa (Platzi)** [74740] (1)

		En este caso debe ir después porque el objeto `html` tiene que estar creado antes de poder tomar su propiedad `body`. `html` se crea al momento de asignar el resultado del método `createHTMLDocument()`.
		```     	const html = document.implementation.createHTMLDocument();
		    
		```

* **Sebastián Pineda Duque** (1) [64784](https://platzi.com/comentario/681844/) 
¿Esto mismo se podría hacer con un template engine?

	* **Manuel Ojeda** [64784] (1)

		Diga característica no te será de ayuda, pues el template de Pug se compila y se añade desde JavaScript.  
		Al ejecuta una función para hacer la inserción DOM no se interpretará de la manera deseada, por lo que tendrás que usar en el template literal el HTML normal.

* **odpinerosh** (1) [43271](https://platzi.com/comentario/395475/) 
¿Por qué tiene que hacer un archivo HTML completo para el ejemplo? ¿Acaso no hay ya uno funcionando donde se muestra el título Platzi Vid...

	* **Diego Alexander Forero Higuera (Platzi)** [43271] (5)

		Se hace con fines didácticos para enseñar como poder manipular y crear un DOM completo usando JavaScript puro.

* **Yassr** (1) [40334](https://platzi.com/comentario/364429/) 
Tengo una duda en lo siguiente, por qué crear un nuevo documento HTML con const html = document.implementation.createHTMLDocument(...

	* **Leonidas Esteban Gonzalez** [40334] (13)

		No hay lío en como quieras hacerlo, la diferencia de hacer esto en una función aparte es la capacidad que reutilización

* **rodrigo-haiache** (0) [1020752](https://platzi.com/comentario/1020752/) 

	Actualmente hay un problema al hacer el fetch.Tengo exactamente el mismo codigo que Leonidas, asi que el error estara del lado de la api.
	
	Access to fetch at ‘<https://yts.am/api/v2/list_movies.json?genre=action>’ from origin ‘null’ has been blocked by CORS policy: No ‘Access-Control-Allow-Origin’ header is present on the requested resource. If an opaque response serves your needs, set the request’s mode to ‘no-cors’ to fetch the resource with CORS disabled.

	* **Andres Condo** [1020752] (3)

		Que tal buenas noches, el motivo por el que no coge tu código se debe a que actualmente el sitio tiene la extensión **([yts.mx](http://yts.mx) )**, solo corrige eso y tu código funcionara perfectamente

## 0110. Reutilizando funciones [12303](https://platzi.com/clases/1316-jquery-js/12303-reutilizando-funciones/)

### Descripción:


En esta clase vamos a modificar nuestro código que se encarga de renderizar la plantilla de video para volverlo una función que pueda ser reutilizable. Dicha función recibirá dos parámetros, uno para saber sobre qué lista de películas va a iterar y otro para saber en que contenedor va a renderizar las películas.

### Comentarios:

* **Sergio Minei** (19) [335763](https://platzi.com/comentario/335763/) 

	Este es el código de esta clase:
	```     functioncreateTemplate(HTMLString) {
	        const html = document.implementation.createHTMLDocument();
	        html.body.innerHTML = HTMLString;
	        return html.body.children[0];
	      }
	    
	      functionrenderMovieList(list, $container) {
	        $container.children[0].remove();
	        list.forEach((movie) => {
	          const HTMLString = videoItemTemplate(movie);
	          const movieElement = createTemplate(HTMLString);
	          $container.append(movieElement);
	        });
	      }
	    
	      renderMovieList(actionList.data.movies, $actionContainer);
	      renderMovieList(dramaList.data.movies, $dramaContainer);
	      renderMovieList(animationList.data.movies, $animationContainer);
	    
	```

	* **Oscar Barajas Tavares (Platzi)** [335763] (2)

		gracias.

	* **edesalla17** [335763] (2)

		genial!

	* **Luis Rangel Castro** [335763] (1)

		```    function videoItemTemplate(src, title) {
		      return (
		        `<div class="primaryPlaylistItem">
		          <div class="primaryPlaylistItem-image">
		            <img src="${src}">
		          </div>
		          <h4 class="primaryPlaylistItem-title">
		            ${title}
		          </h4>
		        </div>`
		      )
		    }
		    
		    function loadingImage() {
		      return (`<img src="src/images/loader.gif" width="50" height="50" alt=""></img>`);
		    }
		    
		    async function loadMovie(type) {
		      try {
		        const $movieContainer = document.getElementById(type);
		        $movieContainer.innerHTML = loadingImage();
		    
		        const res = await fetch('https://yts.lt/api/v2/list_movies.json?genre=' + type);
		        const data = await res.json();
		    
		        renderMovieList(data, $movieContainer);
		      }
		      catch (err) {
		        console.log('error al ejectura loadMovie', err);
		      }
		    }
		    
		    function renderMovieList(data, $container) {
		      let stringHTML = "";
		    
		      data.data.movies.forEach((movie) => {
		        const HTMLString = videoItemTemplate(movie.medium_cover_image, movie.title);
		        stringHTML += HTMLString;
		      });
		      $container.innerHTML = stringHTML;
		    }
		    
		    loadMovie('drama');
		    loadMovie('action');
		    loadMovie('animation');
		    
		```

* **samvarcia** (11) [337797](https://platzi.com/comentario/337797/) 

	Vamo a retornarlo : Leonidas Esteban 2018 😃

* **Uralberto** (8) [600163](https://platzi.com/comentario/600163/) 

	Tuve dificultad para entender a plenitud el contenido de esta clase. Me propuse no avanzar a la siguiente hasta no dominar con certeza como declarar y reutilizar una función. La clave: constancia, paciencia y respiración profunda.

* **José Ricardo Romo Gutiérrez** (6) [372644](https://platzi.com/comentario/372644/) 

	Tal vez se pueda ahorrar aun mas lineas de codigo, ustedes me diran si es entendible:
	```     function videoItemTemplate(movie) {
	        return (
	          `<div class="primaryPlaylistItem">
	            <div class="primaryPlaylistItem-image">
	            <img src="${movie.medium_cover_image}">
	            </div>
	            <h4 class="primaryPlaylistItem-title">
	            ${movie.title}
	            </h4>
	          </div>`
	        )
	      }
	    function renderMovieList(list, $container) {
	        list.data.movies.forEach((movie)=> {
	          $container.innerHTML +=  videoItemTemplate(movie);
	        })
	      }
	    
	      renderMovieList(actionList,$actionContainer)
	      renderMovieList(dramaList,$dramaContainer)
	      renderMovieList(animationList,$animationContainer)
	    
	```

	* **Esteban Ciudad** [372644] (4)

		totalmente entendible, compañero solo agregaría una línea de código a la función renderMovieList para eliminar la imagen de precarga.
		```     function renderMovieList(list, $container) {
		        $container.children[0].remove();
		        list.data.movies.forEach((movie)=> {
		          $container.innerHTML += videoItemTemplate(movie);
		        })
		      }
		    
		```

* **Jonatan David Padilla** (5) [348239](https://platzi.com/comentario/348239/) 

	Tu ya eres mas colombiano que el sancocho leonidas 😂 😂 😂

* **Karla Agraz** (4) [973299](https://platzi.com/comentario/973299/) 

	Me funcionó esta API: <https://yts.mx/api/v2/list_movies.json?genre=action>  
	Antes me aparecía el error de CORS en la consola.

* **urieelmm** (4) [838784](https://platzi.com/comentario/838784/) 

	Menos mal que si me di cuenta del error de los selectores 😄

* **Cesar Augusto Barco de Gouveia** (4) [553176](https://platzi.com/comentario/553176/) 

	Guardo por aqui, voy a mi navegador… recargo. - Leonidas.

* **David Daniel Castillo Nava** (4) [350400](https://platzi.com/comentario/350400/) 

	Bueno lo tengo así!! Un poco distinto pero funciona bien!
	```     const api = 'https://yts.am/api/v2';
	    
	    (async function load(){
	    
	    	let [ reqAction, reqDrama, reqAnimation ] = await Promise.all([
	    		fetch(`${api}/list_movies.json?genre=action`),
	    		fetch(`${api}/list_movies.json?genre=drama`),
	    		fetch(`${api}/list_movies.json?genre=animation`)
	    	])
	    
	    	let actionData = await reqAction.json()
	    	let actionList = actionData.data.movies
	    
	    	let dramaData = await reqDrama.json()
	    	let dramaList = dramaData.data.movies
	    
	    	let animationData = await reqAnimation.json()
	    	let animationList = animationData.data.movies
	    
	    	const $actionContainer = document.querySelector('#action');
	    	const $dramaContainer = document.getElementById('drama');
	    	const $animationContainer = document.getElementById('animation');
	    
	    	const $featuringContainer = document.getElementById('#featuring');
	    	const $form = document.getElementById('#form');
	    	const $home = document.getElementById('#home');
	    
	    
	    	const $modal = document.getElementById('modal');
	    	const $overlay = document.getElementById('overlay');
	    	const $hideModal = document.getElementById('hide-modal');
	    
	    	const $modalTitle = $modal.querySelector('h1');
	    	const $modalImage = $modal.querySelector('img');
	    	const $modalDescription = $modal.querySelector('p')
	    
	    	const addEventClick = ($element) => {
	    		$element.addEventListener('click', () => {
	    	      alert('click')
	    	    })
	    	}
	    
	    	const templateContent = (template, content) => {
	    		let hmtlContent;
	    
	    		switch(template) {
	    			case 'listPlayer':
	    				hmtlContent = `
	    					<div class="primaryPlaylistItem">
	    						<div class="primaryPlaylistItem-image">
	    							<img src="${content.medium_cover_image}">
	    						</div>
	    						<h4 class="primaryPlaylistItem-title">
	    							${content.title_long}
	    						</h4>
	    					</div>
	    				`
	    				break;
	    			default:
	    				hmtlContent = ''
	    		}
	    
	    		const html = document.implementation.createHTMLDocument();
	        	    	html.body.innerHTML = hmtlContent;
	    
	    		return html.body.children[0];
	    	}
	    
	    	const renderList = (list, elemParent, template) => {
	    		list.forEach((content)=>{
	    			const elementContent = templateContent(template, content)
	    			elemParent.append(elementContent)
	    			addEventClick(elementContent)
	    		})
	    	}
	    
	    
	    	renderList(actionList, $actionContainer, 'listPlayer')
	    	renderList(dramaList, $dramaContainer, 'listPlayer')
	    	renderList(animationList, $animationContainer, 'listPlayer')
	    
	    })()
	    
	```

* **Giancarlo Vera** (3) [470504](https://platzi.com/comentario/470504/) 

	En menos líneas de código me resulta más legible:
	```     function renderMovieList(list, $container) {
	        $container.children[0].remove();
	        list.data.movies.forEach((movie) => {
	          const HTMLString = videoTemplate(movie);
	          return $container.innerHTML += HTMLString;
	        })
	      }
	    
	      renderMovieList(actionList, $actionContainer);
	      renderMovieList(dramaList, $dramaContainer);
	      renderMovieList(animationList, $animationContainer);
	    
	```

* **mermib** (2) [1101980](https://platzi.com/comentario/1101980/) 

	“que oso” jajajaja

* **danielmorales917** (2) [1091212](https://platzi.com/comentario/1091212/) 

	Que Grande!!! A repasar las 3 clases anteriores para entender mejor que fue lo que se hizo. Gracias!

* **Diego Andres Cabrera Rojas** (2) [1058805](https://platzi.com/comentario/1058805/) 

	listo listo listo ,listo calisto jaajajajajaj

* **Fernando Salazar** (2) [944925](https://platzi.com/comentario/944925/) 

	Buenas, me sale un error y no me cargan las peliculas … Access to fetch at ‘<https://yts.am/api/v2/list_movies.json?genre=action>’ from origin ‘null’ has been blocked by CORS policy: No ‘Access-Control-Allow-Origin’ header is present on the requested resource. If an opaque response serves your needs, set the request’s mode to ‘no-cors’ to fetch the resource with CORS disabled si alguien es tan amable de ayudarme

	* **Juan Osio** [944925] (1)

		Podrías mostrar tu código para intentar ayudarte?

	* **Germán González** [944925] (1)

		Debes instalar una extension en el navegador, Moesif CORS se llama.

	* **Karla Agraz** [944925] (1)

		Me funcionó esta API: <https://yts.mx/api/v2/list_movies.json?genre=action>

	* **Omar Garrido García** [944925] (1)

		(async function load(){  
		const proxyurl=“<https://cors-anywhere.herokuapp.com/>”;  
		async function getData(url){  
		const response = await fetch(proxyurl + url);  
		const data = await response.json();  
		return data;  
		}
		
		a mi me funciono asi heroku salvando vidas

* **ronaldjgarciav** (2) [928922](https://platzi.com/comentario/928922/) 

	Me salia todo en un solo contenedor, luego me fije del error depurando un poco y listo lo resolví

	* **Andres Condo** [928922] (1)

		¿Cómo lo arreglaste?

* **asismelgarejo** (2) [883166](https://platzi.com/comentario/883166/) 

	RECUERDA

* **Jean Lucas Castillo Vasquez** (2) [787880](https://platzi.com/comentario/787880/) 

	Soy yo el único que tuvo problemas con el sidebar una vez que cargaban los elementos de drama y animation después de arreglar el bug de los # ?

	* **Daniel Esteves** [787880] (1)

		Hola Jean! Aún te sigue dando el error? Y si es así por favor podrías montar por aquí una captura para poder ayudarte?

	* **Manuel Rivera** [787880] (1)

		Revisa que no hayas quitado # de querySelector, ya que es en getElementById donde no se utiliza.

* **Aaron Felipe Isaacs** (2) [626041](https://platzi.com/comentario/626041/) 

	Este es mi código hasta el momento 😎😎😎
	```     const API_URL = 'https://yts.lt/api/v2/list_movies.json'
	    
	    const getData = async (url, genre) => {
	      const action = await fetch(`${url}?genre=${genre}`)
	      const data = await action.json()
	      return data
	    }
	    
	    const videoItemTemplate = ({ medium_cover_image, title }) => {
	      return (
	      `<div class="primaryPlaylistItem">
	        <div class="primaryPlaylistItem-image">
	          <img src="${medium_cover_image}">
	        </div>
	        <h4 class="primaryPlaylistItem-title">
	          ${title}
	        </h4>
	      </div>`
	      )
	    }
	    
	    const putComponent = (lstMovies, element, ) => {
	      let htmlComponent = ""
	      lstMovies.forEach(movie => {
	        const htmlString = videoItemTemplate(movie)
	        htmlComponent += htmlString
	      })
	      element.innerHTML = htmlComponent
	    }
	    
	    const renderMovies = async genre => {
	      const apidata = await getData(API_URL, genre)
	      const $container = document.getElementById(genre)
	      putComponent(apidata.data.movies, $container)
	    }
	    
	    const load = async () => {
	      const movies = ['action', 'drama', 'animation']
	      movies.forEach(movie => renderMovies(movie))
	    }
	    
	    load()
	    
	```

* **rsanchez0** (2) [503349](https://platzi.com/comentario/503349/) 

	12:00  
	Cuando Leonidas dice: "Yo se que ustedes se dieron cuenta y no tuvieron esto"  
	Yo: Te he fallado Leonidas 😦

* **heysoypaez** (2) [502518](https://platzi.com/comentario/502518/) 

	¿Qué consideran es lo más recomendable para ocultar un elemento en pantalla con javascript? ¿De què dependerá el usar alguna opciòn u otra?
	
	Conozco que podemos quitar elementos del DOM con  
	-Display: none inyectado por js
	
	  * con .remove()
	
	¿Que funciona mejor?

	* **Erik Ochoa (Platzi)** [502518] (7)

		La diferencia entre la funcion **remove **y **display: ‘none’** es que con remove el elemento es eliminado y ya no esta disponible de forma programatica a tavés de Javascript.
		
		En cambio con **display: none **sí se tiene acceso al elemento que no estamos mostrando.

* **Andony Orellana** (2) [432372](https://platzi.com/comentario/432372/) 

	Porque el primer querySelector si lleva el # y los otros dos no?

	* **Juan David Castro (Platzi)** [432372] (4)

		El “ _`#`_ ” nos ayuda a seleccionar nuestros elementos por su `id`, en los últimos dos casos NO utilizamos este símbolo porque queremos seleccionar los elementos por su etiqueta, no por sus atributos 👍.

	* **Facundo Nicolás García Martoni (Platzi)** [432372] (3)

		@juandc En este caso concreto que pregunta @Andony_O, los dos últimos selectores no llevan el ‘#’ porque son formados por la función ‘getElementById’ y no por ‘querySelector’

	* **Juan David Castro (Platzi)** [432372] (1)

		👍

* **eddy** (2) [417206](https://platzi.com/comentario/417206/) 

	Por que en la parte de document.querySelector(’#action); usa el # pero en las otras dos lineas no?  
	document.querySelector(‘drama’);  
	document.querySelector(‘animation’);

	* **eddy** [417206] (5)

		ya me di cuenta el drama y animation usa usa getElementById

	* **LRosas** [417206] (1)

		Exacto!

* **evertescalante1** (2) [401594](https://platzi.com/comentario/401594/) 

	Me costo un rato entenderlo pero, el bug que tiene @Leonidas es por la forma que coloca el id en los selectores que usa
	```     //necesitamos poner el id con #,  asi tambien puedes usar clases, tags
	     document.querySelector('#action');
	    
	    //no necesita # par  obtener el id
	    document.getElementById('action');
	    
	```

* **carlosextra1** (1) [1115916](https://platzi.com/comentario/1115916/) 

	Respuesta a:  
	Reutilizando funciones  
	Excelente clase, solo que veo que la App funciona lenta, Esto es normal? será por la API?  
	hice un console.time() y console.timeEnd(); y mi resultado fue
	```     default: 11207.330078125ms
	    
	```
	
	Comparte tu resultado por favor!

* **Pablo Victor Vargas** (1) [1110352](https://platzi.com/comentario/1110352/) 

	Alguien que haya tenido problemas con el forEach!!??? a mi me esta dando problemas eso:
	
	home.js:110 Uncaught (in promise) TypeError: Cannot read property ‘forEach’ of undefined.

	* **Gabriel De Andrade (Platzi)** [1110352] (2)

		Al parecer estás tratando de hacer un forEach dentro de un elemento vacío, verifica que los nombres de las variables están bien escritos y trata de seguir el flujo de los datos con console.log e identificar dónde se está rompiendo todo

	* **Pablo Victor Vargas** [1110352] (1)

		seeeee me habia olvidado de cambiar un par de variables!!! cosas que pasan dijo bart!! jejeje  
		graciasssss!!!

	* **carlosextra1** [1110352] (1)

		Puedes compartir el código por favor!

* **wlm.morant** (1) [1050325](https://platzi.com/comentario/1050325/) 

	la clase de ahora me exploto la cabeza , Leonidas es un excelente profesor pero creo que la mayoria tiene que verlo mas de 2 veces  
	.

* **lmoran** (1) [996487](https://platzi.com/comentario/996487/) 

	```    function videoItemTemplate(movie) {
	        return `<div class="primaryPlaylistItem">
	                <div class="primaryPlaylistItem-image">
	                  <img src="${movie.medium_cover_image}" />
	                </div>
	                <h4 class="primaryPlaylistItem-title">${movie.title}</h4>
	              </div>`;
	      }
	    
	      function createMovieTemplate(HTMLString) {
	        const html = document.implementation.createHTMLDocument();
	        html.body.innerHTML = HTMLString;
	        return html.body.children[0];
	      }
	    
	      function renderMovieList(list, $container) {
	        if ($container.children[0] != null) {
	          $container.children[0].remove();
	        }
	        list.forEach(movie => {
	          const HTMLString = videoItemTemplate(movie);
	          const movieView = createMovieTemplate(HTMLString);
	          $container.append(movieView);
	        });
	      }
	    
	      const $actionContainer = document.querySelector("#action");
	      renderMovieList(actionList.data.movies, $actionContainer);
	      const $dramaContainer = document.getElementById("drama");
	      renderMovieList(dramaList.data.movies, $dramaContainer);
	      const $animationContainer = document.getElementById("animation");
	      renderMovieList(thrillerList.data.movies, $animationContainer);```
	    
	```

* **Kenet Chungandro** (1) [994260](https://platzi.com/comentario/994260/) 

	Excelente clase! 😃  
	ya vamos avanzando

* **oskr-lopez** (1) [986728](https://platzi.com/comentario/986728/) 

	Buena manera crack de ver mejor nuestro código hasta el momento estoy feliz con el curso.

* **jumper-andrew** (1) [983748](https://platzi.com/comentario/983748/) 

	No me funciona la API

	* **Omar Garrido García** [983748] (1)

		(async function load(){  
		const proxyurl=“<https://cors-anywhere.herokuapp.com/>”;  
		async function getData(url){  
		const response = await fetch(proxyurl + url);  
		const data = await response.json();  
		return data;  
		}  
		me pasaba algo parecido y agregue esto y el error se fue

* **Andres Felipe Triana Manrique** (1) [830581](https://platzi.com/comentario/830581/) 

	Excelente clase!

* **mxmacias** (1) [818247](https://platzi.com/comentario/818247/) 

	Que mal yo no me di cuenta  
	const $dramaContainer = document.getElementById(’#drama’);

* **Jonathan Uriel Jiménez Soveranes** (1) [803698](https://platzi.com/comentario/803698/) 

	Excelente!

* **eltonchavezt** (1) [793580](https://platzi.com/comentario/793580/) 

	function renderMovieList(list, container){  
	list.data.movies.forEach( (movie) => {  
	const HtmlString = videoItemTemplate(movie);  
	const movieElement = createTemplate(HtmlString);  
	container.append(movieElement);  
	});  
	}  
	renderMovieList(actionList,$actionContainer);

* **David Alejandro Mosquera Moreno** (1) [726236](https://platzi.com/comentario/726236/) 

	Estoy un poco perdido. ¿Cómo hace el parámetro _list_ para reemplazar a _actionList.data.movies_ , haciendo que llegue la lista de películas?

	* **Yair Vargas** [726236] (2)

		Porque al momento de llamar el metodo renderMovieList le envia el parametro actionList.data.movies, por lo tanto el parametro List del metodo ya tendra el array de las 20 peliculas

	* **David Alejandro Mosquera Moreno** [726236] (2)

		Gracias @yairvargasr me queda más claro

* **Cristobal Vega** (1) [682723](https://platzi.com/comentario/682723/) 

	Una manera elegante de quitar el elemento con el GIF.

* **onlinejaime** (1) [656632](https://platzi.com/comentario/656632/) 

	One hour later…
	
	Solucionado!!!
	
	**_ ‘<https://yts.lt/api/v2/list_movies.json?genre=action>’_**
	
	La API cambió de nombre en la url… Ahora es:
	
	**[yts.lt](http://yts.lt)**

	* **Karla Agraz** [656632] (1)

		Ahora cambio a [yts.mx](http://yts.mx)

* **Gustavo83** (1) [629492](https://platzi.com/comentario/629492/) 

	Funciones => Son bloques declarados de código que se pueden reutilizar en otra parte del programa según su funcionalidad y la necesidad que se presente.
	
	  * OJITO cuando interactues con el DOM, porque estas asumiendo cosas que vana existir, entonces tienes dos opciones, o utilizas una condicional como if para mostrar al programa el camino a seguir el tal o cual caso, de lo contrario estar 100% seguro de que ese elemento existe

* **jesus-gallardo** (1) [602709](https://platzi.com/comentario/602709/) 

	**Este es mi codigo de esta clase** es otra forma de hacerlo.
	```     //Funcion para poblar la lista de peliculas.
	    
	    const renderMovieList = (list, container) => {
	        list.data.movies.forEach( movie => {
	          let HTMLString = videoItemTemplate(movie)
	          container.insertAdjacentHTML('beforeend', HTMLString)
	        })
	        removeFirstChild(container)
	      }
	    
	    //Luego la uso asi:
	    
	      renderMovieList(actionList, $actionContainer)
	      renderMovieList(dramaList, $dramaContainer)
	      renderMovieList(animationList, $animationContainer)
	    
	```

* **VictorIsaacChavez** (1) [590291](https://platzi.com/comentario/590291/) 

	Comparto mi implementación:
	```     function stringToHtmlElement(hmtmlStr){
	        const htmlObj = document.implementation.createHTMLDocument()
	        htmlObj.body.innerHTML = hmtmlStr
	        return htmlObj.body.children[0]
	      }
	    
	      let createMovieTemplate = (movie) => 
	            stringToHtmlElement(movieItemTemplate(movie)) 
	    
	      let renderMovieList = (movieList, $container) => {
	        $container.children[0].remove()
	        movieList.forEach(element => 
	            $container.append(createMovieTemplate(element))
	        )
	      }
	     
	      renderMovieList(listActionMovie, $actionContainer)
	      renderMovieList(listDramaMovie, $dramaContainer)
	      renderMovieList(listAnimationMovie, $animationContainer)
	    
	```

* **Alejandro González Reyes** (1) [575821](https://platzi.com/comentario/575821/) 

	Este es mi super apunte.
	```     // Esta función se encarga de insertar cada template de pelicula generado
	    	// dentro de un contenedor especifico dentro del documento HTML
	    	// Contenedor peliculas de accion .... muchos template que muestran peliculas de accion
	    	function insertDOMTemplate(HTMLString, HTMLcontainer) {
	    		// Insertamos dentro del contenedor, el HTML que representa el template de cada pelicula
	    		// Se concatena debido a que innerHTML sobreescribe todo el contenido 
	    		HTMLcontainer.innerHTML += HTMLString
	    	}
	    
	    	// Esta función se encarga de renderizar las peliculas devueltas por la consulta a una API
	    	// Para ello recibe un arreglo de peliculas, y el contenedor HTML donde debe de insertarse
	    	function renderMovieList(listMovies, HTMLcontainer) {
	    		// Limpiar el contenido interno que pueda existir dentro del contenedor
	    		// En este caso, el contenedor HTML contiene un elemento de imagen (loader)
	    		// Por tanto accedemos a su primer hijo y lo removemos.
	    		// Otra forma de hacerlo es mediante una consulta de selector.
	    		// 
	    		// Importante: Debemos estar seguros de que exista, de lo contrario marcará un error
	    		// Para ello se puede hacer uso de un condicional
	    		HTMLcontainer.children[0].remove()
	    		// Iterar por cada una de las peliculas
	    		listMovies.forEach(function(movie) {
	    			// Invocamos la función que se encarga de interpolar los datos de la pelicula actual
	    			// dentro de un template de pelicula
	    			const HTMLString = videoItemTemplate(movie)
	    			// El template de pelicula devuelto por la función anterior, se inserta dentro del DOM
	    			// a través de la invoación de la siguiente función
	    			insertDOMTemplate(HTMLString, HTMLcontainer)
	    		})
	    	}
	    
	    	// Invocar la función que se encargará de renderizar las peliculas devuektas
	    	// por la consutla a la API.
	    	// En este caso le enviamos un array de peliculas y el contenedor HTML donde queremos insertarlas
	    	renderMovieList(actionList.data.movies, $actionContainer)
	    	renderMovieList(dramaList.data.movies, $dramaContainer)
	    	renderMovieList(animationList.data.movies, $animationContainer)
	    
	```

* **Isaias Chávez** (1) [549103](https://platzi.com/comentario/549103/) 

	¿Creen que sea una mejor manera de implementar render?
	```     function renderMovieList(list,$container){
	            $container.children[0].remove()
	            list.forEach( item => $container.append(createTemplate(videoItemTemplate(item))))
	          }```
	    O es menos legible?
	    
	```

	* **Jair Israel Avilés Eusebio** [549103] (1)

		Es otra forma, depende de como lo vean. A mi gusto no hay tema en reducir lineas de codigo usando arrow functions 😄

* **Diego Andres Cardenas Caro** (1) [534975](https://platzi.com/comentario/534975/) 

	No me gusto mucho la forma como se inserto el htmlString, me parecio algo enredado, asi que propuse mi propia version con menos lineas de codigo y un poco mas legible.
	```     function videoItemTemplate(movie) {
	        return (
	          `<div class="primaryPlaylistItem">
	            <div class="primaryPlaylistItem-image">
	              <img src="${movie.medium_cover_image}">
	            </div>
	            <h4 class="primaryPlaylistItem-title">
	              ${movie.title}
	            </h4>
	          </div>`
	        )
	      }
	    
	      function renderMoveList(list, $container){
	        $container.children[0].remove(); //remove loader gif    
	        const movies = list.data.movies    
	        for (let movie of movies){     //Usando ciclo for of de ES6 
	          $container.innerHTML += videoItemTemplate(movie);  
	        }
	      }
	    
	      renderMoveList(actionList, $actionContainer);
	      renderMoveList(dramaList, $dramaContainer);
	      renderMoveList(animationList, $animationContainer);```
	    
	```

* **Félix Alejandro Zelaya Orellana** (1) [529169](https://platzi.com/comentario/529169/) 

	Genial!!

* **pablojorgeandres** (1) [485280](https://platzi.com/comentario/485280/) 

	Copio el código del curso hasta esta clase:
	```     (asyncfunctionload(){
	        const moviesURL = `https://yts.am/api/v2/list_movies.json?genre=`;
	    
	        // contenedor de películas por género
	        const $actionContainer = document.getElementById(`action`)
	        const $dramaContainer = document.getElementById(`drama`)
	        const $animationContainer = document.getElementById(`animation`)
	    
	        // otros contenedores
	        const $featuringContainer = document.getElementById(`featuring`)
	        const $form = document.getElementById(`form`)
	        const $home = document.getElementById(`home`)
	    
	        // otros elementos del DOM
	        const $modal = document.getElementById(`modal`)
	        const $overlay = document.getElementById(`overlay`)
	        const $hideModal = document.getElementById(`hide-modal`)
	    
	        // elementos dentro del modal
	        const $modalTitle = $modal.querySelector(`h1`)
	        const $modalImg = $modal.querySelector(`img`)
	        const $modalDescription = $modal.querySelector(`p`)
	    
	        //-- función para hacer el request a la API --//
	        asyncfunctiongetMovies (url) {
	            const response = await fetch(url)
	            const data = await response.json()
	            return data
	        }
	    
	        // listas de objetos-películas por género traidos desde la API
	        const actionList = await getMovies(`${moviesURL}action`)
	        const dramaList = await getMovies(`${moviesURL}drama`)
	        const animationList = await getMovies(`${moviesURL}animation`)
	    
	        // listas de películas por género
	        const actionMovies = actionList.data.movies
	        const dramaMovies = dramaList.data.movies
	        const animationMovies = animationList.data.movies
	    
	        //-- función para generar el código HTML por cada película --//
	        functionvideoTemplate(img, title) {    
	            return(
	                `<div class="primaryPlaylistItem"}>
	                    <div class="primaryPlaylistItem-image">
	                    <img src="${img}">
	                    </div>
	                    <h4 class="primaryPlaylistItem-title">
	                    ${title}
	                    </h4>
	                </div>`
	            )
	        }
	    
	        //-- función para imprimir código html por cada película --//
	        functionprintMoviesByGenre(genre, container){
	            container.children[0].style = `display: none`
	            genre.forEach( movie => {
	                container.innerHTML += videoTemplate(movie.medium_cover_image, movie.title)
	            })
	        }
	    
	        // imprimir cada película por género en la página
	        const actionMoviesPrinted = printMoviesByGenre(actionMovies, $actionContainer)
	        const dramaMoviesPrinted = printMoviesByGenre(dramaMovies, $dramaContainer)
	        const animationMoviesPrinted = printMoviesByGenre(animationMovies, $animationContainer)
	    })()
	    
	    
	```

* **pablojorgeandres** (1) [485059](https://platzi.com/comentario/485059/) 

	Alguien se da cuenta de qué dice Leonidas [acá](https://platzi.com/clases/1316-jquery-js/12303-reutilizando-funciones/?time=399)?  
	…“Vamos a tener una aplicación que tiene un montón de líneas. Luego … lo pueden hacer con el curso de WebPack”…
	
	Esa palabra después de “Luego”…

	* **jbarriospd** [485059] (2)

		Convertir el codigo Js en modulos. Saludos!

	* **pablojorgeandres** [485059] (1)

		Bueno, supongo que Leonidas quiso poner todo eso en una palabra.  
		Dice algo así como ‘modulizarlo’, puede ser?  
		Bueno, ya no importa de todas maneras, con lo que decís ya puedo entender el punto.  
		Gracias!

* **JhonColorado07** (1) [483819](https://platzi.com/comentario/483819/) 

	Wauuu aquí veo muy bien la forma de saber reutilizar el código con las funciones. Gracias, me quedan aún más claro

* **Qenta** (1) [481908](https://platzi.com/comentario/481908/) 

	¿Que opinan de este que hice?  
	¿Donde tengo errores para tenerlos en cuenta para un futuro?
	```     (async function load() {
	    
	    
	    
	        async function getData (url) {
	         const response = await fetch(url)
	         const data = await response.json()
	         return data;
	        }
	        
	        const ditto = await getData('https://pokeapi.co/api/v2/pokemon/ditto/')
	        const pikachu = await getData('https://pokeapi.co/api/v2/pokemon/pikachu/')
	        const espeon = await getData('https://pokeapi.co/api/v2/pokemon/espeon/')
	        const dragonair = await getData('https://pokeapi.co/api/v2/pokemon/dragonair/')
	    
	    // Imagen de pokeball
	    
	        const pokeImagen = 'assets/imagenes/pokeball.png';
	    
	    
	    // Seccion donde van las habilidades y la imagen
	    
	        function itemTemplate(item) {
	            return (
	              
	              `<div class="Habilidad-e-imagen">
	            
	                <div class="imagen-de-habilidad" style="text-align: center">
	                  <img style="width: 30px; height: 30px;" src="${pokeImagen}">
	                </div>
	                <h4 style="text-align: center" class="habilidad"> ${item.ability.name} </h4>
	              </div>`
	            )
	          }
	    
	    // Creación de secciones
	    
	          function crearSeccion (pokemon, habilidadesPokemon) {
	            pokemon.abilities.forEach((item) => {
	              const HTMLString = itemTemplate(item)
	              const html = document.implementation.createHTMLDocument();
	              html.body.innerHTML = HTMLString;
	              $(habilidadesPokemon).append(html.body.children[0])
	            })
	          }
	    
	          
	    
	            
	    // Lista de habilidades de Ditto 
	    
	          let habilidadesDitto = document.querySelector('#habilidades1')
	    
	          ditto.abilities.forEach((item) => {
	            const HTMLString = itemTemplate(item)
	            const html = document.implementation.createHTMLDocument();
	            html.body.innerHTML = HTMLString;
	            $(habilidadesDitto).append(html.body.children[0])
	            console.log(HTMLString)
	            console.log(html)
	            
	          })
	    
	    // Lista de habilidades de Espeon  
	    
	          let habilidadesEspeon = document.querySelector('#habilidades2')
	         
	          crearSeccion(espeon, habilidadesEspeon)
	    
	    // Lista de habilidades de Ditto  Pikachu
	            
	          let habilidadesPikachu = document.querySelector('#habilidades3')
	    
	          crearSeccion(pikachu, habilidadesPikachu);
	    
	    // Lista de habilidades de Dragonair
	    
	          let habilidadesDragonair = document.querySelector('#habilidades4')
	    
	          crearSeccion(dragonair, habilidadesDragonair);
	    
	    
	    // CONSOLE LOG 
	    
	        console.log('Ditto: ', ditto)
	        console.log('Pikachu: ', pikachu)
	        console.log('Espeon: ', espeon)
	        console.log('Dragonair: ', dragonair)
	    })()```
	    
	```

	* **jbarriospd** [481908] (1)

		Genial, maestro pokemon!

* **Francisco Ezequiel Baños Ramirez** (1) [466721](https://platzi.com/comentario/466721/) 

	Les dejo mi version de las cosas hasta este punto, he cambiado varias cosas pero las tengo comentadas de tal manera que puedan entender :3
	```     /* Esta funcion se ejectuara al iniciar y funciona para cargar las peliculas.
	        ademas de cargarlas las escribira dentro del dom.*/
	    (asyncfunctionload(){
	        try {
	            // Esta funcion obtiene las listas de peliculas
	            const getData = async genre => {
	                url = `https://yts.am/api/v2/list_movies.json?genre=${genre}`;
	                //Hace una peticion get a la url con el genero de la pelicula
	                const response = await fetch(url);
	                const response_json = await response.json();
	                response_json.genre = genre;
	                // Retornamos solo el json de la respuesta.
	                return response_json;
	            };
	            //Creamos una lista con los generos y la mapeamos para que tenga las lista de peliculas
	            const promises = ['action', 'drama', 'animation'].map(genre => getData(genre));
	            //Esperamos a que todas esten listas para seguir
	            const global_movie_list = await Promise.all(promises);
	            //Declaramos un objeto con los contenedores donde iran las peliculas con su respectivo genero
	            const $movie_containers = {
	                action: document.getElementById('action'),
	                drama: document.getElementById('drama'),
	                animation: document.getElementById('animation')
	            };
	    
	            const $featuring_container = document.getElementById('featuring');
	            const $form = document.getElementById('form');
	            const $home = document.getElementById('home');
	            const $modal = {
	                modal: document.getElementById('modal'),
	                img: this.modal.querySelector('img'),
	                hide: document.getElementById('hide-modal'),
	                title: this.modal.querySelector('h1'),
	                description: this.modal.querySelector('title')
	            };
	    
	            const $overlay = document.getElementById('overlay');
	            //Funcion que crea un div que contendra la pelicula recibe dos parametros la fuente de la imagen y el titulo
	            // de la pelicula
	            const videoItemTemplate = (src, title) => {
	                return (
	                `<div class="primaryPlaylistItem">
	                            <div class="primaryPlaylistItem-image">
	                                <img alt='' src="${src}">
	                            </div>
	                            <h4 class="primaryPlaylistItem-title">
	                                ${title}
	                            </h4>
	                        </div>`)
	            };
	            //Por cada Promesa que declaramos anteriormente
	            global_movie_list.forEach(movie_list => {
	                //Obtenemos el genero
	                let genre = movie_list.genre;
	                // Y por cada pelicula
	                let movies = movie_list.data.movies;
	                let lenght = movies.length;
	                movies.forEach(movie => {
	                    //Obtenemos la url de la imagen y su titulo
	                    let {medium_cover_image, title} = movie;
	                    //Creamos el template
	                    let html_str = videoItemTemplate(medium_cover_image, title);
	                    //Obtenemos el contenedor donde los pondremos
	                    let $movie_container = $movie_containers[genre];
	                    //Y le agregamos el template de la pelicula
	                    $movie_container.innerHTML += html_str;
	                    //Borramos el iconito de carga :3
	    
	                    if (movies.indexOf(movie) === lenght - 1){
	                       $movie_container.querySelector('.loader').remove()
	                    }
	                })
	            })
	        }catch (e) {
	            console.log(e)
	        }
	    })();```
	    
	```

* **Marcos Saul Ardissino** (1) [434479](https://platzi.com/comentario/434479/) 

	no me trae los titulos de las movies… no puedo dar en el porque! ! ! =(

	* **pablojorgeandres** [434479] (2)

		Lo has podido resolver?  
		Si no, pon tu código aquí y si puedo te doy una mano.

* **eddy** (1) [417218](https://platzi.com/comentario/417218/) 

	me manda este error:  
	Failed to load resource: the server responded with a status of 404 ()  
	Creo que una de las imagenes ya no esta disponibles lo que provoca que no me muestre ninguna de las peliculas de Drama y Animation.  
	Que puedo hacer en esta situacion?

	* **Edward Steven Ramos Palacios** [417218] (1)

		Es un error en donde ya no existe el recurso que buscas. Puedes buscar un nuevo recurso, o implementar que en caso de error se muestre una imagen por defecto.

	* **Daniel Esteves** [417218] (1)

		Hola! Eso significa que el recurso no está más disponible, lo mejor sería intentar con otros endpoints los cuales si funcionen las imágenes para que puedas tener la experiencia completa

* **wlm.morant** (1) [84421](https://platzi.com/comentario/1050629/) 
tengo un problema con drama GET 

* **Ang.O** (1) [74814](https://platzi.com/comentario/859603/) 
9:00 con $container.children(0).remove(); leonidas borro la imagen de espera de recarga de imagenes pero solo para que no se quede la ima...

	* **diego rodriguez** [74814] (1)

		si debuggeas el codigo antes de $container.children(0).remove(), te daras cuenta que $container tiene estos elementos
		```     <div class="primaryPlayList-list" id="action">
		    	<img src="src/images/loader.gif"width="50" height"50">
		    </div>
		    
		```
		
		cuando pones children[0] toma el primer hijo de container que en este caso seria la etiqueta img, que contiene el gif de carga, y con remove lo quita, espero que me hayas podido entender😉

* **Ang.O** (1) [74813](https://platzi.com/comentario/859592/) 
que es ese cuadrito en amarillo dentro de unas llaves en las funciones en verde?

	* **Erik Ochoa (Platzi)** [74813] (1)

		Si te refieres al cuadro amarillo con los tres puntos, eso es parte del editor y significa que ahí hay código plegado, si le das click se despliega el código. Sirve para ocultar código mientras estas trabajando y aprovechar mejor el espacio.

* **hendersonnozartgibajareyes** (1) [69950](https://platzi.com/comentario/766698/) 
querySelector(’#action’); getElementById(‘drama’); la diferencia getElementById es superior ya que puede seleccionar cualquier elemento q...

	* **Juan David Castro (Platzi)** [69950] (1)

		Con **`getElementById`** solo podemos buscar elementos por medio de su ID.
		
		Con **`querySelector`** podemos buscar cualquier elementos que cumpla con las reglas que le especifiquemos. Puedes por el nombre de clase, id, atributos, tipo de etiqueta, etc.
		
		Además, podemos usar **`querySelectorAll`** para encontrar todos los elementos que cumplan con las reglas. Podemos buscar todas las etiquetas **`<p>`** o todos los elementos con una cierta clase.

* **Isaias Chávez** (1) [56134](https://platzi.com/comentario/549103/) 
¿Creen que sea una mejor manera de implementar render? function renderMovieList(list,$container){ $container.children[0].re...

	* **Jair Israel Avilés Eusebio** [56134] (1)

		Es otra forma, depende de como lo vean. A mi gusto no hay tema en reducir lineas de codigo usando arrow functions 😄

* **heysoypaez** (1) [52218](https://platzi.com/comentario/502518/) 
¿Qué consideran es lo más recomendable para ocultar un elemento en pantalla con javascript? ¿De què dependerá el usar alguna opciòn u otr...

	* **Erik Ochoa (Platzi)** [52218] (7)

		La diferencia entre la funcion **remove **y **display: ‘none’** es que con remove el elemento es eliminado y ya no esta disponible de forma programatica a tavés de Javascript.
		
		En cambio con **display: none **sí se tiene acceso al elemento que no estamos mostrando.

* **pablojorgeandres** (1) [50912](https://platzi.com/comentario/485059/) 
Alguien se da cuenta de qué dice Leonidas acá</...

	* **jbarriospd** [50912] (2)

		Convertir el codigo Js en modulos. Saludos!

* **Qenta** (1) [50640](https://platzi.com/comentario/481908/) 
¿Que opinan de este que hice? ¿Donde tengo errores para tenerlos en cuenta para un futuro? (async function load() { async fu...

	* **jbarriospd** [50640] (1)

		Genial, maestro pokemon!

* **Andony Orellana** (1) [46504](https://platzi.com/comentario/432372/) 
Porque el primer querySelector si lleva el # y los otros dos no?

	* **Juan David Castro (Platzi)** [46504] (4)

		El “ _`#`_ ” nos ayuda a seleccionar nuestros elementos por su `id`, en los últimos dos casos NO utilizamos este símbolo porque queremos seleccionar los elementos por su etiqueta, no por sus atributos 👍.

* **eddy** (1) [45231](https://platzi.com/comentario/417218/) 
me manda este error: Failed to load resource: the server responded with a status of 404 () Creo que una de las imagenes ya no esta dispon...

	* **Edward Steven Ramos Palacios** [45231] (1)

		Es un error en donde ya no existe el recurso que buscas. Puedes buscar un nuevo recurso, o implementar que en caso de error se muestre una imagen por defecto.

## 0120. Eventos [12304](https://platzi.com/clases/1316-jquery-js/12304-eventos6747/)

### Descripción:


Toda aplicación web necesita lidiar con interacciones del usuario, desde un click hasta arrastrar algún elemento, estas interacciones son escuchadas por el navegador mediante algo llamado eventos. Existen muchos tipos de eventos, el más común es el evento de click.  
En esta clase vamos a trabajar con el evento click y submit.  
Para que un elemento HTML pueda escuchar algún evento debemos usar el método addEventListener. Este método recibe dos parámetros, el nombre del evento que va a escuchar y la función que se va a ejecutar al momento de que se accione el evento.  
La página se recarga al momento de ejecutarse el evento submit, para evitar esto debemos quitarle la acción por defecto que viene en submit usando el método event.preventDefault().

### Comentarios:

* **Sergio Minei** (25) [335790](https://platzi.com/comentario/335790/) 

	## Eventos
	
	Son una forma de notificar a la aplicación cuando algo interesante ha sucedido.
	
	**jQuery**
	```     $("div").on("click", function(event) {
	    
	    })
	    
	```
	
	**JavaScript**
	```     const $element = document.getElementById("element");
	    $element.addEventListener("click", function(event) {
	    
	    })
	    
	```
	
	**Nota** : cuando se activa el evento submit, el browser de refresca por defecto. Para evitar esto se usa `event.preventDefault()`.
	
	Para ver la lista de eventos:  
	<http://developer.mozilla.org/en-US/docs/Web/Events>
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **oteka21** [335790] (4)

		Buen resumen

	* **Sergio Minei** [335790] (3)

		Muchas gracias @oteka21

	* **Oscar Barajas Tavares (Platzi)** [335790] (3)
👍

	* **CarlosGJ** [335790] (2)

		muchas gracias 😃-

	* **Sergio Minei** [335790] (1)

		@CarlosGJ 😄

	* **Maximiliano Andres Villalobos Coilla** [335790] (1)

		Tambien es válido poner en JQuery el evento directamente
		```     <code>
		    $("div").click( function(event) {
		    
		    })
		    
		    
		```

* **Alan Santiago** (8) [363810](https://platzi.com/comentario/363810/) 

	Cuando una arrow function solo tiene **un parametro** , se puede escribir sin el parentesis. Ejemplo:
	```     event => {
	        // Codigo
	    }
	    
	```

* **brapastor** (7) [337206](https://platzi.com/comentario/337206/) 

	<https://developer.mozilla.org/en-US/docs/Web/Events>

* **pablojorgeandres** (4) [485328](https://platzi.com/comentario/485328/) 

	## Eventos
	
	Los eventos del DOM son enviados para notificar al código de cosas interesantes que han sucedido o están sucediendo.  
	Cada evento está representado por un objeto que se basa en la Interfaz _Event_ , e incluso puede contener campos y/o funciones adicionales personalizadas para obtener más información a cerca de lo ‘sucedido’. Los eventos pueden representar cualquier cosa, desde las interacciones básicas del usuario hasta notificaciones automatizadas de las cosas que suceden en el modelo de representación.
	
	_Referencia jQ:<http://api.jquery.com/on/>_
	
	_Referencia JS:<http://bibliotecadigital.tamaulipas.gob.mx/archivos/descargas/b716d48a5_evolucion.pdf>_
	
	Con vanilla JS, el método _addEventListener()_ se utiliza para invocar cualquier evento.  
	Este método recibe dos parámetros; (evento, function)
	```     $element.addEventListener('event', () => {})
	    
	```
	
	Con jQuery:
	```     $element.on( events, handler )
	```

	* **pablojorgeandres** [485328] (3)

		*Agregando al resumen
		
		Llamando al método del event _.preventDefault()_ evitamos que el sitio se recargue cada vez que usamos el formulario.
		```         $form.addEventListener('submit', event => event.preventDefault())
		```

* **Melina Jacqueline onoriaga** (4) [445518](https://platzi.com/comentario/445518/) 

	Pregunta… Porque crea todas las funciones dentro de la función principal load y no por fuera de ella y luego invocarlas cuando se necesitan?

	* **Nathaly Stefani Riaño Bejarano** [445518] (2)

		Tengo esa misma duda! 🤔

	* **beaps** [445518] (1)

		Yo también!

	* **Isaias Chávez** [445518] (1)

		Me imagino que tiene que ver con el scope, estas son funciones que solo funcionan para esta parte de la página. Sería como la encapsulación en POO y evitar que otras funciones que por ejemplo también se llamen “createTemplate” No sean afectadas. Pero es mi teoría.

	* **David Alejandro Mosquera Moreno** [445518] (1)

		Lo que entiendo es que esta función nos ayuda, por ejemplo, a cargar las peticiones a las películas inmediatamente se carga la app.

* **pinfradev** (3) [953598](https://platzi.com/comentario/953598/) 

	<https://developer.mozilla.org/es/docs/Web/Events>

	* **carlosextra1** [953598] (1)

		Gracias!

* **Isaias Chávez** (3) [549114](https://platzi.com/comentario/549114/) 

	Cuando una arrow function solo retorna un valor o solo realiza una acción, no es necesario colocar los corchetes **Ejemplo**
	```     functionaddEventClick(element){
	            element.addEventListener("click",() => alert("Click"))
	          }
	    
	```

	* **Facundo Nicolás García Martoni (Platzi)** [549114] (1)

		Llaves*

* **Yassr** (3) [364814](https://platzi.com/comentario/364814/) 

	Hola, me encuentro atascado en esta clase super sencilla , si alguien quiere echarme una mano.
	
	tengo lo siguiente:
	```     function clickMovie($movieItem){
	      
	            $movieItem.addEventListener('click', function() {
	                 alert( 'clicked');
	             })
	         }
	    
	        function printMovieList ( movieList, $container) {
	     
	            movieList.forEach((movie) => {
	                const HTMLString = movieItemTemplate(movie)
	                $container.innerHTML += HTMLString;
	                clickMovie(HTMLString);
	               
	            })
	        }
	    
	```
	
	me sale el siguiente error
	```     home.js:139 Uncaught (in promise) TypeError: $movieItem.addEventListener is not a function
	        at clickMovie (home.js:139)
	        at movieList.forEach (home.js:154)
	        at Array.forEach (<anonymous>)
	        at printMovieList (home.js:150)
	        at load (home.js:181)
	    
	```

	* **Jose Carlo Rojas Mora** [364814] (8)

		Hola @Yassr espero poder explicarte lo mejor posible tu problema pues tiene bastante ciencia detrás para mi,
		
		    1. El error sucede porque tal cual tienes tu código en ningún momento creas un elemento HTML o mejor dicho un objeto del DOM , que es como lo tiene Leonidas, esto lo hiciste para ahorrar código y no esta nada mal tal cual lo hiciste. Por eso cuando envías el HTMLString es una variable de tipo string para javascript y por eso lanza el error de que el addEventListener no es una función y claro esa función no forma parte para los tipos string. Si no queda claro lo que explique trata ver otra vez la clase creación de DOM.
		    2. Para resolver tu problema hay varias maneras voy intentar darte 2, creo la segunda es la mas optima.  
		a. Es que crees el evento dentro del template , osea que utilices el evento onClick de javascript , te puede causar ciertos problemas mas adelante para saber de que película es que debes levantar el modal pero nada que no se pueda resolver.
		```     `<div class="primaryPlaylistItem" onClick="eventoClick">
		    				<divclass="primaryPlaylistItem-image">
		    					<imgsrc="${movie.medium_cover_image}">
		    				</div>
		    					<h4class="primaryPlaylistItem-title">
		    					${movie.title}
		    				</h4>
		    			</div>`
		    
		```
		```     b. Aprovechar la propiedad children de los container, cada vez que vas agregando un elemento existe un nuevo hijo dentro del container, entonces lo que puedes hacer es recorrer hijos de ese contenedor y agregar sus eventos. OJO para poder usar el foreach a un atributo HTML debemos hacer algo distinto a como lo usamos con los arrays mucho cuidado con este caso por que es uno de los casos de javascript que exige  el ";" (el punto y coma), esta idea es muy similar al each de JQuery
		    
		```
		```     printMovieList ( movieList, $container) {
		     
		            movieList.forEach((movie) =&gt; {
		                const HTMLString = movieItemTemplate(movie)
		                $container.innerHTML += HTMLString
		            });
		    	[].forEach.call($container.children, child => clickMovie(child) )
		        }
		    
		```
		
		Te recuerdo el ; antes de la ejecución de esa linea de código mucho cuidado. Espero te ayude Saludos.

	* **Yassr** [364814] (1)

		@jcarlorm Muchas gracias por tomarte el tiempo por responder. Gracias por aclararme cual era el error, ya había visto de nuevo la clase de la creación del HTML, y me di cuenta que estaba imprimiendo un string como bien dices. voy a tratar de entender la solución que me das. Gracias

	* **Giancarlo Vera** [364814] (3)

		Hola! @Yassr tuve el mismo problema que tú, yo lo encaré de la siguiente forma:
		```     function movieClick($container) {
		        const movieList = Array.from($container.children);
		        movieList.forEach(($movieElement) => {
		          $movieElement.addEventListener('click', function () {
		            alert('clicked!');
		          })
		        })
		      }
		    
		      function renderMovieList(list, $container) {
		        $container.children[0].remove();
		        list.data.movies.forEach((movie) => {
		          const HTMLString = videoTemplate(movie);
		          $container.innerHTML += HTMLString;
		        })
		        movieClick($container);
		      }
		    
		```

* **Jhoan Steve Sandoval Meneses** (2) [746238](https://platzi.com/comentario/746238/) 

	En jquery existe la función ‘$( “form” ).serialize();’'  
	te permite obtener todos los campos de un formulario.  
	<https://api.jquery.com/serialize/>
	
	¿ hay algo parecido para javascript ?

	* **Ian Mande** [746238] (5)

		Si claro. Existe la clase FormData aquí te dejo un blogpost muy bueno sobre el y como usarlo con el método POST [Blog](https://desarrolloweb.com/articulos/fetch-post-ajax-javascript.html)

	* **Jonathan Uriel Jiménez Soveranes** [746238] (1)

		Gracias Ian

* **raulmg** (2) [644819](https://platzi.com/comentario/644819/) 

	Hola,  
	Alguno quisiera compartir el código hasta la clase 12

	* **Diego Alexander Forero Higuera (Platzi)** [644819] (1)

		Hola, lo encuentras en la pestaña de archivos y enlaces en la columna derecha de la clase.

	* **Daniel Esteves** [644819] (1)

		Hola Raul! Todo el código utilizado en la clase y las anteriores lo puedes encontrar en la sección de “Archivos y Enlaces”

* **jesus-gallardo** (2) [602719](https://platzi.com/comentario/602719/) 

	Los eventos son la forma en que detectamos una acción del usuario.
	```     let boton = document.getElementById('btn')
	    
	    boton.addEventListener('click', () => {
	    	//Lo que quieras hacer ciando el usuario de click.
	    	alert('Hiciste click en el boton!')
	    })```
	    
	```

* **jorge-winder-avila** (2) [486613](https://platzi.com/comentario/486613/) 

	<https://developer.mozilla.org/es/docs/Web/Events>

* **Sergio Camilo vanegas** (2) [485774](https://platzi.com/comentario/485774/) 

	Aiudaaaaa, después de esta clase no volvieron a aparecer las imagenes de las películas y sus respectivas descripciones.
	
	No sé qué es lo que puede estar sucediendo…
	```     const $form = document.getElementById('form');
	       $form.addEventListener('submit', (event) => {
	      event.preventDefault()
	       })
	       
	       const actionList = await getData('https://yts.am/api/v2/list_movies.json?genre=action')
	       const dramaList = await getData('https://yts.am/api/v2/list_movies.json?genre=drama')
	       const animationList = await getData('https://yts.am/api/v2/list_movies.json?genre=animation')
	       console.log(actionList, dramaList, animationList)
	    
	       function videoItemTemplate(movie) {
	        return (
	                  `<div class="primaryPlaylistItem">
	                   <div class="primaryPlaylistItem-image">
	                     <img src="${movie.medium_cover_image}">
	                   </div>
	                   <h4 class="primaryPlaylistItem-title">
	                     ${movie.title}
	                   </h4>
	                 </div>`
	        )
	      }
	    
	      /* console.log (videoItemTemplate('src/images/covers/bitcoin.jpg', 'bitcoin' )); */
	      
	      function createTemplate(HTMLString) {
	        const html = document.implementation.createHTMLDocument();
	        html.body.innerHTML = HTMLString;
	        return html.body.children[0];
	      }
	    
	    function addEvenClick($element) {
	     $element.addEventListener('click', () => {
	       alert('click')
	     })
	    }
	    
	      function renderMovieList(list, $container) {
	        //actionList.data.movies
	        $container.children[0].remove();
	        list.forEach((movie) =>{
	          const HTMLString = videoItemTemplate(movie);
	          //se crea un documento html vacío
	          const movieElement = createTemplate(HTMLString)
	          //debugger
	          $container.append(movieElement);
	          addEvenClick(movieElement)
	             })
	      }
	    //Jquery
	       //const $home = $('.home .list #item');
	    //Vanilla JS
	       const $actionContainer = document.querySelector('#action');
	       renderMovieList(actionList.data.movies, $actionContainer);
	    
	       const $dramaContainer = document.getElementById('drama');
	       renderMovieList(dramaList.data.movies, $dramaContainer);
	       const $animationContainer = document.getElementById('animation');
	       renderMovieList(animationList.data.movies, $animationContainer);
	    
	```

	* **Ivan Sevilla** [485774] (3)

		Holaa! me esta sucediendo lo mismo pero tranquilo no es un error de tu código. Si te fijas en la consola seguramente te debe estar arrojando un error 520 o 525 no recuerdo bien, pero en fin corresponde a que el host de la página de la cual queremos traer los datos no esta funcionando.

	* **pablojorgeandres** [485774] (2)

		Jajajja, sí, a mi también me está pasando lo mismo…
		
		Trata de cargar en el browser el URL cualquiera de las categorías y verás:
		```     Error 525 Ray ID: 49ced2ebdaa4c881 • 2019-01-22 03:09:50 	UTC
		    SSL handshake failed
		    You
		    Browser
		    Working
		    Miami
		    Cloudflare
		    Working
		    yts.am
		    Host
		    Error
		    
		    What happened?
		    Cloudflare is unable to establish an SSL connection to the origin server.
		    
		    What can I do?
		    If you're a visitor of this website:
		    Please try again in a few minutes.
		    
		    If you're the owner of this website:
		    It appears that the SSL configuration used is not compatible with 	Cloudflare. This could happen for a several reasons, including nos hared cipher suites. Additional troubleshooting information here.
		    
		    Cloudflare Ray ID: 49ced2ebdaa4c881 • Your IP: 190.189.40.187 • Performance & security by Cloudflare
		    
		```
		
		Este tipo de cosas pasan. O algunas APIs se van volviendo pagas también…

	* **JhonColorado07** [485774] (2)

		Acabo de realizar pruebas y tienes toda la razón, ya no funciona pero te comento que esto es lo que logre averiguar de la api.
		
		**This content has been removed from public view on request.**
		
		En pocas palabras la API fue eliminada para el público.

	* **Sergio Camilo vanegas** [485774] (2)

		Ya volvió la normalidad. Se había caído.

	* **JhonColorado07** [485774] (1)

		Gracias @sergiovanegas, pensé que no iba poder terminar el curso.

	* **Ivan Sevilla** [485774] (1)

		@cantante_carlit si vuelve a pasar, hazte un get de otra api publica. Deberas cambiar algunas otras cosas en tu codigo pero te permitira finalizar el curso, saludos 😃

	* **jbarriospd** [485774] (1)

		Ya la api funciona con normalidad 😃

	* **JhonColorado07** [485774] (1)

		Gracias @ivansevillaa estaba pensando en esa opción. Es más ayer me di a la tarea de buscar otras API públicas de películas.

* **JhonyBurbano08** (2) [422744](https://platzi.com/comentario/422744/) 

	Buena Tarde, porque se utiliza en la function addEventClick($element) el parametro en $ ?

	* **oteka21** [422744] (3)

		Es un estándar personal que usa Leonidas para indicar que esa variable apunta a un elemento del DOM, lo cual es una buena practica para trabajar profesionalmente.  
		Otra buena practica es declarar las constantes en mayusculas.
		```     const $element = document.getElementById('element');
		    const NOMBRE = 'Leonidas Esteban';
		    
		```

* **MatiasBZ** (2) [62637](https://platzi.com/comentario/643626/) 
Como puedo manejar los erores en javascript con async await . En el caso de no usar .then() ni .cach() ???

	* **Favio Náquira** [62637] (2)

		Debes utilizar try y catch
		```     try{
		    	await myFuncion();
		    } catch(error){
		    	console.log(error)
		    }
		    
		```

* **JhonyBurbano08** (2) [45673](https://platzi.com/comentario/422744/) 
Buena Tarde, porque se utiliza en la function addEventClick($element) el parametro en $ ?

	* **oteka21** [45673] (3)

		Es un estándar personal que usa Leonidas para indicar que esa variable apunta a un elemento del DOM, lo cual es una buena practica para trabajar profesionalmente.  
		Otra buena practica es declarar las constantes en mayusculas.
		```     const $element = document.getElementById('element');
		    const NOMBRE = 'Leonidas Esteban';
		    
		```

* **salajhon69** (1) [1006008](https://platzi.com/comentario/1006008/) 

	```    <header>
	            <divclass="header">
	                <h1>Movies.es</h1>
	                <formaction=""id="form" >
	                    <inputtype="text"placeholder="Buscar ahora">
	                </form>
	                <divclass="perfil">
	                    <h2>Albeiro Frito</h2>
	                    <imgstyle="width: 50px; height: 50px; border-radius: 50%;"src="dog.jpg"alt="">
	                </div>
	            </div>
	        </header>```
	    
	```

* **lmoran** (1) [996761](https://platzi.com/comentario/996761/) 

	interesante.

* **Kenet Chungandro** (1) [994292](https://platzi.com/comentario/994292/) 

	This is amazing!

* **Kenet Chungandro** (1) [994290](https://platzi.com/comentario/994290/) 

	Esos formularios que recargaban la página llegaron a su fin -_- 😃

* **pinfradev** (1) [953635](https://platzi.com/comentario/953635/) 

	En el addEventListener la función que se ejecuta cuando el evento ocurre recibe un objeto de evento como primer parámetro. El tipo de objeto de evento depende del evento especificado. Por ejemplo, el evento click pertenece o se relaciona con el objeto MouseEvent. En el siguiente enlace pueden encontrar una lista de eventos con los objetos de evento asociados  
	<https://www.w3schools.com/jsref/dom_obj_event.asp>

* **Jose94** (1) [952403](https://platzi.com/comentario/952403/) 

	Adiós jQuery

	* **carlosextra1** [952403] (1)

		Ya se que triste!! pero tenemos que evolucionar jejeje

* **Camilo Alexander Velandia Velandia** (1) [947673](https://platzi.com/comentario/947673/) 

	excelente clase

* **asismelgarejo** (1) [883276](https://platzi.com/comentario/883276/) 

	RECORDATORIO

* **Jhoan Steve Sandoval Meneses** (1) [841694](https://platzi.com/comentario/841694/) 

	```    $my_btn.addEventListener('click', (event) => {
	        //que tengo que hacer para esta método no retorne = windows
	       //si no el elemento seleccionado 
	        console.log(this);
	    });
	    
	```

	* **Ronald  Gamaliel Perez Talavera** [841694] (1)

		ponerle ante .bind(this)

* **Camilo Andrés Santana Lizcano** (1) [687862](https://platzi.com/comentario/687862/) 

	¿Si usamos el metodo event.preventDefault(), el método POST se sigue ejecutando ?

	* **Enrique Felix Garcia Stave** [687862] (3)

		Claro que si, el metodo que mencionas solo es para que el navegador no salte a la URL que le designas por medio de fetch, AJAX, etc.

* **Renier Ferrer** (1) [674947](https://platzi.com/comentario/674947/) 

	Excelente clase…

* **Jose Luis Colmenares** (1) [673536](https://platzi.com/comentario/673536/) 

	Evento relacionado con el formulario

* **Gustavo83** (1) [629541](https://platzi.com/comentario/629541/) 

	Un evento es el producto de la interacción entre el usuario y el programa, un click, un submit, posicionarse sobre un elemento y todo lo imaginable es un evento.  
	$(‘button’).on(‘click’, notify) => Evento en JQuery  
	$element.addEventListener(‘click’ , function()) => Evento en JS
	
	  * Porque la página recarga cada vez que hacemos un submit? => Por defecto el formulario envía cualquier actualización de algún input, a través de los métodos GET o POST dependiendo del input, de modo a que el backend pueda visualizarlos y enviar al servidor para actualizarlo, se evita el mismo con event.preventDefault()

* **Aram Ceballos Gonzalez** (1) [609904](https://platzi.com/comentario/609904/) 

	Una caracteristica de las arrow functions es que si recibe un solo parametro se pueden quitar los parentesis. Ejemplo.
	
	Con parentesis
	```     $form.addEventListener('submit', (event) => {
	        event.preventDefault()
	      })```
	    
	    Sin parentesis
	    
	```
	
	$form.addEventListener(‘submit’, event => {  
	event.preventDefault()  
	})```

* **Raul Alfaro** (1) [556957](https://platzi.com/comentario/556957/) 

	Hola, tengo un problema y no entiendo el porque, en la parte de asignar una funcion a cada pelicula con el addEventListener yo trato de hacerlo manipulando cada children[i] del contendedor, ya que estoy usando el innerHTML del mismo, pero solo lo asigna a la ultima pelicula por alguna razon, Alguien que me pueda decir porque porfavor
	```     function videoItemtemplate(src,title){
	        return(  
	                `<div class="primaryPlaylistItem">
	                <div class="primaryPlaylistItem-image">
	                  <img src="${src}">
	                </div>
	                <h4 class="primaryPlaylistItem-title">
	                  ${title}
	                </h4>
	                </div>`
	              )
	      }
	    
	      function renderMoviesList(list,$container){
	        $container.children[0].remove()
	        list.forEach( (item,index) => {
	            const HTMLStrig = videoItemtemplate(item.homeworld,item.name)
	               
	            $container.innerHTML +=HTMLStrig;
	            addEventClick( $container.children[index],item.name );
	            //console.log($container.children[index])
	            
	        });
	        
	      }
	    
	```
	
	pd: estoy usando SWAPi solo con fines de aprendizaje ya que la de [yts.am](http://yts.am) no esta disponible donde yo vivo, gracias

	* **Angel Hernandez** [556957] (1)

		Hola 😄 debido a que no puedo observar su funcionamiento global no se muy bien lo que sucede, puede que el error se deba a la manera en que se agrega el html y el tiempo que se demora el navegador en procesarlo y considerar al nuevo elemento como un children, quizá podrías probar la función [insertAdjacentElement](https://developer.mozilla.org/es/docs/Web/API/Element/insertAdjacentElement)

	* **Angel Hernandez** [556957] (1)

		Disculpa, es [insertAdjacentHTML](https://developer.mozilla.org/es/docs/Web/API/Element/insertAdjacentHTML)

	* **Raul Alfaro** [556957] (2)

		Gracias Angel998 por tu respuesta, solo para que puedas mirar el codigo completo aqui te va
		```     (async function cargar(){
		      
		      
		      async function getData(url,tipo){
		        
		        sw = await fetch(url+tipo);
		        swData = await sw.json();
		        return swData
		      }
		    
		      function videoItemtemplate(src,title){
		        return(  
		                `<div class="primaryPlaylistItem">
		                <div class="primaryPlaylistItem-image">
		                  <img src="${src}">
		                </div>
		                <h4 class="primaryPlaylistItem-title">
		                  ${title}
		                </h4>
		                </div>`
		              )
		      }
		    
		      function renderMoviesList(list,$container){
		        $container.children[0].remove()
		        list.forEach( (item,index) => {
		            const HTMLStrig = videoItemtemplate(item.homeworld,item.name)
		               
		            $container.innerHTML +=HTMLStrig;
		            addEventClick( $container.children[index],item.name );
		            //console.log($container.children[index])
		            
		        });
		        
		      }
		      
		      function addEventClick($element,msg){
		        $element.addEventListener('click',()=>alert(msg))
		        console.log($element)
		      }
		        
		      
		      const url = 'https://swapi.co/api/'
		      
		      const ppl = await getData(url,'people')
		      const species = await getData(url,'species')
		      const starships = await getData(url,'starships')   
		      
		      const $featuringContainer = document.getElementById('featuring')
		      const $form = document.getElementById('form')
		      const $home = document.getElementById('home')
		    
		      const $actionContainer = document.getElementById('action')
		      const $dramaContainer = document.getElementById('drama')
		      const $animationContainer = document.getElementById('animation')
		    
		      const $modal = document.getElementById('modal')
		      const $overlay = document.getElementById('overlay')
		      const $hideModal = document.getElementById('hide-modal')
		      
		      const $modalTitle = $modal.querySelector('h1')
		      const $modalImg = $modal.querySelector('img')
		      const $modalDesc = $modal.querySelector('p')
		    
		      
		      
		      
		      renderMoviesList(ppl.results, $actionContainer)
		      renderMoviesList(species.results, $dramaContainer)
		      renderMoviesList(starships.results, $animationContainer)
		      //console.log(ppl.results,planets.results,starships.results)
		      
		      
		      
		    }
		    )() 
		    
		```
		
		Mas que encontrar otra manera de hacer que esto funcione me gustaria saber porque no funciona ya que se puedo aprender mas de estos errores  
		muchas gracias

	* **Rodrigo Cuenca Mayuri** [556957] (1)

		@bikrae encontré una solución. Al parecer con el código que pasaste solo se activa el ultimo hijo del container. Yo utilicé un for normal y funcionó y no sé el por qué la verdad. En fin… dejo el código.
		```     function renderMovieList(list,$container)
		      {
		        const gif=$container.querySelector("img");
		        gif.remove();
		    
		        list.forEach((movie,index)=>{
		    
		          const HTMLString=videoItemTemplate(movie)
		          const movieElement=createTemplate(HTMLString,$container)
		          for(let i=0;i<=index;i++)
		          {
		            addEventClick($container.children[i])
		          }
		        })
		      }
		    
		```

* **Jhon_Marin** (1) [540559](https://platzi.com/comentario/540559/) 

	Gran curso, me encantaaaaaaa, me siento como niño chiquito jajaja ❤️

* **Félix Alejandro Zelaya Orellana** (1) [530971](https://platzi.com/comentario/530971/) 

	Genial!!

* **Alfonso Navarro** (1) [487329](https://platzi.com/comentario/487329/) 

	Buenas a todos,
	
	En el proceso de pasar Jquery a JSVanilla me encontre con este caso:
	
	Al crear un DataTable es normal q cada fila existan botones de Crear, Editar, Actualizar o Eliminar, y se le deben agregar Eventos a estos botones.
	
	En Jquery
	```     $('#gridUser tbody').on('click', 'td button', function (){
	        //todo
	    });```
	    
	    En JavaScript Vanilla e intentado varias formas pero aun no doy 
	    
	    cuando intento 
	    
	    
	    
	    
	```
	
	document.getElementById(’.elemento’).addEventListener(‘click’, function(){  
	//codigo  
	})```
	
	No funciona solo toma un boton, pero no los toma todos.
	
	Alguien a trabajado ya en este caso??
	
	De antemano, gracias.

	* **cris-medina** [487329] (1)

		Hola Alfonso, no se si ya resolviste este problema pero te sugiero que en vez de usar getElementById() utiliza getElementByClassName(), debido a que el id es único y HTML reconoce solo el último elemento con dicho id.

	* **Alfonso Navarro** [487329] (1)

		Compañero muchas gracias por su respuesta, lo q hice fue colocarle un onClick a cada registro.

* **JhonColorado07** (1) [483914](https://platzi.com/comentario/483914/) 

	Excelente lo de submit para no recargar la página.

* **Alfonso Navarro** (1) [477582](https://platzi.com/comentario/477582/) 

	Excelente clase Leonidas, esta clase como todas han sido muy utiles.

* **Kevin Brayan Luna Figueroa** (1) [437921](https://platzi.com/comentario/437921/) 

	Una pregunta para asignar a dos selectores un evento en jQuery es así:
	```     $('.abrir,.cerrar').click(function(){
	    
	    });
	    
	```
	
	Como puedo poner 2 selectores con JavaScript??  
	O tengo que hacer esto??
	```     button.addEventListener('click', () => {
	    	alert('abrir');
	    });
	    button2.addEventListener('click', () => {
	    	alert('abrir');
	    });
	    
	```

	* **pablojorgeandres** [437921] (2)

		Hola @kevinbrayanluna, lamentablemente el método .addEventListener() no acepta más de un elemento.  
		De todas maneras tenés dos maneras de aproximarte al asunto.
		
		  * Podés usar un array y un for:
		```     	const arrayDeElementos = document.querySelectorAll('div');
		    
		    	arrayDeElementos.forEach( (elem) => {
		    	    elem.addEventListener( ' click', () => {
		    	        // lo que quieras hacer ...
		    	    });
		    	});
		    
		    
		```
		
		  * O podés usar lo que se llama en inglés **Event Bubbling** que es básicamente escuchar todos los eventos de la página agregando el listener al _document_.
		```     document.addEventListener('click', (event) => {
		        if ( event.target.classList.contains( 'classX' ) ) {
		            // lo que quieras hacer ...
		        } elseif (event.target.classList.contains( 'classZ' ) ) {
		    	// lo que quieras hacer ...
		        }
		    }, false);
		    
		    
		```

* **Diego Fernando Rojas Quintero** (1) [85990](https://platzi.com/comentario/1086439/) 
donde encuentro ejercicios para practicar sobre Eventos, y aprender más de ellos?

	* **Wonder Jhonny Diaz Gonzalez** [85990] (1)

		Hola, Aquí puedes ver un listado de eventos y aprender más sobre cada uno [Event](https://developer.mozilla.org/es/docs/Web/Events)
		
		Saludos

* **Diego Fernando Rojas Quintero** (1) [85989](https://platzi.com/comentario/1086418/) 
No entiendo bien que hace el evento submit? ¿Busca algo en la base de datos o que?

	* **Sneyder Alfonso Barreto Buitrago** [85989] (2)

		¡Hola!  
		El evento _**submit**_ se dispara cada vez que un formulario es enviado dentro de la página. Para darte más contexto, el formulario se envía cada vez que Leonidas escribe algo en la barra de búsqueda y preciona _enter_.  
		Simplemente es eso, lo que pasa luego de que se dispara el evento dependerá de la lógica de tu aplicación y lo que quieras que ocurra. En el caso del curso, lo que se hará es una petición a la API para buscar una película en específico.  
		Espero te sirva, un saludo.

* **wlm.morant** (1) [84434](https://platzi.com/comentario/1050872/) 
jquery esta decadencia??? , ya que un curso de jquery y no hay profundida por lo que veo es para conocer ya que vamos encontrar eso en pa...

	* **Gabriel De Andrade (Platzi)** [84434] (1)

		jQuery es cada vez menos utilizado en la industria, este Curso se enfoca en demostrar como ahora con JavaScript Vanilla podemos hacer casi todas las cosas que hacíamos con jQuery. 😄

* **Ang.O** (1) [83616](https://platzi.com/comentario/1030860/) 
donde esta $element no evo la variable en ningun lado

	* **raziel.carvajal** [83616] (1)

		Podrías poner el código para ayudarte.

* **Raul Alfaro** (1) [56788](https://platzi.com/comentario/556957/) 
Hola, tengo un problema y no entiendo el porque, en la parte de asignar una funcion a cada pelicula con el addEventListener yo trato de h...

	* **Angel Hernandez** [56788] (1)

		Hola 😄 debido a que no puedo observar su funcionamiento global no se muy bien lo que sucede, puede que el error se deba a la manera en que se agrega el html y el tiempo que se demora el navegador en procesarlo y considerar al nuevo elemento como un children, quizá podrías probar la función [insertAdjacentElement](https://developer.mozilla.org/es/docs/Web/API/Element/insertAdjacentElement)

* **Sergio Camilo vanegas** (1) [50970](https://platzi.com/comentario/485774/) 
Aiudaaaaa, después de esta clase no volvieron a aparecer las imagenes de las películas y sus respectivas descripciones. No sé qué es lo q...

	* **Ivan Sevilla** [50970] (3)

		Holaa! me esta sucediendo lo mismo pero tranquilo no es un error de tu código. Si te fijas en la consola seguramente te debe estar arrojando un error 520 o 525 no recuerdo bien, pero en fin corresponde a que el host de la página de la cual queremos traer los datos no esta funcionando.

## 0130. Clases y estilos CSS [12305](https://platzi.com/clases/1316-jquery-js/12305-clases-y-estilos-css/)

### Descripción:


En esta clase vamos a aprender a manipular las clases de CSS y estilos de nuestros elementos mediante JavaScript.  
Dentro de cada elemento tenemos un método llamado classList, con este podemos ver las clases que tiene nuestro elemento y además llamar a otros métodos para añadir, borrar o hacer toggle a alguna clase.  
De igual forma podemos acceder a todas las propiedades de CSS algún elemento mediante element.style.

### Comentarios:

* **Sergio Minei** (44) [335862](https://platzi.com/comentario/335862/) 

	 **Truquitos con el Chrome Dev Tools**
	
	`$0` para coger al elemento seleccionado.
	
	Ahora todos vamos a querer un curso de **Chrome Dev Tools** 😄

	* **Leonidas Esteban Gonzalez** [335862] (34)
También lo estaba pensando

	* **samvarcia** [335862] (7)

		Buena idea

	* **Sergio Minei** [335862] (6)

		Creo que nos ayudaría un montón

	* **Jhon edison lozano Montenegro** [335862] (4)

		uff seria genial!!

	* **Oscar Barajas Tavares (Platzi)** [335862] (5)

		genial

	* **Yerson Arce Arbildo** [335862] (5)

		No es signo de pesos es signo de DOLAR

	* **Starling Mercedes** [335862] (3)

		yo tambien quisiera un curso de chrome dev tools, y aprender a medir el perfomance

	* **tomas-alegre146** [335862] (3)

		good

	* **SEBASTIAN PADUANO** [335862] (3)

		gracias saludos

	* **pablojorgeandres** [335862] (4)

		Sí!  
		Sería muy interesante y útil un curso de **chrome dev tools**.

	* **emanueljtc** [335862] (2)

		+1 me gusta la idea. pero que el curso lo de Leonidas.

* **Sergio Minei** (14) [335865](https://platzi.com/comentario/335865/) 

	## Clases y estilos CSS
	
	**Clases**
	```     //agrega una clase
	    $element.classList.add("clase");
	    
	    //remueve una clase
	    $element.classList.remove("clase");
	    
	    //intercambia entre agregar y remover una clase
	    $element.classList.toggle("clase");
	    
	```
	
	**Estilos Inline**
	```     $modal.style.animation = "modalOut .8s forwards";
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

* **emanueljtc** (12) [589723](https://platzi.com/comentario/589723/) 

	Sin ánimos de nada pero Leonidas es uno de los los profesores que mejor enseña acá en Platzi. al menos en su area.

	* **GUIFOR PAUL PLAZA AGUIRRE** [589723] (3)

		X2

* **oteka21** (12) [340090](https://platzi.com/comentario/340090/) 

	Creo que agregando un setTimeOut para esconder el overlay se puede obtener un buen efecto visual:
	```     function hideModal(){
	    		setTimeout(()=>{
	    		$overlay.classList.toggle('active');
	    		},1000);
	    		$modal.style.animation = 'modalOut .8s forwards';
	    	}
	    
	```

	* **heysoypaez** [340090] (1)

		Lo incorpore en mi codigo! Realmente creo que da un buen efecto visual

	* **Facundo Nicolás García Martoni (Platzi)** [340090] (1)

		Lo usé también, muchas gracias 😉

* **Juan David Castro (Platzi)** (11) [341135](https://platzi.com/comentario/341135/) 

	Todos vamos por el curso de **_debugging_** 💪

	* **Oscar Barajas Tavares (Platzi)** [341135] (5)

		¿Que te gustaría ver en un curso de debugging?

	* **Juan David Castro (Platzi)** [341135] (3)

		Con Leo! :fire

	* **Joel Alejandro Morales García** [341135] (3)

		X2 😃

* **Pablo Colqui** (8) [535508](https://platzi.com/comentario/535508/) 
	
	![](https://static.platzi.com/media/user_upload/Anotaci%C3%B3n%202019-03-14%20174444-83b66348-05d5-4ac6-9a21-fb154812374c.jpg)
	
	Me aparece ese espacio en blanco al lado del featuring y no logro ver por que. Ya probe descargando todos los archivos del curso y sigue apareciendo.  
	Logre hacerlo desaparecer por defecto agregandole:
	```     .home {
	    	overflow: hidden;
	    }
	    
	```
	
	Igual no creo que sea eso lo que hayan aplicado, alguna sugerencia?

	* **Yezid Garcia Medina** [535508] (1)

		Acabo de encontrar el Featuring y ahora me pasa igual, aparece ese rectangulo blanco a la izquierda 😦

	* **Facundo Nicolás García Martoni (Platzi)** [535508] (1)

		Es natural que suceda ese error, yo lo solucioné poniendo un ‘display: none’ por defecto al featuringContainer, y añadiendo en el evento de submit un ‘display:grid’

	* **Alejandro Sáez** [535508] (1)

		Yo lo arregle cuando me di cuenta que en la constante $featuringContainer le había puesto un #.
		
		Se arreglo quitandole el #
		```     const $featuringContainer = document.getElementById('featuring');
		    
		```

* **JsOR** (4) [1020270](https://platzi.com/comentario/1020270/) 

	Que nos falte todo, pero que no nos falte Leonidas en Platzi. El mejor maestro desde mi punto de vista

* **Juan Gaybre Flores** (4) [930445](https://platzi.com/comentario/930445/) 

	Después de quitar ‘search-active’, el home-featuring se seguía mostrando hasta abajo, por fuera de todo lo demás.  
	.  
	Se debe jugar un poco con lo que ya se ha visto en la misma clase, yo agregué display: none en .home-featuring y después al ejecutar el ‘submit’ le agregue la clase .dnone que lleva el display: grid al $featuringContainer para que se muestre nuevamente.
	
	![featuringContainer.JPG](https://static.platzi.com/media/user_upload/featuringContainer-3a239083-847b-4a6c-9db2-fe4ea313aae0.jpg) ![css.JPG](https://static.platzi.com/media/user_upload/css-61fc91bf-baf6-41b3-a65f-735f69d794e5.jpg)

	* **Karla Agraz** [930445] (2)

		Gracias!

	* **Diegoalesco95** [930445] (1)

		También se podría hacer el arreglo directamente cambiando el valor de la propiedad de la siguiente forma
		```     $featuringContainer.style.display = "grid";
		    
		```

	* **hdmjesus** [930445] (1)

		gracias, justo estaba diciendo, y esto qlq xdd

* **Jonatan David Padilla** (3) [349588](https://platzi.com/comentario/349588/) 

	`modalIn` Es una animacion que leonidas creo en una hoja de estilos de CSS por medio de `@keyframes`. para los que utilizan su propio ejemplo y no el de leonidas y se preguntan de donde salio ese `modalIn`, pasa igual con el `modalOut`.  
	muy recomendado en [Curso de animaciones para la web](https://platzi.com/clases/animaciones-web/)

* **oteka21** (3) [340085](https://platzi.com/comentario/340085/) 

	Creo que algo tan simple como la palabra clave debugger puede servir de mucha a la hora de programar en js!!

* **salajhon69** (2) [1005946](https://platzi.com/comentario/1005946/) 

	Las clases con Leonidas son las mejores, es al unico que logro entender bien

* **Kivindlg** (2) [966608](https://platzi.com/comentario/966608/) 

	buenas, lo que pasa es que me aparece este fallo " Uncaught (in promise) TypeError: Failed to fetch " y no e podido llegar al asunto para areglarlo

	* **Germán González** [966608] (1)

		Tienes que enviar el error Completo para poder ayudarte amigo

	* **Kenet Chungandro** [966608] (1)

		Lo que dice es que no se pudo establecer la conexión con la Api, esto se debe a veces a que el link está mal o que la página mismo se cayó, el link que Leonidas usa desde el inicio ya no funciona,  
		estos son los que me funcionan actualmente:  
		<https://yts.mx/api/v2/list_movies.json?genre=action>

* **Camilo Alexander Velandia Velandia** (2) [948372](https://platzi.com/comentario/948372/) 

	genial reutilizar el modal del curso de animaciones para la web

* **Jose Luis Colmenares** (2) [673547](https://platzi.com/comentario/673547/) 

	al escribir $0 en la cónsola del inspector de elementos, el navegador manipula el elemento que está seleccionado

* **Edgar Ramirez** (2) [386399](https://platzi.com/comentario/386399/) 

	de donde sale modaIn modalOut? es una propiedad de animaciones ? la declaramos anteriormente ?

	* **davidtoca (Platzi)** [386399] (3)

		Es una animacion custom, la encuentras en `css/modal.css` en los archivos de ese material

	* **Edgar Ramirez** [386399] (2)

		gracias

* **carlosextra1** (1) [1116132](https://platzi.com/comentario/1116132/) 

	Excelente

* **Juan Diego Luque Linares** (1) [1107528](https://platzi.com/comentario/1107528/) 

	Leonidas y las 300 formas de enseñar ! Gratzie

* **Angélica María Rocha García** (1) [1051254](https://platzi.com/comentario/1051254/) 

	muy buen vídeo y una excelente explicación, entendí varias cosas. Gracias Leonidas mi maestro favorito 😄

* **salajhon69** (1) [1006017](https://platzi.com/comentario/1006017/) 

	El overlay se cierra desde cualquier parte hasta en el panel, no se como hacer para que el panel no sea responsivo a esto
	```     (asyncfunctionload() {
	      // await
	      // action
	      // terror
	      // animation
	      // Funcion dinamica para la url de la api 
	      asyncfunctiongetData(url) {
	        // Una función asíncrona va a ser como una función normal, pero poniendo código asíncrono de forma que sea más fácil de leer de forma síncrona.
	        //Para declarar una función asíncrona se usa la palabra reservada async, luego de eso declaras tu función de forma normal
	        const response = await fetch(url);
	        const data = await response.json()
	        return data;
	      }
	    
	      // try | es lo que hace cuando la operacion se completa satisfactoriamente
	      try{
	    
	    
	        //xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
	        //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	    
	        const overlay = document.getElementById('overlay')
	        const overbuton = document.getElementById('overbuton')
	        const form = document.getElementById('form')
	    
	        // remove hace que se que quite la clase active
	        overbuton.addEventListener('click', (event) => {
	            overlay.classList.remove('active')
	        })
	        // add anade la clase active
	        overlay.addEventListener('click', (event) => {
	            overlay.classList.remove('active')
	        })
	        // preventDefault hace que al buscar algo en el form no se recargue la pagina
	        form.addEventListener('submit', (event) =>{
	            event.preventDefault()
	        })
	    
	        functionAbrirInfoPeli(element) {
	          element.addEventListener('click',() => {
	            overlay.classList.add('active')
	          })
	        }
	    
	        //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	        
	        //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	    
	        // obtener elemento por id
	        const $actionContainer = document.querySelector('#action');
	        const $dramaContainer = document.querySelector('#drama');
	        const $animationContainer = document.querySelector('#animation');
	    
	    
	        // lista de peliculas de la api
	        // la palabra reservada llamada await, lo que hará esta palabra es indicar que se debe esperar a que termine de ejecutarse ese fragmento de código antes de continuar.
	        const actionList = await getData('https://yts.mx/api/v2/list_movies.json?genre=action')
	        const dramaList = await getData('https://yts.mx/api/v2/list_movies.json?genre=comedy')
	        const animationList = await getData('https://yts.mx/api/v2/list_movies.json?genre=animation')
	        
	        console.log('actionList',actionList ,  animationList , dramaList)
	        //----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	        //xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
	      
	        // escribe codigo html en java y lo representa en el navegador
	        functionvideoItemTemplate(movie) {
	          return (
	            `<div class="primaryPlaylistItem" id="primaryPlaylistItem" >
	            <div class="primaryPlaylistItem-image">
	            <img src="${movie.medium_cover_image}" >
	            </div>
	            <h4 class="primaryPlaylistItem-title">
	            ${movie.title}
	            </h4>
	            </div>`
	            )
	        }
	        
	        // Para convertir nuestra plantilla de texto a un Document Object Model necesitamos crear dentro de memoria un documento HTML, 
	        //esto es posible gracias al método document.implementation.createHTMLDocument. A este documento HTML le vamos a añadir al body, mediante innerHTML, 
	        //nuestra plantilla de texto. Una vez añadida le pedimos al body el primer elemento hijo que tenga y este lo añadimos a nuestro container.
	        functioncreatemplate(HTMLString) {
	          const html = document.implementation.createHTMLDocument();
	          html.body.innerHTML = HTMLString;
	          return html.body.children[0];
	        }
	    
	        // lista de peliculas - (container)= encontrar elemento por id
	        functionrendermovielist(list , container) { 
	          container.children[0].remove(); //remover gif (cargando)
	          list.forEach((movie) => {
	    
	            const HTMLString = videoItemTemplate(movie);
	            const movieElement = createmplate(HTMLString);
	            container.append(movieElement)
	            AbrirInfoPeli(movieElement)
	    
	          })
	        }
	        // rendermovielist toma 2 parametros ( ruta de la lista del api , id del elemento )
	        rendermovielist(actionList.data.movies, $actionContainer)
	        rendermovielist(animationList.data.movies, $animationContainer)
	        rendermovielist(dramaList.data.movies, $dramaContainer)
	        // console.log(videoItemTemplate('src/images/covers/bitcoinjpg', 'bitcoin'));
	        console.log('Todo Correcto')
	        
	      }
	      // catch se usa para notificar que hubo un error y el try no se ejecuto
	      catch{
	        console.log('Algo Salio Mal')
	      }
	    
	    
	    
	    
	    })()
	    
	    
	    
	```

	* **Andres Condo** [1006017] (1)

		En el caso de que se cierra desde cualquier lado, creo que es algo que se vera en el siguiente vídeo, y para que sea responsivo pues creo que deberías hacerlo en el CSS aplicando el **@media** dependiendo de la medida en que este la pantalla. mas información de esto en el curso de **Desarrollo web online**

* **arlex_ee10** (1) [1002683](https://platzi.com/comentario/1002683/) 

	Genial anda super la clase :0

* **lmoran** (1) [997215](https://platzi.com/comentario/997215/) 

	Cada vez se va entendiendo más y más.

* **Kenet Chungandro** (1) [994400](https://platzi.com/comentario/994400/) 

	Va quedando genial!

* **Andrés Campuzano Garzón** (1) [982874](https://platzi.com/comentario/982874/) 

	Tengo el error: Access to fetch at ‘<https://yts.am/api/v2/list_movies.json?genre=action>’ from origin ‘<http://127.0.0.1:5500>’ has been blocked by CORS policy: No ‘Access-Control-Allow-Origin’ header is present on the requested resource. If an opaque response serves your needs, set the request’s mode to ‘no-cors’ to fetch the resource with CORS disabled.
	
	He tenido que usar un plugin para solventar esto, solo que necesito encontrar la solucion para poder hacer deploy, sugerencias?

	* **Karla Agraz** [982874] (1)

		Cambia el link por este: <https://yts.mx/api/v2/list_movies.json?genre=action>

* **Kivindlg** (1) [966612](https://platzi.com/comentario/966612/) 

	y tampoco me cargan las peliculas??

	* **Germán González** [966612] (1)

		No estas teniendo conexion con la API, envianos tu codigo para verlo

	* **Karla Agraz** [966612] (2)

		Utiliza este link:

	* **Karla Agraz** [966612] (1)

		<https://yts.mx/api/v2/list_movies.json?genre=action>

* **ronaldjgarciav** (1) [931256](https://platzi.com/comentario/931256/) 

	Super util para crear dinamismo a nuestras web. 😄

* **mxmacias** (1) [866099](https://platzi.com/comentario/866099/) 

	Excelente, buenos Trucos !!

* **Manuel Rivera** (1) [856109](https://platzi.com/comentario/856109/) 

	Trucasoo!!

* **Cristhian Hernandez** (1) [699645](https://platzi.com/comentario/699645/) 

	A alguien mas le queda el cuadro de “pelicula encontrada” afuera del viewport despues de quitar la clase search-active al inicio?

	* **fsanchezcal** [699645] (1)

		a mi

	* **Enrique Felix Garcia Stave** [699645] (1)

		De hecho es por los 150px del segundo row que añade la clase ‘search-active’, puesto que la clase ‘home’ por si sola tiene en 0 esa segunda fila. Creo que habrían algunas maneras de que desapareciera por completo, como colocarle “display: none” a la clase featuring, por ejemplo, y que solo apareciera cuando se usara el buscador.

	* **Manuel Rivera** [699645] (3)

		Lo puedes solucionar modificando el display de la clase featuring, lo pones en none y luego lo vuelves a modificar en el evento submit 😉

* **ponchout** (1) [689863](https://platzi.com/comentario/689863/) 

	Excelente clase

* **poolrosales** (1) [664875](https://platzi.com/comentario/664875/) 

	Excelente.!

* **jesus-gallardo** (1) [602724](https://platzi.com/comentario/602724/) 

	**¡ Que buena clase ! **

* **GUIFOR PAUL PLAZA AGUIRRE** (1) [595013](https://platzi.com/comentario/595013/) 

	Super bien explicado

* **Yezid Garcia Medina** (1) [543226](https://platzi.com/comentario/543226/) 

	A mi no me aparece el Overlay = sigue mostrando el fondo normal, y en cuanto al Modal, este se desaparece tan pronto como apareció, no alcanzo a darle click al boton de abajo 😦 . Ayuda , no sé que hacer?

	* **Cristian Orozco Benjumea** [543226] (3)

		Debes mirar si tienes la clase active en el archivo de modal.css.
		```     .overlay.active {
		    display:flex
		    }
		    
		```

	* **Yezid Garcia Medina** [543226] (1)

		Si lo tengo. pero sigue con el mismo problema 😦

	* **Daniel Esteves** [543226] (1)

		Hola Yezid! Sigues tenendo el mismo problema? Te podemos ayudar en algo?

* **Félix Alejandro Zelaya Orellana** (1) [531000](https://platzi.com/comentario/531000/) 

	Super!!

* **pablojorgeandres** (1) [485556](https://platzi.com/comentario/485556/) 

	## Clases y estilos CSS
	
	HTML se va a encargar de la estructura de nuestro proyecto. CSS de nuestros estilos y JS de la interactividad.  
	Convinando CSS con JS podemos generar una interfaz muy rica e interactiva.
	
	Una herramienta útil para este trabajo es el **Chrome Dev Tool**. En el console, el comando _$0_ se refiere al elemento seleccionado.
	
	Cada elemento del DOM tiene un _classList_ como propiedad en JavaScript. Este _classList_ contiene propiedades.  
	Por ejemplo nos pueden ser útil:
	
	  * _add_ : agrega una clase,
	  * _remove_ : quita una clase,
	  * _toogle_ : agregarla si no está y quitarla si está.
	```     	$element.classList.add('class')
	    	$element.classList.remove('class')
	    	$element.classList.toggle('class')
	    
	    
	```
	
	Para generar estilos inline usamos el método _.style_ :
	```     	$element.style.animation = "modalIn .8s forwards";
	    	$element.style = 'display: none'
	    
	    
	```

* **JhonColorado07** (1) [483937](https://platzi.com/comentario/483937/) 

	Que lindo es la programación en Javascript

* **Juan Cruz Rey** (1) [412866](https://platzi.com/comentario/412866/) 

	Gracias Leonidas, ahora me voy al curso de animaciones al terminar este!

* **crloshro** (1) [408255](https://platzi.com/comentario/408255/) 

	Señores porque me sale este error
	
	[ts] ‘addEventClick’ is declared but its value is never read.  
	(local function) addEventClick($element: any): void

	* **Jcion** [408255] (1)

		¿al final que era?

	* **Daniel Esteves** [408255] (1)

		Hola @crloshro sigues teniendo el mismo problema? Suele ser un error con la interpretación de JS en el navegador o en tu editor de código que usa un linter para avisarte

* **Byhako** (1) [342829](https://platzi.com/comentario/342829/) 

	@LeonidasEsteban, ¿porqué usas el punto y coma al final de cada línea, si las nuevas especificaciones de JS no lo necesitan?

	* **Miguel Cobas** [342829] (3)

		Creo que es mas por costumbre

	* **Nicolás Jara** [342829] (2)

		Creo que se considera buena práctica de alguna forma, son muchos los lenguajes que utilizan el **punto y coma** , y facilitan de alguna forma la lectura del código al saber cuando terminas una línea o un bloque.

	* **Diego Alexander Forero Higuera (Platzi)** [342829] (6)

		Como lo mencionan los compañeros no es algo obligatorio desde hace ya un par de versiones de JavaScript sin embargo es una buena práctica, es como un estándar al momento de escribir código, si trabajas con más desarrolladores el ideal es que tengan un acuerdo si lo usan o no, nada se va a romper.

	* **Alejandro Velásquez Molina** [342829] (1)

		Buena practica, por gusto y estandar, que algo no sea necesario no significa que es obligatorio no usarlo, por lo general yo también prefiero usar ;

	* **Byhako** [342829] (1)

		<https://standardjs.com/>
		
		El estandar dice que ya no se usan.

	* **Diego Alexander Forero Higuera (Platzi)** [342829] (8)

		<https://standardjs.com/> es una librería para linter del lenguaje, no es la guía oficial de estilo del lenguaje, para este linter recomiendan no usar `;`.

	* **pablojorgeandres** [342829] (2)

		Ciertamente está en desuso el ‘;’ .  
		En general javascript no lo necesita para funcionar correctamente, “es opcional”.  
		Pero quiero agregar que sí hay dos situaciones donde de no usarlo, el código nos va a arrojar error.
		
		  * 1 - cuando comienzo una nueva línea escribiendo un array:
		```     		console.log('Lorem ipsum dolor...') ; 
		    		[1, 2, 3].forEach( n => {console.log(n * 2)} )
		    
		    		// también se puede usar al inicio de la nueva línea
		    		console.log('Lorem ipsum dolor...')
		    		; [1, 2, 3].forEach( n => {console.log(n * 2)} )
		    
		    
		```
		
		  * 2 - Si comienzo una línea con comillas invertidas “`”, en la línea anterior o al principio de la misma va “;”.
		```     	const x = console.log('Lorem ipsum dolor...') ;
		    	`${nombre} ipsum dolor.`
		    
		    	//también se puede usar al inicio de la nueva línea
		    	const x = console.log('Lorem ipsum dolor...')
		    	; `${nombre} ipsum dolor.`
		    
		    
		```
		
		Otra situación que puede dar problemas es el salto de línea con enter.  
		Inmediatamente después de un _‘return’_ un salto de línea es interpretado como **“;”**.  
		Si le doy un enter a la llave que viene después de este da error.
		```     	// Esta manera funciona
		    	functioncalcularDoble(numero) {
		    		return {
		    			original: numero, doble: numero * 2
		    		}
		    	}
		    
		    	// ERROR
		    	functioncalcularDoble(numero) {
		    		return// ...enter aquí...
		    		{
		    			original: numero, doble: numero * 2
		    		}
		    	}
		    
		    
		```
		
		Y todavía otra (que me crucé al inicio de este curso); es al llamar una función que se auto-ejecute [ _(function myFunction(){} )()_ ] después de un string con acento grave o comillas invertidas _" ` "_.
		```     	const str = `lorem ipsum dolor`
		    
		    	( functionmyFunction(){
		    		alert('Si usas ";" sí funciono!')
		    	})()
		    	
		    	// Uncaught TypeError: "lorem ipsum dolor" is not a function at <anonymous>:4:2
		    
		    
		```
		
		La forma correcta sería:
		```     	const str = `lorem ipsum dolor` ;
		    	( functionmyFunction(){
		    		alert('Si usas ";" sí funciono!')
		    	})()
		    
		    	// ó
		    	const str = `lorem ipsum dolor`
		    	; ( functionmyFunction(){
		    		alert('Si usas ";" sí funciono!')
		    	})()
		    
		    
		```

	* **pablojorgeandres** [342829] (1)

		Perdón, acabo de chequear y esta última excepción es con cualquier tipo de comillas " , ’ y `.

* **Diego Fernando Rojas Quintero** (1) [86410](https://platzi.com/comentario/1095416/) 
si yo tengo una etiqueta de html solamente con id y sin ninguna clase. ¿La propiedad classList no podrá leer el identificador id para así...

* **Ang.O** (1) [74828](https://platzi.com/comentario/859739/) 
2:25 porque cuando quito la clase search active el grid se extendio?

* **Gustavo83** (1) [61954](https://platzi.com/comentario/630656/) 
Definiciones básicas, el trío perfecto HTML => Se encarga de la estructura del proyecto CSS => estilos y personalizaciones a los el...

	* **Edward Steven Ramos Palacios** [61954] (1)

		Buen resumen.  
		Ahora, hoy por hoy con los frameworks/librerias como react, relativamente todo es javascript. Asi que es indispensable que tengas unas bases muy sólidas en el lenguaje.

* **Yezid Garcia Medina** (1) [55619](https://platzi.com/comentario/543226/) 
A mi no me aparece el Overlay = sigue mostrando el fondo normal, y en cuanto al Modal, este se desaparece tan pronto como apareció, no al...

	* **Cristian Orozco Benjumea** [55619] (3)

		Debes mirar si tienes la clase active en el archivo de modal.css.
		```     .overlay.active {
		    display:flex
		    }
		    
		```

* **Pablo Colqui** (1) [54913](https://platzi.com/comentario/535508/) 
Me aparece ese espacio en blanco al lado del featuring y no logro ver por que. Ya probe descargando todos los archivos del curso y sigue...

	* **Yezid Garcia Medina** [54913] (1)

		Acabo de encontrar el Featuring y ahora me pasa igual, aparece ese rectangulo blanco a la izquierda 😦

* **crloshro** (1) [44468](https://platzi.com/comentario/408255/) 
Señores porque me sale este error [ts] ‘addEventClick’ is declared but its value is never read. (local function) addEventClick($element: ...

	* **Jcion** [44468] (1)

		¿al final que era?

* **Edgar Ramirez** (1) [42402](https://platzi.com/comentario/386399/) 
de donde sale modaIn modalOut? es una propiedad de animaciones ? la declaramos anteriormente ?

	* **davidtoca (Platzi)** [42402] (3)

		Es una animacion custom, la encuentras en `css/modal.css` en los archivos de ese material

* **Byhako** (1) [38308](https://platzi.com/comentario/342829/) 
@LeonidasEsteban, ¿porqué usas el punto y coma al final de cada línea, si las nuevas especificaciones de JS no lo necesitan?

	* **Miguel Cobas** [38308] (3)

		Creo que es mas por costumbre

## 0140. Creación de elementos y asignación de atributos [12306](https://platzi.com/clases/1316-jquery-js/12306-creacion-de-elementos-y-asignacion-de-atributos/)

### Descripción:


Vamos a crear un elemento HTML sin usar un template string. Para crear el elemento desde cero vamos a usar el método document.createElement, este recibe como parámetro la etiqueta html del elemento que se quiere crear, no funciona mandándole el template string.  
Para añadirle un atributo al elemento que acabamos de crear haremos uso del método setAttribute. Este recibe dos parámetros, uno indicando el nombre del atributo que vamos a añadir y el segundo parámetro indicando el valor de dicho atributo.  
Vamos a crear una función para poder añadir múltiples atributos a un solo elemento.

### Comentarios:

* **Juan David Castro (Platzi)** (40) [341160](https://platzi.com/comentario/341160/) 

	🤕 😅 **Resumen de la clase** :
	
	![](https://cdn.memegenerator.es/imagenes/memes/full/29/63/29639070.jpg)

	* **David Daniel Castillo Nava** [341160] (4)

		jajajajaja **_buenisimo_** cosas que pueden pasar cuando usan mas jquery que javascript

	* **Joel Alejandro Morales García** [341160] (4)

		Jajaj truestory

	* **Jorge-Delfino** [341160] (6)

		Cursos de Leonidas + memes = Felicidad asegurada

	* **Alan Santiago** [341160] (3)

		JAJAJAJA

	* **jesusmurf** [341160] (2)

		A mi también me suele suceder bastante 😂

	* **tomas-alegre146** [341160] (1)

		link roto :c

	* **Juan David Castro (Platzi)** [341160] (18)

		@tomas-alegre146
		
		![](https://cdn.memegenerator.es/imagenes/memes/full/30/13/30137059.jpg)

	* **SEBASTIAN PADUANO** [341160] (1)

		gracias

	* **Johan_Marcel** [341160] (1)

		🤣🤣🤣

	* **emanueljtc** [341160] (1)

		Jajajaja “Leo Basta”

	* **alexpaucar14** [341160] (1)

		No lo vi :C

	* **jesus-gallardo** [341160] (1)

		ajajajja

* **Sergio Minei** (11) [335900](https://platzi.com/comentario/335900/) 

	## Creación de elementos
	```     const $loader = document.createElement("img");
	    
	```
	
	## Asignación de Atributos
	
	**jQuery**
	```     $("#element").attr({
	      src: "",
	      height: ""
	    })
	    
	```
	
	**JavaScript**
	```     const $element = document.getElementById("element");
	    
	    //setear el atributo en un elemento DOM
	    $element.setAttribute("src", "img/foto.png");
	    
	    //obtener un atruto de un elemento DOM
	    const src = $element.setAttribute("src")
	    
	```
	
	También se puede crear una función para asignar múltiples atributos a un elemento DOM.
	```     functionsetAttributes($element, attributes) {
	      for(const attribute in attributes) {
	        $element.setAttribute(attribute, attributes[attribute]);
	      }
	    }
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **Sergio Minei** [335900] (6)

		Hola @blackhawk! Yo también pensé lo mismo, pero si te das cuenta Leonidas también usa `const` y le funciona! 😮
		
		Lo que me parece que sucede es que cada vez que itera el `for` se está declarando la variable `const attribute` y como dentro del for, attribute no cambia, entonces no se genera el error. 😉

	* **Juan García** [335900] (4)

		@MineiToshio Hice una prueba en la cosola del navegador y funcionó. Yo también pienso lo mismo que se declara la constante. Gracias por la respuesta. Un saludo

	* **Sergio Minei** [335900] (3)

		Genial! Siempre aprendemos cosas nuevas! 😄

	* **Alejandro González Reyes** [335900] (3)

		Señores permitanme corregir su error.  
		La constante se declaran a nivel de bloque, por tanto al inicio es valido asignarle un valor… posteriormente itera una vez dicho bloque y es posible acceder al valor de dicha constante — al finalizar la primera iteración, la constante se destruye — y nuevamente al iniciar la segunda iteración — la constante es creada y nuevamente inicializada — se repite el proceso.

	* **Facundo Nicolás García Martoni (Platzi)** [335900] (4)

		@jsconestilo Dijiste lo mismo que los demás, pero con más elegancia 😆 Como diría Freddy, un “purista”

* **Gabriel De Andrade (Platzi)** (10) [410684](https://platzi.com/comentario/410684/) 

	Para los que les costó entender la parte del “for in”
	
	El for in funciona de la siguiente manera
	```     for (variablein objeto) { ... }
	    
	```
	
	Si lo traducimos de alguna manera al español sería
	
	  * “Para cada variable que existe en un objeto { . . . }”
	
	Entonces en este caso específico que tenemos
	```     function setAttributes($element, attributes) {
	        for (const attribute in attributes) {
	          $element.setAttribute(attribute, attributes[attribute]);
	        }
	      }
	    
	```
	
	Si lo desglosamos vemos que
	
	  * Primero la función recibe el elemento que se le va añadir al atributo y luego una varible que almacena el atributo a colocar y su correspondiente valor
	```     function setAttributes($element, attributes);
	    
	```
	
	  * Dentro de la funcion a la que le pasamos los parametros nos encontramos obviamente con el ciclo “for in” que se va a ejecutar para asignar los atributos al elemento dinámicamente
	```     for (const attribute in attributes)
	    
	```
	
	  * En esta línea específica lo que pasa es que se crea una constante con el valor de el valor del elemento que se esté iterando, es decir el valor del elemento que el for esté leyendo en ese momento, y esta constante contiene tanto el atributo como el valor que se le va a asignar
	```     $element.setAttribute(attribute, attributes[attribute]);
	    
	```
	
	  * Aquí y quizás la parte más compleja estamos llamando a la función `setAttribute` que recibe `(atributo, valor)` (Como explica Leonidas), entonces si lo vemos en el código estamos llamando al la constante que se creó en el ciclo `for` (`attribute`) que contiene, el atributo y valor a asignar, pero si llamamos a esta constante, lo que nos va a devolver es el nombre del atributo a cambiar por ejemplo: `src`. Y para llamar al valor de ese atributo (Again, como lo explica Leonidas) tenemos que llamar al segundo parámetro de la función allá arriba (Que contiene todos los atributos y valores a asignar) y entre corchetes ponerle el atributo al cual queremos asignarle el valor algo así como `todosLosAtributos[atributoEspecifcoACambiar]` .
	
	Espero haber aportado a la comunidad y haciendole más fácil a las personas entender esto
	
	Source: [MDN](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/for...in)

	* **Facundo Nicolás García Martoni (Platzi)** [410684] (1)

		Excelente explicación!

	* **horacio-garcia** [410684] (1)

		Gracias

* **Carlos Alex Becerra Chavita** (4) [723202](https://platzi.com/comentario/723202/) 

	Uno de los mejores curso donde me siento como en mi salsa jajajajajaja excelente curso

* **Aaron Felipe Isaacs** (4) [626587](https://platzi.com/comentario/626587/) 

	Aquí otra forma encontré de hacer el for al objeto
	```     constaddAttributes = (element, attributes) => {
	        Object.keys(attributes).forEach(attr => element.setAttribute(attr, attributes[attr]))
	      }
	    
	```

* **Yezid Garcia Medina** (4) [543242](https://platzi.com/comentario/543242/) 

	Que confusión con esos nombres de variables y funciones: atributo en singular, en plural, como función local por el usuario y como función propia de html 😄

* **arlex_ee10** (3) [1002761](https://platzi.com/comentario/1002761/) 

	jajaja :V ya me estoy enamorando de HTML , CCS y JS

* **lmoran** (3) [997801](https://platzi.com/comentario/997801/) 

	Despues de ver 6 veces el video e investigando al fin lo pude compreder correctamente.

* **horacio-garcia** (3) [913475](https://platzi.com/comentario/913475/) 

	excelente curso, hay cosas que sigo sin recordar para poder entender el codigo sabiendo que asíncrono, pero creo que voy por buen camino, cuestion de practica creo, no entendi muy bien la función setAttributes, entiendo que hay una funcion que esta afuera del addEventListener el cual corresponde al evento cuando buscamos, a este le agregamos un atributo de carga que es la imagen gif, dentro de esta funcion tenemos la variable loader que llama a la imagen, pero no creo entender $featuringContainer.append($loader); aca estamos llamando la variable que es el featuring en el html DOM con el append($loader) y la funcion setAttributes para que tienen el ciclo for in ? compañeros agradeceria la ayuda para entender esta parte.

	* **Gabriel De Andrade (Platzi)** [913475] (3)

		Hola! Para la parte de asincronísmo, estamos preparando un [Curso de Asíncronismo con JavaScript](https://platzi.com/clases/asincronismo-js/) y para lo del for in aquí dejé un comentario hace tiempo tratando este tema <https://platzi.com/comentario/410684/> 😄

	* **Gabriel Alejandro Delgado Álvarez** [913475] (1)

		Ufff, dos meses tarde ;v  
		Lo que entendí yo es que el ciclo for in es para pasarle atributos al elemento HTML $loader que acabamos de crear, es una manera de pasarle varios atributos como con el ejemplo de Jquery.  
		Y el append lo usamos para agregarle al $featuringContainer el elemento HTML $loader que acabamos de crear, el cual ahora tiene atributos, los cuales son el src, el width y el height.

* **ikarioshimai** (3) [687301](https://platzi.com/comentario/687301/) 

	la verdad estoy harto de seguir este curso y que no este actualizado, yo sigo al pie de la letra todo lo que escribe este vato y no cargan las peliculas, solo cargaban las de accion ahora ninguna. ya busque errores de sintaxis y no los hallo

	* **David Perez** [687301] (1)

		Busca la API y lee su documentación

	* **dipacata** [687301] (1)

		A mi tampoco me funcionó, pero buscar otras APIs, por ejemplo yo usé esta, que la vi en alguno de los comentarios. me ha funcionado perfecto.
		```     https://rickandmortyapi.com/documentation```
		    
		```

	* **Andrés Felipe Eslava Zuluaga** [687301] (4)

		Hola [Ikariosimai](https://platzi.com/@ikarioshimai/).  
		Mire, quizá usted no es de los que lee comentarios. Me pasó lo mismoy fue algo irritante. EL LINK CAMBIÓ! ESTE ES EL NUEVO:
		```         /////////////////////////////////////////////////////
		        const BASE_API = 'https://yts.lt/api/v2/' 
		        /////////////////////////////////////////////////////
		        ```
		    
		```

	* **Espasa** [687301] (1)

		El URL de la API cambió, ahí te lo envío:
		```     const API_URL = 'https://yts.lt/api/v2/list_movies.json?genre=';
		    
		```
		
		tras el genre pon el género, por ejemplo:
		```     const API_URL = 'https://yts.lt/api/v2/list_movies.json?genre=action';
		    
		```

	* **Cristhian Hernandez** [687301] (2)

		jajajaj calma, solo actualiza la API <https://yts.lt/api>

	* **ElianaHuamaniChirre** [687301] (1)

		Hola,  
		lo q sucede es q la url q se menciono al inicio no es la correcta, yo uso estas y SI funcionan :
		
		const actionList = await getData(‘<https://yts.lt/api/v2/list_movies.json?genre=action>’)  
		const dramaList = await getData(‘<https://yts.lt/api/v2/list_movies.json?genre=drama>’)  
		const animationList = await getData(‘<https://yts.lt/api/v2/list_movies.json?genre=animation>’)

	* **darioarcos** [687301] (2)

		vaya loser amigo, el cambio es un simple link que encuentras en los enlaces, no es solo coger el código copiar y pegar. Hay que usar un poco el sentido común y algo de iniciativa personal

	* **Daniel Esteves** [687301] (1)

		Hola @ikarioshimai, como te comentan tus compañeros esto no se debe a que el curso esté desactualizado, se debe a que cambiaron el endpoint de donde se jalan los datos; añadido a esto también tus compañeros te compartieron enlaces a otras API que puedes usar. Si tienes algún otro problema puedes avisar 💪

	* **Jhon Alexander Alvarez Romero** [687301] (1)

		4 meses despues de tu comentario aun funciona bien la app, no es limitarse a escribir textualmente lo que el hace, por lo menos sigue sus pasos, te esperaste a que el copiara el link y tu lo transcribieras? era mas facil entrar a la pagina y copiar el link, ademas de mas seguro y eficiente

	* **33andres33** [687301] (1)

		yo estoy en abril de 2020 y ningun error

* **Yezid Garcia Medina** (3) [543244](https://platzi.com/comentario/543244/) 

	}Cual es el código del panel inferior rojo que tiene Leonidas? a mi me aparece solito.  
	Si pude colocar el icono de cargando pero solo esta eso, sin el fondo rojo y sin esa otra imagen.

	* **Cristian Orozco Benjumea** [543244] (2)

		Es el featuring y está en el html antes de finalizar la `section`:
		```     <divclass="home-featuring"id="featuring">
		            <divclass="featuring">
		                <divclass="featuring-image">
		                    <img src="src/images/covers/solar-sailer.jpg" width="70" height="100" alt="">
		                </div>
		                <divclass="featuring-content">
		                    <p class="featuring-title">Película encontrada</p>
		                    <p class="featuring-album">Título de la Peli</p>
		                </div>  
		            </div>
		        </div>
		    
		```

* **GaBo-FuNk** (2) [1096280](https://platzi.com/comentario/1096280/) 

	Si quieres entender mejor el for in de Leonidas, pasa por este video ^^ Hecho con mucho amor ❤️
	
	<https://platzi.com/tutoriales/1316-jquery-js/5269-apoyo-para-la-clase-14-for-in-dd/>

	* **carlosextra1** [1096280] (1)

		Gracias!

* **Kenet Chungandro** (2) [1002102](https://platzi.com/comentario/1002102/) 

	Hay cosas que ni sabía que se podían hacer! Está genial el curso 😃

* **beaps** (2) [523526](https://platzi.com/comentario/523526/) 

	En el bucle _for … in_ se pone: _for ( **const** attribute in attributes)_ {}  
	¿Por qué no da error la constante attribute si va cambiando de valor?

	* **Angel Hernandez** [523526] (2)

		En un ciclo for, la primera variable es leida, por lo general nunca cambia, mientras que la segunda es solo un atributo de la primera o un valor a leer por lo cual si es cambiante

	* **beaps** [523526] (1)

		Gracias por responder angel998 pero aún no me queda del todo claro 😅
		
		Este sería el código entero
		```     function setAttributes($element, attributes) {
		        for (const attribute in attributes) {
		          $element.setAttribute(attribute, attributes[attribute]);
		        }
		      }
		    
		```
		```     setAttributes($loader, {
		          src: 'src/images/loader.gif',
		          height: 50,
		          width: 50,
		        })
		    
		```
		
		Y yo entiendo que const attribute va obteniendo el valor de _src_ , _height_ y _width_

	* **Diego Andres Cardenas Caro** [523526] (2)

		Esto pasa porque estas iterando en las propiedades de un objeto, en este enlace esta explicado. [Enlace](https://yeisondaza.com/var-let-y-const-en-javascript)

	* **beaps** [523526] (2)

		Muchas gracias diegocardenas !!! 👌😄
		
		“…es una variable de sólo lectura, que no puede reasignarse el puntero en memoria, pero las cosas dentro de los valores por referencia si pueden ser modificadas.  
		Con valores primitivos como string o números, no podrán ser modificados.”

	* **Facundo Nicolás García Martoni (Platzi)** [523526] (2)

		@Beaps Claro, jamás lo pensé así 😮 tiene sentido

* **pablojorgeandres** (2) [486470](https://platzi.com/comentario/486470/) 

	## Creación de elementos y asignación de atributos
	
	**Creación de Elementos**
	
	Es posible crear elementos independientes en el DOM a partir de JavaScript con el método _createElement()_.
	```     const $element = document.createElement('img')
	    
	```
	
	**Asignación de Atributos**
	
	Para asignar atributos al elemento creado podemos usar _setAttribute()_
	```     $element.setAttribute("src", "img/foto.png");
	    
	```
	
	Y si quiero agregar varios:
	```     	functionaddAttributes($element, attributes) {
	           	  for(const attribute in attributes ){
	           	    $element.setAttribute(attribute, attributes[attribute])
	           	  }
	        	}  
	    
	    
	```
	
	Para agregarlo al lugar del código que quiero:
	```     	$container.append($element)
	    
	    
	```

* **Giorgio Ventura** (2) [452682](https://platzi.com/comentario/452682/) 

	Vanilla JS!!!, JavaScript a pelo! (sin ninguna ayuda)

* **encinajavier7** (2) [54987](https://platzi.com/comentario/536440/) 
La función setAttribute() no entiendo bien de donde viene. Si alguien me podria explicar le agradezco

	* **Diego Alexander Forero Higuera (Platzi)** [54987] (2)

		Es un método que tienen todos los elementos html para poder configurar esos atributos, por ejemplo con setAttribute puedes asignarle una clase a una etiqueta html.

* **carlosextra1** (1) [1116471](https://platzi.com/comentario/1116471/) 

	Con algunos fallos en el código, pero logre aplicar corrección! vamos al siguiente!

* **Wandy Rafael Santana Evangelista** (1) [1084994](https://platzi.com/comentario/1084994/) 

	Me encanto esta clase, es super interesante como desde JS puedes crear elementos HTML y a la vez colocarle esos atributos.

* **Nigtdreams** (1) [994370](https://platzi.com/comentario/994370/) 

	Solo te recomendaria no usar nombres similares en funciones y metodos , puede llegar a ser muy confuso al leer

* **asismelgarejo** (1) [884843](https://platzi.com/comentario/884843/) 

	FOR IN

* **Enrique Felix Garcia Stave** (1) [741429](https://platzi.com/comentario/741429/) 

	Apunte:
	
	Para obtener el valor de un atributo, tambien se usa attr en jQuery:
	```     $(selector).attr('atributo')
	    
	```

* **encinajavier7** (1) [536440](https://platzi.com/comentario/536440/) 

	La función setAttribute() no entiendo bien de donde viene. Si alguien me podria explicar le agradezco

	* **Diego Alexander Forero Higuera (Platzi)** [536440] (2)

		Es un método que tienen todos los elementos html para poder configurar esos atributos, por ejemplo con setAttribute puedes asignarle una clase a una etiqueta html.

* **Félix Alejandro Zelaya Orellana** (1) [531020](https://platzi.com/comentario/531020/) 

	Super!!

* **JhonyBurbano08** (1) [424036](https://platzi.com/comentario/424036/) 

	Codigo
	```     // setea los atributos de un elemento
	      const setAttributes = ($element, attributes) => {
	          for (const attribute in attributes) {
	              $element.setAttribute(attribute, attributes[attribute])
	          }
	      }
	    
	      $form.addEventListener('submit', (event) => {
	          event.preventDefault();
	          $home.classList.add('search-active');
	          const $loader = document.createElement('img');
	          setAttributes($loader, {
	            src: 'src/images/loader.gif',
	            width: 50,
	            heigth: 50
	          })
	          $featuringContainer.append($loader);
	      })
	    
	```

* **Julian Andres Cajiao** (1) [399540](https://platzi.com/comentario/399540/) 

	  * setAttribute(atributo,valor del atributo)

* **arlex_ee10** (1) [82318](https://platzi.com/comentario/1002742/) 
cual es la diferencias de foreach vs for in

	* **Kenet Chungandro** [82318] (2)

		Al usar **forEach** , simplemente tenemos que especificar una función de devolución de llamada. Esta devolución de llamada se ejecutará en cada elemento de la matriz.
		
		**for in** se usa para iterar sobre las propiedades enumerables de los objetos. Cada propiedad en un objeto tendrá un Enumerable valor: si ese valor se establece en true, entonces la propiedad es Enumerable.

* **Yezid Garcia Medina** (1) [55621](https://platzi.com/comentario/543244/) 
}Cual es el código del panel inferior rojo que tiene Leonidas? a mi me aparece solito. Si pude colocar el icono de cargando pero solo est...

	* **Cristian Orozco Benjumea** [55621] (2)

		Es el featuring y está en el html antes de finalizar la `section`:
		```     <divclass="home-featuring"id="featuring">
		            <divclass="featuring">
		                <divclass="featuring-image">
		                    <img src="src/images/covers/solar-sailer.jpg" width="70" height="100" alt="">
		                </div>
		                <divclass="featuring-content">
		                    <p class="featuring-title">Película encontrada</p>
		                    <p class="featuring-album">Título de la Peli</p>
		                </div>  
		            </div>
		        </div>
		    
		```

* **beaps** (1) [53930](https://platzi.com/comentario/523526/) 
En el bucle for … in se pone: for (const attribute in attributes) {} ¿Por qué no da error la constant...

	* **Angel Hernandez** [53930] (2)

		En un ciclo for, la primera variable es leida, por lo general nunca cambia, mientras que la segunda es solo un atributo de la primera o un valor a leer por lo cual si es cambiante

## 0150. Formularios [12307](https://platzi.com/clases/1316-jquery-js/12307-formularios/)

### Descripción:


### Comentarios:

* **Sergio Minei** (18) [335972](https://platzi.com/comentario/335972/) 

	## Formularios
	
	`FormData()` es una interfaz que te permite obtener los valores de un formulario.
	```     //FormData va a abstraerr todos los valores de los elementos del formulario que cuenten con un atributo 'name' asignado y los va a setear en un objeto de tipo FormData.
	    const data = new FormData($form);
	    
	    //retorna el valor del elemento con el atributo name="nombre"
	    data.get("nombre");
	    
	    //setea el valor avengers en la key pelicula 
	    data.set("pelicula", "avengers");
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **Luis Carlos Juarez Azuara** [335972] (2)

		gracias

	* **jmen1995** [335972] (2)

		Buen resumen!

* **ivan_acg** (11) [417880](https://platzi.com/comentario/417880/) 

	Me parecen geniales estas clases , pero siento que no estoy aprendiendo a pensar en Js , siento que solo veo a Leonidas escribir.

	* **Jcion** [417880] (3)

		Acabo de llegar a la misma conclusión.

	* **Giancarlo Vera** [417880] (6)

		Hola @ivan_acg antes me pasaba lo mismo cuando veía los videos y trataba de replicar cada línea de código, ahora trato de resolver por mi cuenta antes de ver la clase, si lo hice bien aprendo un nueva forma de resolver con el video y si quedé atascado puedo ver en que fallé siguiendo el código de la clase.

	* **Antonio Madrid** [417880] (3)

		Lo que yo hago para sacarle partido es tomar notas de cada cosa. Al crear mis propias flashcards, me fuerzo a razonar para que sirve cada cosa.
		
		De esta forma, intento no automatizar el proceso de consumir el video.  
		Añadir, que se saca muchisima información de esta forma, y lo que no tienes claro, te fuerzas a buscarlo en Internet para entenderlo, o practicarlo.

	* **Jorge Palacios** [417880] (1)

		Te recomiendo ver la clase sin escribir código, y luego intentes reproducirlo por tu cuenta. Esto para cada clase.

	* **Daniel Esteves** [417880] (1)

		Antes de seguir con el curso te recomiendo que tomes descansos utilizando la técnica de pomodoro ya que te puedes estar bloqueando y aparte has ejercicios pequeños utilizando lógica para que vayas familiarizándote con el lenguaje poco a poco

* **David Daniel Castillo Nava** (5) [351332](https://platzi.com/comentario/351332/) 

	 ** _Así va mi código hasta el momento_**
	```     const API_BASE = 'https://yts.am/api/v2';
	    
	    (async function load(){
	    
	    	/*
	    	** Initial Variables
	    	*/
	    	const $home = document.getElementById('home');
	    	const $form = document.getElementById('form');
	    	const $featuringContainer = document.getElementById('featuring');
	    
	    	const $actionContainer = document.querySelector('#action');
	    	const $dramaContainer = document.getElementById('drama');
	    	const $animationContainer = document.getElementById('animation');
	    
	    	const $modal = document.getElementById('modal');
	    	const $overlay = document.getElementById('overlay');
	    
	    	const $hideModal = document.getElementById('hide-modal');
	    	
	    	const $modalTitle = $modal.querySelector('h1');
	    	const $modalImage = $modal.querySelector('img');
	    	const $modalDescription = $modal.querySelector('p')
	    
	    	/*
	    	** Initial Fetch
	    	*/
	    	let [ reqAction, reqDrama, reqAnimation ] = await Promise.all([
	    		fetch(`${API_BASE}/list_movies.json?genre=action`),
	    		fetch(`${API_BASE}/list_movies.json?genre=drama`),
	    		fetch(`${API_BASE}/list_movies.json?genre=animation`)
	    	])
	    
	    	let actionData = await reqAction.json()
	    	let actionList = actionData.data.movies
	    
	    	let dramaData = await reqDrama.json()
	    	let dramaList = dramaData.data.movies
	    
	    	let animationData = await reqAnimation.json()
	    	let animationList = animationData.data.movies
	    
	    	renderList(actionList, $actionContainer, 'listPlayer',{click:true, render: 'append'})
	    	renderList(dramaList, $dramaContainer, 'listPlayer',{click:true, render: 'append'})
	    	renderList(animationList, $animationContainer, 'listPlayer',{click:true, render: 'append'})
	    
	    	
	    
	    	/*
	    	** Functions
	    	*/
	    	async function getData(url) {
	    		const response = await fetch(url);
	    		const data = await response.json()
	    		return data;
	    	}
	    
	    	function setAttributes($element, attributes) {
	    		for (const attribute in attributes) {
	    			$element.setAttribute(attribute, attributes[attribute])
	    		}
	    	}
	    
	    	function templateContent (template, content) {
	    		let hmtlContent;
	    		switch(template) {
	    			case 'listPlayer':
	    				hmtlContent = `
	    					<div class="primaryPlaylistItem">
	    						<div class="primaryPlaylistItem-image">
	    							<img src="${content.medium_cover_image}">
	    						</div>
	    						<h4 class="primaryPlaylistItem-title">
	    							${content.title_long}
	    						</h4>
	    					</div>
	    				`
	    				break;
	    			case 'featuringTemplate':
	    				hmtlContent = `
	    					<div class="featuring">
	    						<div class="featuring-image">
	    							<img src="${content.medium_cover_image}" width="70" height="100" alt="">
	    						</div>
	    						<div class="featuring-content">
	    							<p class="featuring-title">Pelicula encontrada</p>
	    							<p class="featuring-album">${content.title_long}</p>
	    						</div>
	    					</div>
	    				`
	    				break;
	    			default:
	    				hmtlContent = ''
	    		}
	    
	    		const html = document.implementation.createHTMLDocument();
	        	html.body.innerHTML = hmtlContent;
	    
	    		return html.body.children[0];
	    	}
	    
	    	function renderList (list, elemParent, template, options) {
	    		list.forEach((content)=>{
	    			const elementContent = templateContent(template, content)
	    
	    			if (options.render === 'html') {	
	    				elemParent.innerHTML = elementContent.outerHTML
	    			} else if( options.render === 'append') {
	    				elemParent.append(elementContent)
	    			}
	    
	    			if (options.click)
	    				addEventClick(elementContent)
	    			
	    		})
	    	}
	    
	    	function showModal() {
	    		$overlay.classList.add('active')
	    		$modal.style.animation = 'modalIn .8s forwards'
	    	}
	    
	    	function hideModal() {
	    		$overlay.classList.remove('active')
	    		$modal.style.animation = 'modalOut .8s forwards'
	    	}
	    
	    	function addEventClick ($element) {
	    		$element.addEventListener('click', () => {
	    	      showModal()
	    	    })
	    	}
	    
	    	/*
	    	** Events on DOM element
	    	*/
	    	$hideModal.addEventListener('click', hideModal)
	    
	    	$form.addEventListener('submit', async (e) => {
	    		e.preventDefault()
	    		$featuringContainer.innerHTML  =''
	    
	    		$home.classList.add('search-active')
	    
	    		const $loader = document.createElement('img')
	    
	    		setAttributes($loader, {
	    			src: 'src/images/loader.gif',
	    			height: 50,
	    			width: 50
	    		})
	    
	    		$featuringContainer.append($loader)
	    
	    		const data = new FormData($form)
	    		const {data: {movies}} = await getData(`${API_BASE}/list_movies.json?limit=1&query_term=${data.get('name')}`)
	    
	    		renderList(movies, $featuringContainer, 'featuringTemplate', {click: false, render: 'html'} )
	    	})
	    
	    })()
	    
	    
	```

	* **Rodolfo Nicacio Ugalde Ochoa** [351332] (1)

		Muy buen ejemplo, gracias.

* **oteka21** (5) [340133](https://platzi.com/comentario/340133/) 

	Si alguien había usado los puntos de interrupción anteriormente en visual studio, la key word debugger le será familiar ya que hacen prácticamente lo mismo.

	* **Joel Alejandro Morales García** [340133] (1)

		Fue justo lo que pensé cuando empecé a ver qué Leónidas lo usaba xD

* **ilan.vivanco** (4) [678272](https://platzi.com/comentario/678272/) 

	Al parecer a diferencia de con `append()` para `innerHTML()` no hace falta convertir el string a HTML como había hecho en clases anteriores con la función `createTemplate()`.

	* **Daniel Esteves** [678272] (1)

		Exacto, ya que `innerHTML` toma por defecto que todo lo que se le pase va a ser un elemento HTML y no tendríamos que convertir nada, por ende nos ahorrariamos algunos códigos demás 😎

* **Anthony Gonzalez** (4) [359163](https://platzi.com/comentario/359163/) 

	En caso de que no se encuentren resultados, **¿Cual seria la manera correcta para manejar ese error?** , se me ocurre hacer un condicional if que valide que peli.data.movie_count sea mayor a 0, y de no se así, imprimir otro template en el $featuringContainer

	* **Diego Alexander Forero Higuera (Platzi)** [359163] (2)

		Como lo mencionas puedes contar tener ese valor del conteo de resultados o en su defecto puedes obtener el tamaño del arreglo de resultados y si es igual a 0 muestras un mensaje que no se encuentra información con esa búsqueda, si es igual o mayor a 1 entonces muestras los resultados.

	* **Efrén Sánchez** [359163] (3)

		```    const text = new FormData($form);
		        constresult = await getData (`${BASE_API}list_movies.json?limit=1&query_term=${text.get('name')}`)
		        .then(function(result) {
		          $featuringContainer.children[0].remove();
		          constHTMLString = featureTemplate (result.data.movies[0]);
		          $featuringContainer.innerHTML = HTMLString;
		        })
		        .catch(function(){
		          constHTMLString = featureErrorTemplate();
		          $featuringContainer.innerHTML = HTMLString;
		        }); 
		    
		```
		
		Yo hize ésto. Cree un templale llamado = **featureErrorTemplate** e implementé el **.catch** y el **.then**

	* **Nathaly Stefani Riaño Bejarano** [359163] (1)

		Genial, yo lo implementé así:
		```     const data = new FormData($form);
		        const peli = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`)
		          .then((peli) => {
		            const HTMLString = featuringTemplate(peli.data.movies[0]);
		            $featuringContainer.innerHTML = HTMLString;
		          })
		          .catch(() => {
		            const HTMLString = featuringTemplate({
		              medium_cover_image: 'src/images/covers/404.png',
		              title: `Lo siento, ${data.get('name')} no se encontró`
		            });
		            $featuringContainer.innerHTML = HTMLString;
		          });
		    
		```

* **Rodrigo Cuenca Mayuri** (3) [1042153](https://platzi.com/comentario/1042153/) 

	Para aquellos que no entendieron lo del query_term. Es un comando que viene en la documentación de la API.
	
	![query_term.PNG](https://static.platzi.com/media/user_upload/query_term-9e8bef17-0bb8-48dc-a8ac-b589bfe9df1d.jpg)

	* **carlosextra1** [1042153] (1)

		Gracias por el aporte! había olvidado la documentación de la API

* **Yeny L. Mendoza** (3) [822282](https://platzi.com/comentario/822282/) 

	hola, me surgido el siguiente problema no se que hacer ¿alguien saber que hacer?  
	Access to fetch at ‘<https://yts.am/api/v2/list_movies.json?genre=action>’ from origin ‘null’ has been blocked by CORS policy: No ‘Access-Control-Allow-Origin’ header is present on the requested resource. If an opaque response serves your needs, set the request’s mode to ‘no-cors’ to fetch the resource with CORS disabled.

	* **akiliki** [822282] (0)

		El problema es el CORS (<https://developer.mozilla.org/es/docs/Web/HTTP/Access_control_CORS>), tu navegador está bloqueando la llamada get al servidor ‘<https://yts.am/>’
		
		Para evitar el problema tienes que hacer la petición con en no-cors mode
		
		request-no-cors: Guarda para un objeto headers obtenido desde una petición creada con Request.mode a no-cors.
		
		Saludos

	* **Daniel Esteves** [822282] (1)

		Hola! Utiliza algún servicio como XAMPP o algo donde puedas levantar el servidor sin necesidad de comandos ya que de la manera en que lo haces o hacemos en clases a través de comandos el sitio nos está bloquenado por CORS, prueba y me dices 💪

	* **Juliette Cetina** [822282] (2)

		Hola, aunque ya pasó mucho tiempo de tu duda, a mi me sucedió lo mismo cuando empezamos a trabajar con el API, mi solución fue actualizar la URL del API, ya que el usado en el curso ya esta obsoleto. Esta es la URL nueva (en este momento):
		
		<https://yts.lt/api/v2/list_movies.json?genre=action>

	* **mxmacias** [822282] (1)

		a mi me susede el mismo problema ni la url <https://yts.lt/api/v2/list_movies.json?genre=action> me funciono 😦

	* **mxmacias** [822282] (1)

		*sucede

	* **Juliette Cetina** [822282] (1)
Revisa si pusiste entre comillas invertidas toda la url ` `

	* **Jhon Alexander Alvarez Romero** [822282] (1)

		tengo el mismo problema, aun no lo logro solucionar

	* **Andrés Campuzano Garzón** [822282] (3)

		Usen la url : <https://yts.mx/api/v2/list_movies.json?genre=action>

* **Luis Rangel Castro** (3) [790951](https://platzi.com/comentario/790951/) 

	Mi codigo:
	```     const BASE_API = 'https://yts.lt/api/v2/';
	    
	    async function getData(uri) {
	      const res = await fetch(uri);
	      const data = await res.json();
	      return data;
	    }
	    
	    function setAttributes($element, attributes) {
	      for (const attribute in attributes) {
	        $element.setAttribute(attribute, attributes[attribute]);
	      }
	    }
	    
	    const $form = document.getElementById('form');
	    const $featuringContainer = document.getElementById('featuring');
	    
	    function featureTemplate(movie) {
	      return (
	        `
	    <div class="featuring">
	      <dif class="faturing-image">
	        <img = src="${movie.medium_cover_image}" width="70px" height="100" alt="">
	      </div>
	    <div class="featuring-content">
	      <p class="featuring-title">Pelicula encontrada</p>
	      <p class="featuring-album">${movie.title}</p>
	    </div>
	      `);
	    }
	    
	    function featureErrorTemplate() {
	      return (
	        `
	        <div class="featuring">
	        <div class="featuring-content">
	          <p class="featuring-title">Lo sentimos</p>
	          <p class="featuring-album">La pelicula solicitada no fue encontrada</p>
	        </div>
	      </div>
	      `);
	    }
	    
	    $form.addEventListener('submit', async (e) => {
	      // evita que haga el submit
	      e.preventDefault();
	    
	      const $home = document.getElementById('home');
	    
	      $home.classList.add('search-active');
	    
	      const $loader = document.createElement('img');
	      setAttributes($loader, {
	        src: 'src/images/loader.gif',
	        height: 50,
	        width: 50
	      });
	    
	      $featuringContainer.firstChild.remove();
	      $featuringContainer.append($loader);
	    
	      const data = new FormData(form);
	      const movie = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`)
	        .then((res) => {
	          $featuringContainer.children[0].remove();
	          debugger
	          let HTMLString;
	          if (res.data.movies.length != 0) {
	            HTMLString = featureTemplate (res.data.movies[0]);  
	          } else {
	            HTMLString = featureErrorTemplate();  
	          }
	          $featuringContainer.innerHTML = HTMLString;
	        })
	        .catch(() => {
	          const HTMLString = featureErrorTemplate();
	          $featuringContainer.innerHTML = HTMLString;
	        });
	    });
	    
	    const $hideModal = document.getElementById('hide-modal');
	    $hideModal.addEventListener('click', hideModal);
	    
	    function hideModal() {
	      const $overlay = document.getElementById('overlay');
	      const $modal = document.getElementById('modal');
	    
	      $overlay.classList.remove('active');
	      $modal.style.animation = 'modalOut .8s forwards';
	    }
	    
	    function showModal() {
	      const $overlay = document.getElementById('overlay');
	      const $modal = document.getElementById('modal');
	    
	      $overlay.classList.add('active');
	      $modal.style.animation = 'modalIn .8s forwards';
	    }
	    
	    function addClickMovie(movieId) {
	      const $movie = document.getElementById(`movie_${movieId}`);
	      $movie.addEventListener('click', function () {
	        showModal();
	      });
	    }
	    
	    function videoItemTemplate(movieId, src, title) {
	      return (
	        `<div class="primaryPlaylistItem" id="movie_${movieId}">
	          <div class="primaryPlaylistItem-image">
	            <img src="${src}">
	          </div>
	          <h4 class="primaryPlaylistItem-title">${title}</h4>
	        </div>`
	      );
	    }
	    
	    function loadingImage() {
	      return (`<img src="src/images/loader.gif" width="50" height="50" alt="cargando peliculas">`);
	    }
	    
	    async function loadMovie(type) {
	      try {
	        const $movieContainer = document.getElementById(type);
	        $movieContainer.innerHTML = loadingImage();
	    
	        const data = await getData(`${BASE_API}list_movies.json?genre=${type}`);
	    
	        renderMovieList(data, $movieContainer);
	      }
	      catch (err) {
	        console.log('error al ejectura loadMovie', err);
	      }
	    }
	    
	    function renderMovieList(data, $container) {
	      let stringHTML = "";
	    
	      data.data.movies.forEach((movie) => {
	        const HTMLString = videoItemTemplate(movie.id, movie.medium_cover_image, movie.title);
	        stringHTML += HTMLString;
	      });
	      $container.innerHTML = stringHTML;
	    
	      data.data.movies.forEach((movie) => {
	        addClickMovie(movie.id);
	      });
	    }
	    
	    loadMovie('drama');
	    loadMovie('action');
	    loadMovie('animation');
	    
	```

* **Wilson Marino Pablo Mendez** (3) [700033](https://platzi.com/comentario/700033/) 

	La **consola del navegador** se convierte en nuestro amigo

	* **Daniel Esteves** [700033] (1)

		La consola será tu mejor amigo a lo largo de tu uso con JavaScript, es lo mejor para detectar errores, ver elementos si así lo deseas, ver como se comporta un array, y muchísimo más; de hecho la consola aparte de `console.log()` tiene alternativas como `console.warn()`, `console.error()` y mucho más, puedes mirar toda la documentación de la consola [aquí](https://www.w3schools.com/jsref/met_console_log.asp)

* **Alejandro González Reyes** (3) [576483](https://platzi.com/comentario/576483/) 

	Este es mi aporte.  
	Muestro un template diferente si la pelicula no fué localizada.
	```     // Añadir un manejador de eventos de tipo submit al elemento de formulario
	    	// El segundo parametro es un HANDLER, una función controladora de evento que contiene
	    	// el código a ejecutar 
	    	// Esta función recibe un obejo de tipo Event con informacion propia del evento que ha desencadenado
	    	// dicha funcipon (enn este caso el tipo submit)
	    	$form.addEventListener('submit', async function(event) {
	    		// Algunos elementos HTML tienen un comportamiento por defecto en el navegador.
	    		// En este caso cuando se envia el formulario, por defecto se refresca la página o se redirige a otro lugar
	    		// Para prevenirlo se emplea...
	    		event.preventDefault()
	    		$home.classList.add('search-active')
	    
	    		// Crear un elemento Nodo de HTML
	    		const $loader = document.createElement('img')
	    		// Invocar una función que permite setear multiples atributos
	    		// a un determinando elemento de nodo HTML
	    		setAttributes($loader, {
	    			src: 'src/images/loader.gif',
	    			width: 50,
	    			heigth: 50
	    		})
	    		// Añadir la imagen en la sección de feature
	    		// append( ) agrega el elemento al final de su padre (igual que appendChild)
	    		// Acepta n cantidad de elementos a insertar (uno despues del otro)
	    		// Acepta tambien DomString, cadenas de texto que pueden ser insertadas sin la intervencion de un createTextNode
	    		$featuringContainer.append($loader)
	    
	    		// Crear una instancia de la interfaz FormData para obtener los valores de los campos de formulario
	    		// Es importante que los controles de formulario tengan asignado un atributo name.
	    		const data = new FormData($form)
	    		// Acceder al valor de un control de formulario
	    		const query_terms = data.get('name')
	    		// Setear de manera programatica un valor. esto puede servir para colocar información adicional para localizar en un recurso externo
	    		// data.set('newField', 'otro valor')
	    		
	    
	    		// La función encargada de renderizar el template con la pelicula solicitada
	    		// contiene instrucciones asincronas. Es decir, promete a devolver algo en el futuro (el template interpolado)
	    		// Por tanto indico que espere.... 
	    		// Pero para ello el handler controlador de este evento ('sbumit') debe prefijarse como async
	    		const HTMLString = await featuringTemplate(query_terms)
	    		// Con innerHTML retiro reemplazo todo lo que haya adentro....
	    		$featuringContainer.innerHTML = HTMLString
	    	})
	    
	    
	    	// Esta función contiene instrucciones asincronas
	    	// Para simplificar y mejorar la lectura de código hago uso de instrucciones async/await
	    	async function featuringTemplate(query) {
	    		let peli = await searchMovies(`https://yts.am/api/v2/list_movies.json?limit=1&query_term=${query}`)
	    		// Una vez resuelta la promesa
	    		// Verifico en la data devuelta si hay resultados, (ver documentación API)
	    		if(peli.data.movie_count) {
	    			// Si hay resultados, obtengo la primer peliculas que coincide con el termino de busqueda
	    			peli = peli.data.movies[0]
	    			// Ahora interpolo esa info dentro del literal template y lo retorno.
	    			// Para que la parte invocadora lo coloque donde necesite
	    			return (
	    				`
	    				<div class="featuring">
	    					<div class="featuring-image">
	    					  <img src="${peli.medium_cover_image}"width="70" height="100" alt="">
	    					</div>
	    					<div class="featuring-content">
	    					  <p class="featuring-title">Pelicula encontrada</p>
	    					  <p class="featuring-album">${peli.title}</p>
	    					</div>
	    				</div>
	    				`
	    		    )
	    		} else {
	    			return (
	    				`
	    				<div class="featuring">
	    					<div class="featuring-content">
	    					  <p class="featuring-title">Lo sentimos</p>
	    					  <p class="featuring-album">La pelicula solicitada no fue encontrada</p>
	    					</div>
	    				</div>
	    				`
	    		    )
	    		}
	    	}
	    
	    
	    	// Esta función permite agregar multiples atributos a un elemento de HTML
	    	// JS solo permite setear un valor a la vez con setAttribute( )
	    	function setAttributes($element, attributes) {
	    		// Recorrer el objeto de propiedades para insertar en el elemento
	    		for(const attr in attributes) {
	    			// Para el atributo actual procedemos a establecerlo dentro del elemento
	    			// El valor de un atributo en un objeto se accede mediante 
	    			// objeto.atributo --- sin embargo tambien es posible accder en forma de arreglo
	    			$element.setAttribute(attr, attributes[attr])
	    			// con getAttribute( ) puedo acceder al valor de un determinado atributo
	    			console.log($element.getAttribute(attr))
	    		}
	    	}
	    
	    
	```

* **pablojorgeandres** (3) [487351](https://platzi.com/comentario/487351/) 

	## Formularios
	
	Para obtener los datos de un formulario con Vanilla JS usamos el constructor _FormData()_ que recibe como parámetro el formulario HTML al que queramos acceder:
	```     const formData = new FormData($form)
	    
	```
	
	A este nuevo objeto podemos setearle datos nuevos y también pedirle:
	```     // agregar
	    	formData.set('serie', 'Mr. Robot')
	    
	    // pedir
	    	formData.get('serie')
	    	// "Mr. Robot"
	    
	```
	
	Para obtener el input del formulario al que quiero acceder utilizo el valor del atributo _‘name’_ previamente seteado en el tag html.
	```     	<formaction=""class="search"id="form">
	                	<inputtype="text"name="search-this"placeholder="Buscar un artista o tema favorito"/>
	      </form>
	    
	    	<script>
	    	    formData.get('search-this')
	    	</script>
	    
	    
	```

* **luisglopez7777** (3) [79075](https://platzi.com/comentario/933126/) 
soy el unico al que siempre le sale que encontró la pelicula de Making Waves? Jaja

	* **Erik Ochoa (Platzi)** [79075] (1)

		🤔yo recuerdo haber hecho este curso y todo funcionó bien.

* **Diego Fernando Rojas Quintero** (2) [1089237](https://platzi.com/comentario/1089237/) 

	Este es el resumen de la clase de formularios (Esta es una pequeña muestra de lo que se puede hacer ****new FormData**** en nuestros proyectos).
	
	Podemos tomar la información que se deja en un formulario de un sitio web al fijarla cuando escribimos sobre ella. Uno de los atributos de form o un input en HTML es **name** , en ella guarda en memoria la información de que dejamos escrito.
	```     <form action=""class="search" id="form">
	    <inputtype="text"name="name"placeholder="Buscar un artista o tema favorito"/>
	    </form>
	    
	```
	
	Para obtener toda la información a javaScript la conseguimos creando un nuevo objeto (inf de FormData: [https://developer.mozilla.org/es/docs/Web/Guide/Usando_Objetos_FormData#Creación_de_un_objeto_FormData_desde_cero](https://developer.mozilla.org/es/docs/Web/Guide/Usando_Objetos_FormData#Creaci%C3%B3n_de_un_objeto_FormData_desde_cero))
	```     const data = new FormData($form);
	    
	```
	
	ó
	```     const data = new FormData(document.getElementById('form'));
	    
	```
	
	para obtener el dato que pusimos por parámetro en nuestro formulario es por el método get.
	```     const informacion = data.get('name');
	    console.log(informacion);
	    	// Avengers
	    
	```
	
	** _Nota:_ **para establecer datos de un formulario en JavaScript utilizamos el método get.
	```     const nuevaData = new FormData(document.getElementById('form') )
	    nuevaData.set('name','avengers')
	    
	```
	
	para verla la podemos visualizar en cosola utilizado el método get ya anteriormente dicho.
	```     data.get('name');
	    
	```

	* **carlosextra1** [1089237] (1)

		Gracias por el aporte

* **Rodrigo Cuenca Mayuri** (2) [1041233](https://platzi.com/comentario/1041233/) 

	Los que quieran profundizar en el tema de FormData, leonidas explica más a detalle en su canal de youtube.
	
	[https://www.youtube.com/watch?v=5M3HYc8PoVw](url)

	* **wlm.morant** [1041233] (1)

		copy y pega el link

	* **carlosextra1** [1041233] (1)

		ORALE gracias por el aporte!

* **arlex_ee10** (2) [1002817](https://platzi.com/comentario/1002817/) 

	Genial 😃 ya soy pro xd

* **iridian** (2) [889232](https://platzi.com/comentario/889232/) 

	Primero no entendía lo de limit=1 pero buscando es para traer el numero de información que quieres traer de tu API y mostrar, aquí solo queremos traer una coincidencia de la pelicula, pero si quisiéramos traer dos entonces cambiaríamos solo el número, así lo puso el profesor en la clase:
	```     const peli = await getData (`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`);
	    
	```
	
	También puede servir de esta manera que es para tomar los elementos que tu necesitas mostrar de tu API: .slice(1,1) abajo el ejemplo y funciona bien:
	```     const peli = await getData (`${BASE_API}list_movies.json?.slice(1,1)&query_term=${data.get('name')}`);
	    
	```

* **cris-medina** (2) [781976](https://platzi.com/comentario/781976/) 

	Quiero compartirles un cambio que hice en el código para que el ‘loader’ se muestre con los estilos del ‘featuring’, además, siempre que se hace una nueva búsqueda se quite el resultado de la búsqueda anterior y se muestre el loader mientras carga.  
	Aquí van mis cambios:
	```     // creé esta función para la plantilla del $loader
	    function loaderTemplate($element) {
	        return (
	          `<div class="featuring">
	            <div class="featuring-image">
	              ${$element}
	            </div>
	            <div class="featuring-content">
	              <p class="featuring-title">Buscando</p>
	            </div>
	          </div>`
	        );
	      }
	    // Dentro de la función del evento submit agregue esta línea:
	    const HTMLLoader = loaderTemplate($loader.outerHTML);
	    // y reemplazé a $loader por HTMLLoader
	    $featuringContainer.innerHTML = HTMLLoader;
	    
	```
	
	Y con eso quedaron listos los cambio.

* **Gustavo83** (2) [633426](https://platzi.com/comentario/633426/) 

	Pequeña pero muy util herramienta: debugger, en otros cursos no le abia dado importancia, ni siquiera lo usaba, ahora sé el peso del mismo

* **Aaron Felipe Isaacs** (2) [626671](https://platzi.com/comentario/626671/) 

	Realicé un cambio en la funcion getData para que reciba un objeto con los parámetros del GET.
	```     const getData = async (url, filters) => {
	        let paramGet = ''
	        if (filters) {
	          paramGet += '?'
	          Object.keys(filters).forEach(filter => {
	            if (paramGet.length > 1) {
	              paramGet += '&'
	            }
	            paramGet += `${filter}=${filters[filter]}`
	          })
	        }
	        const action = await fetch(`${url}${paramGet}`)
	        const data = await action.json()
	        return data
	      }
	    
	```

* **Alfonso Navarro** (2) [479364](https://platzi.com/comentario/479364/) 

	La duda q tengo es si la variable $form al ser declarada aqui, previamente tomo el elemento formulario a traves de
	
	`$form = documento.getElementById('nombre_formulario');`
	
	Luego esto  
	`const data = new FormData($form);`
	
	De antemano, gracias.

	* **heysoypaez** [479364] (1)

		Hola anRoswell !
		
		Si toma (obtiene ) primero el elemento formulario a través del getElementById
		
		De caso contrario sería imposible acceder a la variable $form para crear el objeto formData
		
		¿Me entiendes?
		
		Espero haber respondido tu antigua duda,o ya lo habrás analizado tú 😃

	* **Daniel Esteves** [479364] (1)

		Hola Alfonso! Lo primero que hacemos es declarar la variable para poder usarla:
		```     const $form = document.getElementById("myForm")
		    
		```
		
		Y ahora lo que hacemos es obtener los datos de ese formulario para poder manipularlo:
		```     const data = new FormData($form)
		    
		```
		
		De esta manera primero declaramos el form y luego lo usamos en una nueva declaración en donde la encerramos para obtener esos datos utilizando la API de `FormData()`

* **iridian** (2) [76552](https://platzi.com/comentario/889233/) 
alguien sabe para que más sirve query_term en javascript? Gracias

	* **Erik Ochoa (Platzi)** [76552] (1)

		Podrías ser más especifico, no recuerdo algo llamado “query_term” en Javascript.

* **Kenet Chungandro** (1) [1005720](https://platzi.com/comentario/1005720/) 

	Genial! funcionó perfecto!

* **lmoran** (1) [999681](https://platzi.com/comentario/999681/) 

	un poco confuso con lo del query_term y el limit. pero averiguando se comprende.

* **Jhoan Steve Sandoval Meneses** (1) [869201](https://platzi.com/comentario/869201/) 

	FormData

	* **Jhon Alexander Alvarez Romero** [869201] (2)

		lo lograste hacer por completo? tengo el error de bloqueo by CORS

* **Luis Rangel Castro** (1) [790954](https://platzi.com/comentario/790954/) 

	Me tiene flipado este curso!!!

* **Wilson Marino Pablo Mendez** (1) [700007](https://platzi.com/comentario/700007/) 

	Una clase estupenda!!

* **Jose Luis Colmenares** (1) [673886](https://platzi.com/comentario/673886/) 

	Leonidas. Qué tema de VSCode es que el utilizas durante este curso?

	* **gabarodr** [673886] (1)

		No es VSCoce es otro editor: Sublime Text

* **Jose Luis Colmenares** (1) [673681](https://platzi.com/comentario/673681/) 

	¿ Por qué alrededor del min 5:30 Leonidas decido convertir la función del formulario en una función asíncrona?

	* **gabarodr** [673681] (4)

		Ya que esta reutilizando la función getData(), la cual es una función asíncrona, dentro de otra función esta función contenedora debe ser una función asíncrona también.  
		Es por eso que la primera función que envuelve a todas las demás load() tiene dos funciones: es asíncrona y también se invoca automáticamente (llamado formalmente Immediately Invoked Function Expression (IIFEs)), así en esta función se puede ejecutar cualquier función asíncrona y no necesita de una fusione asíncrona encima de ella porque se ejecuta automáticamente.

* **basanchez** (1) [632182](https://platzi.com/comentario/632182/) 

	Excelente clase!  
	Ahora si que se aprende muchísimo sabiendo las bases.

* **Uralberto** (1) [604705](https://platzi.com/comentario/604705/) 

	En las siguientes líneas de código Leonidas cambió a comillas inversas. Sospecho que se trata de un detalle menor pero quiero saber la razón. Los leo Platzinautas. Gracias.
	```     const actionList = await getData(`${BASE_API}list_movies.json?genre=action`) 
	     const dramaList = await getData(`${BASE_API}list_movies.json?genre=drama`)
	     const animationList = await getData(`${BASE_API}list_movies.json?genre=animation`) <code>
	    
	```

	* **Maximiliano Fernández** [604705] (4)

		Hola @Uralberto, las comillas invertidas se llaman Template String y es otra característica de la especificación EmacScript 6. Permite que puedas usar un string como un template y llenarlo con lo que deses, hasta puedes evaluar una expresión.
		```     var canDo = true;
		    var action = 'lo que quieras';
		    
		    console.log(`${canDo ? 'Si,' : 'No,' } puedes hacer ${action});
		    
		```

	* **Maximiliano Fernández** [604705] (2)

		Corrijo el código, se me escapo cerrar las colillas
		```     var canDo = true;
		    var action = 'lo que quieras';
		    
		    console.log(`${canDo ? 'Si,' : 'No,' } puedes hacer ${action}`);
		    
		```

	* **Uralberto** [604705] (1)

		Mil gracias <https://platzi.com/@mxml13>. Trabajé tu código y quedó perfecto.

	* **Abner Paul Ccari Lindo** [604705] (1)

		Quisiera aclarar que a inicios de ES2015 se solia llamar Template Strings, pero se fue modificando al termino “Template Literals”.

* **Angel Eduardo** (1) [587548](https://platzi.com/comentario/587548/) 

	Sin duda alguna la mejor clase hasta ahora

* **Jhonny Stiven Agudelo Tenorio** (1) [562876](https://platzi.com/comentario/562876/) 

	muy buenas, me pueden ayudar a pasar eso a Js, muchas gracias.
	```     $.ajax({
	      url: "http://......",
	      context: document.body
	    }).done(function(e) {
	    	if(e.viajes) {
	    		var ulO = document.getElementById("listaID");
	    		if(ulO) {
	    			document.body.removeChild(ulO);
	    		}
	    
	    		var ul = document.createElement("ul");
	    		ul.id = "listaID";
	    
	    		e.viajes.forEach(function(_item) {
	    			var li = document.createElement("li");
	    			var span = document.createElement("span");
	    			span.innerHTML = "Placa: " + _item.placa + " #Interno " + _item.nro_movil + " | viaje: " + _item.id_viaje;
	    			ul.appendChild(li);
	    			li.appendChild(span);
	    		});
	    
	    		document.body.appendChild(ul);
	    	}
	    });
	    
	    
	```

	* **cris-medina** [562876] (2)

		Hola jhonnyagudelo, esta es mi forma de pasar tu codigo a VanilaJS
		```     fetch('http://......')
		      .then(response => {
		        if (response.viajes) {
		          const ulO = document.getElementById("listaID");
		          if (ulO) {
		            document.body.removeChild(ulO);
		          }
		    
		          const ul = document.createElement("ul");
		          ul.id = "listaID";
		    
		          response.viajes.forEach(_item => {
		            const li = document.createElement("li");
		            const span = document.createElement("span");
		            span.innerHTML = `Placa: ${_item.placa} #Interno ${_item.nro_movil} | viaje: ${_item.id_viaje}`;
		            ul.appendChild(li);
		            li.appendChild(span);
		          });
		    
		          document.body.appendChild(ul);
		        }
		      })
		    
		```
		
		Cambie los var por **const** y tambien converti algunas funciones a **ArrowFunctions** , además, los Strings concatenados los pase a un **TemplateLiteral**.

* **Félix Alejandro Zelaya Orellana** (1) [531683](https://platzi.com/comentario/531683/) 

	Genial!

* **heysoypaez** (1) [505771](https://platzi.com/comentario/505771/) 

	¿Que documentaciòn me recomiendan para profundizar en formData, la que yo consigue en principio de Mozzila la senti incompleta o quizá yo no la supe leer 😕 ?

	* **Oscar Barajas Tavares (Platzi)** [505771] (2)

		Para mí la documentación de Mozilla es la que cumple con las características necesarias para entenderlo.
		
		Acá te dejo un recurso gratuito: <https://www.javascripture.com/FormData>

	* **Daniel Esteves** [505771] (1)

		Te recomiendo mucho la documentación de Mozilla, te dejo el enlace directo aquí <https://developer.mozilla.org/es/docs/Web/API/XMLHttpRequest/FormData> es cierto que no tienen muchos ejemplos de como pueden ser usados, pero la teoría te ayuda muchísimo a entender cómo funciona y como implementarlo

* **Qenta** (1) [485535](https://platzi.com/comentario/485535/) 

	Buenas, hay alguna posibilidad de convertir lo que escribe el usuario en el form de string a una variable?  
	Es que el siguiente código no me funciona :c  
	gracias de antemano
	```     var pokeDitto = {
	      nombre: 'Ditto',
	      pasiva: 'Impostor',
	      habilidad1: 'Agilidad',
	      imagen: 'assets/imagenes/ditto.png',
	      descripcion: 'El término ditto se utiliza para referirse a algo que se ha mencionado anteriormente o más arriba, ya sea de forma oral o escrita, para así reafirmar lo dicho, pero evitando la repetición de alguna declaración a la que, con esa expresión, se hace referencia. Significa "lo dicho" en el idioma Toscano.  ',
	      numeroDeModal: 'ocultarModal'
	    }
	    var pokeEspeon = {
	      nombre: 'Espeon',
	      pasiva: 'Sincronizar',
	      habilidad1: 'Magic Bounce',
	      imagen: 'assets/imagenes/espeon.png',
	      descripcion: '196 Espeon es un Pokémon de tipo psíquico introducido en la segunda generación. Es una de las ocho posibles evoluciones de Eevee.',
	      numeroDeModal: 'ocultarModal2'
	    }
	    var pokeLitwick = {
	      nombre: 'Litwick',
	      pasiva: 'Cuerpo de llamas',
	      habilidad1: 'Infiltrado',
	      habilidad2: 'Incendio repentino',
	      imagen: 'assets/imagenes/litwick.png',
	      descripcion: '607 Litwick es un Pokémon de tipo fantasma/fuego introducido en la quinta generación, La luz que desprende absorbe la energía vital de humanos y Pokémon.',
	      numeroDeModal: 'ocultarModal3'
	    }
	    var pokeDragonair = {
	      nombre: 'Dragonair',
	      pasiva: 'Mudar de piel',
	      habilidad1: 'Marvel Scale',
	      imagen: 'assets/imagenes/dragonair.png',
	      descripcion: '148 Dragonair es un Pokémon de tipo dragón introducido en la primera generación. Es la evolución de Dratini.',
	      numeroDeModal: 'ocultarModal4'
	    }
	    var pokeCharizard = {
	      nombre: 'Charizard',
	      pasiva: 'Energia Solar',
	      habilidad1: 'Resplandor',
	      imagen: 'assets/imagenes/charizard.png',
	      descripcion: '006 Charizard es un Pokémon de tipo fuego/volador, introducido en la primera generación. Es la evolución de Charmeleon.',
	      numeroDeModal: 'ocultarModal5'
	    }
	    
	    var pokeLampent = {
	      nombre: "Lampent",
	      pasiva: 'Cuerpo de fuego',
	      habilidad1: 'infiltrator',
	      habilidad2: 'Flash Fire',
	      imagen: 'assets/imagenes/lampent.png',
	      descripcion: '608 Lampent es un Pokémon de tipo fantasma/fuego introducido en la quinta generación. Es la evolución de Litwick.'
	    ,
	      numeroDeModal: 'ocultarModal6'
	    }
	    
	        functiondetallePokemon(pokemon) {
	          return (
	            `
	            <div style="text-align: center; margin: 20px 0;">
	            <div style="box-shadow: 1px 1px 1px 1px #EAEAEA; width: 80%; margin: 0 auto; padding: 10px;">
	              <div class="nombrePokemon">
	                  <h2 class="nombre-pokemon">${pokemon.nombre}</h2>
	              </div>
	              <div class="imagenPokemon">
	                  <img style="max-width: 50%; max-height: 300px" src="${pokemon.imagen}".png">
	              </div>
	              <div class="descripcionPokemon">
	                  <p class="parrafo">${pokemon.descripcion}</p>
	              </div>
	            </div>
	            </div>
	            `
	            //  <form>           
	            //     <input class="btn" id="${pokemon.numeroDeModal}6" type="button" value="Cerrar">
	            // </form>
	          )
	        }
	    
	    // Buscador
	    const $espacio = document.querySelector('#itemEncontrado')
	    
	    functionsetAttributes($element, atributos) {
	      for (const atributo in atributos) {
	        $element.setAttribute(atributo, atributos[atributo]);
	      }
	    }
	    
	      const $buscador = document.getElementById('buscar')
	      $buscador.addEventListener('submit', (evento) => {
	        event.preventDefault();
	        const $loader = document.createElement('div')
	        // setAttributes($loader, {
	        //   src: 'asd',
	        //   height: 200,
	        //   width: 200,
	    
	        // })
	        $espacio.append($loader);
	    
	        const data = new FormData($buscador)
	        debugger
	        const HTMLString = detallePokemon(pokeLitwick)
	        $espacio.innerHTML = HTMLString
	        
	      })
	    
	```

	* **Eduardo Hidalgo Díaz Rugama** [485535] (4)

		Si eres capaz de obtener correctamente el valor dentro del form, el hecho de obtenerlo representa ponerlo dentro de una variable. No te angusties, lo que deseas saber es algo común, y si se puede.
		
		esto es lo que necesitas:
		```     var nameValue = document.getElementById("uniqueID").value;
		    
		```
		
		<https://stackoverflow.com/questions/3547035/javascript-getting-html-form-values/3547123>

* **JhonColorado07** (1) [485447](https://platzi.com/comentario/485447/) 

	Wauuu formData() genial esta funcion de javascript.

* **Liantony Pozo** (1) [449991](https://platzi.com/comentario/449991/) 

	Error => Uncaught (in promise) TypeError: Cannot read property ‘addEventListener’ of null
	```     $form.addEventListener('submit', async (event) => {
	          event.preventDefault();
	          const data = new FormData($form);
	          const peli = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`)
	          const HTMLString = featuringTemplate(peli.data.movies[0]);
	          featuringContainer.innerHTML = HTMLString;
	        }) 
	    
	```
	
	tengo ese error en esa parte cuando intento hacer el form.addEventListener

	* **Sergio Alejandro Niño Monroy** [449991] (1)

		Tienes que pasarle como parámetro peli a la función featuringTemplate asi: function featuringTemplate(peli){}

* **Nahuel Sznajderhaus** (1) [76530](https://platzi.com/comentario/888931/) 
lo unico que no entendi por que fue: por que cortó el HTML <div> con class featuring del index.html en vez de copiarlo?

	* **Germán González** [76530] (1)

		porque si no lo hace, ya habria un elemento en el lugar donde iria el nuevo que se esta creando

* **Ang.O** (1) [75252](https://platzi.com/comentario/866579/) 
pregunta, la clase se trato de que te saliera wonder woman como opcion de autocompletar?

* **Ang.O** (1) [75251](https://platzi.com/comentario/866577/) 
cual es el template?

	* **noemk2** [75251] (1)

		 **google traductor (template)** : _modelo, plantilla_
		
		Se refiere la parte a la estructura escrita en html y css
		
		* * *
		
		  * **Porque hay que descargarse el template**?  
		El curso trata de Javascript, enfocandose en una de sus muchas librerias llamada **Jquery** , a modo de ahorrar tiempo se omite ( **se salta** ) la parte de construccion del template en **html y css** … **creo que existe otro curso para eso** …

* **Ang.O** (1) [75248](https://platzi.com/comentario/866561/) 
cuando se necesita usar async?

	* **Gabriel De Andrade (Platzi)** [75248] (3)

		Hola! Debes de usar async cuando dentro de la función tienes pensado utilizar await para esperar a que una promesa se cumpla 😄

* **Uralberto** (1) [60383](https://platzi.com/comentario/604705/) 
En las siguientes líneas de código Leonidas cambió a comillas inversas. Sospecho que se trata de un detalle menor pero quiero saber la r...

	* **Maximiliano Fernández** [60383] (4)

		Hola @Uralberto, las comillas invertidas se llaman Template String y es otra característica de la especificación EmacScript 6. Permite que puedas usar un string como un template y llenarlo con lo que deses, hasta puedes evaluar una expresión.
		```     var canDo = true;
		    var action = 'lo que quieras';
		    
		    console.log(`${canDo ? 'Si,' : 'No,' } puedes hacer ${action});
		    
		```

* **heysoypaez** (1) [52469](https://platzi.com/comentario/505771/) 
¿Que documentaciòn me recomiendan para profundizar en formData, la que yo consigue en principio de Mozzila la senti incompleta o quizá yo...

	* **Oscar Barajas Tavares (Platzi)** [52469] (2)

		Para mí la documentación de Mozilla es la que cumple con las características necesarias para entenderlo.
		
		Acá te dejo un recurso gratuito: <https://www.javascripture.com/FormData>

* **Qenta** (1) [50955](https://platzi.com/comentario/485535/) 
Buenas, hay alguna posibilidad de convertir lo que escribe el usuario en el form de string a una variable? Es que el siguiente código no ...

	* **Eduardo Hidalgo Díaz Rugama** [50955] (4)

		Si eres capaz de obtener correctamente el valor dentro del form, el hecho de obtenerlo representa ponerlo dentro de una variable. No te angusties, lo que deseas saber es algo común, y si se puede.
		
		esto es lo que necesitas:
		```     var nameValue = document.getElementById("uniqueID").value;
		    
		```
		
		<https://stackoverflow.com/questions/3547035/javascript-getting-html-form-values/3547123>

* **Liantony Pozo** (1) [48019](https://platzi.com/comentario/449991/) 
Error => Uncaught (in promise) TypeError: Cannot read property ‘addEventListener’ of null $form.addEventListener('submit', asyn...

	* **Sergio Alejandro Niño Monroy** [48019] (1)

		Tienes que pasarle como parámetro peli a la función featuringTemplate asi: function featuringTemplate(peli){}

## 0160. Desestructuración de objetos [12308](https://platzi.com/clases/1316-jquery-js/12308-desestructuracion-de-objetos/)

### Descripción:


### Comentarios:

* **Sergio Minei** (27) [335994](https://platzi.com/comentario/335994/) 

	## Desestructuración de objetos
	
	`Destructuring assignment` permite entrar a un objeto o lista y poder sacar un dato para asignarlo a otra variable.
	```     //el fetch devuelve una promesa con la siguiente estructura: promesa.data.movies
	    //con el destructuring assignmen estamos creando una variable que se llama pelis y solo contiene la información de movies.
	    const { 
	      data: {
	        movies: pelis
	      }
	    } = await fetch(`api_url`); 
	    
	    //Lo anterior sería igual a esto:
	    const response = await fetch(`api_url`);
	    const pelis = response.data.movies;
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

* **LeandroVidela** (8) [395840](https://platzi.com/comentario/395840/) 

	También ser puede desestructurar el objeto que está dentro del Array Movies. Aquí va el código:
	```     function featuringTemplate(pelicula) {
	    
	        const {
	          title,
	          medium_cover_image
	        } = pelicula;
	    
	        return (
	          `
	          <div class="featuring">
	            <div class="featuring-image">
	              <img src="${medium_cover_image}" width="70" height="100" alt="">
	            </div>
	            <div class="featuring-content">
	              <p class="featuring-title">Pelicula encontrada</p>
	              <p class="featuring-album">${title}</p>
	            </div>
	          </div>
	          `
	        )
	      }
	    
	      form.addEventListener('submit', asyncfunction(event){
	        
	        event.preventDefault();
	        home.classList.add('search-active');
	        showModal();
	        
	        const loader = document.createElement('img');
	        setAttributes(loader, loaderImage)
	        featuringContainer.append(loader);
	    
	        const data = new FormData(form);
	        const {
	          data:{
	            movies: [pelis]
	          }
	        } = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`);    
	        const HTMLString = featuringTemplate(pelis);
	        featuringContainer.innerHTML = HTMLString;
	        
	      })
	    
	```

	* **evertescalante1** [395840] (1)

		Porque usas el llamado a showModal() en esa parte ? digo pues que haces de diferente en esa funcion me gustaria saber.
		
		Gracias amigo

* **jhurtadojerves** (7) [340629](https://platzi.com/comentario/340629/) 

	Como dato super importante, Leo siempre usa “pelis” aunque esté todo programado en inglés xD

	* **Juan David Castro (Platzi)** [340629] (6)

		#Spanglish 😛

	* **jhurtadojerves** [340629] (6)

		Entre el spanglish y los typos la clase es más divertida xD

* **Alejandro González Reyes** (6) [576527](https://platzi.com/comentario/576527/) 

	Me agrada, me agrada.  
	Esto del destructuring lo vi en el curso de VueJS  
	AL INICIO SE ME HIZO COMO BRUJERÍA  
	pero ahora lo entendí por completo…

	* **Daniel Esteves** [576527] (2)

		El destructuring es de lo mejor, traerte solo la variable o el dato que necesitas es una forma muy práctica de hacer mucho más legible el código

* **Enrique Felix Garcia Stave** (4) [745141](https://platzi.com/comentario/745141/) 

	En pocas palabras, cuando aplicamos desestructuración de objetos, “desmenuzamos” el objeto hasta llegar a la propiedad que necesitamos, a la cual si queremos podemos suministrarle otra variable. Todo lo anterior se igualaría al objeto que contiene las propiedades desmenuzadas.

	* **Daniel Esteves** [745141] (2)

		Si, de hecho es así, por ejemplo en React cuando le estás pasando propiedades a un componente se utiliza `{...props}` para que todos los datos que se le pasen lo puedas manipular en el componente hijo, es muy bueno para hacer el código más corto y legible

* **pablojorgeandres** (4) [487427](https://platzi.com/comentario/487427/) 

	## [Destructuring assignment](https://github.com/pablojorgeandres/notas-clases/blob/master/jQuery-JavaScript.md#clase16)
	
	Destructuring assignment o asignación por desestructuración nos permite introducirnos en un objeto para así extraer un dato para asignarlo a otra variable y así limpiar nuestro código.  
	Esto se hace usando llaves; ‘{}’ y ‘:’.
	
	Ej. dónde podríamos aplicar esto:
	```     // ...
	    
	      const peliculaBuscada = await getMovies(`${finder_API}${data.get('name')}`)
	      $featuringContainer.innerHTML = featuringTemplate(peliculaBuscada.data.movies[0])
	    
	    // ...
	    
	    
	```
	
	El objeto que nos arroja la función asíncrona del ejemplo nos obliga a entrar en el sub-objeto _‘data’_ y este a la vez en _‘movies’_ , lo que nos hace usar esta declaración para acceder a los datos que necesitamos:
	```     peliculaBuscada.data.movies[0]
	    
	```
	
	Desestructurando la variable en la que voy a guardar el resultado de la función asíncrona que nos arroja el objeto nuestra variable se vería más limpia y el código quedaría así:
	```     // ...
	      const { 
	    	    data: {
	    	    	movies: peliculaBuscada 
	    	    }
	    	} = await getMovies(`${finder_API}${data.get('name')}`)
	            $featuringContainer.innerHTML = featuringTemplate(peliculaBuscada[0])
	    // ...
	    
	    
	```

* **Efrén Sánchez** (3) [395219](https://platzi.com/comentario/395219/) 

	Que pena que no carge los APIs…

	* **Daniel Esteves** [395219] (1)

		Hola! Esto se debe a que la API ha cambiado la manera en que puede ser llamada, pero para no complicarnos mucho podemos usar los conocimientos que estamos aprendiendo y usar algo como <https://rickandmortyapi.com/>

* **mexicansauce** (2) [1008590](https://platzi.com/comentario/1008590/) 

	Creo que a mí se me hace más claro cómo queda sin necesidad de renombrar la variable “movies” a “pelis”. Con la variable “movies” con el mismo nombre del key “movies”:
	```     const {
	          data : { movies } 
	        } = await getData(
	          `${BASE_API}list_movies.json?limit=1&query_term=${formData.get('name')}`
	        );
	        const HTMLString = featuringTemplate(movies[0]);
	    
	```

* **Gustavo83** (2) [637215](https://platzi.com/comentario/637215/) 

	desestructuración de objetos es el procedimiento que nos permite desglosar un objeto con el fin de asignar una variable y simplificar el código, genial no?

	* **Daniel Esteves** [637215] (1)

		Es de lo mejor, digamos que solamente quieres traer una variable de todos los datos, pero no quieres hacer algo como `data.name` es mucho más legible y práctico hacerlo así:
		```     const { name } = data
		    
		```

* **Félix Alejandro Zelaya Orellana** (2) [531695](https://platzi.com/comentario/531695/) 

	Genial!!

* **carlosextra1** (1) [1116604](https://platzi.com/comentario/1116604/) 

	Variables por desestructuración. Ojo esto es un marcador!

* **Juan Diego Luque Linares** (1) [1108519](https://platzi.com/comentario/1108519/) 

	Maravilloso, esplendido, magnific, preciso, corto

* **DanielVM19** (1) [1099624](https://platzi.com/comentario/1099624/) 

	M esta costando en verdad entender T.T RIP for me

* **Angélica María Rocha García** (1) [1053140](https://platzi.com/comentario/1053140/) 

	no sabía esto, gracias

* **Kenet Chungandro** (1) [1005939](https://platzi.com/comentario/1005939/) 

	wo! funciona perfecto, excelente clase 😃

* **lmoran** (1) [1005846](https://platzi.com/comentario/1005846/) 

	Excelente explicación.

* **arlex_ee10** (1) [1004387](https://platzi.com/comentario/1004387/) 

	a seguir aprendiendo

* **Kivindlg** (1) [968839](https://platzi.com/comentario/968839/) 

	la verdad hasta este punto estoy perdido la pagina mia no esta funcionando bien, y no me carga haci mismo

	* **Germán González** [968839] (1)

		Así*, Envia cuales son tus problemas para que te ayudemos

	* **Andrés Campuzano Garzón** [968839] (2)

		No te preocupes, usa mi repo en donde tengo la página actualizada y funcionando perfecto hasta este curso: <https://github.com/AndresCampuzano/API-jQuery-to-JavaScript-VideoPlatform>

	* **lmoran** [968839] (2)

		sería factible que nos muestres tu código para encontrar el problema y poder ayudarte.

	* **Kenet Chungandro** [968839] (1)

		Tranquilo, adjunta tu código y encontraremos el problema 😃

* **Juan Gaybre Flores** (1) [938494](https://platzi.com/comentario/938494/) 

	Excelente practica !!

* **ronaldjgarciav** (1) [936267](https://platzi.com/comentario/936267/) 

	excelente

* **Espasa** (1) [699370](https://platzi.com/comentario/699370/) 

	Genial explicación Leonidas!

* **wlm.morant** (1) [84923](https://platzi.com/comentario/1062162/) 
La verdad veo menos complicada como estaba antes, cual es la mas usada?

* **wlm.morant** (1) [84922](https://platzi.com/comentario/1062119/) 
Cuando usar el destructuring asigned??

* **Ang.O** (1) [75254](https://platzi.com/comentario/866594/) 
y esto de que sirvio?

	* **fernandajofili (Platzi)** [75254] (1)

		Hola, de que estas hablando exactamente?

## 0170. DataSet [12309](https://platzi.com/clases/1316-jquery-js/12309-dataset2219/)

### Descripción:


### Comentarios:

* **Sergio Minei** (23) [336164](https://platzi.com/comentario/336164/) 

	## Dataset
	
	`Dataset` permite acceder a un objeto con todos los atributos `data` de un elemento DOM.
	```     <divid="element"data-id="10"data-category="action">
	    </div>
	    
	```
	```     const $element = document.getElementById("element");
	    
	    //guarda el valor de data-id
	    const id = $element.dataset.id;
	    //guarda el valor de data-category
	    const category = $element.dataset.category;
	    
	```
	
	## Transformar tipos de datos
	
	Cambiar un string a entero
	```     //parseInt("número", base)
	    let n = parseInt("500", 10)
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **SEBASTIAN PADUANO** [336164] (1)

		Hola tu div está erroneo, te falta un espacio entre el div y el id saludos.

	* **Sergio Minei** [336164] (3)

		hola @soypaduano, es un bug en el transformador de código de Platzi. Por algún motivo se eliminan algunos espacios cuando escribes un bloque de código.

	* **Facundo Nicolás García Martoni (Platzi)** [336164] (3)

		Bug que se solucionó a día de la fecha 😎

* **Fabián Veliz** (15) [380998](https://platzi.com/comentario/380998/) 

	Buenas!  
	La parte de la asignación de: `id` y `category` se puede hacer también con `Destructuring assignment` y queda mucho mejor.
	```     const { category, id } = $element.dataset;
	    
	```

	* **heysoypaez** [380998] (1)

		Gracias Fabian, no me habia fijado, lo implemente en mi código

	* **emanueljtc** [380998] (1)

		correcto.

	* **Alfonso Navarro** [380998] (1)

		Excelente aporte compañero…

* **Daniel Ramírez Mendoza** (8) [634807](https://platzi.com/comentario/634807/) 

	Aquí es un claro ejemplo de cuando pueden usar destructuring, en lugar de escribir:
	```     const id = element.dataset.id
	    const category = element.dataset.category
	    
	```
	
	Pueden hacer destruncturing del objeto:
	```     const { id, category } = element.dataset
	    
	```

	* **efrainjaimes** [634807] (1)

		Joya de comentario, gracias.

* **america jimenez alberdi** (7) [837877](https://platzi.com/comentario/837877/) 

	Mi solucion es distinta a la de Leo, trato de reutilizar a la pelicula ya invocada en un primera instancia y solo lo pinto usando template string, que aprendimos en las primeras clases. Espero sus feedback 😉
	```     function  eventoClickPelicula(elemento,movie){
	      elemento.addEventListener('click',function(){
	        $overlay.classList.add('active')
	        llenarModalInfo(movie)
	        $modal.style.animation = 'modalIn .8s forwards'
	      })
	      eventoCerrarModal()
	    }
	    function modalTemplate(movie){
	      return ` <h1>${movie.title_long}</h1>
	      <div class="modal-content">
	        <img src=${movie.medium_cover_image} alt="" width="170" height="256">
	        <p>${movie.synopsis}</p>
	      </div>`
	    }
	    function llenarModalInfo(movie){
	      const htmlString =  modalTemplate(movie)
	      $modalBody.innerHTML = htmlString
	    }
	    function  eventoCerrarModal(){
	      $hideModal.addEventListener('click',function(){
	        $overlay.classList.remove('active')
	        $modal.style.animation = 'modalOut .4s forwards'
	      })
	    }```
	    
	```

	* **Juan Osio** [837877] (1)

		Máquina!, muy buena manera de simplificar todo

	* **Germán González** [837877] (1)

		Esta muy ingenioso, eso lo pensaste tu solo o ya tienes experiencia previa?, wow.

	* **hdmjesus** [837877] (1)

		Si esta muy bueno tu metodo America, pero Leonidas lo hace asi, para en la proxima clase explicar otros conceptos.

	* **Pau Pregoni Juan** [837877] (1)

		Usted sí que sabe de **SOLID**.

* **CRISTIAN CAMILO SOSA PINZON** (7) [340988](https://platzi.com/comentario/340988/) 

	Cuando le paso los atributos como objeto { height : 10, width : 20 …} cómo hago para pasarle atributos que tiene guión como por ejemplo el z-index, o font-size?

	* **Oscar Barajas Tavares (Platzi)** [340988] (10)

		Se elimina el guion y se pasa de esta forma: `fontSize zIndex`

	* **Juan David Castro (Platzi)** [340988] (7)

		🔖 Este tipo de nombramiento se llama [Camel Case](https://www.w3schools.com/js/js_conventions.asp).

	* **Oscar Barajas Tavares (Platzi)** [340988] (3)

		@juandc exacto y es un estándar ya que hay mas formas de asignar nombres.

	* **Pedro Caicedo** [340988] (4)

		lowerCamelCase para ser más exacto 😃

* **JhonColorado07** (4) [487939](https://platzi.com/comentario/487939/) 

	Ahora vengo a comprender el por qué en muchos archivos html me encontraba con este atributo dataset="…"

	* **heysoypaez** [487939] (1)

		Siii, en la clase de fundamentos de javascript lo usan

* **Yoel Ribero** (3) [732475](https://platzi.com/comentario/732475/) 

	Tuve el error que se menciona bastante seguido en los comentarios, la solución que me funcionó fue abrir el sitio con localhost, o sea, lo abrí con static server que nos ensaña a usar en el curso de desarrollo web, creo que es bastante fácil de usar y muy práctico.

	* **Daniel Esteves** [732475] (1)

		Hola Yoel! Si de hecho ese error es de CORS ya que si no se está utilizando un servidor ahora la API lo está bloqueando

* **Alejandro González Reyes** (3) [576602](https://platzi.com/comentario/576602/) 

	Mi aporte, más sencillo que el presentado por Leonidas.  
	Mi función de template para cada pelicula
	```     	function videoItemTemplate(movie) {
	    		// Otro punto importante a destacar en esta sección, son los atributos "data-"" declarados en la firma del elemento
	    		// Estos atributos permiten pasar información adicional al elemento, informacion que no sera visible en pantalla, pero....
	    		// que puede recuperarse a través de javascript para darle usos especificos. (pasarla a otros elementos)
	    		// En este caso, declaramos esta información para cada pelicula, cuando se hace clic en el elemento, esta info se pasa al modal para que se muestre en pantalla 
	    		return `<div class="primaryPlaylistItem" data-title="${movie.title}" data-description="${movie.synopsis}" data-cover="${movie.medium_cover_image}">
	    			<div class="primaryPlaylistItem-image">
	    				<img src="${movie.medium_cover_image}">
	    			</div>
	    			<h4 class="primaryPlaylistItem-title">${movie.title}</h4>
	    		</div>`
	    	}
	    
	```
	
	La función controladora del evento click para cada mostrar el modal
	```     functionaddEventClick($container) {
	    		// Seleccionamos todos los elementos con clase primaryPlaylistItem (peliculas)
	    		items = $container.getElementsByClassName('primaryPlaylistItem')
	    		// Recorremos la lista de nodos
	    		for(let $item of items) {
	    			// Por cada elemento HTML (pelicula), añadirmos un manejador de eventos de tipo click
	    			// El segundo parametro es una funcion que se activa para controlar las tareas asociadas a el evento
	    			// Recibe un objeto de tipo Event que contiene informacipon del evento
	    			$item.addEventListener('click', function(event) {
	    				// Creamos un objeto con los datos genericos de la pelicula seleccionada
	    				// para mostrarlos internamente en el modal.
	    				// 
	    				// Cada elemento item en su firma HTML tiene atributos "data" con información propia de la pelicula
	    				// Para ello hago una consulta a la interfaz DataSet para recuperar sus respectivos valores.
	    				// En este caso "this" hace referencia al elemento actual (contenedor de pelicula con clase primaryPlaylistItem)
	    				const info = {
	    					titulo: this.dataset.title,
	    					descripcion: this.dataset.description,
	    					cover: this.dataset.cover
	    				}
	    				// La instrucción es ejecutar una función que internamente muestra el modal en pantalla.
	    				// Recibe un parametro, que es un objeto con los datos genericos de la pelicula a mostrar dentro del modal 
	    				showModal(info)
	    			})
	    		}
	    	}
	    
	```
	
	La funcipo n modal
	```     function showModal(infoMovie) {
	    		// Añadir una clase de CSS al elemento sleccionado
	    		$overlay.classList.add('active')
	    		// La animación se llama modalIn, dura .8seg y cuando finaliza se queda en el estado final
	    		$modal.style.animation = 'modalIn .8s forwards'
	    		// Insertamos como contenido de texto, los datos de la pelicula seleccionada
	    		$modalTitle.textContent = infoMovie.titulo
	    		$modalDescrition.textContent = infoMovie.descripcion
	    		// En este caso, la imagen se le asigna como valor de atributo src, el dato de cover correspondiente a la imagen seleccionada
	    		// Esto se puede hacer mediante setAttribute( )
	    		$modalImage.src = infoMovie.cover
	    	}
	    
	```

	* **Alejandro González Reyes** [576602] (1)

		Con estas instrucciones nos evitamos el tener que buscar el id de la pelicula dentro de nuestras listas, y usar la estrutura switch y todas esas cosas.

* **Uralberto** (2) [610069](https://platzi.com/comentario/610069/) 

	Tal cual me sucede a mi, danielazocardev. Me sumo a tu voz para pedir ayuda.
	
	La consola dice:
	
	_Access to fetch at ‘<https://yts.am/api/v2/list_movies.json?genre=action>’ from origin ‘null’ has been blocked by CORS policy: No ‘Access-Control-Allow-Origin’ header is present on the requested resource. If an opaque response serves your needs, set the request’s mode to ‘no-cors’ to fetch the resource with CORS disabled.  
	home.js:408 Uncaught (in promise) TypeError: Failed to fetch__

	* **Daniela Yumi Ota** [610069] (3)

		Te pongo mi respuesta a la que dí a danielazocardev y espero que les sirva.
		
		“Yo tuve el mismo problema, lo que pasa es que no podés realizar consultas de la API si estás abriendo como file://. Tenés que subir tu proyecto a https:// o http://, para eso podés usar GitHub.  
		Espero haberme expresado bien.”

	* **Daniel Esteves** [610069] (1)

		Hola @Uralberto, este error es debido a que ahora la nueva versión de la API solo acepta solicitudes de páginas que estén en algún servidor, para simular esto puedes utilizar un servicio como XAMPP que te ayudará a montar tu servidor local y servir la página para que puedas realizar solicitudes HTTP

* **Daniel Azocar** (2) [608721](https://platzi.com/comentario/608721/) 

	Hola amigos como esta? alguien me puede ayudar con un problema que tengo a la hora de hacer el proyecto… creo que me bloquearon el acceso al API miren lo que me sale en la consola
	
	been blocked by CORS policy: No ‘Access-Control-Allow-Origin’ header is present on the requested resource. If an opaque response serves your needs, set the request’s mode to ‘no-cors’ to fetch the resource with CORS disabled.

	* **Daniela Yumi Ota** [608721] (2)

		Yo tuve el mismo problema, lo que pasa es que no podés realizar consultas de la API si estás abriendo como file://. Tenés que subir tu proyecto a https:// o http://, para eso podés usar GitHub.  
		Espero haberme expresado bien.

	* **Daniel Azocar** [608721] (2)

		Hey gracias @Yumi lo intentare y te aviso

	* **Daniela Yumi Ota** [608721] (4)

		Espero que te sirva, también tuve que cambiar la url que daba Leonidas. En vez de <https://yts.am/> puse <https://yts.lt/>.

	* **Nadir Antonio Soza Solis** [608721] (1)

		Instala una extensión de Chrome para que gestiones las CORS.
		
		Te recomiendo leer sobre esto en [CORS](https://developer.mozilla.org/es/docs/Web/HTTP/Access_control_CORS)

	* **Daniel Esteves** [608721] (2)

		Hola Daniel 😎! la nueva directiva de la API obliga a que todas las solicitudes sean hechas a través de un servidor, serivicios como XAMPP te ayudarán a poder realizar este tipo de cosas 💪

* **pablojorgeandres** (2) [487465](https://platzi.com/comentario/487465/) 

	## [DataSet](https://github.com/pablojorgeandres/notas-clases/blob/master/jQuery-JavaScript.md#clase17)
	
	Con DataSet puedo agregar datos a los tags HTML para luego usarlos de cualquier forma necesaria en JavaScript.  
	La forma de hacerlo es agregar un atributo _“data-nombre”_ a cualquier tag html.
	
	Ej:
	```     En el template agrego _data-id="${movie.id}"_ y _data-category="${category}"_
	    
	```
	```     	functionvideoTemplate(movie, category) {    
	    		return(
	    		    `<div class="primaryPlaylistItem" data-id="${movie.id}" data-category="${category}" >
	    			<div class="primaryPlaylistItem-image">
	    			<img src="${movie.medium_cover_image}">
	    			</div>
	    			<h4 class="primaryPlaylistItem-title">
	    			    ${movie.title}
	    			</h4>
	    		    </div>`
	    		)
	    	}
	    	
	    
	    
	```
	
	A los que después puedo acceder desde JS con el método _.dataset.nombre_.  
	En este caso:
	```     	$element.dataset.id
	    	$element.dataset.category
	    
	    
	```

* **carlosextra1** (1) [1116721](https://platzi.com/comentario/1116721/) 

	uso de dataset para los atributos de data- en el elemento html. Ojo esto es un marcador

* **Kenet Chungandro** (1) [1006005](https://platzi.com/comentario/1006005/) 

	A seguir aprendiendo! va genial

* **powerangelito17** (1) [648752](https://platzi.com/comentario/648752/) 

	Alguien tiene este mismo problema
	```     Accessto fetch at 'https://yts.am/api/v2/list_movies.json?genre=action' from origin 'http://jquery-a-javascript.test' has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present on the requested resource. If an opaque response serves your needs, set the request's mode to'no-cors' to fetch the resource with CORS disabled
	    
	```

	* **Diego Alexander Forero Higuera (Platzi)** [648752] (1)

		Tienes el dominio <http://jquery-a-javascript.test> en tu computador? si es así prueba usando localhost para ver si te funciona, el problema puede ser por el dominio que usas en tu maquina local que te da problema de CORS

	* **onlinejaime** [648752] (4)

		Lo contesté en el vídeo correspondiente. La página web (API) ahora es
		
		<https://yts.lt/api/v2/>
		
		Dadme corazoncitos!!!

	* **Nadir Antonio Soza Solis** [648752] (1)

		Este tema me sucedió con una api que yo mismo cree y la estaba consumiendo desde un sitio. Lo solucione instalando una extencion de chrome que te ayude a gestionar las CORS.
		
		Te dejo un link que habla sobre las [CORS](https://developer.mozilla.org/es/docs/Web/HTTP/Access_control_CORS)

* **Uralberto** (1) [610378](https://platzi.com/comentario/610378/) 

	Tratando de explorar que sucede con el problema reportado en el comentario precedente me encuentro con esta advertencia cuando abro _**<https://yts.lt/>  
	**_  
	_¡Advertencia! Descargar solo con VPN …  
	La descarga de torrents es un riesgo para usted: su ISP y las agencias gubernamentales están rastreando activamente su IP y los datos privados filtrados. ¡Protégete de costosas demandas y multas AHORA! Debes usar una VPN como Windscribe. Es la única forma de descargar torrents de forma totalmente anónima cifrando todo el tráfico con registros cero.
	
	Datos personales que revelan su identidad real: su dirección IP, 191.109.89.36 está expuesta, lo que apunta directamente a su ubicación en Bogotá, COLOMBIA. Está navegando con Chrome 75.0.3770.80 en Windows 10 con una resolución de pantalla de 1920x1080 y la CPU de su dispositivo tiene 4 núcleos.
	
	″ ¡No te arriesgues! Protégete ahora mismo descargando Windscribe VPN ″ - William  
	_  
	  
	Me estoy preocupando. ¿Qué dice la comunidad Platzi?

	* **Diego Alexander Forero Higuera (Platzi)** [610378] (1)

		Hola, es una advertencia para invitarte a comprar un servicio de VPN, si bien esa información que muestra es pública porque es lo que se puede extraer, al pasar por un vpn cambias los datos y toma los del servidor VPN, este sitio es de piratería de películas y no deberías descargar contenido pirata ya que puede infectar tu computador con virus.

* **Félix Alejandro Zelaya Orellana** (1) [531702](https://platzi.com/comentario/531702/) 

	Genial!!

* **LUISFER24** (1) [393832](https://platzi.com/comentario/393832/) 

	me parece súper importante y súper útil esta clase

* **Diego Fernando Rojas Quintero** (1) [86432](https://platzi.com/comentario/1095846/) 
¿porque debo poner en base 10 como parámetro en parseInt?

* **Ang.O** (1) [75257](https://platzi.com/comentario/866605/) 
para que es el parseint?

	* **Gabriel De Andrade (Platzi)** [75257] (1)

		Es para convertir Strings en Integers (Números). [parseInt()](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/parseInt) 😄

* **Ang.O** (1) [75256](https://platzi.com/comentario/866604/) 
7:58 para que es el $0?

	* **Gabriel De Andrade (Platzi)** [75256] (1)

		Hola! El $0 en la consola del navegador te permite acceder rápidamente al elemento body del documento 😄

* **Ang.O** (1) [75255](https://platzi.com/comentario/866599/) 
el valor dinamico viene de la libreria?

	* **Cesar Eliezer Gomez Gutierrez** [75255] (1)

		No, es nativo de JavaScript desde 2015. El símbolo ‘$’ en los valores dinámicos no esta relacionado con jQuery

* **Jair Sebastian Lozano Moron** (1) [61406](https://platzi.com/comentario/621219/) 
me aparece mi data-category= undefined, alguien podría ayudarme? no logro ver donde está mi error, gracias! (async function load() ...

	* **Fernando Alejandro Yerena Ramos** [61406] (2)

		Lo que sucede es que en la función `renderMovieList`, no le estas pasandole el parametro `category` a `videoItemTemplate`:
		```     function renderMovieList(list, $container, category) {
		    	$container.children[0].remove()
		    	list.forEach((movie) => {
		    
		    		const HTMLString = videoItemTemplate(movie); /*Aquí <-- Falta pasarle category*/
		    
		    		const movieElement = createTemplate(HTMLString)
		    		$container.append(movieElement)
		    		addEventClick(movieElement)
		    	})
		    }
		    
		```

## 0180. Encontrando elementos en la lista [12336](https://platzi.com/clases/1316-jquery-js/12336-encontrando-elementos-en-la-lista/)

### Descripción:


### Comentarios:

* **Alan Riveros** (27) [514003](https://platzi.com/comentario/514003/) 

	Si queres Saber cuanto tiempo en Horas te va a tomar hacer un curso de platzi te dejo este sencillo SCRIPT
	```     var asd = document.querySelectorAll('span.MaterialContent-duration')
	    window.counter = 0;
	    asd.forEach(function(el){
	    	var strel = el.innerHTML.split(':')[0];
	    	if(strel.length > 0){
	    		window.counter += parseInt(strel, 10)
	    	} else {
	    		return
	    	}
	    });
	    console.log('Este Curso te tomara: ', window.counter / 60)
	    
	```

	* **Jose Padron (Platzi)** [514003] (1)

		excelente script, tome su like buen señor

	* **emanueljtc** [514003] (1)

		Oye Genial jeje.

	* **Aaron Felipe Isaacs** [514003] (1)

		Me será muy útil para hacer estimaciones!
		
		👊👊👊

	* **ikarioshimai** [514003] (1)

		discula mi ignoranca pero como lo uso

	* **Nadir Antonio Soza Solis** [514003] (2)

		  * Te ubicas sobre la pagina principal del curso.
		
		  * Luego abres la consola
		
		  * Pegas el script presionas Enter. 😉

* **Sergio Minei** (17) [336238](https://platzi.com/comentario/336238/) 

	## Encontrando elementos en lista (find)
	
	find() devuelve el primer elemento de un array que cumpla con el criterio de búsqueda. Si no se encuentra ningún elemento devuelve undefined.
	```     functionfind(list, id) {
	      return list.find(movie => movie.id === id)
	    }
	    
	```
	
	Información referencial:  
	<https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Array/find>
	
	## switch
	```     switch (category) {
	      case"action": {
	        //código de action
	      }
	      case"drama": {
	        //código de drama
	      }
	      default: {
	        //código por defecto
	      }
	    }
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

	* **Pedro Caicedo** [336238] (8)

		Si se utiliza {} en los cases del switch no hace falta usar el break 😃 no lo sabía…

	* **Sergio Minei** [336238] (2)

		si, a mi también me pareció interesante eso.

	* **tomas-alegre146** [336238] (2)

		interesante

	* **Facundo Nicolás García Martoni (Platzi)** [336238] (3)

		No lo sabía tampoco, tome su merecido like

	* **jesus-gallardo** [336238] (2)

		Interesante, aunque me parece mas legible usar Breack

* **Sergio Minei** (9) [336237](https://platzi.com/comentario/336237/) 

	También se puede usar `~~` ( _double not bitwise operator_ ) para convertir un texto a decimal.
	```     const numero = "10";
	    
	    const decimal_parse = parseInt(numero, 10);
	    const decimal_bitwise = ~~numero;
	    
	```
	
	En los 2 casos sería igual.

	* **Juan David Castro (Platzi)** [336237] (8)

		El termino científico para el carácter `~` es: _**colita de chancho**_ 🐷 😬

	* **Sergio Minei** [336237] (3)

		hahaha. Aprendiendo a usar el operador “colita de chancho” 😛

* **Carlos Coronel Blancas** (5) [984750](https://platzi.com/comentario/984750/) 

	Aunque por un momento hubieron varios buggs… me pareció bien que aparecieran, asi no solo nos muestran el camino ideal de codificar un problema, sino tambien en que cosas podemos pensar que estan fallando y que debemos hacer para corregirlos, ademas que creo que eso hace un poco mas humano al programador…

	* **Rodrigo Cuenca Mayuri** [984750] (1)

		Tal cual

* **ivan_acg** (5) [418738](https://platzi.com/comentario/418738/) 

	Me encanta cuando tiene bugs en medio del desarrollo , asi se aprende a pensar como resolver errores .

* **jlquichimbo** (4) [478769](https://platzi.com/comentario/478769/) 

	Ctrl + Shift + K  
	Se puede usar para borrar la línea completa donde esté ubicado el cursor.

* **arlex_ee10** (3) [1006140](https://platzi.com/comentario/1006140/) 

	Que bueno :V ya casi terminamos 😃 tendré que ver otra vez el curso para quedar al 100%

* **asismelgarejo** (3) [886910](https://platzi.com/comentario/886910/) 

	También existe un método llamado **filter** que devuelve los elementos que cumplan con la condición especificada.

	* **rafagonzalez99** [886910] (3)

		Pero el quiere retornar el primer registro encontrado.

* **Giancarlo Vera** (3) [473970](https://platzi.com/comentario/473970/) 

	Esta clase estuvo muy buena. Para los que usan VSCode como yo, pueden instalar la extensión Live Server que incluye live reload cada vez que guardamos los cambios en el editor .  
	[](https://marketplace.visualstudio.com/items?itemName=ritwickdey.LiveServer)

* **Alan Santiago** (3) [364064](https://platzi.com/comentario/364064/) 

	Paso de ser un **Encontrando elementos en la lista** a un **Cazando bugs >:c**

* **Germán González** (2) [1020816](https://platzi.com/comentario/1020816/) 

	La API de yts cambió ahora es [yts.mx](http://yts.mx), de nada.

* **Kenet Chungandro** (2) [1006496](https://platzi.com/comentario/1006496/) 

	Extenso pero super nutritiva esta clase!

* **ilan.vivanco** (2) [678760](https://platzi.com/comentario/678760/) 

	Nunca había visto esa manera de declarar los switch, usando `{}` en vez de `breaks`.

	* **ricardocelis (Platzi)** [678760] (1)

		JS es un lenguaje muy permisivo!

* **pablojorgeandres** (2) [488132](https://platzi.com/comentario/488132/) 

	## [Encontrando elementos en la lista](https://github.com/pablojorgeandres/notas-clases/blob/master/jQuery-JavaScript.md#clase18)
	
	<h3>[Find](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Array/find)</h3>
	
	Para seleccionar un elemento particular de un array usamos el método [_find()_](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Array/find).
	
	El método [_find()_](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Array/find) itera por los elementos de un array y devuelve el valor del primer elemento que cumple con la condición que le pasamos a travéz de la función parámetro que recibe.  
	Si ningún valor coincide con la condición de búsqueda devuelve _undefined_.  
	Vale remarcar que _no itera por todos los elementos del array_ , sino sólo hasta el elemento que devuelve.  
	Recibe una función de parámetro y esta a su vez un parámetro _this_ que referencia a mismo array.
	```     	var arrayX = [5, 12, 8, 130, 44, 180]
	    	
	    	var found = arrayX.find( e => e > 10 )
	    	var found2 = arrayX.find( e => e < 190 )
	    
	    	console.log(found)
	    	// 12
	    
	    	console.log(found2)
	    	// 5
	    
	    
	```
	
	<h3>[Switch](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/switch)</h3>
	
	Otra estructura usual de datos es el [_switch_](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/switch).  
	Es una estructura multiple-condicional y se utiliza normalmente cuando los condicionales son 3 o más.  
	Su funcionamiento es similar a un if- else if - else.
	
	Este evalúa una expresión y la compara con el valor que asignamos en cada _case_ , si coincide con alguno ejecuta el código dentro de ese _case_ , sino sale por el _default_ case.
	
	Sintaxis:
	```     	var frut = 'Papayas';
	    	
	    	switch (frut) {
	    	  case'Oranges':
	    	    console.log('Oranges are $0.59 a pound.')
	    	    break
	    	  case'Mangoes':
	    	  case'Papayas':
	    	    console.log('Mangoes and papayas are $2.79 a pound.')
	    	    break
	    	  default:
	    	    console.log('Sorry, we are out of ' + expr + '.')
	    	}
	    	
	    	// "Mangoes and papayas are $2.79 a pound."
	    
	    
	```

* **edesalla17** (2) [342266](https://platzi.com/comentario/342266/) 

	vamos bien, me gusta este curso

* **carlosextra1** (1) [1116929](https://platzi.com/comentario/1116929/) 

	El if se me hace más ligero y mas limpio que el switch. Esto es algo que me gusta a mi en lo personal.
	```     functionfindMovie(id, category){
	        if(category === 'action')
	          return findById(actionList, id)
	        elseif(category === 'drama')
	          return findById(dramaList, id)
	        else
	          return findById(animationList, id)
	      }
	    
	```

* **33andres33** (1) [1080867](https://platzi.com/comentario/1080867/) 

	Gracias a un error de data por falta de unas comillas entendí mucho mejor el código

* **asismelgarejo** (1) [886930](https://platzi.com/comentario/886930/) 

	😄

* **Luis Rangel Castro** (1) [791402](https://platzi.com/comentario/791402/) 

	```    const BASE_API = 'https://yts.lt/api/v2/';
	    
	    async function getData(uri) {
	      const res = await fetch(uri);
	      const data = await res.json();
	      return data;
	    }
	    
	    function setAttributes($element, attributes) {
	      for (const attribute in attributes) {
	        $element.setAttribute(attribute, attributes[attribute]);
	      }
	    }
	    
	    const $form = document.getElementById('form');
	    const $featuringContainer = document.getElementById('featuring');
	    
	    function featureTemplate(movie) {
	      return (
	        `
	    <div class="featuring">
	      <dif class="faturing-image">
	        <img = src="${movie.medium_cover_image}" width="70px" height="100" alt="">
	      </div>
	    <div class="featuring-content">
	      <p class="featuring-title">Pelicula encontrada</p>
	      <p class="featuring-album">${movie.title}</p>
	    </div>
	      `);
	    }
	    
	    function featureErrorTemplate() {
	      return (
	        `
	        <div class="featuring">
	        <div class="featuring-content">
	          <p class="featuring-title">Lo sentimos</p>
	          <p class="featuring-album">La pelicula solicitada no fue encontrada</p>
	        </div>
	      </div>
	      `);
	    }
	    
	    $form.addEventListener('submit', async (e) => {
	      // evita que haga el submit
	      e.preventDefault();
	    
	      const $home = document.getElementById('home');
	    
	      $home.classList.add('search-active');
	    
	      const $loader = document.createElement('img');
	      setAttributes($loader, {
	        src: 'src/images/loader.gif',
	        height: 50,
	        width: 50
	      });
	    
	      $featuringContainer.firstChild.remove();
	      $featuringContainer.append($loader);
	    
	      const data = new FormData(form);
	    
	      let xx;
	      const movie = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`)
	        // .then(({ data: { movies: pelis } }) => {
	        .then((res) => {
	          xx = res;
	          $featuringContainer.children[0].remove();
	          let HTMLString;
	          if (res.data.movies.length != 0) {
	            HTMLString = featureTemplate(res.data.movies[0]);
	          } else {
	            HTMLString = featureErrorTemplate();
	          }
	          $featuringContainer.innerHTML = HTMLString;
	        })
	        .catch(() => {
	          let HTMLString = featureErrorTemplate();
	          $featuringContainer.innerHTML = HTMLString;
	        });
	      console.log(movie);
	      console.log(xx);
	    });
	    
	    const $hideModal = document.getElementById('hide-modal');
	    $hideModal.addEventListener('click', hideModal);
	    
	    function hideModal() {
	      const $overlay = document.getElementById('overlay');
	      const $modal = document.getElementById('modal');
	    
	      $overlay.classList.remove('active');
	      $modal.style.animation = 'modalOut .8s forwards';
	    }
	    
	    function showModal($element) {
	      const $overlay = document.getElementById('overlay');
	      const $modal = document.getElementById('modal');
	    
	      const $modalTitle = $modal.querySelector('h1');
	      const $modalImage = $modal.querySelector('img');
	      const $modalDescription = $modal.querySelector('p');
	    
	      $overlay.classList.add('active');
	      $modal.style.animation = 'modalIn .8s forwards';
	    
	      const id = $element.dataset.id;
	      const category = $element.dataset.category;
	      const title = $element.dataset.title;
	    
	      const data = findMovie(id, category);
	    
	      $modalTitle.textContent = data.title;
	      $modalImage.setAttribute('src',data.medium_cover_image);
	      $modalDescription.textContent = data.description_full;
	    }
	    
	    function findList(list, id) {
	      return list.find(movie => movie.id === parseInt(id));
	    }
	    
	    function findMovie(id, category) {
	      switch (category) {
	        case "action": {
	          return findList(actionList, id);
	        }
	        case "drama": {
	          return findList(dramaList, id);
	        }
	        default: {
	          return findList(animationList, id);
	        }
	      }
	    }
	    
	    function addClickMovie(movieId) {
	      const $movie = document.getElementById(`movie_${movieId}`);
	      $movie.addEventListener('click', function () {
	        showModal($movie);
	      });
	    }
	    
	    function videoItemTemplate(movieId, src, title, movie, category) {
	      return (
	        `<div class="primaryPlaylistItem" id="movie_${movieId}" data-id="${movie.id}" data-category="${category}" data-title="${title}">
	          <div class="primaryPlaylistItem-image">
	            <img src="${src}">
	          </div>
	          <h4 class="primaryPlaylistItem-title">${title}</h4>
	        </div>`
	      );
	    }
	    
	    function loadingImage() {
	      return (`<img src="src/images/loader.gif" width="50" height="50" alt="cargando peliculas">`);
	    }
	    
	    async function loadMovie(type) {
	      try {
	        const $movieContainer = document.getElementById(type);
	        $movieContainer.innerHTML = loadingImage();
	    
	        const data = await getData(`${BASE_API}list_movies.json?genre=${type}`);
	    
	        renderMovieList(data, $movieContainer, type);
	    
	        return data.data.movies;
	      }
	      catch (err) {
	        console.log('error al ejectura loadMovie', err);
	      }
	    }
	    
	    function renderMovieList(data, $container, category) {
	      let stringHTML = "";
	    
	      data.data.movies.forEach((movie) => {
	        const HTMLString = videoItemTemplate(movie.id, movie.medium_cover_image, movie.title, movie, category);
	        stringHTML += HTMLString;
	      });
	      $container.innerHTML = stringHTML;
	    
	      data.data.movies.forEach((movie) => {
	        addClickMovie(movie.id);
	      });
	    }
	    
	    var animationList = null;
	    var actionList = null;
	    var dramaList = null;
	    
	    const promise1 = loadMovie('drama')
	      .then((res) => {
	        dramaList = res;
	      });
	    const promise2 = loadMovie('action')
	      .then((res) => {
	        actionList = res;
	      });
	    const promise3 = loadMovie('animation')
	      .then((res) => {
	        animationList = res;
	      });
	    
	```

* **Camilo Andrés Santana Lizcano** (1) [695834](https://platzi.com/comentario/695834/) 

	La clase más extensa y nutrida del curso

* **Félix Alejandro Zelaya Orellana** (1) [531718](https://platzi.com/comentario/531718/) 

	Super!!

* **heysoypaez** (1) [511237](https://platzi.com/comentario/511237/) 

	Debiamos haber hecho el código de esta forma con los objetos desestructurados desde el principio, leemos mucho mejor los datos de la pelicula en la consola :) ¿El problema? ¡No sabiamos como hacerlo!

* **jlquichimbo** (1) [478813](https://platzi.com/comentario/478813/) 

	Se puede extraer un html completo con fetch?  
	Lo que necesito es hacer un reporte en Django, lo hacía con ajax, y tenía en un template table_result.html la estructura del resultado con los botones correspondientes por cada fila (editar, ver, eliminar).  
	Quiero hacerlo con fetch, puesto que el table_result.html crea el número de filas con un for y llena los registros que le envío con un QuerySet.  
	Cómo puedo conseguir esto con fetch? Puesto que crear el html directo en JS me ha tomado más de 60 líneas de código para crear cada tr, td y botones.  
	Su ayuda por favor.

	* **Eduardo Hidalgo Díaz Rugama** [478813] (3)

		un fetch te sirve para hacer una recuperación. tal cual. En el caso de tu problema, deberías usar fetch para recuperar información de una API.
		
		Es algo que se hace muchísimo, pides a la API el html que te falta por rendedear. Next.js funciona asi, hacer server side rendering, significa que cuando tu entras a una página web, la página fué generada en el servidor, y tu pc simplemente la dibuja.

* **Byhako** (1) [345307](https://platzi.com/comentario/345307/) 

	Creo que se cayo la api de las peliculas 😦
	
	![error.png](https://static.platzi.com/media/user_upload/error-36cb0e8b-6304-4f0d-9e7e-79a395e0cb07.jpg)

	* **JPabloMayorgaM** [345307] (1)

		Actualmente funciona, una forma sencilla de probar es usar la extensión de chrome “Cliente de servicios Rest Full”

	* **Jair Israel Avilés Eusebio** [345307] (1)

		Sustituye la url de <https://yts.am> por <https://yts.lt> y listo.

* **Diego Fernando Rojas Quintero** (1) [86193](https://platzi.com/comentario/1090404/) 
¿Porque al transformar un array a un numero, leonidas debe poner un 10 como parametro en parseInt()?

	* **Stiven Alejandro Manosalvas** [86193] (1)

		No es obligatorio colocar el 10 como parámetro en el parseInt, pero es recomendable usarlo, ya que este indica la base del número (podría en algún caso ahorrarte un dolor de cabeza 😄) , por ejemplo algunos son 10 Decimal, 8 Octal y 16 es Hexadecimal.  
		También podrias hacer una prueba en tu navegador
		
		parseInt(10, 16)  
		16  
		parseInt(10, 8)  
		8  
		parseInt(10, 10)  
		10  
		parseInt(10, 2)  
		2  
		Espero te sirva 😃

* **hdmjesus** (1) [84102](https://platzi.com/comentario/1042999/) 
Wowww , excelente.

* **diego rodriguez** (1) [83022](https://platzi.com/comentario/1017795/) 
soy al unico al que siempre la primer pelicula de cada categoria es batman vs robin? es curioso y no se como se puede resolver ese eror

* **jlquichimbo** (1) [50350](https://platzi.com/comentario/478813/) 
Se puede extraer un html completo con fetch? Lo que necesito es hacer un reporte en Django, lo hacía con ajax, y tenía en un template tab...

	* **Eduardo Hidalgo Díaz Rugama** [50350] (3)

		un fetch te sirve para hacer una recuperación. tal cual. En el caso de tu problema, deberías usar fetch para recuperar información de una API.
		
		Es algo que se hace muchísimo, pides a la API el html que te falta por rendedear. Next.js funciona asi, hacer server side rendering, significa que cuando tu entras a una página web, la página fué generada en el servidor, y tu pc simplemente la dibuja.

## 0190. Animaciones [12337](https://platzi.com/clases/1316-jquery-js/12337-animaciones7551/)

### Descripción:


### Comentarios:

* **Sergio Minei** (15) [336240](https://platzi.com/comentario/336240/) 

	Se puede agregar una animación a un elemento agregándole la clase de la animación respectiva.
	
	Dejo mi código de la función `renderMovieList` por si a alguien le interesa.
	```     functionrenderMovieList(list, $container, category) {
	      $container.children[0].remove();
	      list.forEach((movie) => {
	        const HTMLString = videoItemTemplate(movie, category);
	        const movieElement = createTemplate(HTMLString);
	        $container.append(movieElement);
	        const image = movieElement.querySelector("img");
	    
	        image.addEventListener("load", (event) => {
	          event.srcElement.classList.add("fadeIn");
	        })
	    
	        addEventClick(movieElement);
	      });
	    }
	    
	```

* **Juan David Castro (Platzi)** (13) [341233](https://platzi.com/comentario/341233/) 

	😅 No conocía el evento de `load` o carga de las imágenes, es muy útil! 🎉

	* **Oscar Barajas Tavares (Platzi)** [341233] (9)

		Te salva la vida en momentos donde el rendimiento es 10000% necesario.

* **Sneyder Alfonso Barreto Buitrago** (11) [905678](https://platzi.com/comentario/905678/) 

	La propiedad **srcElement** ya no es recomendada de usar. En cambio, se puede utilizar la propiedad **target**.  
	[Un poco de documentación aquí.](https://developer.mozilla.org/en-US/docs/Web/API/Event/srcElement)

	* **Lluis Pitarch Ripolles** [905678] (1)

		Funciona perfecto con el código que comentas:
		```     <image.addEventListener('load', (event) => event.target.classList.add('fadeIn'))>
		    
		```

* **pablojorgeandres** (6) [488276](https://platzi.com/comentario/488276/) 

	## [Animaciones](https://github.com/pablojorgeandres/notas-clases/blob/master/jQuery-JavaScript.md#clase19)
	
	Tanto como en CSS puro, con javaScript también podemos hacer efectos de fade con CSS.
	
	En esta clase obtenemos el objeto _image_ del template que generamos para cada película y le agregamos una clase previamente declarada en nuestro CSS.  
	Usamos el eventListener _‘load’_ para imprimir todas las imágenes juntas una vez traidas de la API.
	```     	.fadeIn {
	    	  animation: 2000ms fadeIn;
	    	}
	    
	    	@keyframes fadeIn {
	    	  0% {
	    	    opacity: 0;
	    	  }
	    
	    	  100% {
	    	    opacity: 1;
	    	  }
	    	}
	    
	    
	```
	```     const image = movieElement.querySelector('img')
	    image.addEventListener( 'load', event => event.srcElement.classList.add('fadeIn') )
	    
	    
	```
	
	También separamos el código de los llamados asíncronos a la API para que al obtener un género de películas las imprima directamente. Esto va a ayudar a optimizar nuestra aplicación en cuanto a tiempo de carga.

* **Juan David Castro (Platzi)** (6) [341230](https://platzi.com/comentario/341230/) 

	✨ Las animaciones y la interactividad son muy importantes en el desarrollo frontend.  
	👌 Excelente clase!

* **arlex_ee10** (4) [1006882](https://platzi.com/comentario/1006882/) 

	Genial a piratear películas

* **Enrique Felix Garcia Stave** (3) [750331](https://platzi.com/comentario/750331/) 

	 **srcElement:** Es aquel elemento desde el cual se origina el evento (en este caso seria el elemento de imagen).

* **Giancarlo Vera** (2) [474880](https://platzi.com/comentario/474880/) 

	Dado que opté por usar el “+= innerHTML” en las primeras clases en lugar de primero crear el html element y luego agregarlo al container con el append (tal como lo enseño Leonidas), al llegar a este punto donde se tiene que hacer el addEventListener me quedé atascado por unos minutos, pero logré resolverlo con unas lineas más de código:
	```     function movieClick($container) {
	        const movieList = Array.from($container.children);
	        movieList.forEach(($movieElement) => {
	          $movieElement.addEventListener('click', function () {
	            showModal($movieElement)
	          })
	        })
	      }
	    
	      function renderMovieList(list, $container, category) {
	        $container.children[0].remove();
	        list.forEach((movie) => {
	          const HTMLString = videoTemplate(movie, category);
	          $container.innerHTML += HTMLString;
	        })
	        const imageList = Array.from($container.querySelectorAll('img'))
	        console.log(imageList)
	        imageList.forEach((image) => {
	          image.addEventListener('load', (event) => {
	            event.srcElement.classList.add('fadeIn')
	          })
	        })
	        movieClick($container);
	      }
	    
	```

	* **Giancarlo Vera** [474880] (1)

		Ahora conozco dos formas de hacerlo, pero el mejor método es el de Leo por tener código más limpio 😃

	* **David Alejandro Mosquera Moreno** [474880] (1)

		Estaba atascado aquí también. Y cuando traté de hacerlo sin Array.from solo aplicaba el efecto a la primera película de cada lista.

* **carlosextra1** (1) [1117002](https://platzi.com/comentario/1117002/) 

	siempre es un plus y es de pros agregar animaciones que hagan que las aplicaciones se vean mas chingonas!

* **Kenet Chungandro** (1) [1006763](https://platzi.com/comentario/1006763/) 

	Excelente va quedando genial el proyecto! 😃

* **iridian** (1) [891881](https://platzi.com/comentario/891881/) 

	muy buena clase 😄

* **GUIFOR PAUL PLAZA AGUIRRE** (1) [595318](https://platzi.com/comentario/595318/) 

	Se puede ampliar mas en las animaciones.  
	El curso de animaciones muy pronto lo tomare.

* **GUIFOR PAUL PLAZA AGUIRRE** (1) [595305](https://platzi.com/comentario/595305/) 

	Excelente clase.

* **Cesar Augusto Barco de Gouveia** (1) [555103](https://platzi.com/comentario/555103/) 

	Cuando se intenta hacer un Fetch, de alguna API, me pasa que me lanza un error. Algo que tiene que ver con seguridad a la hora de un servidor solicitar datos de otros. Pero si lo hago con Jquery con la funcion $.getJSON si funciona. ¿Me pueden explicar como obtener ese dato con JS Vanilla?

	* **Jann Carlo Moreno Jiménez** [555103] (1)

		podrias subir el codigo con Javascript puro y con JQuery por favor? asi sera mas facil ayudarte

	* **Cesar Augusto Barco de Gouveia** [555103] (1)

		Ya entendí que era lo que sucedía. Estaba usando una API que funcionaba con JSONP. Un json envuelta en una función. Con $.getJSON se resolvñia ese problema.
		
		  * Muchas gracias.

	* **powerangelito17** [555103] (1)

		cesar, tenías este mismo error
		```     Accessto fetch at 'https://yts.am/api/v2/list_movies.json?genre=action' from origin 'http://jquery-a-javascript.test' has been blocked by CORS policy: Response to preflight request doesn't pass access control check: Redirect isnot allowed for a preflight request.
		    
		```

* **Félix Alejandro Zelaya Orellana** (1) [531728](https://platzi.com/comentario/531728/) 

	Super!!

* **heysoypaez** (1) [512791](https://platzi.com/comentario/512791/) 

	¿Por què sientes que las animaciones son importantes, ùtlies, valiosas para una aplicación web?

	* **Eduardo Hidalgo Díaz Rugama** [512791] (4)

		Las animaciones casi no tienen ningún valor por si solas.
		
		La web se define como “una interfaz de interacción”. De hecho, la web suele ser orientado a eventos (es decir, que una web debe actuar en respuesta a eventos para interactuar con el usuario).
		
		Por lo tanto, una animación debe ejecutarse como respuesta a un evento, para demostrar interacción con el usuario. Yo lo englobaría en la UX.
		
		entonces: las animaciones son un elemento clave para llevar experiencia de usuario al cliente. Es la manera de comunicar la experiencia.

* **heysoypaez** (1) [512130](https://platzi.com/comentario/512130/) 

	Dejo mi codigo:
	
	JS
	```     //Funcion para mostrar en consola las peliculas
	    		movieList.forEach( (movie) => {
	    			
	    			const HTMLString = videoItemTemplate( movie , category )
	    			const movieElement = createTemplate(HTMLString) //Convirtiendo en HTML
	    
	    			$container.append(movieElement) //Pasando elemento al DOM
	    
	    			const image = movieElement.querySelector("img")
	    			image.addEventListener("load",(event) => {
	    
	    				/*colocamos event.srcElement, srcElement es el elemento
	    				que lanzó el evento*/
	    
	    				event.srcElement.classList.add("fadeIn") //agregando animacion css usando clases
	    
	    			})
	    		
	    
	```
	
	CSS
	```     .fadeIn {
	      animation: 5000ms fadeIn;
	    }
	    
	    @keyframes fadeIn {
	      0% {
	        opacity: 0;
	      }
	    
	      100% {
	        opacity: 1;
	      }
	    }
	    
	    
	```

* **JhonColorado07** (1) [490634](https://platzi.com/comentario/490634/) 

	Excelente clase. La animación es super importante en Fronted.

* **Ricardo Medina** (1) [425783](https://platzi.com/comentario/425783/) 

	Excelente clase el poder de javascript empoderando a los desarrolladores para crear web dinámicas. Muy buena clase Leonidas,

* **lantog** (1) [416122](https://platzi.com/comentario/416122/) 

	También se podría agregar el evento en la misma generación del template de las movies, proof of concept:
	
	function get_film_template(src_image, title_image) {  
	return (  
	`<div class="primaryPlaylistItem"> <div class="primaryPlaylistItem-image"> <img src="${src_image}" alt="Logo de ${title_image}" onload="add_animation(event)"> </div> <h4 class="primaryPlaylistItem-title"> ${title_image} </h4> </div>`  
	)  
	}
	
	La función que llama cuando se carga:  
	function add_animation(event) {  
	event.srcElement.classList.add(‘fadeIn’);  
	}

* **Alex Monroy** (1) [386207](https://platzi.com/comentario/386207/) 

	Muchachos
	
	Tengo un error con el listener animationend, resulta que quita la clase active, al dar click en la segunda película y queda el modal sin el $overlay activo, porque sucede esto si se supone que al dar click en boton es que ocurre y no cuando voy a abrir otra película.
	
	Ayuda

	* **evertescalante1** [386207] (0)

		Podrias compartir tu codigo seria bueno

* **David Daniel Castillo Nava** (1) [351405](https://platzi.com/comentario/351405/) 

	 ** _Mi código hasta ahora._**
	```     const API_BASE = 'https://yts.am/api/v2';
	    
	    (async function load(){
	    
	    	/*
	    	** Initial Variables
	    	*/
	    	const $home = document.getElementById('home');
	    	const $form = document.getElementById('form');
	    	const $featuringContainer = document.getElementById('featuring');
	    
	    	const $actionContainer = document.querySelector('#action');
	    	const $dramaContainer = document.getElementById('drama');
	    	const $animationContainer = document.getElementById('animation');
	    
	    	const $modal = document.getElementById('modal');
	    	const $overlay = document.getElementById('overlay');
	    
	    	const $hideModal = document.getElementById('hide-modal');
	    	
	    	const $modalTitle = $modal.querySelector('h1');
	    	const $modalImage = $modal.querySelector('img');
	    	const $modalDescription = $modal.querySelector('p')
	    
	    	/*
	    	** Initial Fetch
	    	*/
	    	let [ reqAction, reqDrama, reqAnimation ] = await Promise.all([
	    		fetch(`${API_BASE}/list_movies.json?genre=action`),
	    		fetch(`${API_BASE}/list_movies.json?genre=drama`),
	    		fetch(`${API_BASE}/list_movies.json?genre=animation`)
	    	])
	    
	    	let { data: {movies: actionList }} = await reqAction.json()
	    	renderList(actionList, $actionContainer, 'listPlayer',{click:true, render: 'append', fadeIn: true}, 'action')
	    	let { data: {movies: dramaList }} = await reqDrama.json()
	    	renderList(dramaList, $dramaContainer, 'listPlayer',{click:true, render: 'append', fadeIn: true}, 'drama')
	    	let { data: {movies: animationList }} = await reqAnimation.json()
	    	renderList(animationList, $animationContainer, 'listPlayer',{click:true, render: 'append', fadeIn: true}, 'animation')
	    
	    	/*
	    	** Functions
	    	*/
	    	async function getData(url) {
	    		const response = await fetch(url);
	    		const data = await response.json()
	    		return data;
	    	}
	    
	    	function setAttributes($element, attributes) {
	    		for (const attribute in attributes) {
	    			$element.setAttribute(attribute, attributes[attribute])
	    		}
	    	}
	    
	    	function templateContent (template, content) {
	    		let hmtlContent;
	    		switch(template) {
	    			case 'listPlayer':
	    				hmtlContent = `
	    					<div data-id="${content.id}" data-category="${content.category}" class="primaryPlaylistItem">
	    						<div class="primaryPlaylistItem-image">
	    							<img src="${content.medium_cover_image}">
	    						</div>
	    						<h4 class="primaryPlaylistItem-title">
	    							${content.title_long}
	    						</h4>
	    					</div>
	    				`
	    				break;
	    			case 'featuringTemplate':
	    				hmtlContent = `
	    					<div class="featuring">
	    						<div class="featuring-image">
	    							<img src="${content.medium_cover_image}" width="70" height="100" alt="">
	    						</div>
	    						<div class="featuring-content">
	    							<p class="featuring-title">Pelicula encontrada</p>
	    							<p class="featuring-album">${content.title_long}</p>
	    						</div>
	    					</div>
	    				`
	    				break;
	    			default:
	    				hmtlContent = ''
	    		}
	    
	    		const html = document.implementation.createHTMLDocument();
	        	html.body.innerHTML = hmtlContent;
	    
	    		return html.body.children[0];
	    	}
	    
	    	function renderList (list, $elemParent, template, options, category) {
	    		$elemParent.children[0].remove();
	    		list.forEach((content)=>{
	    			content.category = category
	    			const $elementContent = templateContent(template, content)
	    
	    			if (typeof options.fadeIn != "undefined") {
	    				const image = $elementContent.querySelector('img')
	    
	    				image.addEventListener('load', (e)=>{
	    					e.srcElement.classList.add('fadeIn')
	    				})
	    			}
	    
	    			if (options.render === 'html') {	
	    				$elemParent.innerHTML = elementContent.outerHTML
	    			} else if( options.render === 'append') {
	    				$elemParent.append($elementContent)
	    			}
	    
	    			if (options.click)
	    				addEventClick($elementContent)
	    		})
	    	}
	    	function findById(list, id) {
	    		return list.find(movie => movie.id === parseInt(id, 10))
	    	}
	    
	    	function findMovie(id, category) {
	    		switch (category) {
	    			case 'action' : {
	    				return findById(actionList, id)
	    		}
	    			case 'drama' : {
	    				return findById(dramaList, id)
	    		}
	    			default: {
	    				return findById(animationList, id)
	    			}
	    		}
	    	}
	    	function addEventClick ($element) {
	    		$element.addEventListener('click', (e) => {
	    	      showModal($element)
	    	    })
	    	}
	    
	    	function showModal($element) {
	    		$overlay.classList.add('active')
	    		$modal.style.animation = 'modalIn .8s forwards'
	    		const id = $element.dataset.id;
	    		const category = $element.dataset.category;
	    		const data = findMovie(id, category);
	    
	    		$modalTitle.textContent = data.title
	    		$modalImage.setAttribute('src', data.medium_cover_image)
	    		$modalDescription.textContent = data.synopsis
	    	}
	    
	    	function hideModal() {
	    		$overlay.classList.remove('active')
	    		$modal.style.animation = 'modalOut .8s forwards'
	    	}
	    
	    	/*
	    	** Events on DOM element
	    	*/
	    	$hideModal.addEventListener('click', hideModal)
	    
	    	$form.addEventListener('submit', async (e) => {
	    		e.preventDefault()
	    		$featuringContainer.innerHTML  =''
	    
	    		$home.classList.add('search-active')
	    
	    		const $loader = document.createElement('img')
	    
	    		setAttributes($loader, {
	    			src: 'src/images/loader.gif',
	    			height: 50,
	    			width: 50
	    		})
	    
	    		$featuringContainer.append($loader)
	    
	    		const data = new FormData($form)
	    		const {data: {movies}} = await getData(`${API_BASE}/list_movies.json?limit=1&query_term=${data.get('name')}`)
	    
	    		renderList(movies, $featuringContainer, 'featuringTemplate', {click: false, render: 'html'} )
	    	})
	    
	    })()```
	    
	```

* **edesalla17** (1) [342275](https://platzi.com/comentario/342275/) 

	buenaza´

* **Diego Fernando Rojas Quintero** (1) [86209](https://platzi.com/comentario/1090611/) 
Alguien me puede explicar, que es lo que hace Leonidas cuando pone event.srcElement en el minuto 6:00 ¿porque lo pone?¿que hace?

* **heysoypaez** (1) [53036](https://platzi.com/comentario/512791/) 
¿Por què sientes que las animaciones son importantes, ùtlies, valiosas para una aplicación web?

	* **Eduardo Hidalgo Díaz Rugama** [53036] (4)

		Las animaciones casi no tienen ningún valor por si solas.
		
		La web se define como “una interfaz de interacción”. De hecho, la web suele ser orientado a eventos (es decir, que una web debe actuar en respuesta a eventos para interactuar con el usuario).
		
		Por lo tanto, una animación debe ejecutarse como respuesta a un evento, para demostrar interacción con el usuario. Yo lo englobaría en la UX.
		
		entonces: las animaciones son un elemento clave para llevar experiencia de usuario al cliente. Es la manera de comunicar la experiencia.

* **Alex Monroy** (1) [42393](https://platzi.com/comentario/386207/) 
Muchachos Tengo un error con el listener animationend, resulta que quita la clase active, al dar click en la segunda película y queda el ...

	* **evertescalante1** [42393] (0)

		Podrias compartir tu codigo seria bueno

* **evertescalante1** (0) [411606](https://platzi.com/comentario/411606/) 

	no me di cuenta cuando se agrego esta linea, solo por si alguien puede aclararme la duda, aqui le decimos que cuando se llame a la funcion lo primero que haga es remover lo que tiene ese container en su elemento hijo 0
	```     $container.children[0].remove()```
	    
	```

	* **ivan-rodriguez** [411606] (2)

		esta linea se agrego en el momento que se empezó a renderizar cada pelicula, con esta linea de código se esta quitando el gif que da el efecto de cargando en cada uno de los div donde se van a imprimir las listas de películas.

* **evertescalante1** (0) [44754](https://platzi.com/comentario/411606/) 
no me di cuenta cuando se agrego esta linea, solo por si alguien puede aclararme la duda, aqui le decimos que cuando se llame a la funcio...

	* **ivan-rodriguez** [44754] (2)

		esta linea se agrego en el momento que se empezó a renderizar cada pelicula, con esta linea de código se esta quitando el gif que da el efecto de cargando en cada uno de los div donde se van a imprimir las listas de películas.

## 0200. Manejo de errores [12338](https://platzi.com/clases/1316-jquery-js/12338-manejo-de-errores/)

### Descripción:


### Comentarios:

* **Paula Johana Sánchez Guzmán** (17) [348730](https://platzi.com/comentario/348730/) 

	No se ve el momento en que Leonidas pone el Trow new Error 😦

	* **David Daniel Castillo Nava** [348730] (2)

		 **No se ve pero en el sistema de archivo esta!!**

	* **Rodolfo Nicacio Ugalde Ochoa** [348730] (1)

		Tambien me di cuenta.

	* **David Isaac Flores Medrano** [348730] (6)

		por tanto otro error… hahaha un error en el video de manejo de errores :v

	* **gosunkugi** [348730] (1)

		errorception!!

	* **Jose Padron (Platzi)** [348730] (8)

		este es el trozo de código que no se vio en el vídeo cuando lo puso:
		```     asyncfunctiongetData(url) {
		        const response = await fetch(url);
		        const data = await response.json();
		        if (data.data.movie_count > 0) {
		          //aqui se acaba
		          return data;
		        }
		        thrownewError('No se encontró ningun resultado');
		      }```
		    
		    
		```

* **williamsantiagoalzate** (15) [745729](https://platzi.com/comentario/745729/) 

	Alrededor del minuto 11 del video se corta una parte de la clase, en la cual se introduce el (else) del (if) que está dentro de la función (getData) aquí les dejo el código.
	```     asyncfunctiongetData(url) {
	        const response = await fetch(url);
	        const data = await response.json();
	        if (data.data.movie_count > 0) {
	          // aquí se acaba
	          return data;
	        }
	        // si no hay pelis aquí continua
	        thrownewError('No se encontró ningun resultado');
	      }
	    
	```

	* **Oscar Barajas Tavares (Platzi)** [745729] (1)

		Gracias por tu observación lo vamos a revisar a detalle.

	* **Nahuel Sznajderhaus** [745729] (1)

		el video sigue igual que cuando fue reportado hace 8 meses :lol:
		
		se podria agregar algo mas visible?

* **Jorge-Delfino** (14) [360020](https://platzi.com/comentario/360020/) 

	El error lo crea despues del IF
	```     if(data.data.movie_count > 0){
	    	returndata;
	    }
	    
	    throw new Error('No se encontró ningun resultado');
	    
	    
	```

	* **jesus-gallardo** [360020] (1)

		**Gracias**

* **Sergio Minei** (13) [336245](https://platzi.com/comentario/336245/) 

	## Manejo de errores
	
	El manejo de errores se hace con un bloque `try/catch`. Se intenta ejecutar un bloque de instrucciones (try) y se especifica una respuesta en caso suceda un error (catch).
	```     try {
	      //codigo a evaluar
	    }
	    catch(error) {
	      //código por si sucede un error
	      alert(error.message);
	    }
	    
	```
	
	  * Se puede crear un error customizado con Error().
	  * Se puede lanzar un error con throw.
	```     thrownewError('No se encontró ningún resultado');
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

* **LUISFER24** (10) [394279](https://platzi.com/comentario/394279/) 

	aqui dejo mi solucion al reto ![Captura.PNG](https://static.platzi.com/media/user_upload/Captura-7c4cd5b8-adcf-4a26-b715-662d8f4aab90.jpg)!
	```     (async function load(){
	      const BASE_API = 'https://yts.am/api/v2/';
	    
	      async function getData(url){
	        const response = await fetch(url);
	        const data = await response.json();
	          if( data.data && data.data.movie_count) {
	            return data
	          }else if( data.results) {
	            return data
	          }
	          throw new Error("don't found any results");
	      }
	    
	      const $form = document.getElementById('form');
	      const $home = document.getElementById('home');
	      const $featuringContainer = document.getElementById('featuring');
	    
	      function setAttributes($element, attributes) {
	        for(const attribute in attributes){
	          $element.setAttribute( attribute, attributes[attribute])
	        }
	      } 
	    
	      function featuringTemplate(movie){
	        return (
	          `<div class="featuring">
	            <div class="featuring-image">
	              <img src="${movie.medium_cover_image}" width="70" height="100" alt="">
	            </div>
	              <div class="featuring-content">
	                <p class="featuring-title">Pelicula encontrada</p>
	                <p class="featuring-album">${movie.title}</p>
	              </div>
	            </div>`
	        )
	      }
	    
	      function userTemplate(user){
	        return(
	          ` <li class="playlistFriends-item" data-uuid=${user.login.uuid}>
	             <a href="#">
	               <img src="${user.picture.thumbnail}" alt="user picture" />
	               <span>
	                 ${user.login.username}
	                       </span>
	             </a>
	           </li>`
	        )
	      }
	    
	      $form.addEventListener('submit', async (event) => {
	        event.preventDefault();
	        $home.classList.add('search-active');
	        $loader = document.createElement('img');
	        setAttributes($loader, {
	          src: 'src/images/loader.gif',
	          height: 50,
	          width: 50,
	        })
	        $featuringContainer.append($loader);
	        
	        const data = new FormData($form);
	        try {
	          const {
	            data: {
	              movies
	            }
	          } = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`)
	          
	          const HTMLString = featuringTemplate(movies[0]);
	          $featuringContainer.innerHTML = HTMLString;
	        }catch(error){
	          alert(error.message);
	          $loader.remove();
	          $home.classList.remove('search-active');
	    
	        }
	      })
	    
	      function videoItemTemplate(movie, category){
	        return(
	          `<div class="primaryPlaylistItem" data-id=${movie.id} data-category=${category}>
	            <div class="primaryPlaylistItem-image">
	              <img src="${movie.medium_cover_image}">
	            </div>
	            <h4 class="primaryPlaylistItem-title">
	              ${movie.title}    
	            </h4>
	          </div>`
	        )
	      }
	    
	      function userDescriptionTemplate(user) {
	          return(
	            `<ul class="userDescriptionTemplate"> 
	              <li id='userModalName'><span class="userModalField">name</span> : <span>${user.name.first} ${user.name.last}</span></li>
	              <li id='userModalAge'><span class="userModalField">age</span> : <span>${user.dob.age}</span></li>
	              <li id='userModalGender'><span class="userModalField">gender</span> : <span>${user.gender}</span></li>
	              <li id='userModalEmail'><span class="userModalField">Email</span> : <span>${user.email}</span></li>
	              <li id='userModalCity'><span class="userModalField">location</span> : <span>${user.location.city}</span></li>
	              <li id='userModalUuid'><span class="userModalField">uuid</span> : <span>${user.login.uuid}</span></li>
	            </ul>`
	          )
	      }
	    
	      function createTemplate(HTMLString){
	        const html = document.implementation.createHTMLDocument();
	        html.body.innerHTML =   HTMLString;
	        return html.body.children[0];
	      }
	    
	      function addEventClick ($element) {
	        $element.addEventListener('click', () => {
	          showModal($element);
	        })
	      }
	    
	      function renderMovieList(list, $container, category) {
	        $container.children[0].remove();
	        list.forEach(movie => {
	          const HTMLString = videoItemTemplate(movie, category);
	          const movieElement = createTemplate(HTMLString);
	          $container.append(movieElement);
	          const image = movieElement.querySelector('img')
	          image.addEventListener('load', event => { event.srcElement.classList.add('fadeIn') } );
	          addEventClick(movieElement);
	        })
	      }
	      
	      function renderUsers(list, $container) {
	        $container.children[0].remove();
	        list.forEach(user => {
	          const HTMLString = userTemplate(user);
	          const userElement = createTemplate(HTMLString);
	          $container.append(userElement);
	          const thumbnail = userElement.querySelector('img')
	          thumbnail.addEventListener('load', event => { event.srcElement.classList.add('fadeIn') } )
	          addEventClick(userElement)
	        })
	      }
	    
	      const { results : users } = await getData('https://randomuser.me/api/?exc=info,registered,timezone,nat&results=20')
	      const $usersContainer = document.getElementById('playlistFriends')
	      renderUsers( users , $usersContainer )
	    
	      const { data: { movies: actionList } } = await getData(`${BASE_API}list_movies.json?genre=action`)
	      const $actionContainer = document.getElementById('action')
	      renderMovieList(actionList, $actionContainer, 'action')
	      
	      const { data: { movies: dramaList } } = await getData(`${BASE_API}list_movies.json?genre=drama`)
	      const $dramaContainer = document.getElementById('drama')
	      renderMovieList(dramaList, $dramaContainer, 'drama')
	      
	      const { data: { movies: animationList } } = await getData(`${BASE_API}list_movies.json?genre=animation`)
	      const $animationContainer = document.getElementById('animation')
	      renderMovieList(animationList, $animationContainer, 'animation')
	    
	      const $modal = document.getElementById('modal')
	      const $overlay = document.getElementById('overlay')
	      const $hideModal = document.getElementById('hide-modal')
	    
	      const $modalTitle = $modal.querySelector('h1')
	      const $modalImage = $modal.querySelector('img')
	      const $modalDescription = $modal.querySelector('p')
	    
	      function findById(list, id) {
	        return list.find( movie => movie.id === parseInt(id, 10))
	      }
	    
	      function findMovie(id, category){
	        switch(category) {
	          case 'action': {
	            return findById(actionList, id)
	          } 
	          case 'drama': {
	            return findById(dramaList, id)
	          }
	          default: {
	            return findById(animationList, id)
	          }
	        }
	      }
	    
	      function findUserByUuid(list, uuid) {
	        return list.find( user => user.login.uuid === uuid )
	      }
	    
	      function showModal($element) {
	        $overlay.classList.add('active');
	        $modal.style.animation = 'modalIn .8s forwards';
	        const id = $element.dataset.id;
	        const uuid = $element.dataset.uuid;
	        const category = $element.dataset.category;
	        try{
	          const data = findMovie(id, category);
	    
	          $modalTitle.textContent = data.title;
	          $modalImage.setAttribute('src', data.medium_cover_image);
	          $modalDescription.textContent = data.description_full
	      
	        }catch(err){
	          const data = findUserByUuid(users, uuid)
	          
	          $modalTitle.textContent = data.login.username;
	          $modalImage.setAttribute('src', data.picture.large);
	          const HTMLString = userDescriptionTemplate(data);
	          const descriptionElement = createTemplate(HTMLString);
	          $modalDescription.append(descriptionElement);
	        }
	      }
	    
	      $hideModal.addEventListener('click', hideModal);
	      
	      function hideModal() {
	        $overlay.classList.remove('active')
	        $modal.style.animation = 'modalOut .8s forwards'
	        setTimeout(() => $modalDescription.textContent = '', 800)
	      }
	    })()
	    
	```

	* **Erick Lugo** [394279] (1)

		Esta excelente!  
		una pregunta, eres principiante o ya tienes experiencia con js, a mi me costo mucho terminar el reto por ser principiante.  
		saludos

	* **SEBASTIAN PADUANO** [394279] (1)

		MAQUINA

* **Angel Omar Ceniceros Berumen** (7) [958261](https://platzi.com/comentario/958261/) 

	En el minuto 11 se corta y no sale como se crea ese nuevo error.  
	Para las personas que están un poco perdidas, así es como debe de quedar el metodo getData.
	```     asyncfunctiongetData(url) {
	            const response = await fetch(url);
	            const data = await response.json()
	            if(data.data.movie_count>0){
	                return data;
	            }
	            thrownewError('No se encontró ningun resultado');
	        }
	    
	```

	* **Germán González** [958261] (1)

		Heroe

* **Espasa** (5) [699552](https://platzi.com/comentario/699552/) 

	Función de getData lanzando el error (la parte que se omitió en el vídeo):
	```     asyncfunctiongetData(url) {
	        const response = await fetch(url);
	        const data = await response.json();
	        if(data.data.movie_count > 0) {
	          return data;
	        }
	        thrownewError('Error: No se ha encontrado ningún resultado');
	      };
	    
	```

* **GUIFOR PAUL PLAZA AGUIRRE** (4) [598576](https://platzi.com/comentario/598576/) 

	No se ve cuando tipea el throw, bug del video 😃

* **emanueljtc** (4) [590287](https://platzi.com/comentario/590287/) 

	Yo le añadi la libreria Sweet Alert y quedo mas agradable. [https://sweetalert.js.org/](url)
	```             swal("Oops", error.message , "error");
	    
	    
	```

* **heysoypaez** (4) [515800](https://platzi.com/comentario/515800/) 

	**Ideas claves del desafio**
	
	Despues de hacerlo con Leonidas Esteban en el curso de Jquery a Js y hacerlo unas dos veces por mismo en mi version de la aplicacion de Platzi Video que estamos desarrollando me di cuenta que hay un proceso simple :
	
	En resumen sería:
	
	-Necesitas obtener la data de la API  
	-Necesitar convertir esa data en un string que usaras para el html  
	-Necesitas convertir ese string en un HTML de verdad  
	-Necesitar mandar ese HTML al DOM (la pantalla) en el lugar que lo quieres (el nodo)

* **neontigermx** (3) [1022064](https://platzi.com/comentario/1022064/) 

	El video se corta, no se implementó aun nada de throws y por lo que veo en los comentarios, dejaron un reto 😕

	* **Emmanuel García** [1022064] (3)

		En el servidor C si está completo 😉

	* **Diego Alexander Forero Higuera (Platzi)** [1022064] (2)

		Vamos a revisar y corregir la clase en los servidores A y B

	* **neontigermx** [1022064] (1)

		Nunca se me ocurriño revisar en los otros servidores xD muchas gracias

	* **Diego Alexander Forero Higuera (Platzi)** [1022064] (1)

		Ya esta solucionado en los servidores A y B

* **salajhon69** (3) [1009234](https://platzi.com/comentario/1009234/) 

	Yo lo que hice fue crear un try - catch y si la busqueda no se encuentra se esconde la barra de busqueda y la limpia, y si encuentra alguna espera a que cargue la busqueda y pinte lo que encontro, asi no se queda alguna busqueda anterior mientras carga la otra
	```         form.addEventListener('submit', async(event) =>{
	          try{
	            event.preventDefault()
	            contenido.classList.add('buscar')
	            featuring.classList.remove('desactive')
	            const data = new FormData(form);
	            const peli = await getData( `${Base_Api}list_movies.json?limit=1&query_term=${data.get('name')}`)
	            const Htmlstring = featureTemplate(peli.data.movies[0]);
	            featuring.innerHTML = Htmlstring;
	            featuring.classList.add('fadeIn');
	    
	          }
	            catch{
	              featuring.innerHTML = null;
	              alert('No se encontro lo que buscabas')
	              contenido.classList.remove('buscar')
	              featuring.classList.add('desactive')
	            }
	        
	        })
	        featuring.classList.remove('fadeIn');
	    
	```

* **Yoel Ribero** (3) [739819](https://platzi.com/comentario/739819/) 

	Dejo por aquí el reto, hice la lista de usuarios y una lista de 10 películas random arriba.  
	Siendo honesto me llevó dos días poder terminarlo, más allá de que varias partes se puede tomar como referencia lo que ya venimos haciendo a la hora de la práctica se vuelve todo un poco más complicado, lo que es bueno, ya que es la forma de aprender (con errores y “dándonos contra la pared”). Invito a que hagan el desafío ya que por momento puede llegar a ser complicado pero es muy satisfactorio poder terminarlo.  
	Saludos!!
	```     /* GET LIST PELIS RANDOM */
	    
	    functiontemplateMovieListRandom(movie) {
	    	return (
	    	`
	    	<li class="myPlaylist-item" data-id="${movie.id}">
	                  <a href="#">
	                    <span>
	                     	${movie.title}
	                    </span>
	                  </a>
	              </li>
	    	`	
	    	)
	    }
	    
	    const movieListRandom = await getData(`${BASE_API}list_movies.json?limit=10`)
	    const movieRandom = movieListRandom.data.movies
	    const $playListContainer = document.querySelector('#container-playlist')
	    
	    functionrenderUserListRandom(listRandom, container) {
	    	listRandom.forEach( (movie) => {
	    		const HTML = templateMovieListRandom(movie)
	    		const movieRandomHTML = createTemplate(HTML)
	    		container.append(movieRandomHTML)
	    		addEventClick(movieRandomHTML)
	    	})
	    } 
	    
	    renderUserListRandom(movieRandom, $playListContainer)
	    
	    // Fetch de usuarios
	    asyncfunctiongetUser(url) {
	    	const response = await fetch(url)
	    	const data = await response.json()
	    	return data;
	    }
	    functiontemplateUsers(user) {
	    	return (
	    	`
	    	<li class="playlistFriends-item" data-id=${user.id.value}>
	    		<a href="#">
	    	  		<img src="${user.picture.thumbnail}" alt="echame la culpa" />
	    	      		<span>
	    	           		${user.name.first}${user.name.last} 
	    	        	</span>
	    		</a>
	    	</li>
	    	`
	    	)
	    }
	    
	    const $usersContainer = document.getElementById('container-users')
	    const dataUsers = await getUser('https://randomuser.me/api/?results=10')
	    const dataUsersResults = dataUsers.results
	    
	    functionrenderUsers(list, container) {
	    	list.forEach( (user) => {
	    		const HTML = templateUsers(user)
	    		const userHTML = createTemplate(HTML)
	    		container.append(userHTML)
	    	})
	    }
	    
	    renderUsers(dataUsersResults, $usersContainer)
	    
	    
	```
	
	También tuve que cambiar el switch de la function findMovie para que tome el caso de que se haga click en la película de la lista
	```     switch (category) {
	    	case'action' : {
	    		return findById(actionList, id)
	    	}
	    	case'drama' : {
	    		return findById(dramaList, id)
	    	}
	    	case'animation': {
	    		return findById(animationList, id)
	    	}
	    	default: {
	    		return findById(movieRandom, id)
	    	}
	    }
	    
	    
	```
	
	Otra cosa es que el modal aveces se rompe con la img, eso es xq la img se está escalando, le falta a la clase “.modal-content img” asignarle flex-shrink: 0;  
	Queda así:
	```     .modal-contentimg {
	      width: 170px;
	      height: 256px;
	      object-fit: cover;
	      margin-right: 15px;
	      flex-shrink: 0;
	    }
	    
	```
	
	Obviamente el código js se puede mejorar mucho pero el dasafío personalmente me funcionó muchísimo para afianzar muchos conceptos.

* **ilan.vivanco** (3) [679616](https://platzi.com/comentario/679616/) 

	La parte de la explicación del Error y de Throw quedó cortada en el video.

	* **Cristobal Vega** [679616] (1)

		Así es me quedé de WTF.

	* **darwistg** [679616] (2)

		si jajaja yo repetí el vídeo pensando que no había prestado atención y resulta que se salto esa parte 😃

* **Jehaann Manzanares** (3) [624846](https://platzi.com/comentario/624846/) 

	Tienes el siguiente error ?  
	**Access to fetch at ‘<https://yts.am/api/v2/list_movies.json>’ from origin ‘<http://127.0.0.1:5500>’ has been blocked by CORS policy: No ‘Access-Control-Allow-Origin’ header is present on the requested resource. If an opaque response serves your needs, set the request’s mode to ‘no-cors’ to fetch the resource with CORS disabled.**
	
	solución: intalar plugin [**Moesif Orign & CORS Changer**](https://chrome.google.com/webstore/detail/moesif-orign-cors-changer/digfbfaphojjndkpccljibejjbppifbc)

	* **Juan mora** [624846] (2)

		👍

* **pablojorgeandres** (3) [488385](https://platzi.com/comentario/488385/) 

	## [Manejo de Errores](https://github.com/pablojorgeandres/notas-clases/blob/master/jQuery-JavaScript.md#clase20)
	
	Para poder manipular situaciones en las que la conexión a la API por cualquier cosa de un error utilizamos la estructura [_**try…catch**_](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/try...catch).
	```     	try {
	    		// ejecuta si todo está bien en el callback
	    	} catch(error) {
	    		console.log(error)
	    	}
	    
	    
	```
	
	Para manipular los errores en JS usamos el prototipo [Error()](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Error). Con este podemos crear nuestro propio _objeto error_ customizado.
	```     new Error(message)
	    
	```
	
	Y para mostrar el error se puede usar [_throw_](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/throw).
	```     throw expresion;
	    
	```

* **Giancarlo Vera** (3) [478013](https://platzi.com/comentario/478013/) 

	La segunda parte!  
	![reto02.jpg](https://static.platzi.com/media/user_upload/reto02-39c256b9-8471-4a2a-a807-5808122a70d1.jpg)  
	El html queda así:
	```     <div class="sidebarPlaylist">
	              <h2 class="sidebarPlaylist-title">Mi Top Ten de Películas</h2>
	              <ol class="myPlaylist" id="myPlaylist">
	                <img src="src/images/loader.gif"width="50" height="50" alt="">
	              </ol>
	    </div>
	    
	```
	
	Y así me queda el js de esta parte:
	```     const $myPlaylistContainer = document.querySelector('#myPlaylist');
	    
	    function itemTemplate(movie, category) {
	        return (
	          `<li class="myPlaylist-item" data-id="${movie.id}" data-category="${category}">
	          <a href="#">
	            <span>
	            ${movie.title}
	            </span>
	          </a>
	        </li>`
	        )
	      }
	    
	    function renderFavoriteList(list, $container, category) {
	        $container.children[0].remove();
	        list.forEach(movie => {
	          const HTMLString = itemTemplate(movie, category);
	          $container.innerHTML += HTMLString;
	        })
	        movieClick($container);
	      }
	    
	    const { data: { movies: scifiList } } = await getData(`${BASE_API}list_movies.json?genre=sci-fi&limit=10`)
	    renderFavoriteList(scifiList, $myPlaylistContainer, 'scifi');
	    
	    
	```

	* **Yezid Garcia Medina** [478013] (2)

		Excelente aporte 😄

* **Giancarlo Vera** (3) [477988](https://platzi.com/comentario/477988/) 

	Aquí va la primera parte del reto!  
	![reto01.jpg](https://static.platzi.com/media/user_upload/reto01-f1e19650-694d-4367-a8c0-9470cc4794ec.jpg)  
	Un pequeño cambio en el html:
	```     <div class="sidebarPlaylist">
	              <h2 class="sidebarPlaylist-title">Amigos conectados</h2>
	              <ul class="playlistFriends" id="friendsList">
	                <img src="src/images/loader.gif"width="50" height="50" alt="">
	              </ul>
	    </div>
	    
	```
	
	Y así quedó el js para esta parte:
	```     async function getUsersData(url) {
	        const response = await fetch(url);
	        const data = await response.json();
	        if (data.info.results > 0) {
	          return data;
	        }
	        throw new Error('No hay amigos conectados!')
	      }
	      
	      const USERS_API = 'https://randomuser.me/api/?results=8';
	      const { results: usersList } = await getUsersData(USERS_API);  
	      const $friendsListContainer = document.querySelector('#friendsList');
	    
	      function capitalize(string){
	        return string.charAt(0).toUpperCase() + string.slice(1);
	      }
	    
	      function friendTemplate(user) {
	        const { name, picture } = user
	        return (
	          `<li class="playlistFriends-item" data-firstname="${name.first}">
	          <a href="#">
	            <img src="${picture.thumbnail}" alt="${name.first} ${name.last} avatar" />
	            <span>
	              ${capitalize(name.first)} ${capitalize(name.last)}
	            </span>
	          </a>
	        </li>`
	        )
	      }
	    
	      function findByName(name, list) {
	        return list.find(element => element.name.first === name)
	      }
	    
	      function showFriendModal($element) {
	        $overlay.classList.add('active');
	        $modal.style.animation = 'modalIn .8s forwards';
	        const { firstname } = $element.dataset;
	        const { 
	          name, 
	          picture, 
	          email,
	          dob,
	          location,
	          phone 
	        } = findByName(firstname, usersList);
	        $modalTitle.textContent = `${capitalize(name.first)} ${capitalize(name.last)}`;
	        $modalImage.setAttribute('src', picture.large);
	        $modalDescription.innerHTML = `
	          <strong>Email:</strong> ${email}<br>
	          <strong>Phone:</strong> ${phone}<br>
	          <strong>Age:</strong> ${dob.age}<br>
	          <strong>Location:</strong> ${capitalize(location.city)}. ${capitalize(location.state)}
	        `;
	      }
	    
	      function userClick($container) {
	        const friendsList = Array.from($container.children);
	        friendsList.forEach(($element) => {
	          $element.addEventListener('click', function () {
	            showFriendModal($element);
	          })
	        })
	      }
	    
	      function renderFriendsList($container, list) {
	        $container.children[0].remove();
	        list.forEach((user) => {
	          const HTMLString = friendTemplate(user);
	          $container.innerHTML += HTMLString;
	        })
	        userClick($container)
	      }
	    
	      renderFriendsList($friendsListContainer, usersList);
	    
	```
	
	Tuve problemas usando el data-id del api de randomusers ya que por momentos devolvía null, así que opté por utilizar data-name en su lugar.

	* **Yezid Garcia Medina** [477988] (1)

		Excelente aporte 😄

* **Wandy Rafael Santana Evangelista** (2) [1087318](https://platzi.com/comentario/1087318/) 

	No es por nada, pero me siento tan orgulloso. 😃  
	![Challenge.png](https://static.platzi.com/media/user_upload/Challenge-e08cc654-e335-4b1c-bada-aef34a1f7435.jpg)

	* **Diego Fernando Rojas Quintero** [1087318] (1)

		¿cambiaste algo en el html? ¿puedes mostrar tu trabajo, no tu codigo, si no tu resultado en web?

* **fryma** (2) [959981](https://platzi.com/comentario/959981/) 

	Resolución del reto.
	```     // *********************** PELIS **************************
	        const BASE_API = 'https://yts.lt/api/v2/';
	        function myPelisCreateElement(HTMLString) {
	            const html = document.implementation.createHTMLDocument();
	            html.body.innerHTML = HTMLString;
	            return html.body.children[0];
	        }
	    
	        function myPelisTemplate(url, title) {
	            return(`<li class="myPlaylist-item">
	                        <a href="${url}">
	                        <span>
	                            ${title}
	                        </span>
	                        </a>
	                    </li>`);
	        }
	    
	        let getDataPelis = async (url) => {
	            const response = await fetch(url);
	            if (response.status != 404) {
	                const data = await response.json();
	                return data;
	            }
	            throw new Error('No se pueden traer peliculas');
	        }
	    
	        function renderUserMyPelis(list, $container) {
	            $container.children[0].remove();
	            list.forEach( element => {
	                const HTMLString = myPelisTemplate(element.url, element.title);
	                const createElement = myPelisCreateElement(HTMLString);
	                $container.append(createElement);
	            });
	        }
	    
	        $containerMyPelis = document.getElementById("mypelis");
	        let url = `${BASE_API}list_movies.json?sort=seeds&limit=9`;
	        try {
	            const  { data: { movies: mypelis } }  = await getDataPelis(url);
	            renderUserMyPelis(mypelis, $containerMyPelis);
	        } catch (error) {
	            console.log(error.message);
	        }
	    
	        // *********************** USER ***************************
	        const $userContainer = document.getElementById('friends');
	        function userCreateElement (HTMLString) {
	            const html = document.implementation.createHTMLDocument();
	            html.body.innerHTML = HTMLString;
	            return html.body.children[0];
	        }
	    
	        function userTemplate(firstName, lastName, picture) {
	            return(`<li class="playlistFriends-item">
	                        <a href="#">
	                        <img src="${picture}" alt="echame la culpa" />
	                        <span>
	                            ${firstName} ${lastName}
	                        </span>
	                        </a>
	                    </li>`);
	        }
	    
	        async function getDataUsers(url) { 
	            const response = await fetch(url);
	            if (response.status != 404) {
	                const data = await response.json();
	                return data;
	            }
	            throw new Error('No se pueden traer usuarios');
	        }
	    
	        function renderUserList(list,$container) {
	            $container.children[0].remove();
	            list.forEach( element => {
	                const HTMLString = userTemplate(element.name.first, element.name.last, element.picture.thumbnail);
	                const userElement = userCreateElement(HTMLString);
	                $container.append(userElement);
	            });
	        };
	    
	        try {
	            const {results: userlist} = await getDataUsers('https://randomuser.me/api/?results=8');
	            renderUserList(userlist, $userContainer);
	        } catch (error) {
	            console.log(error.message);
	        }
	    
	```

* **Daniel Felipe Merchan Fuquen** (2) [625406](https://platzi.com/comentario/625406/) 

	Completé el reto. Se que puedo mejorar, por ello quedo atento a sus aportes:  
	![Capture2.JPG](https://static.platzi.com/media/user_upload/Capture2-22d4e082-c2f3-430d-bbf0-1e7dd9694188.jpg)
	```       async function getUser(url) {
	        const response = await fetch(url);
	        const data = await response.json();
	        return data;
	      }
	    
	      function userItemTemplate(user) {
	        return (
	          `
	          <li>
	            <a href="">
	              <img class="playlist-friend-image" width="100" src="${user.picture.thumbnail}" alt="">
	              <span class="playlist-friend-name">${user.name.first} ${user.name.last}</span>
	            </a>
	          </li>
	          `
	        )
	      }
	    
	      function playlistItemplate(movie) {
	        return (
	          `
	          <li>
	            <a class="playlist-item" href="#">
	              <span>${movie.title}</span>
	            </a>
	          </li>
	          `
	        )
	      }
	    
	      function renderUserList(list, $container) {
	        list.forEach((movie) => {
	            const HTMLString = userItemTemplate(movie);
	            const playlistElement = createTemplate(HTMLString);
	            $container.append(playlistElement);
	        })
	      }
	      
	      function renderPlaylist(list, $container) {
	        list.forEach((user) => {
	          const HTMLString = playlistItemplate(user);
	          const userElement = createTemplate(HTMLString);
	          $container.append(userElement);
	        })
	      }
	    
	      const { results: userList } = await getUser('https://randomuser.me/api/?results=10');
	      const $userContainer = document.querySelector('ul');
	      renderUserList(userList, $userContainer);
	    
	      const { data: { movies: fantasyList } } = await getData(`${BASE_API}list_movies.json?genre=fantasy&limit=10`);
	      $playListContainer = document.querySelector('.myPlaylist');
	      renderPlaylist(fantasyList, $playListContainer);```
	    
	```

* **Nathaly Stefani Riaño Bejarano** (2) [524910](https://platzi.com/comentario/524910/) 

	Reto Playlist:
	```     // playlist
	      function movieItemPlaylistTemplate(title) {
	        return(
	          `
	          <li class="myPlaylist-item">
	            <a href="#">
	              <span>
	                ${title}
	              </span>
	            </a>
	          </li>
	          `
	        )
	      }
	    
	      function renderMoviePlaylist(movies, $container) {
	        movies.forEach(movie => {
	          const { title } = movie;
	          const HTMLString = movieItemPlaylistTemplate(title);
	          const moviePlaylistElement = createTemplate(HTMLString);
	          $container.append(moviePlaylistElement);
	        });
	      }
	    
	      const { data: { movies: moviesPlaylist } } = await getData(`${MOVIES_API}list_movies.json?minimum_rating>7&minimum_rating<9&sort_by=rating&limit=9`);
	      renderMoviePlaylist(moviesPlaylist, $playlistContainer);
	    
	```

* **Pedro Caicedo** (2) [389704](https://platzi.com/comentario/389704/) 

	En caso que necesites ejecutar also si y solo sí como por ejemplo mostrar una información ó terminar un proceso se puede utiliza el [finally](https://www.w3schools.com/jsref/jsref_try_catch.asp)
	```     try {
	        tryCode - Block of code totry
	    }
	    catch(err) {
	        catchCode - Block of code to handle errors
	    } 
	    finally {
	        finallyCode - Block of code to be executed regardless ofthetry / catchresult
	    }
	    
	```

* **carlosextra1** (1) [1117330](https://platzi.com/comentario/1117330/) 

	Agregue sólo los estrenos relacionados con una pelicula!
	```     const API = 'https://yts.mx/api/v2/';
	    let estrenoList = [];
	    (async function load() {
	    
	      console.time()
	      async function getData(query) {
	        const result = await fetch(API + query)
	        const data = await result.json();
	        if (data.data.movie_count > 0)
	          return data;
	        throw new Error('No se encontro ningun registro');
	    
	    
	      }
	      const $form = document.getElementById('form');
	      const $featuringContainer = document.getElementById('featuring');
	      const $home = document.getElementById('home');
	    
	      function setAttributes($element, attributes) {
	        for (const attribute in attributes) {
	          $element.setAttribute(attribute, attributes[attribute]);
	        }
	      }
	      function featuringTemplate(peli) {
	        return (`<div class="featuring">
	            <div class="featuring-image">
	              <img src="${peli.medium_cover_image}" width="70" height="100" alt="">
	            </div>
	            <div class="featuring-content">
	              <p class="featuring-title">Pelicula encontrada</p>
	              <p class="featuring-album">${peli.title}</p>
	            </div>
	          </div>`);
	      }
	      $form.addEventListener('submit', async (event) => {
	        event.preventDefault();
	        $home.classList.add('search-active');
	        const $loader = document.createElement('img');
	        setAttributes($loader, {
	          src: 'src/images/loader.gif',
	          heigth: 50,
	          width: 50
	        });
	        $featuringContainer.append($loader);
	    
	        const data = new FormData($form);
	        try {
	          const { data: {
	            movies: pelis
	          }
	          } = await getData(`list_movies.json?limit=1&query_term=${data.get('name')}`);
	          const htmlString = featuringTemplate(pelis[0]);
	          $featuringContainer.innerHTML = htmlString;
	        } catch (error) {
	          alert(error);
	          $loader.remove();
	          $home.classList.remove('search-active')
	        }
	      });
	    
	      function videoItemTemplate(movie, category) {
	        return (` 
	            <div class="primaryPlaylistItem" data-id="${movie.id}" data-category="${category}">
	              <div class="primaryPlaylistItem-image">
	                <img src="${movie.medium_cover_image}">
	              </div>
	              <h4 class="primaryPlaylistItem-title">
	                ${movie.title}
	              </h4>
	            </div>`);
	      }
	      function createTemplate(htmlString) {
	        const $html = document.implementation.createHTMLDocument()
	        $html.body.innerHTML = htmlString;
	        return $html.body.children[0];
	      }
	      function addEventClick($element) {
	        $element.addEventListener('click', () => {
	          showModal($element);
	        });
	      }
	      function renderMovieList(list, $container, category) {
	        $container.children[0].remove();
	        list.forEach(movie => {
	          const htmlString = videoItemTemplate(movie, category);
	          const movieElement = createTemplate(htmlString);
	          $container.append(movieElement);
	          const image = movieElement.querySelector('img');
	          image.addEventListener('load', (event) => {
	            movieElement.classList.add('fadeIn');
	          });
	          addEventClick(movieElement);
	        });
	      }
	    
	      //signo de $ en variables significa que es elemento del dom
	      const { data: { movies: actionList } } = await getData('list_movies.json?genre=action');
	      const $actionContainer = document.querySelector('#action');
	      renderMovieList(actionList, $actionContainer, 'action');
	    
	    
	      const { data: { movies: dramaList } } = await getData('list_movies.json?genre=drama');
	      const $dramaContainer = document.getElementById('drama');
	      renderMovieList(dramaList, $dramaContainer, 'drama');
	    
	    
	      const { data: { movies: animationList } } = await getData('list_movies.json?genre=animation');
	      const $animationContainer = document.getElementById('animation');
	      renderMovieList(animationList, $animationContainer, 'animation');
	    
	    
	      //const $home = $('.home .list #item);// en jquery
	      const $modal = document.getElementById('modal');
	      const $overlay = document.getElementById('overlay');
	    
	    
	      function findById(list, id) {
	        return list.find(movie => parseInt(movie.id, 10) === parseInt(id, 10))
	      }
	      function findMovie(id, category) {
	        if (category === 'action')
	          return findById(actionList, id)
	        else if (category === 'drama')
	          return findById(dramaList, id)
	        else if(category === 'animation')
	          return findById(animationList, id)
	        else
	          return findById(estrenoList, id)
	    
	      }
	    
	      const modalTitle = $modal.querySelector('h1');
	      const modalImage = $modal.querySelector('img');
	      const modalDescription = $modal.querySelector('p');
	    
	      function showModal($element) {
	        $overlay.classList.add('active');
	        $modal.style.animation = 'modalIn .8s forwards';
	        const id = $element.dataset.id;
	        const category = $element.dataset.category;
	    
	        const data = findMovie(id, category);
	    
	        modalTitle.textContent = data.title;
	        modalImage.setAttribute('src', data.medium_cover_image)
	        modalDescription.textContent = data.description_full;
	      }
	    
	      const $hideModal = document.getElementById('hide-modal');
	      $hideModal.addEventListener('click', hideModal);
	      function hideModal() {
	        $overlay.classList.remove('active');
	        $modal.style.animation = 'modalOut .4s forwards';
	      }
	    
	      //para la barra lateral de estrenos
	      const $estrenoContainer = document.getElementById('estrenos');
	      $estrenoContainer.innerHTML='';
	      await getData('movie_suggestions.json?movie_id=1')
	        .then(({ data: { movies: list } }) => {
	          estrenoList = list;
	          list.forEach((movie) => {
	            const htmlString = `<li class="myPlaylist-item" data-id="${movie.id}" data-category="${estrenos}">
	                            <a href="#">
	                              <span>
	                                ${movie.title}
	                              </span>
	                            </a>
	                          </li>`;
	            const estrenoElement = createTemplate(htmlString);
	            $estrenoContainer.append(estrenoElement);
	            addEventClick(estrenoElement);
	          })
	        });
	     
	    
	      console.timeEnd();
	    })()
	    
	    
	```

* **DevNaftan** (1) [1106195](https://platzi.com/comentario/1106195/) 

	**Reto cumplido**
	```     asyncfunctiongetUsers(url) {
	        const response = await fetch(url)
	        const data = await response.json()
	        return data
	      }
	    
	      asyncfunctiongetMovies(url) {
	        const reponse = await fetch(url)
	        const data = await reponse.json()
	        if(data.data.movie_count > 0) {
	          return data
	        } else {
	          thrownewError('No se encontró ningún resultado')
	        }
	      }
	    
	      const BASE_MOVIES_API = 'https://yts.mx/api/v2/'
	      const BASE_USERS_API = 'https://randomuser.me/api/'
	    
	    
	      $myPlaylist = document.querySelector('.myPlaylist')
	    
	      functionmoviesListTemplate(movieTitle) {
	        return (
	          `<li class="myPlaylist-item">
	            <a href="#">
	              <span>
	                ${movieTitle}
	              </span>
	            </a>
	          </li>`
	        )
	      }
	    
	      functionmoviesListHTML(movieString) {
	        const HTML = document.implementation.createHTMLDocument()
	        HTML.body.innerHTML = movieString
	        return HTML.body.children[0]
	      }
	    
	      functionrenderList(list, $selector) {
	        list.forEach(({title}) => {
	          const movieString = moviesListTemplate(title)
	          const movieHTML = moviesListHTML(movieString)
	          $selector.append(movieHTML)
	        })
	      }
	    
	      const {data: {movies: moviesList}} = await getMovies(`${BASE_MOVIES_API}list_movies.json?limit=10`)
	      renderList(moviesList, $myPlaylist)
	    
	    
	      $playlistFriends = document.querySelector('.playlistFriends')
	    
	      function userTemplate(user) {
	        return (
	          `<li class="playlistFriends-item">
	            <a href="#">
	              <img src="${user.picture.thumbnail}" />
	              <span>
	                ${user.name.first}${user.name.last}
	              </span>
	            </a>
	          </li>`
	        )
	      }
	    
	      functionuserHTML(userString) {
	        const HTML = document.implementation.createHTMLDocument()
	        HTML.body.innerHTML = userString
	        return HTML.body.children[0]
	      }
	    
	      functionrenderUsers(userList, $selector) {
	        userList.forEach((user) => {
	          const userToString = userTemplate(user)
	          const userToHTML = userHTML(userToString)
	          $selector.append(userToHTML)
	        })
	      }
	    
	      const {results} = await getUsers(`${BASE_USERS_API}?results=10`)
	      renderUsers(results, $playlistFriends)
	    
	```

* **hdmjesus** (1) [1045739](https://platzi.com/comentario/1045739/) 

	Bueno, asi quedo mi codigo respecto al reto, use la API de github para traer una lista de usuarios.
	```     const BASE_USERS_API = 'https://api.github.com/'
	    const $listaUsuario = document.getElementById('playlistFriends')
	    
	    async function getUsers(url) {
	        const request = await fetch(url)
	        const user = await request.json()
	    
	    
	        return user
	    }
	    
	    function templateUsers(users) {
	        return (
	            `<li class="playlistFriends-item">
	             <a href="#">
	                <img src="${users.avatar_url}" />
	                    <span>
	                     ${users.login}
	                    </span>
	             </a>
	            </li>`
	        )
	    }
	    
	    function createTemplate(htmlSting) {
	    
	        const $html = document.implementation.createHTMLDocument(); //crea una estructura html
	        $html.body.innerHTML = htmlSting; //Seccion en codigo HTML
	        return $html.body.children[0]
	    }
	    
	    function renderUserList(list, $container) {
	    
	        list.forEach(users => { // Con cada uno de los elementos del objetos hacemos :
	    
	            const htmlSting = templateUsers(users) // Seccion en STRING
	            const userElement = createTemplate(htmlSting)
	            $container.append(userElement)
	    
	    
	            addEventClick(movieElement)
	    
	    
	        })
	    
	    }
	    
	    const userList = await getUsers(`${BASE_USERS_API}users`)
	    renderUserList(userList, $listaUsuario)
	    
	```

* **Kenet Chungandro** (1) [1006842](https://platzi.com/comentario/1006842/) 

	Ok a ponernos serios 😄 (troné mis dedos)

* **axlina90** (1) [998718](https://platzi.com/comentario/998718/) 

	![parte1.PNG](https://static.platzi.com/media/user_upload/parte1-63f279fb-dfce-4a1f-bb6b-d2d90be705c2.jpg)![parte2.PNG](https://static.platzi.com/media/user_upload/parte2-6f07c09c-cd5c-45bc-aa42-43f8a9578c98.jpg)
	
	Anexo mi reto, acepto observaciones y críticas, excelente curso

	* **hdmjesus** [998718] (1)

		La api ya no esta funcionando? entro en al pagina y esta caida

	* **Diego Fernando Rojas Quintero** [998718] (1)

		¿cambiaste algo en el html? ¿puedes mostrar tu trabajo, no tu codigo, si no tu resultado en web?

* **Cristian Caraballo** (1) [924005](https://platzi.com/comentario/924005/) 

	¿como puedo modificar la barra de scroll? ¿hay alguna forma de hacerla mas pequeña a lo minimalista?

	* **Gabriel De Andrade (Platzi)** [924005] (3)

		Hola! Al momento no existe un estándar para hacerlo. dentro de navegadores webkit como Chrome o Safari tienes esta opción [Custom Scrollbars](https://www.w3schools.com/howto/howto_css_custom_scrollbar.asp). Pero a manera general como puedes ver aquí: [StackOverflow](https://stackoverflow.com/a/14150577) necesitás contar con librerías externas que te ayuden en este proceso, tal como: [simplebar](https://github.com/Grsmto/simplebar) que es la que utiliza Twitch 😄

	* **Cristian Caraballo** [924005] (1)

		Gracias Gabo!😃. otra pregunta: ¿Que es Webkit? en que curso hablan de eso?..he visto en que me aparecen propiedades css con esa palabra…

	* **Gabriel De Andrade (Platzi)** [924005] (2)

		Hola! Webkit es el nombre del motor que utiliza Safari y distintos navegadores sobretodo en el ecosistema de Apple, sin embargo dentro del mundo de CSS el prefijo webkit se utiliza para Chrome, Safari y las nuevas versiones de Opera, así como también todos los navegadores que corren en iOS. [Acá tienes más info](https://developer.mozilla.org/es/docs/Glossary/Vendor_Prefix) 😄

	* **Hugo Valdes Flores** [924005] (2)

		Es u ejemplo muy básico pero así seria con WebKit
		
		::-webkit-scrollbar-track{  
		-webkit-box-shadow: inset 0 0 6px rgba(0,0,0,0.3);  
		background-color: #F5F5F5;
		
		}
		
		::-webkit-scrollbar{  
		width: 10px;  
		background-color: #F5F5F5;
		
		}
		
		::-webkit-scrollbar-thumb{  
		-webkit-box-shadow: inset 0 0 6px rgba(0,0,0,.3);  
		background-color: #555;  
		}
		
		::-webkit-scrollbar {  
		width: 10px;  
		color: #212529;  
		}

* **rusbel bermúdez rivera** (1) [901789](https://platzi.com/comentario/901789/) 

	La segunda parte del reto trayendo 9 pelis en peticion get de la API
	```     asyncfunctiongetMoviesPlaylist() {
	        const response = await fetch(`${BASE_API}list_movies.json?limit=9`)
	        const data = await response.json()
	        const movies = await data.data.movies
	        return movies
	       
	      }
	    
	      functionmyplaylistTemplate(movie) {
	        return (
	          `<li class="myPlaylist-item">
	              <a href="#">
	                <span>
	                  ${movie.title}
	                </span>
	              </a>
	            </li>`
	        )
	      }
	    
	      functioncreatePlayListTemplate(HTMLString) {
	        const html = document.implementation.createHTMLDocument()
	        html.body.innerHTML = HTMLString
	        return html.body
	      }
	    
	    
	      asyncfunctionrenderPlaylist($container) {
	        const movies = await getMoviesPlaylist()
	        movies.forEach((movie) => {
	          const HTMLString = myplaylistTemplate(movie);
	          const myplayList = createPlayListTemplate(HTMLString)
	          $container.append(myplayList);
	        })
	      }
	    
	      const $myPlaylistContainer = document.getElementById('myPlaylist')
	      renderPlaylist($myPlaylistContainer)```
	    
	```

* **rusbel bermúdez rivera** (1) [901548](https://platzi.com/comentario/901548/) 

	Mi solcion al reto de playlist de amigos
	```     asyncfunctiongetFriend() {
	        const response = await fetch('https://randomuser.me/api/')
	        const data = await response.json()
	        return friend = {
	          'name': data.results[0].name.first,
	          'last': data.results[0].name.last,
	          'picture': data.results[0].picture.thumbnail
	        }
	      }
	    
	      functionplaylistFriendsTemplate(friend) {
	        return (
	          `<li class="playlistFriends-item">
	          <a href="#">
	            <img src="${friend.picture}" />
	            <span>
	              ${friend.name}${friend.last} 
	            </span>
	          </a>
	        </li>`
	        )
	      }
	    
	      functioncreateFriendsTemplate(HTMLString) {
	        const html = document.implementation.createHTMLDocument()
	        html.body.innerHTML = HTMLString
	        return html.body
	      }
	    
	      asyncfunctionrenderPlaylistFriends($container) {
	        for (let i = 0; i < 7; i++) {
	          let friend = await getFriend()
	          const HTMLString = playlistFriendsTemplate(friend);
	          const friendElement = createFriendsTemplate(HTMLString)
	          $container.append(friendElement);
	        }
	      }
	    
	      const $playlistFriendsContainer = document.getElementById('playlistFriends')
	      renderPlaylistFriends($playlistFriendsContainer)```
	    
	```

* **Manuel Rivera** (1) [858712](https://platzi.com/comentario/858712/) 

	Reto cumplido 😉
	```      async function  getDataUser(userUrl) {
	        const response = await fetch(userUrl)
	        const data = await response.json()
	        return data
	      }
	      
	      function UserTemplate(user) {
	        return(`
	        <a href="#">
	        <img src="${user.results[0].picture.thumbnail}" alt="echame la culpa" />
	        <span>
	        ${user.results[0].name.first} ${user.results[0].name.last}
	        </span>
	        </a>
	        `
	        )
	      }
	      
	      const $PlayListFriendsContainer = document.getElementById('PlayListFriends')
	      const $UlHtml = document.getElementById('Ul')
	    
	      async function  renderUsersList() {
	        const $Ul = document.createElement('ul')
	        for(let i = 0; i < 8; i++){
	          const user = await getDataUser(' https://randomuser.me/api/ ')
	          const $Li = document.createElement('li')
	          const HTMLstring =  UserTemplate(user)
	          $Li.innerHTML = HTMLstring
	          $Li.classList = 'playlistFriends-item'
	          $Ul.style.padding = '0'
	          $Ul.append($Li)
	     }
	     // Utilizamos el método replaceChild para cambiar un nodo por otro nodo => nodoPadre.replaceChild(nodo old, new nodo)
	     return $PlayListFriendsContainer.replaceChild($Ul, $UlHtml)
	    }```
	    
	```

* **abdielalvarez** (1) [840931](https://platzi.com/comentario/840931/) 

	Reto terminado 😃
	```     (async function load() {
	    
	      async function getData(url) {
	        const response = await fetch(url);
	        const data = await response.json();
	        if (data.data.movie_count > 0) { 
	          return data;
	        }
	        throw new Error('No se encontró ningun resultado');
	      }
	      const $form = document.getElementById('form');
	      const $home = document.getElementById('home');
	      const $featuringContainer = document.getElementById('featuring');
	    
	      function setAttributes($element, attributes) {
	        for (const attribute in attributes) {
	          $element.setAttribute(attribute, attributes[attribute]);
	        }
	      }
	      const BASE_API = 'https://yts.lt/api/v2/';
	    
	      function featuringTemplate(peli) {
	        return (
	          `
	          <div class="featuring">
	            <div class="featuring-image">
	              <img src="${peli.medium_cover_image}" width="70" height="100" alt="">
	            </div>
	            <div class="featuring-content">
	              <p class="featuring-title">Pelicula encontrada</p>
	              <p class="featuring-album">${peli.title}</p>
	            </div>
	          </div>
	          `
	        )
	      }
	    
	      $form.addEventListener('submit', async (event) => {
	        event.preventDefault();
	        $home.classList.add('search-active')
	        const $loader = document.createElement('img');
	        setAttributes($loader, {
	          src: 'src/images/loader.gif',
	          height: 50,
	          width: 50,
	        })
	        $featuringContainer.append($loader);
	    
	        const data = new FormData($form);
	        try {
	          const {
	            data: {
	              movies: pelis
	            }
	          } = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`)
	    
	          const HTMLString = featuringTemplate(pelis[0]);
	          $featuringContainer.innerHTML = HTMLString;
	        } catch(error) {
	          alert(error.message);
	          $loader.remove();
	          $home.classList.remove('search-active');
	        }
	      })
	    
	      // -----------comedy loader-----------------------------------------------------------------------
	    
	      const $img2 = document.createElement('img');
	      setAttributes($img2, {
	        src: 'src/images/loader.gif',
	        height: 50,
	        width: 50
	      })
	      const $comedyContainer = document.getElementById('comedyList')
	      $comedyContainer.append($img2)
	    
	      // -----------users loader-----------------------------------------------------------------------
	    
	      const $img3 = document.createElement('img');
	      setAttributes($img3, {
	        src: 'src/images/loader.gif',
	        height: 50,
	        width: 50,
	      })
	      const $usersContainer = document.getElementById('usersList');
	      $usersContainer.append($img3)
	    
	      // -------------------------------------------------------------------------------------------------
	    
	      function videoItemTemplate(movie, category) {
	        return (
	          `<div class="primaryPlaylistItem" data-id="${movie.id}" data-category=${category}>
	            <div class="primaryPlaylistItem-image">
	              <img src="${movie.medium_cover_image}">
	            </div>
	            <h4 class="primaryPlaylistItem-title">
	              ${movie.title}
	            </h4>
	          </div>`
	        )
	      }
	      function createTemplate(HTMLString) {
	        const html = document.implementation.createHTMLDocument();
	        html.body.innerHTML = HTMLString;
	        return html.body.children[0];
	      }
	      function addEventClick($element) {
	        $element.addEventListener('click', () => {
	          showModal($element)
	        })
	      }
	      function renderMovieList(list, $container, category) {
	        $container.children[0].remove();
	        list.forEach((movie) => {
	          const HTMLString = videoItemTemplate(movie, category);
	          const movieElement = createTemplate(HTMLString);
	          $container.append(movieElement);
	          const image = movieElement.querySelector('img');
	          image.addEventListener('load', (event) => {
	            event.srcElement.classList.add('fadeIn');
	          })
	          addEventClick(movieElement);
	        })
	      }
	    
	      const { data: { movies: actionList} } = await getData(`${BASE_API}list_movies.json?genre=action`)
	      const $actionContainer = document.querySelector('#action');
	      renderMovieList(actionList, $actionContainer, 'action');
	    
	      const { data: { movies: dramaList } } = await getData(`${BASE_API}list_movies.json?genre=drama`)
	      const $dramaContainer = document.getElementById('drama');
	      renderMovieList(dramaList, $dramaContainer, 'drama');
	    
	      const { data: { movies: animationList } } = await getData(`${BASE_API}list_movies.json?genre=animation`)
	      const $animationContainer = document.getElementById('animation');
	      renderMovieList(animationList, $animationContainer, 'animation');
	    
	      const $modal = document.getElementById('modal');
	      const $overlay = document.getElementById('overlay');
	      const $hideModal = document.getElementById('hide-modal');
	    
	      const $modalTitle = $modal.querySelector('h1');
	      const $modalImage = $modal.querySelector('img');
	      const $modalDescription = $modal.querySelector('p');
	    
	      function findById(list, id) {
	        return list.find(movie => movie.id === parseInt(id, 10))
	      }
	    
	      function findMovie(id, category) {
	        switch (category) {
	          case 'action' : {
	            return findById(actionList, id)
	          }
	          case 'drama' : {
	            return findById(dramaList, id)
	          }
	          default: {
	            return findById(animationList, id)
	          }
	        }
	      }
	    
	      function showModal($element) {
	        $overlay.classList.add('active');
	        $modal.style.animation = 'modalIn .8s forwards';
	        const id = $element.dataset.id;
	        const category = $element.dataset.category;
	        const data = findMovie(id, category);
	    
	        $modalTitle.textContent = data.title;
	        $modalImage.setAttribute('src', data.medium_cover_image);
	        $modalDescription.textContent = data.description_full
	      }
	    
	      $hideModal.addEventListener('click', hideModal);
	      function hideModal() {
	        $overlay.classList.remove('active');
	        $modal.style.animation = 'modalOut .8s forwards';
	      }
	    
	      // ---------Comedy list exercise for playlist item----------
	    
	      const { data: { movies: comedyList } } = await getData(`${BASE_API}list_movies.json?genre=comedy`)
	    
	      function myPlaylistItemTemplate (movie) {
	        return (
	          `<li class="myPlaylist-item">
	            <a href="#">
	              <span>
	                ${movie.title}
	              </span>
	            </a>
	          </li>`
	        )
	      }
	    
	      try {
	        $comedyContainer.children[0].remove();
	        comedyList.forEach(movie => {
	          const HTMLString = myPlaylistItemTemplate(movie)
	          const html = document.implementation.createHTMLDocument()
	          html.body.innerHTML = HTMLString
	          $comedyContainer.append(html.body.children[0])
	          $comedyContainer.classList.add('fadeIn');
	        }) 
	      } catch (error) {
	        throw new Error('Lista de comedia no fue resuelta')
	      }
	      
	    
	      // ----------User random generator exercise for profiles-----------
	    
	      const { results: usersList } = await getUsers('https://randomuser.me/api/?results=20')
	      
	      async function getUsers(url) {
	        const response = await fetch(url);
	        const data = await response.json();
	        return data
	      }
	      
	      function playlistFriendsItemTemplate({ name: { first }, picture: { thumbnail } }) {
	        return (
	          `<li class="playlistFriends-item">
	            <a href="#">
	              <img src="${thumbnail}" alt="${first}" />
	              <span>
	                ${first}
	              </span>
	            </a>
	          </li>`
	        )
	      }
	    
	      try {
	        $usersContainer.children[0].remove();
	        usersList.forEach(user => {    
	          const HTMLString = playlistFriendsItemTemplate(user)
	          const html = document.implementation.createHTMLDocument()
	          html.body.innerHTML = HTMLString
	          $usersContainer.append(html.body.children[0])
	          $usersContainer.classList.add('fadeIn');
	        })
	      } catch (error) {
	        throw new Error('No se pudieron encontrar usuarios')
	      }
	    
	    })()
	    
	```

* **fsanchezcal** (1) [787242](https://platzi.com/comentario/787242/) 

	Buenas tengo una pregunta como puedo filtrar directamente de una lista de generos que yo desee fitrar unicamente quise hacerlo de esta manera pero siempre me trae peliculas con generos que no ocupo en este caso lo quise hacer de esta manera pero no funciono  
	const moviePlayList = await getData(`${BASE_API}list_movies.json?genre=action&genre=drama&genre=&animation&rating>7&rating<10&limit=9`);
	
	se que puedo hacer mi funcion de filtro recorre el objeto hacer un filter de javasscript pero la pregunta es si puede tener directamente y ahorrarse ese proceso nuevo de filtro?

* **darioarcos** (1) [731497](https://platzi.com/comentario/731497/) 

	Reto del vídeo:
	```     const mayus = string => string.charAt(0).toUpperCase() + string.slice(1);
	    const playlistTemplate = friend =>
	        `<li class="playlistFriends-item">
	        <a href="#">
	        <img src=${friend.picture.large}>
	        <span>${mayus(friend.name.first)}</span>
	        </a>
	        </li>`;
	    
	      const { results: friendsPlaylist } = await getData('https://randomuser.me/api/?													 
	      results=10')
	      const $friendsContainer = document.getElementById('friends')
	      friendsPlaylist.forEach(friend => {
	      const CREATE_ITEM = createHTMLItem(playlistTemplate(friend))
	      $friendsContainer.append(CREATE_ITEM)
	      })
	    
	```

* **Espasa** (1) [700120](https://platzi.com/comentario/700120/) 

	Reto (Cambiando la variable NUMBER_PLAYLIST_FRIENDS puedes elegir cuantos friends desplegar):
	```     const RANDOM_USER_URL = 'https://randomuser.me/api/';
	    const NUMBER_PLAYLIST_FRIENDS = 8;
	    
	    functionuppercaseFirstLetter(string){
	        returnstring.charAt(0).toUpperCase() + string.slice(1);
	      }
	    
	      functionplaylistFriendsTemplate(friend) {
	        return (
	          `<li class="playlistFriends-item">
	              <a href="#">
	                 <img src="${friend.picture.medium}" alt="Friend picture" />
	                <span>
	                  ${uppercaseFirstLetter(friend.name.title)}${uppercaseFirstLetter(friend.name.first)}${uppercaseFirstLetter(friend.name.last)}
	                </span>
	              </a>
	          </li>`
	        )
	      }
	    
	    // Render friends playlist
	      for(i = 0; i < NUMBER_PLAYLIST_FRIENDS; i++) {
	        try {
	          let { results : { 0: friend } } = await getFriendData(RANDOM_USER_URL);
	          const HTMLString = playlistFriendsTemplate(friend);
	          const friendTemplate = createTemplate(HTMLString);
	          $playlistFriends.appendChild(friendTemplate);
	        } catch(err) {
	          console.error(err)
	        }
	      }
	    
	```

* **Renier Ferrer** (1) [682078](https://platzi.com/comentario/682078/) 

	Reto:
	```     (async function load(){
	    
	      async function getMovieData(url){
	        const response = await fetch(url)
	        const data = await response.json()
	        // return data
	        // debugger
	        if (data.data.movie_count > 0){
	          return data
	        }
	        throw new Error('No se encontró ningun resultado');
	      }
	    
	    
	      function movieItemTemplate(movie){
	        return(`<h2 class="sidebarPlaylist-title">Mi Playlist <span class="btn warning">Escuchar Playlist</span> </h2>
	        <ul class="myPlaylist">
	          <li class="myPlaylist-item">
	            <a href="#">
	              <span>
	                ${movie.title}
	              </span>
	            </a>
	          </li>
	        </ul>`)
	      }
	    
	    
	      function createTemplate(HTMLString){
	        console.log(HTMLString);
	        const $html = document.implementation.createHTMLDocument();
	        $html.body.innerHTML = HTMLString;
	        return $html.body.children[1]
	      }
	    
	    
	      function renderUserList(list, $container){
	        $container.children[1].remove();
	        list.forEach((movie) => {
	          const HTMLString = movieItemTemplate(movie);
	          const playListElement = createTemplate(HTMLString);
	    
	          $container.append(playListElement);
	        })
	      }
	    
	    
	      const STATIC_API = 'https://yts.lt/api/v2/'
	      // const $movieList = await getMovieData(`${STATIC_API}list_movies.json?limit=9`)
	      const { data: { movies: $movieList }} = await getMovieData(`${STATIC_API}list_movies.json?limit=9`)
	      const $playListContainer = document.querySelector('#moviePlayList')
	      renderUserList($movieList, $playListContainer)
	    
	    })()
	    
	```

* **Renier Ferrer** (1) [682077](https://platzi.com/comentario/682077/) 

	Para completar que se corto de throw new me apoye en los archivos y enlaces, allí encontré lo que se perdió del video.

* **Cristian Orozco Benjumea** (1) [673637](https://platzi.com/comentario/673637/) 

	Hola yo le agregué ésto, para que el featuring esté durante 5seg y desaparezca
	```     $form.addEventListener('submit', async (event) => { //la volvemos asincrona porque vamos a hacer consultas a una API
	        event.preventDefault() //Evita que recarge la página en cada búsqueda
	        $container.classList.add('search-active')
	        $featContainer.classList.remove('featuring-hide')
	    
	        setTimeout(() => {
	          $featContainer.classList.add('featuring-hide')
	          $container.classList.remove('search-active')
	        }, 5000)
	        const loader = document.createElement('img')
	    
	```

* **zalotesta** (1) [625361](https://platzi.com/comentario/625361/) 

	https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/throw

* **zalotesta** (1) [625354](https://platzi.com/comentario/625354/) 

	https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Error

* **zalotesta** (1) [625308](https://platzi.com/comentario/625308/) 

	https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Sentencias/try...catch

* **zalotesta** (1) [625109](https://platzi.com/comentario/625109/) 

	debugger

* **Pablo Benedix** (1) [623155](https://platzi.com/comentario/623155/) 

	Reto cumplido

* **Jehaann Manzanares** (1) [590451](https://platzi.com/comentario/590451/) 

	 **Cumpliendo el Reto.**
	
	![](![peticiones asincronas platzi.PNG](https://static.platzi.com/media/user_upload/peticiones%20asincronas%20platzi-98857cca-23f7-40fc-829a-2106c985fc45.jpg)

* **Isaias Chávez** (1) [550917](https://platzi.com/comentario/550917/) 

	Mi solución al reto de personas randon : primera parte
	```     functioncustomUser(user){
	            return(
	             `<a href="#">
	              <img src="${user.results[0].picture.thumbnail}" alt="echame la culpa" />
	              <span>
	                ${user.results[0].name.first}
	              </span>
	            </a>`
	          )
	          }
	    
	          let friendsList = document.querySelectorAll(".playlistFriends-item")
	          friendsList.forEach(element=>{
	            element.children[0].remove()
	          })
	          const peopleList = newArray(friendsList.length).fill(0)
	          for (let i = 0; i < peopleList.length; i++) {
	            peopleList[i] = await getUser("https://randomuser.me/api/")
	          }
	          peopleList.forEach((element,index) => {
	            const HTMLString = customUser(element)
	            const templateUser = createTemplate(HTMLString)
	            friendsList[index].append(templateUser)
	          })
	    
	```

* **Nathaly Stefani Riaño Bejarano** (1) [524911](https://platzi.com/comentario/524911/) 

	Reto Friends:
	```     // friends
	      asyncfunctiongetFriends(url) {
	        const response = await fetch(url);
	        const data = await response.json();
	        return data;
	      }
	    
	      functioncapitalizeFirstLetter(string) {
	        returnstring.charAt(0).toUpperCase() + string.slice(1);
	      }
	    
	      functionfriendItemTemplate(friend) {
	        const { name: { first } } = friend;
	        const { name: { last } } = friend;
	        return(
	          `
	          <li class="playlistFriends-item">
	            <a href="#">
	              <img src="${friend.picture.thumbnail}" alt="echame la culpa" />
	              <span>
	                ${capitalizeFirstLetter(first)}${capitalizeFirstLetter(last)}
	              </span>
	            </a>
	          </li>
	          `
	        )
	      }
	    
	      functionrenderFriendList(list, $container) {
	        list.forEach(friend => {
	          const HTMLString = friendItemTemplate(friend);
	          const friendElement = createTemplate(HTMLString);
	          $container.appendChild(friendElement);
	        });
	      }
	    
	      const { results: friends } = await getFriends(`${FRIENDS_API}?results=15`);
	      renderFriendList(friends, $friendsContainer);
	    
	```

* **heysoypaez** (1) [515804](https://platzi.com/comentario/515804/) 

	Extracto de mi código del desafio de agregar usuario aleatorio en la playlist
	```     			function friendPlayListTemplate(friend) {
	    
	    				return (
	    
	    					`<li class="playlistFriends-item">
	    	              <a href="#">
	    	                <img src="${friend.picture.thumbnail}" alt="${friend.name.first}-${friend.name.last}-picture" />
	    	                <span>
	    	                  ${friend.name.first} ${friend.name.last}
	    	                </span>
	    	              </a>
	    	            </li>`
	    	      )
	    			}
	    
	    			function renderFriendPlaylist(friend) {
	    
	    				const HTMLString = friendPlayListTemplate(friend);
	    				const $friendPlayListItem = createTemplate(HTMLString)
	    				return $playlistFriendsPrincipal.appendChild($friendPlayListItem)
	    			}
	    
	    			async function renderAllFriendsPlaylist(friendsLimit) {
	    
	    				for (let i = 0; i < friendsLimit; i++) {
	    
	    					let {results: [friend] } = await getData('https://randomuser.me/api/')
	    					renderFriendPlaylist(friend)
	    				}
	    			}
	    
	    
	    
	```
	
	_Falta codigo (por ejemplo los selectores) pero espero te lo imagines o lo veas en[mi github](https://github.com/heysoypaez/platzi-video)_

* **Carlos Jesús Fernández Salazar** (1) [439630](https://platzi.com/comentario/439630/) 

	Asi va mi proyecto por ahora con **los dos desafíos completados** 😄  
	![img](https://i.ibb.co/3vhnv5Y/image.png)
	
	Esto es lo que agregué de nuevo al **home.js**
	```       const MOVIES_API = 'https://yts.am/api/v2/list_movies.json';
	      const MOVIES_GENRE = '?genre=';
	      const MOVIES_SORT = '?sort_by=';
	      const MOVIES_QTY = '&limit=';
	      const USER_API = 'https://randomuser.me/api/?format=json';
	      const USER_QTY = '&results=10';
	    
	      function friendsListTemplate(friend) {
	        return (
	          `
	          <li class="playlistFriends-item" data-email="${friend.email}">
	            <a href="#">
	              <img src="${friend.picture.thumbnail}" alt="${friend.name} image" />
	              <span>
	                ${friend.name.first} ${friend.name.last}
	              </span>
	            </a>
	          </li>
	          `
	        )
	      }
	      
	      function renderFriendsList(friendsList, $container) {
	        friendsList.forEach((friend) => {
	            const HTMLString = friendsListTemplate(friend);
	            const friendElement = createTemplate(HTMLString);
	            $container.append(friendElement);
	    
	            addEventClick(friendElement);
	        })
	      }
	    
	      const { results: friends } = await getData(`${USER_API}${USER_QTY}`);
	      const $playlistFriends = document.getElementById('playlistFriends');
	      renderFriendsList(friends, $playlistFriends);
	    
	      function likedTemplate(movie) {
	          return (
	              `
	                <li class="myPlaylist-item" data-id="${movie.id}">
	                    <a href="#">
	                        <span>
	                            ${movie.title}
	                        </span>
	                    </a>
	                </li>
	              `
	          )
	      }
	      
	      function renderLikedList(likedList) {
	        likedList.forEach(async movie => {
	            const HTMLString = await likedTemplate(movie);
	            const likedElement = await createTemplate(HTMLString);
	            $myPlaylist.append(likedElement);
	    
	            addEventClick(likedElement);
	        })
	      }
	    
	      const { data: { movies: likedList }} = await 					 
	      getData(`${MOVIES_API}${MOVIES_SORT}download_count${MOVIES_QTY}10`);
	      const $myPlaylist = document.getElementById('myPlaylist');
	      renderLikedList(likedList);
	    
	    function findFriend(list, email) {
	          return list.find(friend => friend.email == email);
	      }
	    
	      function showModal($element) {
	        let data;
	        let id;
	        let category;
	    
	        if ($element.dataset.id != null) {
	            id = $element.dataset.id;
	    
	            if($element.dataset.category != null) {
	                category = $element.dataset.category;
	                data = findMovie(id, category);
	            } else {
	                data = findById(likedList, id);
	            }
	            
	            $modalImage.setAttribute('src', data.medium_cover_image);
	            $modalTitle.textContent = data.title;
	            $modalDescription.textContent = data.description_full;
	        } else {
	            const email = $element.dataset.email;
	            data = findFriend(friends, email);
	            $modalTitle.textContent = `${data.name.first} ${data.name.last}`;
	            $modalImage.setAttribute('src', data.picture.large);
	            const detailsString = 
	                `
	                <p style="padding-left: 20px; text-align: left;">
	                <strong>Género</strong><br/>
	                ${data.gender}<br/><br/>
	    
	                <strong>Email</strong><br/>
	                ${data.email}<br/><br/>
	    
	                <strong>Locación</strong><br/>
	                ${data.location.city}, 
	                ${data.location.state}, 
	                ${data.location.postcode}
	                </p>
	                `
	            $modalDescription.textContent = "";
	            $modalDescription.innerHTML = detailsString;
	        }
	    
	```
	
	y un ligero cambio en el **try.catch** de la primera función
	```     asyncfunctiongetData(url) {
	        const payload = await fetch(url);
	        const data = await payload.json();
	        try {
	            if(data.data.movie_count === 0){
	                thrownewError('No se encontró ningun resultado');
	              } 
	              return data;
	        } catch (error) {
	            return data;
	        }
	      }
	    
	```

* **Josias Cubillos Gutierrez** (1) [438690](https://platzi.com/comentario/438690/) 

	Mi reto realizado aunque no es el mas optimizado.
	```     (async function load(){
	    
	      async function getData(url){
	        const response = await fetch(url);
	        const data = await response.json();
	        if (data.data.movie_count >= 1) {
	          // aquí se acaba
	          return data;
	        }
	        // si no hay pelis aquí continua
	        throw new Error('No se encontró ningun resultado');
	      }
	    
	      async function getAmigo(url){
	        const response = await fetch(url);
	        const data = await response.json();
	          return data;
	      }
	    
	      function setAttribute($element, attributes){
	        for(const attribute in attributes){
	          $element.setAttribute(attribute, attributes[attribute]);
	        }
	      }
	    
	      function templateMovie(movie){
	        return(
	          `<div class="featuring">
	            <div class="featuring-image">
	              <img src="${movie.medium_cover_image}" width="70" height="100" alt="">
	            </div>
	            <div class="featuring-content">
	              <p class="featuring-title">Pelicula encontrada</p>
	              <p class="featuring-album">${movie.title}</p>
	            </div>
	          </div>`
	        )
	      }
	    
	      const $form = document.querySelector("#form");
	      const $home = document.querySelector("#home");
	      const $featuringContainer = document.querySelector("#featuring");
	      const BASE_API = "https://yts.am/api/v2/list_movies.json";
	      const AMIGOS_API = "https://randomuser.me/api/";
	    
	      $form.addEventListener("submit", async (event)=>{
	        event.preventDefault();
	        $home.classList.add("search-active");
	        const $loader = document.createElement("img");
	        setAttribute($loader, {
	          src: "src/images/loader.gif",
	          width: 50,
	          height: 50,
	        })
	    
	        $featuringContainer.append($loader);
	        try{
	          const data = new FormData($form);
	          const{
	            data: {
	              movies: pelis
	            }
	          } = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`)
	          $featuringContainer.innerHTML = templateMovie(pelis[0]);
	        }catch(error){
	          alert(error.message);
	          $loader.remove();
	          $home.classList.remove('search-active');
	        }
	        
	      })
	    
	      const recomendadas = await getData(BASE_API);
	      const $recomendadasContainer = document.querySelector(".myPlaylist");
	    
	      for (let index = 0; index < 8; index++) {
	        renderRecomendadas(recomendadas, $recomendadasContainer, index); 
	      }
	      
	      const { data:{ movies: actionList }} = await getData(`${BASE_API}?genre=action`);
	      const $actionContainer = document.querySelector("#action");
	      renderMovies(actionList,$actionContainer, "Accion");
	    
	      const { data:{ movies: dramaList  }} = await getData(`${BASE_API}?genre=drama`);
	      const $dramaContainer = document.querySelector("#drama");
	      renderMovies(dramaList,$dramaContainer,"Drama");
	    
	      const {data:{  movies: animationList }} = await getData(`${BASE_API}?genre=animation`);
	      const $animationContainer = document.querySelector("#animation");
	      renderMovies(animationList,$animationContainer,"Animacion");
	    
	      for (let index = 0; index < 8; index++) {
	        const amigos = await getAmigo(AMIGOS_API);
	        const $amigosContainer = document.querySelector(".playlistFriends");
	        renderAmigos(amigos,$amigosContainer);
	      }
	      
	      function videoItemTemplate(movie, category){
	        return (
	          `<div class="primaryPlaylistItem" data-id="${movie.id}" data-category="${category}">
	            <div class="primaryPlaylistItem-image">
	              <img src="${movie.medium_cover_image}">
	            </div>
	            <h4 class="primaryPlaylistItem-title">
	            ${movie.title}
	            </h4>
	          </div>`
	        )
	      }
	      
	      function renderRecomendadas(recomendada, $container, index){
	        debugger
	        const render = recomendadasTemplate(recomendada, index);
	        const template2 = createTemplate(render);
	        $container.append(template2);
	      }
	    
	    
	      function recomendadasTemplate(recomendada, index){
	        return(
	          `<li class="myPlaylist-item">
	            <a href="#">
	              <span>
	                ${recomendada.data.movies[index].title}
	              </span>
	            </a>
	          </li>`
	        )
	      }
	      
	    
	      function renderAmigos(amigo,$container){
	        const render = amigosTemplate(amigo);
	        const template1 = createTemplate(render);
	        $container.append(template1);
	      }
	    
	      function amigosTemplate(amigo){
	        return(
	          `<li class="playlistFriends-item">
	            <a href="#">
	              <img src="${amigo.results[0].picture.thumbnail}" />
	              <span>
	                ${amigo.results[0].name.first} ${amigo.results[0].name.last}
	              </span>
	            </a>
	          </li>`
	        )
	      }
	    
	    
	      function createTemplate(template){
	        const html = document.implementation.createHTMLDocument();
	        html.body.innerHTML = template;
	        return html.body.children[0]
	      }
	    
	      function eventClick($element){
	        $element.addEventListener("click", () => {
	          showModal($element);
	        })
	      }
	    
	      function renderMovies (list, $container, category){
	        $container.children[0].remove();
	        list.forEach((movie) => {
	          const htmlString = videoItemTemplate(movie, category);
	          const template = createTemplate(htmlString)
	          $container.append(template)
	          const render = template.querySelector("img");
	          render.addEventListener("load",(event)=>{
	            event.srcElement.classList.add("fadeIn");
	          })
	          eventClick(template);
	        });
	      }
	      
	      const $modal = document.querySelector("#modal");
	      const $overlay = document.querySelector("#overlay");
	      const $hideModal = document.querySelector("#hide-modal");
	    
	      const $modalTitle = $modal.querySelector("h1");
	      const $modalImage = $modal.querySelector("img");
	      const $modalDescription = $modal.querySelector("p");
	    
	      function findById(list, id){
	        debugger
	        return list.find(movie => movie.id === parseInt(id,10))
	      }
	    
	      function findMovie(id, category){
	        switch(category){
	          case "Accion": {
	            return findById(actionList,id);
	          }
	    
	          case "Drama": {
	            return findById(dramaList,id);
	          }
	    
	          default: {
	            return findById(animationList,id);
	          }
	        }
	        
	      }
	    
	      function showModal($element){
	        $overlay.classList.add("active")
	        $modal.style.animation = "modalIn .8s forwards";
	        const id = $element.dataset.id;
	        const category = $element.dataset.category;
	        const data = findMovie(id, category);
	    
	        $modalTitle.textContent = data.title;
	        $modalImage.setAttribute("src",data.medium_cover_image); 
	        $modalDescription.textContent = data.description_full;
	    
	      }
	    
	      $hideModal.addEventListener("click",()=>{
	        $overlay.classList.remove("active")
	        $modal.style.animation = "modalOut forwards";
	      })
	    
	    })()
	    
	```

* **Ricardo Medina** (1) [425960](https://platzi.com/comentario/425960/) 

	Muy importante manejar los errores de nuestro código y enviar el error de forma personalizada o con un mensaje mas amigable al usuario.

* **Camilo Castillo** (1) [418106](https://platzi.com/comentario/418106/) 

	Para el error del video
	
	// si no hay pelis aquí continua  
	throw new Error(‘No se encontró ningun resultado’);  
	}

* **Diego Fernando Rojas Quintero** (1) [86363](https://platzi.com/comentario/1094318/) 
Alguien me puede ayudar, intento poner el usuario en la PlayList de amigos, pero que sea de forma mas aleatoria, osea de que no me aparez...

* **wlm.morant** (1) [85047](https://platzi.com/comentario/1064997/) 
el valor de de data me sale pending, porque??

* **hdmjesus** (1) [84211](https://platzi.com/comentario/1045519/) 
La API de ramdon users no esta disponible ya?

	* **Gabriel De Andrade (Platzi)** [84211] (1)

		Hola! Parece que justo ahora está caído el servidor 😛

* **fsanchezcal** (1) [70991](https://platzi.com/comentario/787318/) 
Buenas tengo una pregunta como puedo filtrar directamente de una lista de generos que yo desee fitrar unicamente quise hacerlo de esta ma...

* **Nahuel Sznajderhaus** (0) [83351](https://platzi.com/comentario/1025005/) 
me dice que: GET https://yts.am/api/v2/list_movies.json?genre=action net::ERR_CERT_AUTHORITY_INVALID y cuando navego directo...

	* **Nahuel Sznajderhaus** [83351] (1)

		Si, la API cambio.
		
		Hay que cambiar el URL base a <https://yts.mx/api>, no <https://yts.lt/api> como estaba antes!

## 0210. LocalStorage [12339](https://platzi.com/clases/1316-jquery-js/12339-localstorage/)

### Descripción:


### Comentarios:

* **Sergio Minei** (23) [336246](https://platzi.com/comentario/336246/) 

	## Guardar datos
	
	  * `localStorage` permite almacenar datos sin tiempo de expiración
	  * `sessionStorage` permite almacenar datos. Estos datos se van a borrar cuando se termine la sessión del navegador
	
	En local storage solo se puede guardar texto plano. No se pueden guardar objetos.
	```     //eliminar los datos
	    window.localStorage.clear();
	    
	    //setear un valor
	    window.localStorage.setItem("nombre", "Toshi");
	    
	    //setear un objeto
	    //primero se tiene que convertir el objeto en un string
	    window.localStorage.setItem("objeto", JSON.stringify({"peli": "wonder woman"});
	    
	    //obtener el valor de un key
	    window.localStorage.getItem("nombre");
	    
	    //obtener el valor de un texto objeto y convertirlo a objeto
	    JSON.parse(window.localStorage.getItem("objeto"));
	    
	```
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

* **heysoypaez** (7) [515951](https://platzi.com/comentario/515951/) 

	 _JSON es el brujo que hace útil y relevante localStorage permitiendo guardar objetos en memoria como strings y recuperarlos como objetos_
	```     //haciendo un objeto de la vida ...
	    let laVida = {
	    tristezas: 5,
	    alegrias: 10,
	    anecdotas: 8
	    }
	    
	    //convirtiendo ese objeto a json
	    laVida = JSON.stringify(laVida)
	    
	    localStorage.setItem("laVida",laVida)
	    
	    recuperando ese objeto
	    
	    localStorage.getItem("laVida")
	    laVida = JSON.parse(laVida)
	    
	    
	```

* **gosunkugi** (6) [454503](https://platzi.com/comentario/454503/) 

	joooooer como cuando te das cuenta que platzi graba en localStorage todos tus comentarios y los tiempos de la última reproducción de los últimos videos

	* **Facundo Nicolás García Martoni (Platzi)** [454503] (1)
![](https://img.elcomercio.pe/files/article_content_ec_fotos/uploads/2018/12/07/5c0ade654a7cc.jpeg)

* **pablojorgeandres** (5) [488897](https://platzi.com/comentario/488897/) 

	## [LocalStorage](https://github.com/pablojorgeandres/notas-clases/blob/master/jQuery-JavaScript.md#clase21)
	
	Para respaldar los datos que pedimos a la API utilizamos [_**localStorage**_](https://developer.mozilla.org/es/docs/Web/API/Window/localStorage) ó [_**sessionStorage**_](https://developer.mozilla.org/es/docs/Web/API/Window/localStorage). Son objetos nativos en javascript.
	
	[_**localStorage**_](https://developer.mozilla.org/es/docs/Web/API/Window/localStorage) nos permite guardar los datos sin un tiempo de expiración, es decir persisten entre ventanas/tabs con el mismo origen incluso al cerrar el navegador.
	
	[_**sessionStorage**_](https://developer.mozilla.org/es/docs/Web/API/Window/localStorage) nos permite guardar los datos con tiempo de expiración. Son eliminados cuando finaliza la sesion de navegación, lo que ocurre cuando se cierra el navegador.
	
	[_**localStorage**_](https://developer.mozilla.org/es/docs/Web/API/Window/localStorage) es un objeto dentro de window.
	
	Para acceder:
	```     	window.localStorage
	    	
	    	// por defecto el navegador lo reconoce y no hace falta anteponer el 'window'
	    	localStorage
	    	
	    
	```
	
	Para limpiar/borrar datos:
	```     	window.localStorage.clear()
	    
	    
	```
	
	Setear un dato:
	```     	window.localStorage.setItem('item','value')
	    
	    
	```
	
	Obtener su valor:
	```     	window.localStorage.getItem('item')
	    	// value
	    	
	    
	```
	
	En [_**localStorage**_](https://developer.mozilla.org/es/docs/Web/API/Window/localStorage) no puedo guardar objetos directamente. Para poder hacerlo, hay que previamente convertir el objeto en un string.  
	Esto puedo hacerlo con el método, también nativo de javascript, [_**JASON.stringify()**_](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/JSON/stringify):
	```     	var object = JSON.stringify({'flor': 'roja'})
	    	window.localStorage.setItem('item', object)
	    	window.localStorage.getItem('item')
	    	// "{"flor":"roja"}"
	    
	    
	```
	
	Para re-convertirlo en objeto luego de obtenerlo puedo usar [_**JSON.parse**_](https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/JSON/parse):
	```     	const objectBack = window.localStorage.getItem('item')
	    	
	    	JSON.parse(objectBack)
	    	// {flor: "roja"}
	    	      flor: "roja"
	    	      > __proto__: Object
	    
	    
	```

* **heysoypaez** (4) [515950](https://platzi.com/comentario/515950/) 

	¿Existte algun limite de memoria que se puede guardar en local storage? ¿que determina esa variable?

	* **Eduardo Hidalgo Díaz Rugama** [515950] (7)

		<https://stackoverflow.com/questions/2989284/what-is-the-max-size-of-localstorage-values>
		
		Son limites entre 2 y 10MB, dependiendo del navegador y el dispositivo. ¿Quién lo define? supongo que cada navegador.

* **Jose Rueda** (4) [428760](https://platzi.com/comentario/428760/) 

	esta clase acaba de salvarme en un proyecto para un cliente… infinitas gracias Leonidas

* **Germán González** (3) [1020817](https://platzi.com/comentario/1020817/) 

	La API de yts cambió ahora es [yts.mx](http://yts.mx), de nada.

* **Alfonso Navarro** (3) [491309](https://platzi.com/comentario/491309/) 

	Huy los usos de esta clase esta genial, podemos utilizarla para mejorar mucho la velocidad de nuestros aplicativos y no tener q estar consultando a cada rato la base de datos por informacion repetitiva. 1A

* **Alan Santiago** (3) [364145](https://platzi.com/comentario/364145/) 

	Siempre habia querido aprender como se hacia esto 😄

* **Germán González** (2) [1015204](https://platzi.com/comentario/1015204/) 

	Quien me puede ayudar con este error.  
	DOMException: Failed to read the ‘localStorage’ property from ‘Window’: Access is denied for this document.  
	at <anonymous>:1:8  
	(anonymous) @ VM2903:1

	* **Nahuel Sznajderhaus** [1015204] (1)

		yo lo resolvi y creo que fue por dar permiso a las third party cookies: `chrome://settings/content/cookies`

	* **Germán González** [1015204] (2)

		Si pero lo que pasa es que uso Brave, y no lo encuentro ese apartado.

* **Felipe-Lopez** (2) [822812](https://platzi.com/comentario/822812/) 

	Platzi Reto
	
	![Image 4.png](https://static.platzi.com/media/user_upload/Image%204-60629664-be54-45d3-8230-076874f35476.jpg)

* **Felipe-Lopez** (2) [822790](https://platzi.com/comentario/822790/) 
	
	![Image 2.png](https://static.platzi.com/media/user_upload/Image%202-0ac7d647-d0d6-4484-9fd0-f416fc4eb4aa.jpg)

* **Ismael Caballero Hernandez** (2) [651848](https://platzi.com/comentario/651848/) 

	Mi solución al reto de los usuarios:
	
	`//Obtener los datos  
	async function getUser(url){
	
	const response = await fetch(url);  
	const data = await response.json();
	
	return data;
	
	}
	
	//crear plantilla  
	function templateUser(name,picture){
	
	return(`<li class="playlistFriends-item"> <a href="#"> <img src="${picture}" alt="echame la culpa" /> <span> ${name.first} ${name.last} </span> </a> </li>`  
	)  
	}
	
	//Recorrer el array dde objetos y guardfar en el doom a traves de la plantilla  
	function addUserToList(list){
	
	list.forEach( (user) =>{
	
	console.log(user);  
	const HTMLString = templateUser([user.name](http://user.name),user.picture.thumbnail)  
	const userElement = createTemplate(HTMLString)  
	$userList.append(userElement);
	
	})  
	}
	
	const $userList = document.getElementById(‘userList’);
	
	const userList = await getUser(‘<https://randomuser.me/api/?results=10>’);  
	addUserToList(userList.results);`

* **Edgar Ramirez** (2) [387008](https://platzi.com/comentario/387008/) 

	ejemplo de como guardar un objeto en el localstorage por medio de una key y luego imprimirlo por consola
	```     var testList = [
	       {
	           nombre: "CARLOS"
	       },
	       {
	           nombre: "GERONIMO"
	       },
	       {
	           nombre: "NICOLAS"
	       },
	       {
	           nombre: "LUCAS"
	       }
	    ]
	    
	    if(!localStorage.getItem("list")) {
	    
	       localStorage.setItem("list", JSON.stringify(testList))
	    }
	    
	    
	    functionnombre (key) {
	    
	       console.log(key)
	    
	    
	       if(localStorage.getItem(key)) {
	    
	              returnJSON.parse(localStorage.getItem(key))
	           }
	    
	               else { return []
	    
	               }
	    
	    }
	    
	    console.log(nombre(prompt('Ingresa una key')))```
	    
	```

* **heysoypaez** (2) [53299](https://platzi.com/comentario/515950/) 
¿Existte algun limite de memoria que se puede guardar en local storage? ¿que determina esa variable?

	* **Eduardo Hidalgo Díaz Rugama** [53299] (7)

		<https://stackoverflow.com/questions/2989284/what-is-the-max-size-of-localstorage-values>
		
		Son limites entre 2 y 10MB, dependiendo del navegador y el dispositivo. ¿Quién lo define? supongo que cada navegador.

* **carlosextra1** (1) [1117357](https://platzi.com/comentario/1117357/) 

	Excelente truco para guardar un json

* **33andres33** (1) [1081026](https://platzi.com/comentario/1081026/) 

	los que tienen error con la API significa una cosa están copiando el código de los archivos, yo en ningún momento tuve ese error porque Leonidas al principio del curso dijo donde tomar el link así que no he entendido, porque hay gente que se queja de esto, la idea es aprender.

* **Lluis Pitarch Ripolles** (1) [1054648](https://platzi.com/comentario/1054648/) 

	Funciona pero hay que eliminar window, aquí dejo la documentación -> <https://developer.mozilla.org/es/docs/Web/API/Storage/setItem>

* **hec_lopz** (1) [1010532](https://platzi.com/comentario/1010532/) 

	LocalStorage 😮

* **Kenet Chungandro** (1) [1007297](https://platzi.com/comentario/1007297/) 

	Wo! genial LocalStorage 😮

* **iridian** (1) [892116](https://platzi.com/comentario/892116/) 

	LocalStorage 😮

* **zalotesta** (1) [626128](https://platzi.com/comentario/626128/) 

	https://developer.mozilla.org/es/docs/Web/API/Window/localStorage

* **gosunkugi** (1) [454505](https://platzi.com/comentario/454505/) 

	pero… en algún momento de la clase se nota que en efecto los objetos pueden ser guardados en el localStorage… ¿por qué hay que cambiarlos a cadena?

	* **Edward Steven Ramos Palacios** [454505] (1)

		localStorage es una API que guarda valores en **clave valor**.  
		Puedes guardar un objeto sin parsearlo a string primero pero, cuando lo consultes te va a devolver **[object] [object]**  
		por eso es necesario parsearlos a string para guardarlos y al recuperarlos los parseas a JSON de nuevo.

* **Edgar Ramirez** (1) [387007](https://platzi.com/comentario/387007/) 

	siempre es conveniente verificar si ya existen datos en el storage y luego hacer la solicitud de set, como por ejempo esta condición
	```     if(!localStorage.getItem("list")) {
	    
	       localStorage.setItem("list", JSON.stringify(testList))
	    }```
	    
	```

	* **Facundo Nicolás García Martoni (Platzi)** [387007] (1)

		Acabo de recargar la página varias veces (lo que supuestamente guardaría en múltiples ocasiones los datos en memoria), y la data siguen igual. Se puede deducir entonces que el localStorage ya valida que no exista el elemento cuando vamos a setearlo 🤔

* **Byhako** (1) [345655](https://platzi.com/comentario/345655/) 

	porque sale este error?  
	![error.png](https://static.platzi.com/media/user_upload/error-f9a94b25-e92c-4851-b92a-1948815213bf.jpg)

	* **Diego Alexander Forero Higuera (Platzi)** [345655] (3)

		Revisa esto <https://www.chromium.org/for-testers/bug-reporting-guidelines/uncaught-securityerror-failed-to-read-the-localstorage-property-from-window-access-is-denied-for-this-document> puede ser tu caso.

* **edesalla17** (1) [342287](https://platzi.com/comentario/342287/) 

	muy interesante este tema!

* **gosunkugi** (1) [48438](https://platzi.com/comentario/454505/) 
pero… en algún momento de la clase se nota que en efecto los objetos pueden ser guardados en el localStorage… ¿por qué hay que cambiarlos...

	* **Edward Steven Ramos Palacios** [48438] (1)

		localStorage es una API que guarda valores en **clave valor**.  
		Puedes guardar un objeto sin parsearlo a string primero pero, cuando lo consultes te va a devolver **[object] [object]**  
		por eso es necesario parsearlos a string para guardarlos y al recuperarlos los parseas a JSON de nuevo.

* **Byhako** (1) [38584](https://platzi.com/comentario/345655/) 
porque sale este error?

	* **Diego Alexander Forero Higuera (Platzi)** [38584] (3)

		Revisa esto <https://www.chromium.org/for-testers/bug-reporting-guidelines/uncaught-securityerror-failed-to-read-the-localstorage-property-from-window-access-is-denied-for-this-document> puede ser tu caso.

## 0220. Obteniendo los datos almacenados [12340](https://platzi.com/clases/1316-jquery-js/12340-obteniedo-los-datos-almacenados/)

### Descripción:


### Comentarios:

* **Sergio Minei** (15) [336247](https://platzi.com/comentario/336247/) 

	Revisar si los datos se encuentran en cache.
	```     asyncfunctioncacheExist(category) {
	      const listName = `${category}List`;
	      const cacheList = window.localStorage.getItem(listName);
	    
	      if (cacheList)
	        returnJSON.parse(cacheList);
	      
	      const { data: { movies: data } } = await getData(`${BASE_API}list_movies.json?genre=${category}`);
	      window.localStorage.setItem(listName, JSON.stringify(data));
	      return data;
	    }
	    
	```
	
	Si se desea volver a traer los datos se puede hacer lo siguiente:
	
	  * Poner un botón que traiga los datos
	  * Hacer un setTimeout que borre el localStorage.
	
	Si quieren ver el resumen completo del curso lo pueden ver [aquí](https://github.com/MineiToshio/CursosPlatzi/tree/master/Curso%20de%20jQuery%20a%20JavaScript). Lo iré actualizando a medida que vaya avanzando con el curso.

* **David Jiménez Mirayo** (6) [378240](https://platzi.com/comentario/378240/) 

	Me gustaría compartir esta pequeña modificación, que permite reutilizar la función que renderiza las películas, de forma que podemos incluir tantas categorías como deseemos (siempre que incluyamos el código con el ‘id’ correspondiente en el html). En mi caso, he añadido ‘comedy’ y ‘adventure’ y funciona a la perfección.
	
	Habría que incluir el siguiente cógigo:
	```     const categories = ['action', 'drama', 'animation', 'comedy', 'adventure']
	      let lists = {}
	      asyncfunctionrenderCategoryList (category) {
	        constlist = await cacheExist(category)
	        lists[category] = list
	        const $container = document.querySelector(`#${category}`)
	        renderMovieList(list, $container, category)
	      }
	    
	      categories.forEach((category) => renderCategoryList(category))
	    
	```
	
	Este código sustituiría a esto:
	```       const actionList = await cacheExist('action')
	      const $actionContainer = document.querySelector('#action')
	      renderMovieList(actionList, $actionContainer, 'action')
	    
	      const dramaList = await cacheExist('drama')
	      const $dramaContainer = document.getElementById('drama')
	      renderMovieList(dramaList, $dramaContainer, 'drama')
	    
	      const animationList = await cacheExist('animation')
	      const $animationContainer = document.getElementById('animation')
	      renderMovieList(animationList, $animationContainer, 'animation')
	    
	```
	
	Finalmente, para que funcionen los modales, habría que cambiar la función ‘findMovie’, que quedaría como sigue:
	```     functionfindMovie(id, category){
	        constlist = lists[category]
	        return findById(list, id)
	      }
	    
	```
	
	Espero que les guste!

* **der__leguan** (5) [336652](https://platzi.com/comentario/336652/) 

	Que pasa si los datos almacenados ya no son los datos actuales y a su ves no son validos, pero la aplicacion no da problema, como se cuando realizar nuevamente la peticion si ya tengo cacheado los datos?

	* **Diego Alexander Forero Higuera (Platzi)** [336652] (5)

		Tienes que definir una forma de invalidar ese cache cuando existan cambios, una forma puede ser hacerlo tu mismo cuando hagas cambios y borras el cache para actualizarlo con una nueva petición.

	* **jhurtadojerves** [336652] (7)

		Creo que lo mejor, en este caso, es usar sessionStorage en lugar de localStorage. De esta forma sabemos que cuando se reinicia el navegador traerá los datos de nuevo.
		
		O, en su defecto, podrías definir un botón Actualizar en dónde forces traer la data actualizada

* **Luis Fernando Valladares Castro** (4) [552491](https://platzi.com/comentario/552491/) 

	Hola a todos, soy un viejo alumno del curso y cree el _proyecto_ detalle a detalle como se presento y desarrollo a lo largo del curso más pequeñas correcciones y ciertas mejoras.
	
	Me alegra poder compartirlo para que puedan _utilizarlo como referencia_ y comentarles que cuenta con su _repositorio de GitHub_ para que puedan clonarlo.
	
	Pagina Web: <https://luisvalladaresc.github.io/platzi-video/>  
	Repositorio: [https://github.com/luisvalladaresc/platzi-video](https://github.com/LuisValladaresC/platzi-video)  
	![](https://static.platzi.com/media/porfilio/platzi-video_5954d8c1-a95e-4bff-a6d1-9883cd4f0db2.PNG)

* **Alan Santiago** (4) [364159](https://platzi.com/comentario/364159/) 

	Esto es de las cosas mas utiles que he visto. Nunca antes habia usado la cache 😄

* **jhurtadojerves** (4) [340745](https://platzi.com/comentario/340745/) 

	Me encantó el curso, justo terminé de verlo.
	
	Sobre el reto final, de volver a hacer la petición, lo hice por categorías. Quizá había una forma mejor, pero me gustó como lo hice.
	
	En primer lugar agregué promesas, xq así la petición que se termina primero se muestra primero. Además xq de esa forma si limpio la ceché de la primer categoría, las otras se cargan normal.
	
	Live: <https://juliohurtado.xyz/platzi-video-vanilla-js/>  
	Code: <https://github.com/jhurtadojerves/platzi-video-vanilla-js>
	
	Junto a cada categoría se encuentra el botón de recargar xD

	* **Juan David Castro (Platzi)** [340745] (3)

		Funciona genial! Felicitaciones! 🎉

	* **JhonColorado07** [340745] (1)

		Excelente te quedó.

* **heysoypaez** (3) [517843](https://platzi.com/comentario/517843/) 

	Gracias pr tu tiempo Leonidas
	
	Me queda mucho màs claro la practicidad de las funciones asincronas, veo como ire necesitando llamar y conversar con APIs y como puedo hacerlo nativamente en le navegador con fetch
	
	Nos vemos despues del examen y del ultimo desafio pendiente que tengo de la clase pasada,
	
	De resto los tome todos!!!
	
	Gracias por los desafios, me invitan a accionar, usar, disfrutar lo que aprendi

* **Facundo Petre** (3) [351057](https://platzi.com/comentario/351057/) 

	Complementando un poco lo que menciona Leonidas, Javascript nos permite utilizar otros tipos de datos como si fueran booleanos.
	
	“En JavaScript, un valor verdadero es un valor que se considera true/verdadero cuando es evaluado en un contexto Booleano. Todos los valores son verdaderos a menos que se definan como falso (es decir, excepto false, 0, “”, null, undefined, y NaN).”…  
	[Truthy-MDN](https://developer.mozilla.org/es/docs/Glossary/Truthy)

* **abdielalvarez** (2) [840946](https://platzi.com/comentario/840946/) 

	Mi aporte, código 100% funcional con retos terminados, con caché para la lista de películas de la nav bar y de los usuarios random, espero ser de ayuda para alguien que lo necesite 😃
	```     // GETTING DATA FROM LOCAL STORAGE
	    
	    (async function load() {
	    
	      async function getData(url) {
	        const response = await fetch(url);
	        const data = await response.json();
	        if (data.data.movie_count > 0) { 
	          return data;
	        }
	        throw new Error('No se encontró ningun resultado');
	      }
	      const $form = document.getElementById('form');
	      const $home = document.getElementById('home');
	      const $featuringContainer = document.getElementById('featuring');
	    
	      function setAttributes($element, attributes) {
	        for (const attribute in attributes) {
	          $element.setAttribute(attribute, attributes[attribute]);
	        }
	      }
	      const BASE_API = 'https://yts.lt/api/v2/';
	    
	      function featuringTemplate(peli) {
	        return (
	          `
	          <div class="featuring">
	            <div class="featuring-image">
	              <img src="${peli.medium_cover_image}" width="70" height="100" alt="">
	            </div>
	            <div class="featuring-content">
	              <p class="featuring-title">Pelicula encontrada</p>
	              <p class="featuring-album">${peli.title}</p>
	            </div>
	          </div>
	          `
	        )
	      }
	    
	      $form.addEventListener('submit', async (event) => {
	        event.preventDefault();
	        $home.classList.add('search-active')
	        const $loader = document.createElement('img');
	        setAttributes($loader, {
	          src: 'src/images/loader.gif',
	          height: 50,
	          width: 50,
	        })
	        $featuringContainer.append($loader);
	    
	        const data = new FormData($form);
	        try {
	          const {
	            data: {
	              movies: pelis
	            }
	          } = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`)
	    
	          const HTMLString = featuringTemplate(pelis[0]);
	          $featuringContainer.innerHTML = HTMLString;
	        } catch(error) {
	          alert(error.message);
	          $loader.remove();
	          $home.classList.remove('search-active');
	        }
	      })
	    
	      // -----------comedy loader-----------------------------------------------------------------------
	    
	      const $img2 = document.createElement('img');
	      setAttributes($img2, {
	        src: 'src/images/loader.gif',
	        height: 50,
	        width: 50
	      })
	      const $comedyContainer = document.getElementById('comedyList')
	      $comedyContainer.append($img2)
	    
	      // -----------users loader-----------------------------------------------------------------------
	    
	      const $img3 = document.createElement('img');
	      setAttributes($img3, {
	        src: 'src/images/loader.gif',
	        height: 50,
	        width: 50,
	      })
	      const $usersContainer = document.getElementById('usersList');
	      $usersContainer.append($img3)
	    
	      // -------------------------------------------------------------------------------------------------
	    
	      function videoItemTemplate(movie, category) {
	        return (
	          `<div class="primaryPlaylistItem" data-id="${movie.id}" data-category=${category}>
	            <div class="primaryPlaylistItem-image">
	              <img src="${movie.medium_cover_image}">
	            </div>
	            <h4 class="primaryPlaylistItem-title">
	              ${movie.title}
	            </h4>
	          </div>`
	        )
	      }
	      function createTemplate(HTMLString) {
	        const html = document.implementation.createHTMLDocument();
	        html.body.innerHTML = HTMLString;
	        return html.body.children[0];
	      }
	      function addEventClick($element) {
	        $element.addEventListener('click', () => {
	          showModal($element)
	        })
	      }
	      function renderMovieList(list, $container, category) {
	        $container.children[0].remove();
	        list.forEach((movie) => {
	          const HTMLString = videoItemTemplate(movie, category);
	          const movieElement = createTemplate(HTMLString);
	          $container.append(movieElement);
	          const image = movieElement.querySelector('img');
	          image.addEventListener('load', (event) => {
	            event.srcElement.classList.add('fadeIn');
	          })
	          addEventClick(movieElement);
	        })
	      }
	    
	      async function cacheExist (category) {
	        const listName = `${category}List`
	        const cacheList = localStorage.getItem(listName)
	        if(cacheList) {
	          return JSON.parse(cacheList)
	        }
	        const { data: { movies: data } } = await getData(`${BASE_API}list_movies.json?genre=${category}`)
	        localStorage.setItem(listName, JSON.stringify(data))
	        return data
	      }
	    
	      const actionList = await cacheExist('action')
	      const $actionContainer = document.querySelector('#action');
	      renderMovieList(actionList, $actionContainer, 'action');
	    
	      const dramaList = await cacheExist('drama')
	      const $dramaContainer = document.getElementById('drama');
	      renderMovieList(dramaList, $dramaContainer, 'drama');
	    
	      const animationList = await cacheExist('animation')
	      const $animationContainer = document.getElementById('animation');
	      renderMovieList(animationList, $animationContainer, 'animation');
	    
	      const $modal = document.getElementById('modal');
	      const $overlay = document.getElementById('overlay');
	      const $hideModal = document.getElementById('hide-modal');
	    
	      const $modalTitle = $modal.querySelector('h1');
	      const $modalImage = $modal.querySelector('img');
	      const $modalDescription = $modal.querySelector('p');
	    
	      function findById(list, id) {
	        return list.find(movie => movie.id === parseInt(id, 10))
	      }
	    
	      function findMovie(id, category) {
	        switch (category) {
	          case 'action' : {
	            return findById(actionList, id)
	          }
	          case 'drama' : {
	            return findById(dramaList, id)
	          }
	          default: {
	            return findById(animationList, id)
	          }
	        }
	      }
	    
	      function showModal($element) {
	        $overlay.classList.add('active');
	        $modal.style.animation = 'modalIn .8s forwards';
	        const id = $element.dataset.id;
	        const category = $element.dataset.category;
	        const data = findMovie(id, category);
	    
	        $modalTitle.textContent = data.title;
	        $modalImage.setAttribute('src', data.medium_cover_image);
	        $modalDescription.textContent = data.description_full
	      }
	    
	      $hideModal.addEventListener('click', hideModal);
	      function hideModal() {
	        $overlay.classList.remove('active');
	        $modal.style.animation = 'modalOut .8s forwards';
	      }
	    
	      // ---------Comedy list exercise for playlist item----------
	    
	      const comedyList = await cacheExist('comedy')
	    
	      function myPlaylistItemTemplate (movie) {
	        return (
	          `<li class="myPlaylist-item">
	            <a href="#">
	              <span>
	                ${movie.title}
	              </span>
	            </a>
	          </li>`
	        )
	      }
	    
	      try {
	        $comedyContainer.children[0].remove();
	        comedyList.forEach(movie => {
	          const HTMLString = myPlaylistItemTemplate(movie)
	          const html = document.implementation.createHTMLDocument()
	          html.body.innerHTML = HTMLString
	          $comedyContainer.append(html.body.children[0])
	          $comedyContainer.classList.add('fadeIn');
	        }) 
	      } catch (error) {
	        throw new Error('Lista de comedia no fue resuelta')
	      }
	      
	    
	      // ----------User random generator exercise for profiles-----------
	    
	      async function cacheUsersExist () {
	        const listName = 'userList'
	        const cacheList = localStorage.getItem(listName)
	        if(cacheList) {
	          return JSON.parse(cacheList)
	        }
	        const { results: usersList } = await getUsers('https://randomuser.me/api/?results=20')
	        localStorage.setItem(listName, JSON.stringify(usersList))
	        return usersList
	      }
	    
	      const usersList = await cacheUsersExist()
	      
	      async function getUsers(url) {
	        const response = await fetch(url);
	        const data = await response.json();
	        return data
	      }
	      
	      function playlistFriendsItemTemplate({ name: { first }, picture: { thumbnail } }) {
	        return (
	          `<li class="playlistFriends-item">
	            <a href="#">
	              <img src="${thumbnail}" alt="${first}" />
	              <span>
	                ${first}
	              </span>
	            </a>
	          </li>`
	        )
	      }
	    
	      try {
	        $usersContainer.children[0].remove();
	        usersList.forEach(user => {    
	          const HTMLString = playlistFriendsItemTemplate(user)
	          const html = document.implementation.createHTMLDocument()
	          html.body.innerHTML = HTMLString
	          $usersContainer.append(html.body.children[0])
	          $usersContainer.classList.add('fadeIn');
	        })
	      } catch (error) {
	        throw new Error('No se pudieron encontrar usuarios')
	      }
	    
	    })()
	    
	```

	* **Jhon Alexander Alvarez Romero** [840946] (1)

		gracias amigo, con esto soluciono la falla que tenia

* **Fernando Cordero** (2) [636806](https://platzi.com/comentario/636806/) 

	No se, pero me siento en el limbo con este curso 😕

	* **SGalvez** [636806] (2)

		¿por qué lo dices?

	* **Cristian Orozco Benjumea** [636806] (3)

		Tranquilo, así me sentí yo la primera vez. Hazlo de nuevo ya tendrás un panorama mejor.

* **ivan_acg** (2) [418914](https://platzi.com/comentario/418914/) 

	minuto 8:32 ,
	```     const animationList = awaitawait cacheExist('drama')
	    
	```
	
	Me desconcierta el doble await , ¿Es un error? o ¿porque lo pone?

	* **Esteban Ciudad** [418914] (3)

		fue un typo mas de @LeonidasEsteban pero que esta vez no causó un error de ejecución

* **Anthony Gonzalez** (2) [359677](https://platzi.com/comentario/359677/) 

	¿Porque se utiliza dos veces await en la variables dramaList y animationList?
	```     const dramaList = await await cacheExist('drama');
	    const $dramaContainer = document.getElementById('drama');
	    renderMovieList(dramaList, $dramaContainer, 'drama');
	    
	    const animationList = await await cacheExist('animation');
	    const $animationContainer = document.getElementById('animation');
	    renderMovieList(animationList, $animationContainer, 'animation');
	    
	```

	* **mglBrv** [359677] (2)

		Es un error. Debería estar solo una vez.

	* **Esteban Ciudad** [359677] (1)

		fue un typo mas de @LeonidasEsteban pero que esta vez no causó un error de ejecución

* **David Perez** (2) [67847](https://platzi.com/comentario/730039/) 
Las imágenes de las películas también se están guardando en el Local Storage?

	* **Edward Steven Ramos Palacios** [67847] (2)

		Sii, en el ejemplo que muestra Leonidas, guarda de hecho toda la info de la película. Usa con sabiduría el `localstorage`

* **carlosextra1** (1) [1117397](https://platzi.com/comentario/1117397/) 

	Excelente clase!! y excelente uso de cache!!

* **DevNaftan** (1) [1106889](https://platzi.com/comentario/1106889/) 

	 **Reto cumplido**  
	Si los datos ya existen en cache los muestra pero luego los vuelve a actualizar para una segunda recarga y así sucesivamente
	```     asyncfunctioncacheExist(genre) {
	        const cacheList = window.localStorage.getItem(`${genre}List`)
	        if(cacheList) {
	          setTimeout(async () => {
	            window.localStorage.removeItem(`${genre}List`);
	            const {data: {movies}} = await getMovies(`${BASE_MOVIES_API}list_movies.json?genre=${genre}`)
	            window.localStorage.setItem(`${genre}List`, JSON.stringify(movies))
	          }, 1000)
	          returnJSON.parse(cacheList)
	        } else {
	          const {data: {movies}} = await getMovies(`${BASE_MOVIES_API}list_movies.json?genre=${genre}`)
	          window.localStorage.setItem(`${genre}List`, JSON.stringify(movies))
	          return movies
	        }
	      }
	    
	```

* **ronaldjgarciav** (1) [1077223](https://platzi.com/comentario/1077223/) 

	Increíblemente útil. aunque hay que verlo varias veces para asimilar bien

* **wlm.morant** (1) [1065734](https://platzi.com/comentario/1065734/) 

	todo me da vueltas

* **Andres Condo** (1) [1059314](https://platzi.com/comentario/1059314/) 

	Esta ultima clase fue muy cargada, y muy buena

* **Angélica María Rocha García** (1) [1058484](https://platzi.com/comentario/1058484/) 

	gracias Leonidas

* **Kenet Chungandro** (1) [1007343](https://platzi.com/comentario/1007343/) 

	El curso estuvo genial! 😄

* **José Bryan Aranda Figueroa** (1) [737850](https://platzi.com/comentario/737850/) 

	porque no jala mi codigo ?
	```     <
	    asyncfunctioncacheExist(category)
	          {
	            const listName = `${category}List`;
	            const cacheList = window.localStorage.getItem(listName);
	    
	            if(cacheList) {
	              debugger
	              returnJSON.parse(cacheList);
	            
	            }
	            
	            const { data: { movies: data}} = await getData(`${BASE_API}list_movies.json?genre=${category}`)
	            window.localStorage.setItem(listName , JSON.stringify(data))
	    
	            return data;
	          
	          }
	    
	    
	    
	          const horrorList = await  cacheExist('horror');
	          const horrorContainer = document.querySelector('#action')
	          renderMovieList(horrorList , horrorContainer, 'horror')
	    
	    
	          const dramaList =  await cacheExist('drama');
	          const dramaContainer = document.getElementById('drama')  
	          renderMovieList(dramaList , dramaContainer , 'drama')
	    
	    
	          const animationList = await cacheExist('animation');
	          const animationContainer = document.getElementById('animation')
	          renderMovieList(animationList , animationContainer , 'animation')    
	    >
	    
	```
	
	les explico puse un debugger hay para ver que pasa pues no convierte el texto en objeto, yo con el debugger lo hago manualmente cuando entra el if pero al momento de aplicarle el return me sale el siguiente error  
	VM2064:1 Uncaught SyntaxError: Illegal return statement  
	at cacheExist (home.js:131)  
	at load (home.js:145)  
	at home.js:336

* **Carlos Andres Castañeda Osorio** (1) [719957](https://platzi.com/comentario/719957/) 

	Para una mayor explicación del porqué funciona el condicional de esa manera los invito a revisar la siguiente documentación de [Truthy](https://developer.mozilla.org/es/docs/Glossary/Truthy)

* **Miguel Negrete** (1) [667132](https://platzi.com/comentario/667132/) 

	En esta const no le puso comillas a listName por eso no jala
	
	const cacheList = window.localStorage.getItem(‘listName’);

	* **powerangelito17** [667132] (1)

		Quita las comillas del listName  
		Así debería de quedarte:
		```     const cacheList = window.localStorage.getItem(listName);
		    
		```

* **onlinejaime** (1) [658877](https://platzi.com/comentario/658877/) 

	Tomad nota:
	
	En el vídeo no aparece que se cambia la extensión también del género en la URL, dentro de la función cacheExists. Caso contrario jamás funcionaría:
	
	**const { data: { movies: data } } = await getData(`${BASE_API}list_movies.json?genre=${category}`)**

* **Aaron Felipe Isaacs** (1) [629257](https://platzi.com/comentario/629257/) 

	Buen dái,
	
	Documentación del curso en markdown en mi repositorio
	
	Web page: [](https://github.com/afisaacs/jquerytojavascript)  
	Repository: [](https://github.com/afisaacs/jquerytojavascript)
	```     const load = async () => {
	      const API_MOVIE = 'https://yts.lt/api/v2/list_movies.json'
	      const API_USER = 'https://randomuser.me/api/'
	      const movies = ['action', 'drama', 'animation']
	      const listMovies = {}
	    
	      const $form = document.getElementById('form')
	      const $home = document.getElementById('home')
	      const $featuringContainer = document.getElementById('featuring')
	    
	      const $overlay = document.getElementById('overlay')
	      const $modal = document.getElementById('modal')
	      const $hideModal = document.getElementById('hide-modal')
	    
	      const $modalTitle = document.getElementById('modal-title')
	      const $modalImage = document.querySelector('.modal-content img')
	      const $modalDescription = document.querySelector('.modal-content p')
	    
	      const getDataMovies = async (url, filters) => getData(url, filters, 'movie')
	    
	      const getData = async (url, filters, type) => {
	        let paramGet = ''
	        if (filters) {
	          paramGet += '?'
	          Object.keys(filters).forEach(filter => {
	            if (paramGet.length > 1) {
	              paramGet += '&'
	            }
	            paramGet += `${filter}=${filters[filter]}`
	          })
	        }
	        const action = await fetch(`${url}${paramGet}`)
	        const data = await action.json()
	        if (type === 'movie') {
	          if (data.data.movie_count > 0) {
	            return data
	          } else {
	            throw ('The movie was not found')
	          }
	        }
	        return data
	      }
	    
	      const videoItemTemplate = ({ medium_cover_image, title, id }, category) => {
	        return (
	        `<div class="primaryPlaylistItem" data-id="${id}" data-category="${category}">
	          <div class="primaryPlaylistItem-image">
	            <img style="opacity: 0;" src="${medium_cover_image}">
	          </div>
	          <h4 class="primaryPlaylistItem-title">
	            ${title}
	          </h4>
	        </div>`
	        )
	      }
	    
	      const featuringTemplate = ({ medium_cover_image, title }) => {
	        return (
	          `<div class="featuring">
	            <div class="featuring-image">
	              <img src="${medium_cover_image}" width="70" height="100" alt="">
	            </div>
	            <div class="featuring-content">
	              <p class="featuring-title">Pelicula encontrada</p>
	              <p class="featuring-album">${title}</p>
	            </div>
	          </div>`
	        )
	      }
	    
	      const userTemplate = ({ picture: {medium: image}, name}) => {
	        return (
	          `<li class="playlistFriends-item">
	            <a href="#">
	              <img src="${image}" />
	              <span style="text-transform: capitalize;">
	                ${name.title} ${name.first} ${name.last} 
	              </span>
	            </a>
	          </li>`
	        )
	      }
	    
	      const playListTemplate = ({ title }) => {
	        return (
	          `<li class="myPlaylist-item">
	            <a href="#">
	              <span>
	                ${title}
	              </span>
	            </a>
	          </li>`
	        )
	      }
	    
	      const findMovie = (id, category) => listMovies[category].find(item => item.id === parseInt(id, 10))
	      
	      const showModal = ({ currentTarget: $element }) => {
	        $overlay.classList.add('active')
	        $modal.style.animation = 'modalIn .8s forwards'
	        const { id, category } = $element.dataset
	        const data = findMovie(id, category)
	    
	        $modalTitle.textContent = data.title
	        $modalImage.setAttribute('src', data.medium_cover_image)
	        $modalDescription.textContent = data.description_full
	      }
	    
	      const addHideModalListener = () => {
	        $hideModal.addEventListener('click', () => {
	          setTimeout(() => $overlay.classList.remove('active'), 800)
	          $modal.style.animation = 'modalOut .8s forwards'
	        })
	      }
	    
	      const addAttributes = (element, attributes) => {
	        Object.keys(attributes).forEach(attr => element.setAttribute(attr, attributes[attr]))
	      }
	    
	      const addSubmitListener = () => {
	        $form.addEventListener('submit', async event => {
	          $featuringContainer.innerHTML = ""
	          event.preventDefault()
	          $home.classList.add('search-active')
	          const $loader = document.createElement('img')
	          addAttributes($loader, {
	            src: './src/images/loader.gif',
	            height: '50px',
	            width: '50px'
	          })
	          $featuringContainer.appendChild($loader)
	    
	          const data = new FormData($form)
	          const searchInput = data.get('name')
	          try {
	            const { data: { movies } } = await getDataMovies(API_MOVIE, {query_term: searchInput})
	            const htmlString = featuringTemplate(movies[0])
	            $featuringContainer.innerHTML = htmlString
	          } catch (error) {
	            alert(error)
	            $featuringContainer.removeChild($loader)
	            $home.classList.remove('search-active')
	          }
	        })
	      }
	    
	      const putMovieComponent = (lstMovies, element, genre) => {
	        element.innerHTML = ""
	        lstMovies.forEach(movie => {
	          const htmlString = videoItemTemplate(movie, genre)
	          const html = document.implementation.createHTMLDocument()
	          html.body.innerHTML = htmlString
	          const component = html.body.children[0]
	          component.addEventListener('click', event => showModal(event))
	          const image = component.querySelector('img')
	          image.addEventListener('load', event => event.srcElement.classList.add('fadeIn'))
	          element.appendChild(component)
	        })
	      }
	    
	      const putUserComponent = (lstUser, element) => {
	        element.innerHTML = ""
	        lstUser.forEach(user => {
	          const htmlString = userTemplate(user)
	          const html = document.implementation.createHTMLDocument()
	          html.body.innerHTML = htmlString
	          const component = html.body.children[0]
	          element.appendChild(component)
	        })
	      }
	      
	      const putPlaylistComponent = (lstMovies, element) => {
	        element.innerHTML = ""
	        lstMovies.forEach((movie) => {
	          const htmlString = playListTemplate(movie)
	          const html = document.implementation.createHTMLDocument()
	          html.body.innerHTML = htmlString
	          const component = html.body.children[0]
	          element.appendChild(component)
	        })
	      }
	    
	      const renderMovies = async genre => {
	        let apidata
	        if (!window.localStorage.getItem(genre)) {
	          apidata = await getDataMovies(API_MOVIE, {genre})
	          window.localStorage.setItem(genre, JSON.stringify(apidata))
	        } else {
	          apidata = JSON.parse(window.localStorage.getItem(genre))
	        }
	        const $container = document.getElementById(genre)
	        putMovieComponent(apidata.data.movies, $container, genre)
	        listMovies[genre] = apidata.data.movies
	      }
	      
	      const renderUsers = async () => {
	        const apidata = await getData(API_USER, {results: 10})
	        const $container = document.getElementById('lst-friends')
	        putUserComponent(apidata.results, $container)
	      }
	      
	      const renderPlayList = async () => {
	        const apidata = await getDataMovies(API_MOVIE, {limit: 10})
	        const $container = document.getElementById('playlist')
	        putPlaylistComponent(apidata.data.movies, $container)
	      }
	    
	      addSubmitListener()
	      addHideModalListener()
	      movies.forEach(movie => renderMovies(movie))
	      renderUsers()
	      renderPlayList()
	    }
	    
	    load()
	    
	    
	```

* **heysoypaez** (1) [516831](https://platzi.com/comentario/516831/) 

	¿Por qué es necesario colocar el await en este código?
	```     		const animationList = await cacheExist("animation")
	    
	```
	
	Si hay una sola función según comprendo javascript la deberia ejecutar inmediatamente, y más tomando en cuenta que el código dentro de esa función tiene async await para la información que necesita
	
	Si es una función llamando otra función alli si empieza a jugar su papel el asincronismo
	
	Si alguien me pudiera ilustrar un poco con ejemplos y su experiencia me ayudaria un montón

	* **Eduardo Hidalgo Díaz Rugama** [516831] (7)

		async y await es para que hagas cosas paralelamente. No es tema de “solo tengo una linea de código”.
		
		Si por ejemplo, yo hago una sola petición a una api con fetch sin async/wait, pero usando React, durante los 5 segundos que se demore el fetch la aplicación va a estar freezeada, y el usuario no podrá usar nada de la app. Súmale la pésima experiencia de usuario de que la app esté freezeada.
		
		También te sirve (usando promesas) para ejecutar animaciones como loaders para demostrarle al usuario que debe esperar por el contenido. Mientras está recibiendo la data, tu le dices con un loader “esperame”.

* **Carlos Jesús Fernández Salazar** (1) [439638](https://platzi.com/comentario/439638/) 

	Creo que pude redundar un poco menos pero este es mi código de **cacheExists** :
	```     asyncfunctioncacheExists(item) {
	        if (item == 'friends') {
	          cacheList = window.localStorage.getItem(item);
	          if (cacheList) {
	            returnJSON.parse(cacheList);
	          } else {
	            const { results: friends } = await getData(`${USER_API}${USER_QTY}`);
	            window.localStorage.setItem(friends, JSON.stringify(friends));
	            return friends;
	          }
	        } elseif (item == 'likedList'){
	          cacheList = window.localStorage.getItem(item);
	          if (cacheList) {
	            returnJSON.parse(cacheList);
	          } else {
	            const { data: { movies: likedList }} = await getData(`${MOVIES_API}${MOVIES_SORT}download_count${MOVIES_QTY}10`);
	            window.localStorage.setItem(likedList, JSON.stringify(likedList));
	            return likedList;
	          }
	        } else {
	          const listName = `${item}List`
	          const cacheList = window.localStorage.getItem(listName);
	          if (cacheList) {
	            returnJSON.parse(cacheList);
	          } else {
	            const { data: { movies: data }} = await getData(`${MOVIES_API}${MOVIES_GENRE}${item}`);
	            window.localStorage.setItem(listName, JSON.stringify(data));
	            return data;
	          }
	        }
	      }
	    
	```

* **LUISFER24** (1) [395504](https://platzi.com/comentario/395504/) 

	[](https://github.com/luisFelipeEvilla/platziVideoVainila/blob/master/src/js/home.js) Aquí mi solución al reto 😃

	* **LUISFER24** [395504] (1)

		<https://github.com/luisFelipeEvilla/platziVideoVainila/blob/master/src/js/home.js>

* **Diego Fernando Rojas Quintero** (1) [86490](https://platzi.com/comentario/1097052/) 
No entiendo la lógica del reto dicha por leonadas, alguien me puede ayudar?

	* **María José Ledesma (Platzi)** [86490] (1)

		¡Hola, _Diego_!  
		¿Qué es lo que no entiendes?  
		Cuéntanos así te podemos ayudar.
		
		**¡Nunca pares de aprender!** (Y practicar) ✨🦄

* **wlm.morant** (1) [85087](https://platzi.com/comentario/1065866/) 
porque crea listName y no defrente coloca dentro de getItem(category)???

* **santiagobarrios** (1) [84769](https://platzi.com/comentario/1058357/) 
async function cacheExist(category){ const ListName = ${category}List; const cacheList = window.localStorage.getItem(ListNam...

	* **Erik Ochoa (Platzi)** [84769] (1)

		Imprime tu variable **cacheList** porque parece que el JSON viene con un formato incorrecto:
		```     const cacheList = window.localStorage.getItem(ListName);
		    
		```
		
		Otra cosa rara que veo es que aquí le faltan las comillas:
		```     const ListName = "${category}List";
		    
		```

* **José Bryan Aranda Figueroa** (1) [68278](https://platzi.com/comentario/737956/) 
porque no funciona mi código ? <asyncfunctioncacheExist(category) { const listName = `${category}List`; co...

	* **Edward Steven Ramos Palacios** [68278] (1)

		Una función `async` siempre debe retornar una promesa. Cuando retornas `return JSON.parse(cacheList);` estas retornando un objeto, pero no una promesa resuelta con la data.
		
		Lo que puedes hacer es refactorizar, y extraer en una función nueva lo que tiene que ver con validar y retornar la data desde el local storage.

* **heysoypaez** (1) [53371](https://platzi.com/comentario/516831/) 
¿Por qué es necesario colocar el await en este código? const animationList = await cacheExist("animation") Si hay una sol...

	* **Eduardo Hidalgo Díaz Rugama** [53371] (7)

		async y await es para que hagas cosas paralelamente. No es tema de “solo tengo una linea de código”.
		
		Si por ejemplo, yo hago una sola petición a una api con fetch sin async/wait, pero usando React, durante los 5 segundos que se demore el fetch la aplicación va a estar freezeada, y el usuario no podrá usar nada de la app. Súmale la pésima experiencia de usuario de que la app esté freezeada.
		
		También te sirve (usando promesas) para ejecutar animaciones como loaders para demostrarle al usuario que debe esperar por el contenido. Mientras está recibiendo la data, tu le dices con un loader “esperame”.

* **Anthony Gonzalez** (1) [39894](https://platzi.com/comentario/359677/) 
¿Porque se utiliza dos veces await en la variables dramaList y animationList? const dramaList = await await cacheExist('drama'); co...

	* **mglBrv** [39894] (2)

		Es un error. Debería estar solo una vez.

## 0230. Conclusiones del curso [12341](https://platzi.com/clases/1316-jquery-js/12341-conclusiones-del-curso1078/)

### Descripción:


### Links:

* [JavaScript | MDN](https://developer.mozilla.org/es/docs/Web/JavaScript)

* [Curso de ECMAScript 6+](https://platzi.com/clases/ecmascript-6/)

### Comentarios:

* **Sergio Minei** (27) [336241](https://platzi.com/comentario/336241/) 

	Toma tus 5 estrellas Leonidas! Te lo mereces! ⭐⭐⭐⭐⭐
	
	Gracias por tan excelente curso!

* **Sneyder Alfonso Barreto Buitrago** (7) [913858](https://platzi.com/comentario/913858/) 

	Aquí mi aporte:  
	![PlatziVideo-VanillaJS.jpg](https://static.platzi.com/media/user_upload/PlatziVideo-VanillaJS-afd90795-ace4-44e7-8994-0b57fa66eba8.jpg)  
	**[Repositorio en Github](https://github.com/sneyderdev/platzivideo-vanillajs)**  
	**[Live Action](https://sneyderdev.github.io/platzivideo-vanillajs)**

	* **Cristian Caraballo** [913858] (2)

		¿Como hiciste esa imagen 😮?

	* **Sneyder Alfonso Barreto Buitrago** [913858] (3)

		¡Hola @CrisOMG!  
		La hice con Photoshop y de base utilicé una plantilla que encontré googleando por ahí.  
		Un saludo.

* **Kevin Javier Morales (Platzi)** (7) [335778](https://platzi.com/comentario/335778/) 

	Excelente curso como siempre! 👏 Con la calidad de Leonidas! Sí se merece las cinco estrellitas.
	
	Una recomendación muy offtopic, es usar ublock origin en lugar de Adblock Plus 😅

	* **Miguel Matos** [335778] (1)

		Menos acá si quieren reducir el consumo del navegador 😉

* **brapastor** (6) [337402](https://platzi.com/comentario/337402/) 

	Un excelente curso de vanilla js 😃 ⭐⭐⭐⭐⭐

* **Cristian Orozco Benjumea** (5) [549927](https://platzi.com/comentario/549927/) 

	Muy buen curso pero creo que tendré que tomarlo otra vez para poder asimilar mejor los ejercicios, varias veces me perdí. Voy siguiendo la ruta que propone Leonidas para Frontend pero creo que era mejor ver antes el de Fundamentos de JS en lugar de JS a jQuery. Igual excelente profesor.

	* **zalotesta** [549927] (1)

		Amí me pasó similar a lo que describes, por eso opté por realizar primero completo el de JS y luego volví a este!

	* **Karla Agraz** [549927] (1)

		Estoy de acuerdo. El de fundamentos de JS explica muy bien las bases, este curso es para practicar lo aprendido.

* **Luis Felipe Andrés Barraza Vega** (4) [405191](https://platzi.com/comentario/405191/) 

	Hola amigos les dejo mi archivo JS donde aplique el reto y puse a la izquierda un playlist de peliculas y abajo usuarios con su avatar, con la api randomuser todo hecho con vanillaJS porsupuesto:
	```     (async function load(){
	    
	        async function getUser(url) {
	          const response = await fetch(url);
	          const data = await response.json();
	            users= data;
	        }
	        getUser('https://randomuser.me/api/?inc=name,picture&results=10')
	        //const listUserC = users.results.length;
	    
	    
	        //await
	        async function getData(url) {
	          const response = await fetch(url);
	          const data = await response.json();
	          if (data.data.movie_count > 0) {
	            // aquí se acaba
	            return data;
	          }
	          // si no hay pelis aquí continua
	          throw new Error('No se encontró ningun resultado');
	        }
	    
	        const $form = document.getElementById('form');
	        const $home = document.getElementById('home');
	        const $featuringContainer = document.getElementById('featuring');
	    
	        function setAttributes($element, attributes){
	          for (const attribute in attributes) {
	            $element.setAttribute(attribute, attributes[attribute])
	          }
	    
	    
	        }
	    
	        const BASE_API = 'https://yts.am/api/v2/'
	        const BASE_USER = 'https://randomuser.me/api'
	    
	    
	    
	        function featuringTemplate(peli){
	          return(
	            `<div class="featuring">
	              <div class="featuring-image">
	                <img src="${peli.medium_cover_image}" width="70" height="100" alt="">
	              </div>
	              <div class="featuring-content">
	                <p class="featuring-title">Pelicula encontrada</p>
	                <p class="featuring-album">${peli.title}</p>
	              </div>
	            </div>`
	          )
	        }
	    
	        $form.addEventListener('submit', async (event)=>{
	          event.preventDefault();
	          $home.classList.add('search-active')
	          const $loader = document.createElement('img');
	          setAttributes($loader,{
	            src:'src/images/loader.gif',
	            height: 50,
	            width: 50,
	          })
	          $featuringContainer.append($loader);
	    
	          const data = new FormData($form);
	          //destructuracion de objetos
	          try {
	            const {
	              data: {
	                movies: pelis
	              }
	            } = await getData(`${BASE_API}list_movies.json?limit=1&query_term=${data.get('name')}`)
	            const HTMLString = featuringTemplate(pelis[0])
	            $featuringContainer.innerHTML = HTMLString;
	          } catch(error){
	            alert('Pelicula no encontrada');
	            $loader.remove();
	            $home.classList.remove('search-active');
	          }
	    
	    
	        })
	    
	    
	    
	        function videoItemTemplate(movie, category){
	          return (
	            //templateLiterals:
	            //${} permite integrar variables a html para hacer un html dinamico
	              `<div class="primaryPlaylistItem" data-id="${movie.id}" data-category=${category}>
	                <div class="primaryPlaylistItem-image">
	                  <img src="${movie.medium_cover_image}">
	                </div>
	                <h4 class="primaryPlaylistItem-title">
	                  ${movie.title}
	                </h4>
	              </div>`
	            )
	        }
	    
	        function playListTemplate(movie){
	          return(
	          `
	          <ol class="myPlaylist">
	            <la class="myPlaylist-item">
	              <a href="#">
	                <span>
	                  ${movie.title} - ${movie.genres[0]}
	                </span>
	              </a>
	            </la>
	          </ol>
	    
	          `)
	        }
	    
	    
	        //console.log(videoItemTemplate('src/images/covers/bitcoin.jpg', 'bitcoin'))
	    
	    
	    
	        function createTemplate(HTMLString){
	          const html = document.implementation.createHTMLDocument();
	          html.body.innerHTML = HTMLString;
	          return html.body.children[0];
	        }
	    
	        function addEventClick($element){
	          $element.addEventListener('click',()=>{
	            //alert('click')
	            showModal($element)
	          })
	        }
	    
	        function renderUser(list,$container){
	          $container.children[0].remove();
	    
	          for (var i = 0; i < list; i++) {
	            const HTMLString = `
	    
	            <ul class="playlistFriends">
	              <li class="playlistFriends-item">
	                <a href="#">
	                  <img src="${users.results[i].picture.thumbnail}" />
	                  <span>
	                    ${users.results[i].name.last}, ${users.results[i].name.first}
	                  </span>
	                </a>
	              </li>
	    
	    
	    
	            </ul>
	    
	              `;
	            const userElement = createTemplate(HTMLString);
	            $container.append(userElement)
	          }
	    
	    
	    
	    
	        }
	    
	      
	    
	        function renderPlayList(list, $container, category){
	          $container.children[0].remove();
	          list.forEach((movie) => {
	            const HTMLString = playListTemplate(movie, category);
	            const movieElement = createTemplate(HTMLString)
	            $container.append(movieElement);
	    
	    
	          })
	        }
	    
	        function renderMovieList(list, $container, category){
	          $container.children[0].remove();
	          list.forEach((movie) => {
	    
	            const HTMLString = videoItemTemplate(movie, category);
	            const movieElement = createTemplate(HTMLString)
	            $container.append(movieElement);
	            const image = movieElement.querySelector('img');
	            image.addEventListener('load', () => {
	              event.srcElement.classList.add('fadeIn');
	            })
	            addEventClick(movieElement);
	    
	          })
	        }
	    
	    
	    
	    
	    
	        async function cacheExist(category){
	          const listName = `${category}List`;
	          const cacheList = window.localStorage.getItem(listName);
	    
	          if(cacheList){
	            return JSON.parse(cacheList);
	          }
	          const {data: {movies:data}} = await getData(`${BASE_API}list_movies.json?genre=${category}`)
	          //const { "results":user} = await getUser('https://randomuser.me/api/?inc=name')
	    
	          window.localStorage.setItem('listName', JSON.stringify(data))
	          return data;
	        }
	    
	    
	    
	    
	    
	    
	    
	        //const {data: {movies:actionList}} = await getData(`${BASE_API}list_movies.json?genre=action`)
	        const actionList = await cacheExist('action');
	        //window.localStorage.setItem('actionList', JSON.stringify(actionList))
	        const $actionContainer = document.querySelector('#action');
	        renderMovieList(actionList,$actionContainer, 'action')
	    
	        //const {data: {movies:dramaList}} = await getData(`${BASE_API}list_movies.json?genre=drama`)
	        //window.localStorage.setItem('dramaList', JSON.stringify(dramaList))
	        const dramaList = await cacheExist('drama');
	        const $dramaContainer = document.querySelector('#drama');
	        renderMovieList(dramaList,$dramaContainer, 'drama')
	    
	        // const {data: {movies:animationList}} = await getData(`${BASE_API}list_movies.json?genre=animation`)
	        // window.localStorage.setItem('animationList', JSON.stringify(animationList))
	        const animationList = await cacheExist('animation');
	        const $animationContainer = document.querySelector('#animation');
	        renderMovieList(animationList,$animationContainer, 'animation')
	    
	        const nameMovie = await cacheExist('');
	        const $nameMovie = document.querySelector('#playlist');
	        renderPlayList(nameMovie,$nameMovie);
	    
	        //const pickUser = getUser('https://randomuser.me/api/?inc=name,picture')
	        const $userContainer = document.querySelector('#users');
	        const listPerson = users.results.length;
	        renderUser(listPerson,$userContainer)
	    
	    
	    
	    
	    
	    
	    
	        // selector => .home
	        //solucion de seleccion de selectores con jquery
	        //const $home = $('.home .list #item');
	        //solucion de selectores en vanillaJS
	        const $modal = document.getElementById('modal');
	        const $overlay = document.getElementById('overlay');
	        const $hideModal = document.getElementById('hide-modal');
	    
	        //buscar dentro de clases
	        const $modalImage = $modal.querySelector('img');
	        const $modalTitle = $modal.querySelector('h1');
	        const $modalDescription = $modal.querySelector('p');
	    
	        function findById(list, id){
	          return list.find(movie => movie.id === parseInt(id,10))
	        }
	        function findMovie(id, category){
	          switch (category) {
	            case 'action':{
	              return findById(actionList,id)
	            }
	            case 'drama':{
	              return findById(dramaList,id)
	            }
	            default: {
	              return findById(animationList,id)
	            }
	          }
	        }
	    
	        function showModal($element){
	          $overlay.classList.add('active');
	          $modal.style.animation = 'modalIn .8s forwards';
	          const id = $element.dataset.id;
	          const category = $element.dataset.category;
	          const data = findMovie(id,category)
	    
	          $modalImage.setAttribute('src',data.medium_cover_image);
	          $modalTitle.textContent=data.title;
	          $modalDescription.textContent = data.description_full;
	        }
	    
	        $hideModal.addEventListener('click', hideModal);
	        function hideModal(){
	          $overlay.classList.remove('active');
	          $modal.style.animation = 'modalOut .8s forwards';
	        }
	    
	    
	    
	    
	    
	      })()//sugarSintax para autoejecutar funcion
	    
	```

* **jhurtadojerves** (4) [340747](https://platzi.com/comentario/340747/) 

	Sin lugar a dudas te llevas 5 estrellas. Muy buen curso, me encanta.
	
	⭐⭐⭐⭐⭐

* **fredy bustos** (4) [337992](https://platzi.com/comentario/337992/) 

	Excelente curso como siempre Leonidas Esteban.  
	Ahora fue muy poco de jQuery. Así que realmente el nombre del curso no me parece acorde a lo que se vio.  
	Por lo demás todo muy bien.

	* **Japheth Calzada López** [337992] (2)

		de hecho si, por que era lo que ya sabes hacer con jquery hacerlo con javascript

* **ajosue98** (3) [525218](https://platzi.com/comentario/525218/) 

	Como cuando el curso te enseña fundamentos de JS como JQuery  
	![](https://i.pinimg.com/736x/65/8b/4a/658b4a3479061cc454a13889368e84a1.jpg)

	* **Isaias Chávez** [525218] (1)

		Aún así no hay como tomar el de fundamentos solito.

* **Juan David Castro (Platzi)** (3) [341253](https://platzi.com/comentario/341253/) 

	🎉 Sin errores! _bueno casi_ 😅
	
	🌟 🌟 🌟 🌟 🌟

* **William Reaño Reyes** (3) [336899](https://platzi.com/comentario/336899/) 

	buen curso

* **ronaldjgarciav** (2) [1077226](https://platzi.com/comentario/1077226/) 

	React Ahí te voy prro !

* **richie** (2) [1016273](https://platzi.com/comentario/1016273/) 

	Fue un excelente curso, recuerdo haber visto un comentario muy positivo hacia el profesor, yo no había tomado ningún curso con Leonidas y realmente tenía razón el comentario de nuestro compañero de la comunidad porque definitivamente un magnifico curso en todos los aspectos.

* **José Manuel Puicón Rodas** (2) [955370](https://platzi.com/comentario/955370/) 

	Este fue uno de los mejores cursos básicos que sin duda he tomado

* **Aaron Felipe Isaacs** (2) [629264](https://platzi.com/comentario/629264/) 

	I’m ready for React!! 👊👊👊👊👊

* **Daniel Alejandro Gimenez** (2) [404164](https://platzi.com/comentario/404164/) 

	Excelente curso, supero mis espectativas! Realmente Leonidas merece las 5 estrellas ⭐⭐⭐⭐⭐ sin lugar a dudas.
	
	Les comparto un artículo que me resulto interesante, trata de la estratégia que abordo GitHub para remover JQuery de su sitio y sustituirlo por código Javascript puro.
	
	<https://githubengineering.com/removing-jquery-from-github-frontend/>
	
	Espero se encuentren bien. Saludos!!

* **Nelson Alayón** (2) [392781](https://platzi.com/comentario/392781/) 

	Excelente curso, muy claro en enseñar Leonidas, sin embargo para mí esta informacion es muy compleja y la verdad me toca ver el curso muchas veces para poder aprenderlo realmente.

* **mglBrv** (2) [383988](https://platzi.com/comentario/383988/) 

	Muy buen curso Leonidas. ⭐⭐⭐⭐⭐  
	Algo que eché de menos es el manejo del historial con `window.history`. Algo que es importante para poder ir atrás/adelante con los botones del navegador, o para compartir la url en una SPA.  
	Creo que jQuery no tiene métodos para esto, pero sería un muy buen complemento para este excelente curso.

* **Alan Santiago** (2) [364161](https://platzi.com/comentario/364161/) 

	Antes queria aprender jQuery. Ahora veo que no es necesario 😄

* **Anthony Gonzalez** (2) [359678](https://platzi.com/comentario/359678/) 

	Exelente Curso! Leonidas es un Crack! ⭐⭐⭐⭐⭐

* **cristhian joel Acevedo Tipian** (2) [355004](https://platzi.com/comentario/355004/) 

	⭐⭐⭐⭐⭐ sin duda Leonidas ! genial

* **carlosextra1** (1) [1117405](https://platzi.com/comentario/1117405/) 

	Muy buen curso la verdad. Ya nunca más usar Jquery!!! en nuevo proyectos!!

* **wlm.morant** (1) [1066751](https://platzi.com/comentario/1066751/) 

	esta vez , es la primera vez que apruebo a la primera un curso complejo

* **Andrés Gutierrez** (1) [1044152](https://platzi.com/comentario/1044152/) 

	dejo mi tremendo código aquí, intenté escuchar la explicaciónd de eonidas y hacer todo por mi cuenta antes de ver la solución me gustaría que lo elyeran y me dijeran que puedo mejorar 😄(trambién hay notas ue puse sobre como funcionan algunas cosas, si estoy esquivocado dícanmelo plis)
	```     (async function load() {
	      const $modal = document.getElementById('modal');
	      const $hideModal = document.getElementById('hide-modal');
	      const $seeker= document.getElementsByTagName('input');
	      const $seekerContent= $seeker.value;
	    
	      const $modalTitle = $modal.querySelector('h1');
	      const $modalImage = $modal.querySelector('img');
	      const $modalDescription = $modal.querySelector('p');
	      const $actionContainer = document.querySelector('#action');
	      const $dramaContainer = document.querySelector('#drama');
	      const $animationContainer = document.querySelector('#animation');
	      const $result=document.querySelector('.search-active')
	    
	      const $featuringContainer = document.querySelector('#featuring');
	      const $form = document.getElementById('form');
	      const $overlay = document.getElementById('overlay');
	    
	      const generalMovieList=[]
	      
	      const $home = document.getElementById('home');
	    
	      function setAttributes($element, attributes){
	        for (const attribute in attributes) {
	          $element.setAttribute(attribute, attributes[attribute])
	        }
	      }
	    
	      async function getData(url) {
	          const response = await fetch(url);
	          jsonRseponse= response.json()
	          return jsonRseponse
	          //ANCHOR función para traer las pelis
	      }
	    
	      const displaySeekingResult=async()=>{
	        event.preventDefault()
	        $home.classList.add('search-active')
	        //ANCHOR aquí mostramos la barrita que nos sale al buscar una peli
	        
	        const $loader=document.createElement('img')
	        //NOTE $loader.setAttribute('src', 'loader.gif')
	        setAttributes($loader,{
	          src:'src/images/loader.gif',
	          height: 50,
	          width: 50
	        })
	        $featuringContainer.append($loader)
	        function featuringTemplate(peli){
	          return(
	            `<div class="featuring">
	              <div class="featuring-image">
	                <img src="${peli.medium_cover_image}" width="70" height="100" alt="película encontrada">
	              </div>
	              <div class="featuring-content">
	                <p class="featuring-title">Pelicula encontrada</p>
	                <p class="featuring-album">${peli.title}</p>
	              </div>
	            </div>`)
	        }
	    
	        const data= new FormData($form)
	        const findMovie= (list, data)=>{
	          const action =({category})=> category == 'action'
	          //ese category en realidad viene dentro de un
	          //parametro que se le pasa en el .filter que es el mismo
	          //objeto al que le ponemos esa propiedad
	          const drama =({category})=> category == 'drama'
	          const animation =({category})=> category == 'animation'
	          /*NOTE aquí hay que poner OBLIGATORIAMENTE el parametro, lo que le 
	          estamos diciendo es que vea dentro de todos los list 
	          y que revise el list.category, si no lo ponemos solo va a 
	          revisar por encima
	          */ 
	          
	        const actionMovieList= list.filter(action)
	        console.log(actionMovieList)
	        const dramaMovieList= list.filter(drama)
	        const animationMovieList= list.filter(animation)
	    
	    
	    
	          const movieFound = ()=>list.find(arr => arr.id === parseInt(data));
	          return movieFound
	        }
	        findMovie(generalMovieList, data.get('name'))
	        const peli= getData(`https://yts.mx/api/v2/list_movies.json?limit=1&query_term=${data.get('name')}`)
	        //NOTE limit=1 es una propiedad del método GET, que nos da un límite del primerisimo resultado
	        //NOTE el query_term es de la api para traernos una pelicula en especifico
	        //NOTE El '&' entre los mencionados anteriormente es para denotar que son propiedades distintas
	        .then((data)=>{
	          $HTMLMovieFoundString=featuringTemplate(data.data.movies[0])
	          /**ANCHOR
	          esto por si solo nos retorna el objeto de html con los valores pero 
	          no los pone donde va, de eso de encarga el inner.HTML*/
	          $featuringContainer.innerHTML= $HTMLMovieFoundString
	          featuringTemplate(data)
	          return data
	        })
	        .catch((err)=>{
	          function featuringTemplateDefault(){
	            return(
	              `<div class="featuring">
	                <div class="featuring-image">
	                  <img src="https://www.evisionstore.com/images/notfound.png" width="70" height="100" alt="película encontrada">
	                </div>
	                <div class="featuring-content">
	                  <p class="featuring-title">Pelicula encontrada</p>
	                  <p class="featuring-album">movie '${data.get('name')}'not found :(</p>
	                </div>
	              </div>`)
	          }
	          $HTMLMovieFoundString= featuringTemplateDefault()
	            
	          $featuringContainer.innerHTML= $HTMLMovieFoundString
	    
	    
	        })
	      }
	    
	      $form.addEventListener('submit', displaySeekingResult);
	    
	    
	    
	      function videoItemTemplate(movie, category) {
	        return (
	          `<div class="primaryPlaylistItem" data-id="${movie.id}" data-category=${category}>
	            <div class="primaryPlaylistItem-image">
	              <img src="${movie.medium_cover_image}">
	            </div>
	            <h4 class="primaryPlaylistItem-title">
	              ${movie.title}
	            </h4>
	          </div>`
	        )
	      }
	      //ANCHOR console.log(videoItemTemplate('src/images/covers/bitcoinjpg', 'bitcoin'))
	      
	    
	    
	      function createTemplate(htmlTags){
	          const html = document.implementation.createHTMLDocument();
	          html.body.innerHTML = htmlTags;
	          return html.body.children[0]
	      }
	    
	      function addEventClick($movieElement, movie){
	        const displayOverlay=()=>{
	          $overlay.classList.add('active')
	          //ANCHOR modal es la ventanita que nos sale al hacer click en las pelis
	          $modal.children[0].innerHTML= movie.title
	          $modalDescription.innerHTML= movie.description_full
	    
	          $modalImage.setAttribute('src', `${movie.medium_cover_image}`)
	          $modal.style.animation= 'modalIn .8s forwards'
	    
	          const hideOverlay=()=>{
	            $overlay.classList.remove('active')
	            $modal.style.animation= 'modalOut .8s forwards'
	          }
	    
	          $hideModal.addEventListener('click', hideOverlay)
	        }
	    
	    
	        const id = $movieElement.dataset.id
	        const category= $movieElement.dataset.category
	        $movieElement.addEventListener('click', displayOverlay)
	      }
	    
	    
	      
	      function renderMovies(list, $movieGenreContainer, category){
	    
	        $movieGenreContainer.children[0].remove()
	        
	        list.forEach((movie) => {
	          const id_Category= {
	            id: movie.id,
	            category: category
	          }
	          generalMovieList.push(id_Category)
	    
	          const HTMLString = videoItemTemplate(movie, category);
	          const html = createTemplate(HTMLString)
	          html.style.animation= 'fadeIn 2s forwards'
	          //NOTE aquí le decimos que nos ponga una animacion que ya estaba en 
	          //el styles.css que es fadeIn
	          $movieGenreContainer.append(html);
	          addEventClick(html, movie)
	          //ANCHOR esto además del dlcik pone el título del modal
	        })
	        
	      }
	      if(window.localStorage.dramaList===undefined && window.localStorage.actionList===undefined && window.localStorage.animationList===undefined)
	      //NOTE le decimos que si el localstorage de eso existe lo use, tiene que ser undefined para no, no puede ser false
	      {
	        const actionList= getData('https://yts.mx/api/v2/list_movies.json?genre=action')
	        .then(({data})=>{
	          renderMovies(data.movies, $actionContainer, 'action')
	          window.localStorage.setItem('actionList', JSON.stringify(data))
	        })
	        .then(()=>{
	          const dramaList= getData('https://yts.mx/api/v2/list_movies.json?genre=drama') 
	          return dramaList
	        })
	        .then(({data})=>{
	          renderMovies(data.movies, $dramaContainer, 'drama')
	          window.localStorage.setItem('dramaList',  JSON.stringify(data))
	        })
	        .then(()=>{
	          const animationList= getData('https://yts.mx/api/v2/list_movies.json?genre=animation') 
	          return animationList
	        })
	      //  window.localStorage.setItem('animationList', animationList)
	        .then(({data})=>{
	          renderMovies(data.movies, $animationContainer, 'animation')
	          window.localStorage.setItem('animationList',  JSON.stringify(data))
	        }).catch((err)=>console.log(err))
	      }
	    
	      else{
	        const cacheActionList=JSON.parse(window.localStorage.actionList)
	        const cacheDramaList=JSON.parse(window.localStorage.dramaList)
	        const cacheAnimationList=JSON.parse(window.localStorage.animationList)
	        //NOTE si no lo guardamos en una variable no funciona
	        renderMovies(cacheActionList.movies, $actionContainer, 'action')
	        renderMovies(cacheDramaList.movies, $dramaContainer, 'drama')
	        renderMovies(cacheAnimationList.movies, $animationContainer, 'animation')
	        console.log(ewe)
	        alert('asumaidre')
	      }
	    })()
	    
	```

* **lmoran** (1) [1011333](https://platzi.com/comentario/1011333/) 

	Un excelente curso, se aprendió mucho, a ponerlo en práctica.

* **Kenet Chungandro** (1) [1007354](https://platzi.com/comentario/1007354/) 

	Uno de los mejores cursos sin duda!

* **fryma** (1) [960155](https://platzi.com/comentario/960155/) 

	Excelente curso.

* **Camilo Alexander Velandia Velandia** (1) [949458](https://platzi.com/comentario/949458/) 

	genial, uno de los mejores cursos que he tomado en platzi

* **iridian** (1) [892147](https://platzi.com/comentario/892147/) 

	Me encanto este curso, Leonidas un gran profesor 😄 Muchas gracias aprendí mucho.

* **jordimalla** (1) [752887](https://platzi.com/comentario/752887/) 

	Buen curso, en este repositorio de gitHub les dejo mi codigo final con timestamp en la cache.
	
	<https://github.com/jordimalla/Javascript-MyFirstVanilla-JS.git>

* **Andrés Felipe Eslava Zuluaga** (1) [704747](https://platzi.com/comentario/704747/) 

	Excelente Curso. No sé como se hacen las estrellas,  
	pero yo doy mis cinco estrellas * * * * *
	
	Gracias nuevamente, Leonidas es un gran profesor…  
	Tardé bastante entendiendo alguna parte del curso, pero leyendo y viendo con detenimiento las cosas es como uno aprende. Nada es fácil, si fuera fácil sería aburrido.
	
	MUY EXCELENTE TODO COMO SIEMPRE CON PLATZI

	* **u.mitchel** [704747] (1)

		Hahahahaha cuando pases el examen le vas a poder dar las estrellas.

* **Wilson Marino Pablo Mendez** (1) [700651](https://platzi.com/comentario/700651/) 

	Gran curso!!!

* **Camilo Andrés Santana Lizcano** (1) [696043](https://platzi.com/comentario/696043/) 

	De verdad que después de este curso me siento con super poderes en Js y eso que apenas es el comienzo.

	* **Oscar Barajas Tavares (Platzi)** [696043] (1)

		Ya esta disponible la escuela de JavaScript, hay puedes seguir aprendiendo JS con mas profundidad.

* **Renier Ferrer** (1) [683396](https://platzi.com/comentario/683396/) 

	Buenísimo todo el curso

* **Cristian Orozco Benjumea** (1) [674424](https://platzi.com/comentario/674424/) 

	Segunda vez que tomo el curso y siempre aprendo más

* **Javier González de Lope** (1) [671020](https://platzi.com/comentario/671020/) 

	Excelente curso Leonidas,  
	muchísimas gracias por toda la información

* **hectorJosuemacias** (1) [661116](https://platzi.com/comentario/661116/) 

	Grande Leonidas.  
	Excelente servicio !!!

* **Claudio Jesus Vázquez Villanueva** (1) [657362](https://platzi.com/comentario/657362/) 
Estuvi bueno.

* **Francisco Raimondi** (1) [609643](https://platzi.com/comentario/609643/) 

	Buen Curso. El primero que hice, muy conforme

* **jesus-gallardo** (1) [603979](https://platzi.com/comentario/603979/) 

	Que buen curso **⭐⭐⭐⭐⭐**

* **GUIFOR PAUL PLAZA AGUIRRE** (1) [598708](https://platzi.com/comentario/598708/) 

	Excelente curso, bien bien.  
	Gracias por su aporte.

	* **GUIFOR PAUL PLAZA AGUIRRE** [598708] (1)

		Primeros pasos en javascript

* **laguz81** (1) [580526](https://platzi.com/comentario/580526/) 

	Excelente Leonidas, muchas gracias excelente curso…⭐⭐⭐⭐⭐

* **laguz81** (1) [580525](https://platzi.com/comentario/580525/) 

	**Excelente **Leonidas, muchas gracias excelente curso…

* **laguz81** (1) [580523](https://platzi.com/comentario/580523/) 

	Excelente Leonidas, muchas gracias excelente curso…

* **Jesús Antonio Chacón Rodriguez** (1) [566959](https://platzi.com/comentario/566959/) 

	Excelente curso Leonidas!

* **Luis Fernando Valladares Castro** (1) [552495](https://platzi.com/comentario/552495/) 

	Hola a todos, soy un viejo alumno del curso y cree el _proyecto_ de 0 y detalle a detalle como se presento y desarrollo a lo largo del curso más pequeñas correcciones y ciertas mejoras.
	
	Me alegra poder compartirlo para que puedan _utilizarlo como referencia_ y comentarles que cuenta con su _repositorio de GitHub_ para que puedan clonarlo.
	
	Pagina Web: <https://luisvalladaresc.github.io/platzi-video/>  
	Repositorio: [https://github.com/luisvalladaresc/platzi-video](https://github.com/LuisValladaresC/platzi-video)  
	![](https://static.platzi.com/media/porfilio/platzi-video_5954d8c1-a95e-4bff-a6d1-9883cd4f0db2.PNG)

* **Edward Acu** (1) [535300](https://platzi.com/comentario/535300/) 

	Buen curso, sencillo pero sirvió para reafirmar mi cambio, estaba muy acostumbrado a usar jquery.

* **Cristhian Camilo Delgado Pazos** (1) [531757](https://platzi.com/comentario/531757/) 

	Muy buen curso Leonidas

* **juan david henao cadavid** (1) [516378](https://platzi.com/comentario/516378/) 

	excelente y muy profesional el curso.

* **jorge-winder-avila** (1) [507346](https://platzi.com/comentario/507346/) 

	Un éxito el curso.
	
	Gracias.

* **pablojorgeandres** (1) [489277](https://platzi.com/comentario/489277/) 

	Excelente, señor Leonidas.  
	Este curso no tiene ningún desperdicio.
	
	Muchas gracias por su dedicación.
	
	Aquí le dejo sus ⭐️⭐️⭐️⭐️🌟

* **Giancarlo Vera** (1) [478812](https://platzi.com/comentario/478812/) 

	Muy buen curso Leonidas, saludos desde Perú! ⭐⭐⭐⭐⭐

* **Francisco Ezequiel Baños Ramirez** (1) [467828](https://platzi.com/comentario/467828/) 

	Increible el curso la neta, Leonidas se la rifa siempre xD te ganas tus 5 estrellitas :3 ⭐⭐⭐⭐⭐

* **ManuFerrer** (1) [450263](https://platzi.com/comentario/450263/) 

	⭐⭐⭐⭐⭐, buen curso y buen profesor !

* **ramrikrdo** (1) [440660](https://platzi.com/comentario/440660/) 

	Excelente curso, falto el ejemplo de pokemón comparando pikachu vs Raichu!

* **Matias Nicolas Giorgieff** (1) [431188](https://platzi.com/comentario/431188/) 

	Muy buen curso!

* **Ricardo Medina** (1) [425980](https://platzi.com/comentario/425980/) 

	Excelente curso Leonidas, me gusto el contenido reforcé conceptos y todo lo explicaste muy bien.

* **José Roberto Diaz Rodriguez** (1) [393116](https://platzi.com/comentario/393116/) 

	Gracias Leonidas, Buen curso

* **Rodolfo Nicacio Ugalde Ochoa** (1) [392606](https://platzi.com/comentario/392606/) 

	Excelente curso!!! desde Querétaro, MX.

* **Josue David Pablo Ixbalan** (1) [377040](https://platzi.com/comentario/377040/) 

	Excelente curso Leonidas! Saludos desde **Guatemala** ⭐⭐⭐⭐⭐

* **Yassr** (1) [373953](https://platzi.com/comentario/373953/) 

	Excelente curso! el mejor que he hecho, Leonidas siempre muy pedagógico!  
	⭐⭐⭐⭐⭐

* **José Francisco Ojeda Vera** (1) [366542](https://platzi.com/comentario/366542/) 

	Excelente curso Leonidas Esteban ⭐

* **Jorge-Delfino** (1) [360086](https://platzi.com/comentario/360086/) 

	Excelente curso Leonidas ¡ahora a leer documentación y fortalecer los conocimientos!

* **Joel Alejandro Morales García** (1) [359866](https://platzi.com/comentario/359866/) 

	Excelente curso para la carrera de JavaScript 😃

* **David Daniel Castillo Nava** (1) [354861](https://platzi.com/comentario/354861/) 

	Excelente curso claro que te ganas tus **5** estrellas.

* **jsechirinos** (1) [342869](https://platzi.com/comentario/342869/) 

	Excelente curso sensei Leo…!!!  
	⭐⭐⭐⭐⭐

* **edesalla17** (1) [342296](https://platzi.com/comentario/342296/) 

	excelent!

* **wlm.morant** (1) [85132](https://platzi.com/comentario/1066756/) 
Los links de la documentaicon??

	* **Aaron Gonzalez (Platzi)** [85132] (1)

		¡Hola! 😄  
		Los enlaces de la documentación están listos en la sección de archivos de este curso 😄 ¡Gracias por el aviso!
		
		Adicional, te recomiendo mucho que te pases por el curso de EcmaScript ahora que has terminado este 😄

* **David Daniel Castillo Nava** (0) [354858](https://platzi.com/comentario/354858/) 

	 **Así quedo mi código!!**
	```     const API_BASE = 'https://yts.am/api/v2';
	    
	    (async function load(){
	    
	    	/*
	    	** Initial Variables
	    	*/
	    	const $home = document.getElementById('home');
	    	const $form = document.getElementById('form');
	    	const $featuringContainer = document.getElementById('featuring');
	    
	    	const $actionContainer = document.querySelector('#action');
	    	const $dramaContainer = document.getElementById('drama');
	    	const $animationContainer = document.getElementById('animation');
	    
	    	const $modal = document.getElementById('modal');
	    	const $overlay = document.getElementById('overlay');
	    
	    	const $hideModal = document.getElementById('hide-modal');
	    	
	    	const $modalTitle = $modal.querySelector('h1');
	    	const $modalImage = $modal.querySelector('img');
	    	const $modalDescription = $modal.querySelector('p')
	    
	    	/*
	    	** Initial Fetch
	    	*/
	    
	    	async function cacheVerify(category) {
	    		const listName = `${category}List`;
	    		const cacheList = window.localStorage.getItem(listName);
	    
	    		if (cacheList) return JSON.stringify(cacheList)
	    
	    		const { data: {movies: data }} = await getData(`${API_BASE}/list_movies.json?genre=${category}`)
	    		window.localStorage.setItem(listName, JSON.stringify(data))
	    		return data
	    	}
	    
	    	// let [ reqAction, reqDrama, reqAnimation ] = await Promise.all([
	    	// 	fetch(`${API_BASE}/list_movies.json?genre=action`),
	    	// 	fetch(`${API_BASE}/list_movies.json?genre=drama`),
	    	// 	fetch(`${API_BASE}/list_movies.json?genre=animation`)
	    	// ])
	    
	    	
	    	const actionList = await cacheVerify('action')
	    	// window.localStorage.setItem('actionList', JSON.stringify(actionList))
	    	renderList(actionList, $actionContainer, 'listPlayer',{click:true, render: 'append', fadeIn: true}, 'action')
	    	const dramaList =await cacheVerify('drama')
	    	// window.localStorage.setItem('dramaList', JSON.stringify(dramaList))
	    	renderList(dramaList, $dramaContainer, 'listPlayer',{click:true, render: 'append', fadeIn: true}, 'drama')
	    	const animationList = await cacheVerify('animation')
	    	// window.localStorage.setItem('animationList', JSON.stringify(animationList))
	    	renderList(animationList, $animationContainer, 'listPlayer',{click:true, render: 'append', fadeIn: true}, 'animation')
	    
	    	
	    	/*
	    	** Functions
	    	*/
	    	async function getData(url) {
	    		const response = await fetch(url);
	    		
	    		const data = await response.json()
	    		if(data.data.movie_count > 0){
	    			return data;
	    		}
	    		
	    	}
	    
	    	function setAttributes($element, attributes) {
	    		for (const attribute in attributes) {
	    			$element.setAttribute(attribute, attributes[attribute])
	    		}
	    	}
	    
	    	function templateContent (template, content) {
	    		let hmtlContent;
	    		switch(template) {
	    			case 'listPlayer':
	    				hmtlContent = `
	    					<div data-id="${content.id}" data-category="${content.category}" class="primaryPlaylistItem">
	    						<div class="primaryPlaylistItem-image">
	    							<img src="${content.medium_cover_image}">
	    						</div>
	    						<h4 class="primaryPlaylistItem-title">
	    							${content.title_long}
	    						</h4>
	    					</div>
	    				`
	    				break;
	    			case 'featuringTemplate':
	    				hmtlContent = `
	    					<div class="featuring">
	    						<div class="featuring-image">
	    							<img src="${content.medium_cover_image}" width="70" height="100" alt="">
	    						</div>
	    						<div class="featuring-content">
	    							<p class="featuring-title">Pelicula encontrada</p>
	    							<p class="featuring-album">${content.title_long}</p>
	    						</div>
	    					</div>
	    				`
	    				break;
	    			default:
	    				hmtlContent = ''
	    		}
	    
	    		const html = document.implementation.createHTMLDocument();
	        	html.body.innerHTML = hmtlContent;
	    
	    		return html.body.children[0];
	    	}
	    
	    	function renderList (list, $elemParent, template, options, category) {
	    		$elemParent.children[0].remove();
	    		list.forEach((content)=>{
	    			content.category = category
	    			const $elementContent = templateContent(template, content)
	    			if (typeof options.fadeIn != "undefined") {
	    				const image = $elementContent.querySelector('img')
	    
	    				image.addEventListener('load', (e)=>{
	    					e.srcElement.classList.add('fadeIn')
	    				})
	    			}
	    
	    			if (options.render === 'html') {	
	    				$elemParent.innerHTML = $elementContent.outerHTML
	    			} else if( options.render === 'append') {
	    				$elemParent.append($elementContent)
	    			}
	    
	    			if (options.click)
	    				addEventClick($elementContent)
	    		})
	    	}
	    	function findById(list, id) {
	    		return list.find(movie => movie.id === parseInt(id, 10))
	    	}
	    
	    	function findMovie(id, category) {
	    		switch (category) {
	    			case 'action' : {
	    				return findById(actionList, id)
	    		}
	    			case 'drama' : {
	    				return findById(dramaList, id)
	    		}
	    			default: {
	    				return findById(animationList, id)
	    			}
	    		}
	    	}
	    	function addEventClick ($element) {
	    		$element.addEventListener('click', (e) => {
	    	      showModal($element)
	    	    })
	    	}
	    
	    	function showModal($element) {
	    		$overlay.classList.add('active')
	    		$modal.style.animation = 'modalIn .8s forwards'
	    		const id = $element.dataset.id;
	    		const category = $element.dataset.category;
	    		const data = findMovie(id, category);
	    
	    		$modalTitle.textContent = data.title
	    		$modalImage.setAttribute('src', data.medium_cover_image)
	    		$modalDescription.textContent = data.synopsis
	    	}
	    
	    	function hideModal() {
	    		$overlay.classList.remove('active')
	    		$modal.style.animation = 'modalOut .8s forwards'
	    	}
	    
	    	/*
	    	** Events on DOM element
	    	*/
	    	$hideModal.addEventListener('click', hideModal)
	    
	    	$form.addEventListener('submit', async (e) => {
	    		e.preventDefault()
	    		$featuringContainer.innerHTML  =''
	    
	    		$home.classList.add('search-active')
	    
	    		const $loader = document.createElement('img')
	    
	    		setAttributes($loader, {
	    			src: 'src/images/loader.gif',
	    			height: 50,
	    			width: 50
	    		})
	    
	    		$featuringContainer.append($loader)
	    
	    		try { 
	    			const data = new FormData($form)
	    			const {data: {movies}} = await getData(`${API_BASE}/list_movies.json?limit=1&query_term=${data.get('name')}`)
	    			renderList(movies, $featuringContainer, 'featuringTemplate', {click: false, render: 'html'} )
	    		} catch(err) {
	    			console.error(err)
	    			$loader.remove()
	    			$home.classList.remove('search-active')
	    		}
	    	})
	    
	    })()
	    
	```

